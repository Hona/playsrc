# Map Roadmap

[`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines delivery status, coverage classification, Map package, canonical representation, inventory, and completion terms.

## Completion Denominator

This leaf roadmap contains exactly 21 behavior rows. [`inventories/map-sections.md`](inventories/map-sections.md) defines 27 candidate canonical map sections. Those sections contribute 0 denominator items until a checked-in generator emits the inventory and a denominator review accepts it. The current completion denominator therefore contains 21 items. Acceptance of all 27 candidate sections would make the denominator 48 items.

The target is one immutable, runtime-neutral map representation for each declared Team Fortress 2, Counter-Strike: Source, or legacy Source 1 Counter-Strike: Global Offensive map input. Every representation preserves Source coordinates, carries world geometry references, joins parsed BSP identities to current material, StudioModel, entity, collision, and visibility outputs, and passes complete referential integrity validation. Source 2, console-specific map containers, editable VMF reconstruction, rendering resources, game behavior, publication, and storage are excluded.

The denominator is Not accepted. The section inventory has no generator or review record, required producer roadmaps and interfaces are incomplete, exact declared-content map indexes are Missing, and direct native/WASM compilation evidence does not exist.

## Inputs

- One `MapCompileRequest` whose identity is exactly `{game, contentBuild, map}`. `game` is `tf2`, `css`, or `csgo`; `map` is the exact `maps/<name>.bsp` logical path; `contentBuild` is the configured provider's content-build identity or `Unknown` only when that provider exposes no build identity.
- One successfully parsed BSP document from `packages/formats/bsp` with selected profile, container version, signed map revision, complete typed and opaque lump results, source provenance, and SHA-256 of the exact BSP bytes. The request game, map logical path, content build, profile, and BSP provenance must agree.
- Finalization receives current semantic outputs requested by exact role and identity from `packages/world/material`, `packages/formats/studio-model`, `packages/world/entity`, `packages/world/collision`, and `packages/world/visibility`. Every output supplies its owner, current interface identity, source-map identity and hash when map-derived, assembly-plan identity for collision and visibility, stable item identities, coverage classifications, artifact descriptors, and transitive input hashes.
- A dependency callback that receives `{sectionId, itemId, role, logicalPath}` and returns exactly one current semantic output with provenance and content hash or `Missing`. The Map package never searches providers, probes alternate paths or extensions, parses dependency bytes, or substitutes another dependency.
- One build configuration containing exactly `lightingProfile` and `limits`. `lightingProfile` is `ldr` or `hdr`; there is no automatic profile selection. `limits` contains non-negative unsigned values for `maxItems`, `maxDirectReferences`, `maxTransitiveReferences`, `maxDependencyDepth`, `maxDependencyBytes`, `maxManifestBytes`, and `maxDiagnostics`. No field has a default and no unbounded mode exists.
- One compiler-behavior identity containing the current Map interface identity, repository commit, and lowercase SHA-256 of the checked-in machine-readable behavior manifest used by the compiler.
- Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`: `src/public/bspfile.h`, `gamebspfile.h`, `worldsize.h`, `vphysics_interface.h`; `src/utils/vbsp/writebsp.cpp`; `src/game/shared/mapentities_shared.*`; and the public map consumers in `src/game/server/world.cpp`, `SkyCamera.cpp`, `fogcontroller.cpp`, `WaterLODControl.cpp`, `rope.cpp`, `lights.cpp`, `env_tonemap_controller.cpp`, and `shadowcontrol.cpp`.

## Outputs

- Preparation emits one immutable, non-runtime `MapAssemblyPlan` containing the exact request/BSP identities, build and compiler-behavior hashes, map-owned source occurrence tables, unresolved dependency requests, and typed collision/visibility assembly views. Its identity is exactly `{game,contentBuild,map,bspSha256,buildConfigurationSha256,compilerBehaviorSha256}`. It is the only input from which child map-derived semantic outputs may be built and cannot be published or consumed as a partial map.
- Successful finalization of one plan and all required child outputs emits an immutable `CanonicalMap` containing the exact request identity, plan identity, BSP source identity, Source spatial frame, selected lighting profile, build-configuration hash, compiler-behavior identity, and exactly the 27 section descriptors in inventory order.
- Each section descriptor contains exactly `id`, `classification`, `itemCount`, `artifact`, and `dependencies`. `id` is an accepted inventory identity; `classification` is `Handled` or `Intentionally inert`; `itemCount` is a canonical unsigned decimal string; `artifact` is one immutable `{kind:"artifact",mediaType,byteLength,sha256}` descriptor whose media type is an accepted producer value, byte length is a canonical unsigned decimal string, and SHA-256 is lowercase 64-hex; and `dependencies` is a duplicate-free artifact-descriptor array sorted by kind then SHA-256.
- A frozen `MapRuntimeDescriptor` containing map source identity, every consumed raw object, compiler/configuration identities, canonical section objects, runtime representations, coverage classifications, and diagnostics. Its duplicate-free references are sorted by kind then SHA-256. It is emitted automatically after successful compilation and can run in memory without publication. `packages/asset-store` owns optional persistent storage and publication of the descriptor and referenced objects.
- A deterministic dependency trace ordered by section identity, item identity, dependency role, and logical path. Every entry records the request, result, owner, provenance, content hash, artifact descriptor, and coverage classification.
- A deterministic assembly report containing all direct and transitive input hashes, build-configuration hash, compiler-behavior identity, section and item counts, selected lighting profile, dependency trace identity, root-description identity, and diagnostics sorted by validation stage, section inventory order, item identity, dependency role, logical path, and error code.
- Preparation failure emits no `MapAssemblyPlan`. Finalization failure emits no `CanonicalMap` or `MapRuntimeDescriptor`; the immutable plan remains non-authoritative. Diagnostics use exactly `MalformedRequest`, `ProducerRejected`, `IdentityMismatch`, `DuplicateIdentity`, `MissingSection`, `MissingDependency`, `UnsupportedSection`, `UnknownSection`, `MalformedReference`, `HashMismatch`, `IncompleteLightingProfile`, or `BoundExceeded` and record classification, validation stage, section, item, role, logical path, offending identity or value, and bound when applicable.

## Invariants

- Canonical coordinates remain Source XYZ: X is forward/east, Y is left/north, and Z is up. One Source unit is one inch. Positions are inches, angles are degrees, QAngle order is pitch about Y, yaw about Z, and roll about X. No GLB, renderer, physics-adapter, or application coordinate transform enters canonical map data.
- The nominal Source coordinate interval is `[-16384, 16384]` on each axis. Every canonical coordinate is finite. An encountered finite coordinate outside that interval is `Unsupported` until an accepted declared-content contract establishes its required handling; values are never clamped.
- Bounds retain three distinct identities: the nominal coordinate interval; each BSP brush model's ordered minimum and maximum; and world gameplay bounds from worldspawn `world_mins` and `world_maxs`, which exclude 2D and 3D sky geometry. Missing, non-finite, reversed, or out-of-range required bounds prevent output.
- BSP source indexes remain canonical cross-section identities. Face, displacement, brush-model, leaf, cluster, material-reference, world-light, static-prop, detail-prop, overlay, cubemap, and entity indexes are never renumbered, merged, or inferred from position or name.
- Brush model `0` is the world model. Entity model value `*N` refers only to brush-model index `N`; duplicate entity references remain distinct entity items. A missing world model, overlapping model face ranges, or out-of-range `*N` is Malformed.
- Every displacement has exactly one source displacement index and one parent face identity. Map coordinates displacement ownership and references; collision queries, visibility, lighting evaluation, tessellation policy, and rendering remain with their named owners.
- `ldr` selects standard face, lighting, world-light, leaf-ambient, detail-lighting, cubemap, sky-resource, and static-prop-lighting references. `hdr` selects the corresponding HDR references and is accepted only when the complete profile required by the selected BSP profile exists. Missing HDR members are `IncompleteLightingProfile`; the compiler never selects LDR instead.
- For map logical path `maps/<name>.bsp`, cubemap index `i` at integer origin `(x,y,z)` requests `materials/maps/<name>/c<x>_<y>_<z>.vtf` under `ldr` and `materials/maps/<name>/c<x>_<y>_<z>.hdr.vtf` under `hdr`; `i` remains the item identity when origins duplicate. A required 2D sky named `<sky>` requests exactly `materials/skybox/<sky><suffix>.vmt` under `ldr` and `materials/skybox/<sky>_hdr<suffix>.vmt` under `hdr`, in suffix order `rt`, `lf`, `bk`, `ft`, `up`, `dn`. Material owns all child texture selection and missing profile-selected material or texture bytes fail without trying the other path.
- All four face light-style bytes and every world-light style value are retained. `255` terminates a face's contiguous style list; styles after the first `255`, style values outside 0 through 63, and sample ranges inconsistent with style count are Malformed. Style animation and light contribution belong to entity and rendering owners.
- A valid empty section is emitted as `Intentionally inert` with zero items and an immutable empty-section artifact. A required singleton or a section required by an encountered reference cannot be converted to empty.
- Every section item has exactly one inventory owner. Map may coordinate a child artifact, source index, placement, or relationship but cannot reinterpret child material, model, entity, collision, visibility, physics, game, or rendering semantics.
- Every direct and transitive dependency is resolved from its exact logical identity once. Two responses for one identity must have identical owner, bytes, hash, and descriptor; disagreement is `HashMismatch` or `IdentityMismatch`, never provider precedence.
- Preparation validates BSP-local structure and freezes child input views before any collision or visibility build. Tools invoke child packages with those views, then call finalization with outputs that name the exact plan and BSP hashes. Map, Collision, and Visibility do not import one another's implementations or mutate the plan.
- All arrays retain declared source order unless the output contract explicitly requires sorted identity order. Sets, artifact references, dependency traces, and diagnostics use their declared stable sort keys. Host map iteration, filesystem order, locale, thread completion, and absolute paths cannot affect output.
- Compilation is pure over the request, supplied parsed/semantic values, dependency responses, configuration, and compiler-behavior identity. The package retains no mutable process-global, prior-map, cache, filesystem, renderer, or cross-request state.
- Limits are charged before request dispatch, allocation, hashing, reference insertion, diagnostic insertion, or serialization. Exceeding any caller limit emits `BoundExceeded` and no authoritative partial output.
- Validation cannot mutate parsed or semantic inputs. Successful output is deeply immutable. Failure diagnostics and dependency traces are non-authoritative and cannot be consumed as a partial map.

## Ownership Exclusions

- BSP, game-lump, PAK, and external-lump parsing, byte ranges, compression, versions, and lossless representation belong to `packages/formats/bsp`.
- Exact logical-path normalization, provider order, archive lookup, byte retrieval, caching, and provenance acquisition belong to `packages/content`.
- VMT/VTF parsing and material shader, parameter, proxy, texture, animation, and surface-property semantics belong to `packages/formats/vmt`, `packages/formats/vtf`, and `packages/world/material`.
- MDL, VVD, VTX, ANI, VHV, and PHY parsing and model geometry, skeleton, animation, and collision references belong to their format owners. Map owns only map occurrence and cross-section association.
- Entity tokenization, ordered key/value meaning, class behavior, I/O, lifecycle, fog transitions, rope simulation, controller state, and game-specific entities belong to `packages/world/entity`, runtime packages, and `games/<game>`.
- Collision shape construction, contents meaning, traces, sweeps, overlaps, contacts, and surface query results belong to `packages/world/collision`; rigid-body and constraint behavior belongs to `packages/runtime/physics`.
- PVS/PAS expansion, area-portal state, occlusion decisions, leaf queries, and culling policy belong to `packages/world/visibility`.
- Geometry buffers, lightmap atlases, world-light evaluation, light-style composition, overlay/decal projection, cubemap sampling, sky passes, fog pixels, water rendering, prop lighting, and GPU resources belong to `packages/presentation/rendering`.
- TF2-, CS:S-, and legacy Source 1 CS:GO-specific entities, static-prop flags, objectives, movement, gameplay, and presentation mappings belong to their game modules.
- Derived-cache envelope bytes, object descriptors, graph validation, SHA-256 object identity, retention, storage, synchronization, channels, and publication belong to `packages/asset-store`; command orchestration belongs to `tools/*`. Neither owner can make publication a prerequisite for direct browser compilation.

## Behavior Families

### World environment output checkpoint

| Source behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Worldspawn and selected lighting profile identify 2D sky; each required sky face and cubemap sample resolves through one exact profile-qualified dependency path. | `compile_environment` emits six sky faces in `rt`, `lf`, `bk`, `ft`, `up`, `dn` order, all BSP cubemap identities, nearest/declared sample selection, six sampled faces across retained mips, and `MissingDependency` on an absent exact response without another request. | Exact `jump_beef` compilation retains `sky_day01_01`, three 32×32/seven-source-face/six-mip samples, six selected HDR sky responses, and nearest sample 1 for the fixed camera. Synthetic tie, declared, empty, duplicate-response, and missing-response vectors pass. | Ready |
| Water-bearing selected and unselected face profiles, leaf-water records, leaves, materials, and LOD controls form render-neutral surface and volume occurrences. | Both face profiles retain source identity and selected state. Each warp surface and leaf-water volume carries material state, oriented plane, finite bounds, member leaves, and reflection/refraction/environment requests. `classify_leaf_height` returns only `Outside`, `Above`, or `Below`. | Exact compilation emits two materials, 16 profile-qualified surfaces, eight selected surfaces, one volume, leaves `663,675,886,911`, bounds `[-5216,2304,-2416]..[-4448,3792,-2160]`, plane Z `-2160`, and LOD `1000..2000`; synthetic boundary facts pass. | Ready |
| Static/dynamic `infodecal`, standard overlay, and water-overlay inputs project onto eligible BSP model faces. | Entity decals preserve activation/parent/material/target state and clip material-sized quads to eligible coplanar model faces. Version-0 standard/water overlays retain all projection inputs and clip to declared faces. Missing material and ineligible receiver remain per-record dispositions; malformed records fail whole. | Exact compilation emits 39 records: 34 projected, four ineligible, one missing, 62 fragments, and 248 vertices; ordered geometry identity is `80420c17805c47b733fa473566fc19df81cb8ed80a416992d489e6517f608811`. Synthetic convex clipping passes. | Ready |
| Fog, sky-camera, environment-light, shadow, tone-map, and water-LOD entities retain profile-sensitive consumer inputs. | Ordered controller output has typed finite state plus every raw key/value field; sky-camera area is a Visibility join and selected lighting provenance retains the complete member closure. No exposure, tone mapping, fog pixels, or game policy executes. | Exact compilation emits environment-light entity 170 and water-LOD entity 360 and no fog/sky-camera/shadow/tone-map record. Exact profile counts and malformed typed-field vectors are retained. | Ready |

The active migration checkpoint implements immutable Source-space world geometry preparation from typed BSP faces, edges, surfedges, vertices, compiled primitives, normals, models, texinfo/texdata strings, and explicit LDR/HDR lighting inputs; preserves BSP face/model/material identity; normalizes every nondegenerate triangle to the supplied face-vertex normal convention required by counter-clockwise front-face consumers; derives direct renderer position/normal/UV/lightmap inputs without GLB; and emits exact material dependency identities required by `jump_beef`.

The active HDR compiler profile checkpoint selects one categorical face, RGBExp32 lighting, world-light, leaf-ambient, detail-prop-lighting, and static-prop-lighting input set; validates style/sample closure before output; serializes linear HDR radiance plus flat/directional/SSBump classifications in one bounded native/WASM artifact; and includes profile, source hashes, compiler identity, configuration identity, dependency hashes, and output role in its derived identity. Missing selected members return `IncompleteLightingProfile`; no profile substitutes for another. Exposure, tone mapping, blending, and canvas transfer remain excluded.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Preserve the Source XYZ frame, one-inch units, degree/QAngle convention, nominal coordinate interval, brush-model bounds, and sky-excluding world gameplay bounds without transforming or clamping values. | No Map implementation exists. | Spatial-frame vectors: compare axis labels, unit scale, angle order, all bound bit patterns, and exact rejection classifications against fixed SDK-derived and synthetic boundary inputs. | Not started |
| Bind one `{game,contentBuild,map}` identity to one BSP profile, version, revision, provenance record, exact BSP hash, worldspawn metadata record, build-configuration hash, and compiler-behavior identity. | No canonical map identity or metadata output exists. | Provenance recomputation: independently hash every fixed input and configuration, then compare the complete identity tuple and mutate each field to require exact `IdentityMismatch` or `HashMismatch` output. | Not started |
| Emit exactly the accepted section identities and ownership assignments in [`inventories/map-sections.md`](inventories/map-sections.md), including an explicit zero-item disposition for every valid empty section. | The section inventory is Candidate; no generator, declared-content scan, or accepted producer contract exists. | Inventory reproducibility: run the checked-in generator twice, require byte-identical output and exact set equality with producer registries and declared-content discoveries. | Blocked |
| Coordinate world faces, edges, surfedges, vertices, normals, primitives, material references, model ownership, and non-rasterized faces by source identity without making GLB or render geometry canonical. | Native Map compilation preserves every source face/model/material identity and emits direct position/normal/UV arrays plus encoded/fan triangle topology and draw disposition. Each nondegenerate triangle swaps its second and third indexes only when its geometric normal opposes the summed supplied vertex normals; source records and zero-area triangles remain unchanged. | Exact LDR/HDR `jump_beef` builds produce 3,793 faces, 16,291 face vertices, and 8,705 source triangles; the four known zero-area triangles remain for Rendering classification. Rust signed-surfedge/face-side vectors and runtime verification require every 6,497 drawable model-0 triangle to face its supplied vertex normals. | Partial |
| Coordinate every displacement with its parent face, source sample ranges, neighbor identities, allowed vertices, triangle tags, material, lighting, collision, and visibility references without taking those child semantics. | No displacement map representation exists. | Displacement join-table comparison: compare every source index and referenced range exactly, including duplicate parent, orphan, unsupported power, missing sample stream, and boundary-count vectors. | Blocked |
| Preserve brush model `0` as world and every submodel as one stable `*N` identity with face range, head node, bounds, origin, entity references, collision reference, and visibility policy reference. | No brush-model assembly exists. | Brush-model graph vectors: compare all model fields and `*N` joins exactly; mutate model zero, face ranges, head nodes, duplicate entity references, and out-of-range indexes. | Blocked |
| Select exactly explicit `ldr` or `hdr` map data, require the profile-specific complete set, retain valid unlit LDR maps, and never automatically substitute the other profile. | The compiler selects fixed LDR slots `7/8/15/52/56` or HDR slots `58/53/54/51/55`, requires selected face/sample/world-light/ambient/map-flag members, validates their declared versions, and returns `IncompleteLightingProfile` without consulting the other set. Optional detail/static members are explicit zero-item records; encountered unsupported static-prop lighting fails. General unlit-map policy remains incomplete. | Synthetic dual-profile, missing-HDR-world-light, malformed sample-range, style, and bound vectors pass. Exact WASM mutation of HDR world-light slot 54 returns error `6` while complete LDR remains present. | Partial |
| Coordinate selected world lights, face lightmap sample ranges, ambient samples, static/detail prop lighting references, and all light-style identities without evaluating illumination. | Map emits source slot/game-lump identities, encoded/decoded hashes, all face style bytes, checked style-major flat/four-layer ranges, Material-derived normal/SSBump targets, complete world-light records, checked leaf-ambient ranges/cubes, map flags, and target detail/static-prop dispositions. HDR RGBExp32 samples and ambient cubes become finite linear RGB binary32 without presentation transfer. | Exact `jump_beef` HDR evidence retains 3,793 faces, 3,896,843 samples, 2,984 lightmapped faces, 1,511 directional SSBump faces, 73 world lights, 1,899 ambient indexes, 9,014 ambient samples, zero detail/static props, and linear channels below/equal/above one with maximum `50.94902420043945`. Broader declared maps remain required. | Partial |
| Join every map material occurrence to one current material output and every surface-property occurrence to its material-owned identity without interpreting shaders or physical effects. | Map resolves exact texdata/string-table identities and each face's texinfo/material index, producing 14 exact VMT requests. Final Material output/provenance joins occur in the compiler. | Exact face/material index validation passes; semantic/hash joins remain incomplete. | Partial |
| Join model references, static-prop instances, and detail-prop model/sprite instances to exact model or material artifacts while retaining source indexes, transforms, leaf memberships, skins, solidity metadata, flags, fades, lighting references, and empty occurrence. | BSP and StudioModel roadmaps are Not accepted; detail-prop and Map implementations do not exist. | Prop occurrence matrix: compare dictionary order, instance fields, leaf ranges, model/material hashes, selected VHV identity, detail lighting, and exact Missing/Unsupported outcomes. | Blocked |
| Coordinate entity decals plus compiled overlays and water overlays with source order, material, target face/model identities, render order, fades, activation metadata, and projection input references. | The environment finalizer implements bounded entity-decal, standard-overlay, and water-overlay records and geometry. Displacement projection and runtime decal activation/removal remain outside this checkpoint. | Exact target and synthetic clipping evidence above. | Partial |
| Coordinate every cubemap sample with source index, integer origin, size, selected lighting profile, and exact map-owned cubemap texture logical path without default-texture substitution. | Exact profile-qualified dependency requests/responses, source metadata, six sampled faces, all mip count, stable nearest tie, and declared selection are implemented. | Exact three-sample inventory and missing/duplicate/tie vectors pass. | Ready |
| Coordinate 2D sky, 3D sky, fog, water, ropes, and environment-controller records with the entity, material, geometry, lighting, collision, and visibility identities consumed by child semantic owners. | 2D sky, sky-camera/fog/environment-light/shadow/tone-map/water-LOD occurrences, water, lighting provenance, and Visibility area joins are implemented. Rope occurrence and broader class inventories remain absent. | Exact target environment inventory plus typed synthetic controller/water vectors. | Partial |
| Reference one current entity graph, collision world, and visibility representation derived from the same assembly-plan identity and BSP hash and preserve their stable entity, brush-model, face, leaf, area, portal, cluster, and prop identities. | Entity, Collision, and Visibility roadmaps are Not accepted, expose no implementation, and do not yet name the finalized Map assembly-plan identity. | Cross-owner root comparison: independently recompute each semantic root's plan identity and BSP hash and compare all exported join tables; mismatch any owner, hash, profile, or source index and require exact failure. | Blocked |
| Validate every local and cross-section reference for one owner, existing target, accepted section kind, allowed edge, matching source identity, and duplicate policy before dependency resolution. | No map graph validator exists. | Referential-integrity mutation suite: delete, duplicate, reorder, retarget, and cross-profile every edge type; compare the full ordered diagnostic set and require no root output. | Not started |
| Classify every unresolved exact dependency as `Missing`, retain its requested role and logical path, and fail the map without searching, probing, or substituting another artifact. | Environment finalization requires one exact response for every selected sky/cubemap request and returns the first role/path-qualified `MissingDependency` or duplicate/type `DependencyMismatch`. Other section families remain uncoordinated. | Missing, duplicate, wrong-type, profile-path, and no-sample vectors prove no fallback request. | Partial |
| Reject every encountered unaccepted section, producer classification, record variant, or child behavior as `Unsupported` or `Unknown`; never omit it or emit a partial canonical map. | No coverage gate exists. | Coverage-classification vectors: inject each non-ready classification at every section boundary, compare the retained owner evidence and exact Map diagnostic, and require absent canonical/root outputs. | Not started |
| Validate request, hashes, producer identities, section presence, local identities, cross-references, dependency closure, coverage, and root completeness in that exact stage order under all caller limits. | No staged validator, diagnostic contract, or limit accounting exists. | Stage-permutation and allocation instrumentation: place one fault at each stage and each limit minus one, equal, and plus one; compare diagnostics and prove no later callback or allocation executes after a failed prerequisite. | Not started |
| Freeze one complete `MapRuntimeDescriptor` whose identity and sorted closure include every consumed raw Source object and derived object required to load all 27 sections; successful first-load execution does not require prior publication. | Schema `4` adds a bounded `PSHD` profile descriptor with output/compiler/configuration/BSP/lighting identities, ten selected-member records, per-face sample classifications, world/ambient records, prop disposition, six selected profile materials with parsed VTF metadata and retained bytes, and 131 sorted dependency hashes. The other canonical sections remain incomplete. | Direct exact-map compilation requires no Asset Store or published root; the verifier consumes every schema-4 byte and rejects framing, reserved-byte, count, hash, role, shader, format, and trailing-byte changes. | Partial |
| Produce identical assembly plans plus byte-identical map-owned artifacts, section descriptors, traces, reports, and runtime descriptors from identical input hashes, build configuration, and compiler behavior across repeated native and WASM runs. | One pure Rust serializer owns invariant map/entity bytes and appends supplied owner-resolved material/texture/model outputs by canonical indexes. Schema `3` LDR remains unchanged; schema `4` HDR uses a profile-specific compiler/output identity and returns a separate derived SHA-256. The WASM ABI exposes generation-checked payload, payload-hash, derived-hash, and disposal operations. | The 131-entry, 44,133,575-byte bundle leaves LDR at 39,814,462 bytes and SHA-256 `d0576dff06413848d8712ab6218c8c6f34078a1b347795c5a7a694a108c29725`. Two clean HDR calls emit identical 75,972,411-byte payloads at `22d668cbeac17167d1826efa7fc45e218640ce739cb48bd37f76fd67d289cb80` and derived key `ba166bcc5a7909f3336560d9f19a0a50d26a09f8671447fd5bdda3088b7b5c38`, distinct from LDR key `9393bcbfe6271f11aea392a8befd3cac7c4c047952109d4aa58b82a9694748ff`. Complete section evidence remains absent. | Partial |
| Ensure BSP, content, material, StudioModel, entity, collision, visibility, rendering, games, tools, and Asset Store consume or produce the sole current map identities without a duplicate assembler, GLB authority, fallback, compatibility layer, or stale section inventory. | Required producers and consumers are absent or Draft; no integration exists. | Current-interface integration audit: execute one fixed map through every named producer and consumer, compare root hashes and identities, and search the repository for duplicate assembly, direct BSP joins, fallback, compatibility, and stale generated output. | Blocked |

## Generated Inventories

No generated Map inventory is accepted. Accepted inventory item count: 0.

| Output | Authority identity | Authority revision | Generator command | Candidate items | Accepted items |
|---|---|---|---|---:|---:|
| [`inventories/map-sections.md`](inventories/map-sections.md) | playsrc Owner Registry and Map boundary; official SDK map records and game-side consumers; accepted producer registries; exact declared-content map indexes | playsrc `8ad1ca705dab2ec23c7d1deaf0ee84c5342357f9`; SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; producer registries and content indexes Missing | Missing | 27 | 0 |

The future generator is owned by `tools/playsrc`. It must consume machine-readable current-contract section and producer declarations plus exact archive indexes for one declared TF2, CS:S, and legacy Source 1 CS:GO content build; emit the inventory in stable section order; retain empty, malformed, unsupported, unknown, and missing discoveries; and fail on an unassigned section, producer, item-identity rule, dependency role, artifact media type, cross-section edge, lighting-profile member, or coverage disposition. No command name is declared before that implementation exists.

## Exit Criteria

The Map package is Complete only when all of these predicates pass:

- All 21 behavior rows are Ready.
- The 27-section inventory has an Accepted denominator review with current authority revisions, generator command, output path, item count, reviewer identity, review date, and reviewed commit.
- Exact TF2, CS:S, and legacy Source 1 CS:GO content-build indexes are generator and evidence inputs, and every encountered section, producer, dependency role, record variant, profile member, and artifact media type has one classification and owner.
- Every canonical map contains exactly 27 section descriptors; every section and item is `Handled` or `Intentionally inert`; no required value is Unsupported, Unknown, Missing, Malformed, Partial, or Blocked.
- Every fixed map passes spatial, identity, lighting-profile, cross-reference, dependency-closure, coverage, limit, immutable-root, and repeated-output comparisons.
- Every dependency and map-owned artifact retains exact logical identity, provenance, byte length, SHA-256, build-configuration hash, and compiler-behavior identity.
- Every required producer and consumer uses the current Map interface and stable identities; browsers can compile from a declared BSP source without prior native preprocessing; GLB and other export formats remain optional derived outputs only.
- No duplicate assembler, mandatory prebuilt map root, fallback, compatibility layer, legacy path, unbounded operation, or stale inventory remains.

## Blockers

- **Section inventory generator and authority inputs:** no checked-in command emits [`inventories/map-sections.md`](inventories/map-sections.md), and no exact TF2, CS:S, or legacy Source 1 CS:GO map index is configured. Checked `tools/`, `playsrc.local.example.json`, the Missing `playsrc.local.json`, the root Owner Registry, the assigned Map package, and current format inventories.
- **Producer contracts:** Material, Entity, Collision, and Visibility now have Candidate or Not accepted public roadmaps; BSP and StudioModel are Draft or Not accepted; none exposes a current implementation. Exact accepted semantic output fields, artifact media types, item registries, and cross-section edge contracts are therefore unavailable. Checked every named owner README, current roadmap, inventory, and the root Owner Registry.
- **Rendering consumer contract:** Rendering has no public roadmap, current interface, or integration evidence proving it consumes Map-owned lighting, decal/overlay, cubemap, sky, fog, water, rope, and controller references without becoming another map assembler. This blocks integration readiness but does not make rendering output an input to canonical Map construction.
- **Staged producer seam:** Collision and Visibility currently request immutable assembly values from Map while final Map construction requires their semantic outputs. This roadmap defines a non-runtime `MapAssemblyPlan` to break that cycle, but the Collision and Visibility roadmaps do not yet name its identity, typed view, or preparation/finalization ordering. All three roadmaps must adopt the same seam before integration can proceed.
- **Derived-cache dependency:** the Asset Store roadmap and object-kind inventory are Draft, with 0 accepted inventory items. Their source-object, derived-object, and optional map-root contracts must accept direct first-load compilation, browser IndexedDB cache identity, and server-populated cache equivalence before cache integration can become Ready.
- **Declared-content occurrence:** without exact archive indexes, occurrence and required empty-state behavior cannot be established for HDR profile members, detail props and lighting, static-prop VHV data, cubemap textures, 2D and 3D sky, water, ropes, environment controllers, and coordinates outside the nominal interval. Filesystem discovery was not attempted.
