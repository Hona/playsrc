# Collision Roadmap

[`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines delivery status, coverage classification, inventory, and completion terms.

## Completion Denominator

This leaf roadmap contains exactly 29 behavior rows. The candidate inventories contain 174 items: 76 collision shape, query, filter, solid, flag, and bounds-policy items in [`inventories/shapes-and-queries.md`](inventories/shapes-and-queries.md), and 98 contents, mask, surface, displacement-flag, and shared collision-group items in [`inventories/contents-and-masks.md`](inventories/contents-and-masks.md). They contribute 0 denominator items until checked-in generators emit them and a denominator review accepts them. The current completion denominator is therefore 29 behavior rows. Acceptance of all 174 candidate inventory items without a roadmap change would make the denominator 203 items.

The denominator covers immutable collision representations and deterministic spatial queries required by Team Fortress 2, Counter-Strike: Source, and legacy Source 1 Counter-Strike: Global Offensive. It includes world brushes, transformed brush models, displacements, static props, entity boxes and custom adapters, triggers, StudioModel hitboxes, PHY convex compounds, contents, masks, surfaces, point contents, finite rays and lines, AABB and convex sweeps, overlaps, stateless geometric contacts, filtering, initial overlap, exact result identity, broad phase, narrow phase, malformed input, numerical bounds, concurrency, and bounded performance. It excludes rigid-body advancement, persistent physics contacts, player movement decisions, damage, entity lifecycle, visibility, rendering, and Source 2.

The denominator is Not accepted. Both inventories lack checked-in generators and review records; declared content-build indexes are Missing; stateless contact ownership conflicts with the current physics README; exact equal-hit ordering lacks a retained target comparison; and numeric performance budgets have not been selected.

## Inputs

- Immutable canonical map collision inputs supplied by `packages/world/map`: ordered BSP world and brush-model identities; brush planes, sides, bevel markers, contents, and surfaces; compiled displacement triangles and tags; static-prop instances; and map-wide stable identities. The collision package does not parse BSP bytes or assemble unrelated map domains.
- Immutable decoded PHY inputs supplied by `packages/formats/phy`: ordered solids, convexes, vertices, faces, game data, per-triangle material metadata, contents inputs, and source identities. The collision package does not decode PHY bytes.
- Immutable StudioModel inputs supplied by `packages/formats/studio-model`: selected hitbox set, ordered hitboxes, bones, bone contents, physics-bone indexes, surface-property identities, and validated model references. A query using hitboxes also supplies one immutable ordered bone-transform snapshot.
- One dynamic collision snapshot containing a monotonic snapshot identity and ordered records with stable entity or prop identity, enabled state, collision transform, linear and angular velocity metadata, solid type, solid flags, surrounding-bounds policy, contents, surface identity, collision-group integer, optional trigger bounds, and optional custom query adapter. Collision reads the snapshot and never advances it.
- One query request selecting exactly one operation and query shape from [`inventories/shapes-and-queries.md`](inventories/shapes-and-queries.md), a 32-bit contents mask, one trace scope, trigger selection, ordered ignored identities, an optional pure custom predicate, an optional pure game-owned collision-group predicate, and the dynamic snapshot identity.
- One required limits record containing `maxStaticShapes`, `maxDynamicShapes`, `maxConvexesPerShape`, `maxPlanesPerConvex`, `maxVerticesPerConvex`, `maxTrianglesPerShape`, `maxBvhNodes`, `maxBvhDepth`, `maxCandidateVisits`, `maxNarrowPhaseTests`, `maxOverlapResults`, `maxContactPairs`, `maxContactPoints`, `maxCustomPredicateCalls`, `maxQueryScratchBytes`, and `maxConcurrentQueries`. Every field is a non-negative integer; no build or query has an unbounded mode.
- Source-space geometry coordinates in `[-16384, 16384]`, query positions in `[-32768, 32768]`, and finite query lengths no greater than `MAX_TRACE_LENGTH = 1.732050807569 × 32768`. A query outside these bounds is Malformed and does not run broad phase.
- Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`: `src/public/cmodel.h`, `trace.h`, `gametrace.h`, `bspflags.h`, `const.h`, `coordsize.h`, `worldsize.h`, `collisionutils.{h,cpp}`, `dispcoll_common.{h,cpp}`, `vphysics_interface.h`, `engine/ICollideable.h`, `engine/IEngineTrace.h`, and `engine/IStaticPropMgr.h`; `src/public/bone_setup.cpp`; `src/game/shared/collisionproperty.{h,cpp}`, `util_shared.{h,cpp}`, and `gamemovement.cpp`; and `src/game/shared/tf/tf_shareddefs.h`, `tf_gamerules.cpp`, and `tf_gamemovement.cpp` for the game-owned TF2 seams.
- Exact archive indexes, parsed collision inputs, and controlled target observations for one accepted content build each of TF2, CS:S, and legacy Source 1 CS:GO. These inputs are Missing.

## Outputs

- An immutable `CollisionWorld` containing the selected content build, coordinate contract, static shape table, static broad phase, stable source associations, exact 32-bit contents, surface identities, and build diagnostics. Dynamic state is never copied into this immutable value.
- One validated dynamic collision snapshot view and deterministic dynamic broad-phase index keyed by its snapshot identity. Rebuilding or refitting this index cannot mutate the snapshot.
- A `PointContentsResult` containing the combined unsigned 32-bit contents, ordered contributing shape identities, and the selected target-compatible entity identity when that operation requests one.
- A `TraceResult` containing query identity, requested start and end, result start, fraction, optional leave fraction with an explicit validity flag, end position, plane normal and distance with an explicit validity flag, contents, displacement flags, surface name identity, surface-property identity, surface flags, hit shape, hit entity, static-prop identity, brush/model/convex/triangle feature identity, hitbox, hitgroup, physics bone, `startSolid`, `allSolid`, and `initialOverlap`.
- An `OverlapResult` containing ordered shape and feature identities with contents, surface, and entity associations. A caller-supplied maximum is enforced before adding each result; overflow returns a limit error and no partial authoritative result.
- A stateless `ContactResult` containing ordered shape pairs and bounded contact sets. Each contact set contains one oriented normal, non-negative depth, ordered points, contents on both sides, surface identities, and source feature identities. No impulse, friction state, solver constraint, or persistent contact handle is emitted.
- A structured error containing operation, query identity, snapshot identity, stable input identity, violated invariant, offending value, configured limit, and exactly one `Malformed`, `Unsupported`, `Unknown`, or `Missing` classification.

## Invariants

- All public positions, distances, bounds, planes, normals, and velocities use Source XYZ coordinates and inches. Shape construction performs no game, rendering, transport, or asset coordinate conversion.
- Every static and dynamic shape has one stable identity. Broad-phase nodes, cache slots, allocation addresses, thread schedules, and traversal stacks never become public identity or tie-break inputs.
- Contents and masks are unsigned 32-bit bitsets. Every named value in [`inventories/contents-and-masks.md`](inventories/contents-and-masks.md) retains its exact bits; bit 31 remains observable and Unknown unless a future accepted authority assigns it.
- A candidate enters narrow phase only when its enabled/solid/trigger state, query scope, ignored-identity set, contents intersection, game-owned collision-group predicate, and custom predicate all accept it in that order. A rejected candidate cannot affect result ordering or counters other than declared diagnostics.
- An AABB or hull retains caller mins and maxs. The centered broad-phase form is derived as `(mins + maxs) / 2` and `(maxs - mins) / 2`; result positions remain relative to the caller's original reference point.
- A no-hit trace has `fraction == 1`, `endPosition == requestedEnd`, `startSolid == false`, `allSolid == false`, no valid plane, and no hit identity. A non-initial hit has `0 <= fraction < 1`, `endPosition == requestedStart + fraction × (requestedEnd - requestedStart)`, and a valid unit plane facing against entry motion.
- `startSolid` means the query shape overlaps at least one accepted target at the requested start. `allSolid` means the selected target operation cannot find a non-overlapping query position on the complete requested segment. `allSolid` implies `startSolid` and `fraction == 0`.
- Leave fraction is valid only for query/shape families whose accepted target contract defines it. Invalid leave fractions are never represented as meaningful zero. When valid, `0 <= leaveFraction <= 1` and its position is derived from the original requested segment.
- Brush and displacement entry calculations use the accepted family epsilon. Source brush and displacement profiles use `DIST_EPSILON == DISPCOLL_DIST_EPSILON == 1/32` inch; box, hitbox, and PHY profiles retain their separately accepted tolerance. One family never silently borrows another family's epsilon.
- Tangency, coplanarity, shared edges, shared vertices, seams, zero motion, zero extents, thin solids, acute corners, and equal fractions have categorical outcomes in evidence. Epsilon is applied to signed separation tests and never by moving stored geometry.
- Surface name, surface-property identity, surface flags, displacement flags, contents, hit entity, static-prop identity, hitbox, hitgroup, physics bone, and feature identity come from the selected narrow-phase hit. A broad-phase bound or fallback box cannot supply final hit metadata.
- Static world geometry is tested before eligible dynamic geometry. Every remaining equal-hit choice uses the accepted stable tie order. The result is independent of insertion order except where the accepted target comparison explicitly establishes source order as observable.
- Moving geometry is queried at one supplied snapshot transform. A query never interpolates, predicts, advances, or samples an entity transform; a caller requiring a continuous relative-motion query supplies start and end transforms through the moving-geometry operation.
- Broad-phase omission of a shape whose narrow phase would pass is a failure. Broad-phase false positives are permitted and must be removed by narrow phase. Running exhaustive narrow phase over the same accepted candidates must produce the same observable result.
- Every point, scalar, plane, transform, bound, count, index, product, sum, recursion step, candidate visit, predicate call, result, contact point, and scratch allocation is validated against finite arithmetic and the required limits before use or allocation.
- A failed world build emits no `CollisionWorld`. A failed query emits no partial authoritative result. Diagnostic candidate counts and ranges cannot be consumed as collision truth.
- `CollisionWorld` and shape records are immutable and shareable. Each query owns or borrows disjoint bounded scratch state. Concurrent queries over identical world, snapshot, request, and limits produce bit-identical ordered outputs without locks that serialize read-only narrow phase.

## Ownership Exclusions

- BSP headers, lump records, game-lump records, branch layouts, compression, and lossless bytes belong to `packages/formats/bsp`. Collision consumes typed records and does not become another BSP parser.
- PHY headers, collision payloads, convex record decoding, keydata syntax, and model association belong to `packages/formats/phy`. Collision consumes validated runtime-neutral convexes.
- MDL/VVD/VTX/ANI parsing, hitbox record decoding, skeleton construction, and animation data belong to `packages/formats/studio-model`. Collision consumes selected hitbox records and supplied bone transforms.
- Map-wide model, entity, prop, displacement, material, and surface reference assembly belongs to `packages/world/map`. Material and surface-property meaning belongs to `packages/world/material`; collision retains only supplied identities and flags.
- Entity enablement, transforms, parenting, custom behavior, trigger lifecycle, touch phases, mover scheduling, rollback, blocking, and crush decisions belong to `packages/world/entity`, `packages/runtime/simulation`, and the selected game. Collision answers snapshot queries only.
- Player hull selection, ground classification, stair choice, velocity clipping, water-level decisions, movement caching, and command advancement belong to `packages/runtime/movement` and the selected game. Movement consumes collision results without redefining collision.
- Rigid-body integration, persistent contact creation and deletion, solver manifolds, friction, restitution, constraints, sleeping, and collision response belong to `packages/runtime/physics`. Collision owns only immutable shapes and stateless geometric query results after the contact-query boundary is resolved.
- Shared collision-group integers are retained by collision. `TF_COLLISIONGROUP_GRENADES`, `TFCOLLISION_GROUP_OBJECT`, `TFCOLLISION_GROUP_OBJECT_SOLIDTOPLAYERMOVEMENT`, `TFCOLLISION_GROUP_COMBATOBJECT`, `TFCOLLISION_GROUP_ROCKETS`, `TFCOLLISION_GROUP_RESPAWNROOMS`, `TFCOLLISION_GROUP_TANK`, `TFCOLLISION_GROUP_ROCKET_BUT_NOT_WITH_OTHER_ROCKETS`, TF2 team aliases, every game-specific group extension, and every group-pair decision belong to `games/tf2`, `games/css`, or `games/csgo`.
- Hitgroup damage meaning, team barriers, projectile policy, class filters, owner filters, and game-specific contents policy belong to the selected game. Collision invokes supplied pure predicates and returns identities.
- Visibility, PVS, areas, areaportals, and culling belong to `packages/world/visibility`; pixels and debug drawing belong to `packages/presentation/rendering` and tools.
- Inventory generation, collision inspection commands, captures, benchmarks, and publication belong to `tools/*` and `packages/asset-store`. Source 2 collision and physics are excluded.

## Behavior Families

The active migration checkpoint implements one immutable Source-space brush collision world from BSP planes, brushes, brush sides, leaves, leaf-brush indexes, nodes, and models; validates all encoded ranges; and exposes deterministic point/hull sweeps with contents masks, start-solid/all-solid state, hit fraction, brush identity, and contact plane required by `jump_beef` movement.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Every required collision representation, query shape, operation, filter, solid selector, flag, and surrounding-bounds policy has one stable item in [`inventories/shapes-and-queries.md`](inventories/shapes-and-queries.md). | The inventory is manually derived, has no generator, and is Not accepted. | Generated-inventory audit: compare retained SDK declarations and exact declared-build collision indexes with stable inventory identities, counts, classifications, and owner assignments; require no unclassified occurrence. | Blocked |
| World brushes construct bounded convex half-space shapes from exact planes, sides, bevel markers, contents, and surface identities without using render triangles as collision. | Native compilation retains every plane/side/brush identity, bevel/displacement/texinfo field, contents, leaves, nodes, and models; validates all encoded ranges; and limits world traces to the unique brushes reachable through model 0's head-node leaves. Surface names remain a later Material join. | Synthetic six-plane convex isolates its model-0 brush; exact `jump_beef` movement advances in native and WASM sessions without treating inline-model brushes as world solids. | Partial |
| Non-world brush models retain their own brush sets and answer queries at supplied translated and rotated transforms, including zero-render-face collision models. | No brush-model collision implementation exists. | Fixed inline-model vectors at identity, translation, every axis rotation, compound brush seams, zero render faces, and inverse transforms; compare world-space fractions, normals, contents, surfaces, and model/brush identities. | Not started |
| Compiled displacement patches answer point/ray, hull, overlap, and initial-overlap queries using their displaced triangles, triangle flags, contents, and surface-property selection. | No displacement collision implementation exists; the integrated target result for displacement hull `startSolid` and zero-motion overlap is not established. | SDK displacement-oracle vectors over powers 2 through 4, both triangle parities, holes, walkable/buildable/property flags, one- and two-sided rays, AABB sweeps, seams, and start overlap; compare fraction, plane, flags, contents, and selected triangle exactly or within declared binary32 tolerance. | Blocked |
| Static props classify `SOLID_NONE`, `SOLID_BBOX`, and `SOLID_VPHYSICS`, preserve prop identity, and query the selected box or PHY compound at the exact instance transform and scale. | No static-prop collision implementation exists. | Declared-map and synthetic prop sets covering all three modes, rotated/scaled instances, absent PHY, multiple convexes, contents filters, equal hits, and point contents; compare trace results and prop identity with controlled target observations. | Not started |
| Dynamic collideables select none, BSP, AABB, OBB, yaw OBB, custom adapter, or PHY compound behavior from the supplied solid type, flags, transforms, and bounds policy. | No dynamic collideable representation exists. | One vector per accepted solid type, flag, and bounds policy plus every incompatible combination; compare selected narrow phase, world bounds, custom-adapter calls, result metadata, and exact classified errors. | Not started |
| Trigger shapes use their ordinary brush or separate expanded trigger bounds only in trigger-selected overlaps and enumerations; ordinary solid traces never acquire trigger effects. | No trigger collision representation or query path exists. | Fixed brush and bounds triggers with ordinary/non-solid states, asymmetric and uniform expansion, debris flag, enabled changes, and solid/trigger/both scopes; compare overlap identity and prove zero entity lifecycle calls. | Not started |
| StudioModel hitbox queries filter ordered transformed OBBs by bone contents and return hitbox, hitgroup, physics bone, hitbox surface flag, contents, surface property, and entity identity. | No hitbox collision implementation exists. | Fixed multi-hitbox models with contents inclusion/exclusion, scaled and unscaled poses, equal hits, initial overlap, ray and hull inputs, and invalid bone transforms; compare every trace field with SDK `TraceToStudio`/`SweepBoxToStudio` outputs. | Not started |
| PHY convexes and compounds preserve solid/convex/triangle identity, per-convex contents, material metadata, transformed support points, bounds, and closest-hit selection without creating rigid bodies. | No PHY-derived query shape exists; the BSP and PHY roadmaps disagree on whether runtime physics or PHY supplies decoded payloads and whether physics or collision constructs query shapes. | Fixed decoded PHY shapes covering one and many convexes, rotations, scales, duplicate support points, per-convex contents, material indexes, and equal hits; compare support, bounds, ray, sweep, and identity output with `IPhysicsCollision` observations. | Blocked |
| Point contents combines world leaf/brush contents and selected volume-shape contents under exact transform, current, static-prop, and entity-identity rules. | No point-contents implementation exists; overlapping volume-entity selection and current-bit normalization lack a retained target comparison. | Grid and boundary vectors sample brush faces, overlapping water/slime/current volumes, brush models, volume entities, static props, empty space, and world exterior; compare unsigned contents and target-compatible selected identity exactly. | Blocked |
| Finite ray traces return the closest accepted world, prop, entity, hitbox, displacement, or PHY hit under the selected scope and filters. | No ray-trace implementation exists. | Fixed rays at zero and `MAX_TRACE_LENGTH`, axis/diagonal/coplanar/tangent rays, every shape family, and world-before-dynamic competition; compare the complete trace record with controlled target observations. | Not started |
| Line traces use exact start/end parameterization and produce the same result as the equivalent finite ray without direction-normalization drift. | No line-trace interface exists. | Paired line/ray vectors over short, long, reversed, zero-length, sub-coordinate-resolution, and maximum-length segments; require exact classifications and binary32-equal fractions/end positions where the target emits equal bits. | Not started |
| AABB and hull sweeps preserve asymmetric mins/maxs and answer box, standing hull, crouched hull, and zero-motion position tests across every accepted target shape. | Brush sweeps derive center/half-extents once, preserve the caller reference point, and compute point/hull entry across every matching brush. | Point, centered hull, start-inside, and invalid-hull vectors pass. Standing/crouched/asymmetric and other shape families remain incomplete. | Partial |
| A validated convex or compound can be swept against one shape or the selected world at fixed supplied orientations, including a zero-motion overlap. | No arbitrary collideable-sweep implementation exists. | Convex-pair vectors covering face/edge/vertex impacts, compounds, rotations, near misses, zero motion, initial overlap, and iteration limits; compare fraction, normal, contents, and stable feature identity with fixed `TraceCollide` observations. | Not started |
| Point, AABB, convex, and compound overlap queries return every accepted overlapping shape and feature in stable order without treating broad-phase contact as overlap. | No overlap-query implementation exists. | Exhaustive-small-scene comparison: enumerate every shape pair by brute-force narrow phase and compare ordered overlap outputs with broad-phase-backed queries over boundary, tangent, contained, and disjoint cases. | Not started |
| Stateless contact queries return bounded deterministic geometric contact sets and never expose persistent solver contacts or response state. | `packages/world/collision/README.md` assigns contact queries here while `packages/runtime/physics/README.md` assigns contacts to physics; no agreed interface distinguishes both outputs. | Boundary review plus fixed convex-pair contact vectors comparing overlap state, oriented normal, depth, ordered points, contents, surfaces, limits, and zero calls into rigid-body state. | Blocked |
| All 98 contents, mask, surface, displacement-flag, and shared collision-group identities retain the exact values in [`inventories/contents-and-masks.md`](inventories/contents-and-masks.md). | The inventory is manually derived, has no generator, and is Not accepted. | Generated constant-expansion comparison against the pinned SDK plus declared-build occurrence audit; require all 32 bits represented, every mask expansion exact, and all game-owned aliases excluded from this owner. | Blocked |
| Query filtering applies scope, solid/trigger state, contents mask, ignored identities, owner exclusions supplied as identities, game-owned group predicate, and custom predicate in declared order. | No filter pipeline exists. | Decision-table vectors cross every filter dimension, static-prop mode, and duplicate ignore identity; compare candidate/predicate call order and final hit with SDK filter consumers and selected game integration. | Not started |
| Every hit returns exact contents, displacement flags, surface identity, entity/prop/model/feature identity, hitbox, hitgroup, physics bone, fraction, end position, and valid plane; misses return the canonical empty result. | Brush results expose fraction/end, contents, brush identity, and plane; misses use the canonical fraction/end/empty state. Surface and non-brush identities remain absent. | Synthetic hit/miss fields pass. | Partial |
| Initial overlap distinguishes recoverable `startSolid`, path-wide `allSolid`, zero-motion overlap, and valid leave fraction for each shape/query family. | Brush traces distinguish start-solid and all-solid, but do not expose leave fraction and mixed-family overlap remains unresolved. | A contained point segment produces start-solid/all-solid. | Partial |
| Each shape family applies its accepted epsilon to signed separation and classifies exact touch, near touch, coplanarity, parallel motion, and zero delta categorically. | No numeric-policy implementation exists. | Epsilon ladder at `-2ε`, `-ε`, adjacent binary32 values around zero, `+ε`, and `+2ε` for brush, displacement, box, hitbox, and PHY families; compare classifications, fractions, and normals with family-specific oracles. | Not started |
| Shared brush/displacement seams, 16-unit stairs, diagonal walls, thin solids, acute/obtuse corners, and maximum-speed one-tick sweeps produce stable non-tunnelling collision outputs consumable by movement. | No collision implementation or movement integration exists. | Controlled integration vectors use fixed Source-space scenes and query sequences; compare every collision result first, then movement-owned ground/stair decisions separately, retaining both outputs. | Not started |
| Equal-fraction hits select one exact stable winner across world brushes, displacements, props, dynamic shapes, compound children, and hitboxes. | World-before-dynamic precedence is established, but equal-hit ordering inside and across the remaining shape families has no retained controlled target comparison. | Equal-plane capture matrix: permute construction and insertion order for coincident shapes, compare target and playsrc winner identity/metadata, and require repeated and concurrent equality on each supported architecture. | Blocked |
| Queries against moving geometry use one explicit snapshot or an explicit relative start/end transform pair; broad-phase updates cannot expose mixed revisions. | No snapshot or moving-geometry query implementation exists. | Fixed translated and rotated model sequences with revision barriers, simultaneous readers, support carry inputs, side contacts, and transform rollback owned by the consumer; compare query outputs and prove each result names one snapshot identity. | Not started |
| Static and dynamic broad phases return bounded candidates while exhaustive narrow phase remains the sole final-hit authority. | No acceleration structure exists. | Exhaustive-small-scene differential test plus declared-map candidate audit: compare every query result with acceleration disabled, require no false negative, stable candidate order, and bounded visits at the configured limit. | Not started |
| Malformed shapes, transforms, bounds, indexes, planes, convexes, requests, non-finite values, and arithmetic overflow fail before publication or traversal with one stable classification. | No validator or error model exists. | Property and boundary vectors mutate every scalar, count, index, range, normal, transform, bound, and query field; instrument reads/allocations and compare exact code, identity, offending value, and no partial output. | Not started |
| World construction and every query obey caller limits, and accepted cold-build, warm-query, memory, candidate, and concurrency budgets are numeric and retained as evidence. | Count and scratch-limit fields are defined, but product/device benchmark scenes and numeric time and memory budgets have not been selected. | Checked benchmark command over fixed synthetic and declared-map scenes records build time, p50/p95/p99 query time, peak owned bytes, candidate/narrow counts, and scaling at each limit; compare every measurement with the accepted numeric budget. | Blocked |
| Fixed world, snapshot, request, limits, and platform arithmetic produce bit-identical ordered results across repeats and concurrent readers. | No implementation or concurrency evidence exists. | Repeat each vector 1,024 times sequentially and across `1..maxConcurrentQueries` workers; compare serialized result bytes, classifications, counters, and world immutability hashes exactly. | Not started |
| Map, entity, movement, physics, simulation, games, replay, tools, and inspector consume one current collision interface without duplicate query engines, render-geometry collision, fallback boxes, compatibility schemas, or stale inventories. | No collision package implementation or consumer integration exists. | Integration audit exercises each named producer and consumer through the current interface and repository search proves one collision authority, no embedded duplicate, no fallback, no compatibility layer, no legacy path, and no stale generated artifact. | Not started |

## Generated Inventories

No generated collision inventory is accepted. Accepted inventory item count: 0.

| Output | Authority identity | Authority revision | Generator command | Candidate items | Accepted items |
|---|---|---|---|---:|---:|
| [`inventories/shapes-and-queries.md`](inventories/shapes-and-queries.md) | SDK collision, trace, displacement, hitbox, VPhysics, solid, flag, and bounds contracts plus exact declared-build collision indexes | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; declared content-build indexes Missing | Missing | 76 | 0 |
| [`inventories/contents-and-masks.md`](inventories/contents-and-masks.md) | SDK contents, masks, surfaces, displacement flags, shared collision groups, and selected-game alias registries | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; declared content-build indexes Missing | Missing | 98 | 0 |

Current manually derived candidate count: 174. Current generated count: 0.

The future generator is owned by `tools/playsrc`. It must consume retained SDK authority snapshots, the accepted BSP/PHY/StudioModel inventory identities, and exact archive indexes for the three declared content builds; emit both assigned files in stable-identity order; retain every unsupported, malformed, unknown, missing, inert, and blocked occurrence; verify all expanded constants; and fail on an unclassified shape source, solid mode, solid flag, bounds policy, query operation, contents bit, mask, surface flag, displacement flag, shared group, or game-owned alias. No command name is declared before that implementation exists.

## Exit Criteria

The collision package is Complete only when all of these predicates pass:

- All 29 behavior rows are Ready.
- Both collision inventories have Accepted denominator reviews with current authority revisions, generator command, output paths, item counts, reviewer identity, review date, and reviewed commit.
- Exact TF2, CS:S, and legacy Source 1 CS:GO content-build indexes are generator inputs; every encountered collision source, value, and policy has exactly one classification and one owner.
- Every world brush, brush model, displacement, static prop, entity collideable, trigger, hitbox, PHY convex, legacy prop hull, query shape, operation, filter, contents bit, mask, surface, collision group, result field, malformed input, and limit boundary satisfies its evidence predicate.
- Equal-hit order and initial-overlap behavior are established by retained target comparisons for every accepted shape-family pair.
- Broad-phase and exhaustive narrow-phase results are identical for every fixed vector; no accepted query exceeds its candidate, narrow-phase, scratch-memory, output, or concurrency limit.
- The accepted numeric build, query, memory, and concurrency budgets pass on every declared platform profile.
- Every named producer and consumer uses the current collision interface, and no duplicate query engine, render-geometry substitute, fallback box, compatibility layer, legacy path, or stale generated inventory remains.
- Collision has no dependency on runtime, game, ruleset, application, service, presentation, or renderer packages; game-owned filtering enters only through declared pure inputs.
- No required item remains Blocked, Unsupported, Unknown, Missing, Malformed, or Partial.

## Blockers

- **Inventory generators:** no checked-in command emits either assigned collision inventory. Checked `tools/`, root manifests, and the current collision package tree.
- **Declared content builds:** `playsrc.local.json` and exact TF2, CS:S, and legacy Source 1 CS:GO archive indexes are Missing. Exact occurrence of static-prop modes, PHY compounds, hitboxes, displacement flags, custom collideables, legacy prop hulls, contents values, surfaces, and game-owned aliases cannot enter an accepted generated inventory.
- **Stateless contact ownership:** [`README.md`](README.md) assigns contact queries to collision, while [`../../runtime/physics/README.md`](../../runtime/physics/README.md) assigns contacts to physics. The root owner registry does not distinguish stateless geometric contact output from persistent solver contacts. Both roadmaps must identify collision as owner of the former and physics as owner of the latter before the two contact rows can proceed.
- **PHY shape producer ownership:** [`../../formats/bsp/ROADMAP.md`](../../formats/bsp/ROADMAP.md) assigns VPhysics payload decoding and collision-shape construction to runtime physics, while [`../../formats/phy/ROADMAP.md`](../../formats/phy/ROADMAP.md) assigns supplied payload parsing to PHY and query-shape construction to collision. Resolve the adjacent roadmaps so BSP owns model-block framing, PHY owns supplied payload parsing, collision owns immutable query shapes, and physics owns rigid-body state and response.
- **Equal-hit target order:** the official interfaces define trace fields and world/entity scopes but do not specify every equal-fraction winner across brushes, displacements, props, PHY convexes, hitboxes, and dynamic entities. Checked the SDK files named in Inputs and the current package/game READMEs. No retained controlled target comparison exists.
- **Point contents and initial overlap:** the official interfaces and movement consumers do not establish overlapping volume-entity selection, current-bit normalization, displacement hull `startSolid`, overlapping-compound leave behavior, or mixed world/entity initial overlap. Checked `IEngineTrace.h`, `gametrace.h`, public displacement collision, generic movement, TF2 movement, and existing collision evidence. No retained controlled target comparison covers these cases.
- **Performance budgets:** no accepted decision names the fixed benchmark scenes, browser/native device profiles, maximum build time, p95/p99 query latency, peak owned bytes, or concurrent-reader target. Checked the collision README, root roadmap, application READMEs, and tool READMEs.
- **Legacy prop-hull input:** the current BSP inventory leaves the complete non-empty legacy CS:GO prop blob contract Blocked, so collision has no accepted typed input for `shape.legacy-prop-hull`. Checked [`../../formats/bsp/inventories/lumps.md`](../../formats/bsp/inventories/lumps.md) and [`../../formats/bsp/ROADMAP.md`](../../formats/bsp/ROADMAP.md).
