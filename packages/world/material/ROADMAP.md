# Material Roadmap

[`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines delivery statuses, coverage classifications, inventory, logical path, and Complete.

## Completion Denominator

The current completion denominator contains exactly the 50 Behavior Families rows below. The candidate inventories enumerate 130 PC shader registrations, 484 parameter identities, 50 material-state flags, and 84 TF2-client proxy registrations. These 748 candidates contribute 0 denominator items until checked-in generators reproduce them from exact authorities and a denominator review accepts this roadmap.

The current denominator is Not accepted. Its target is runtime-neutral Source 1 material resolution for the exact declared PC content builds of Team Fortress 2, Counter-Strike: Source, and legacy Source 1 Counter-Strike: Global Offensive. Source 2 material resources and GPU execution are excluded.

## Inputs

- One immutable `VmtEffectiveDocument`, its canonical `materials/<name>.vmt` logical identity, complete VMT composition trace, and provenance from `packages/formats/vmt`.
- One explicit material-selection environment containing game, content build, PC shader profile, HDR mode (`None`, `Integer`, or `Float`), shader-model support level, pixel-shader-2_b support, sRGB-correct-blending state, low-fill state, editor-material permission, and model-use context. Material resolution never derives this environment from the browser or GPU.
- One shader registry containing ASCII-insensitive shader and alias identities, ordered parameters, declared types, declared defaults, effective initialization rules, declared shader-selection edges, derived flags, and semantic-family contracts.
- One proxy registry containing ASCII-insensitive proxy identities, ordered argument contracts, required material variables, evaluation-input dependencies, output mutations, and game-owned evaluator dependencies.
- A texture dependency callback receiving the parent material identity, exact decoded reference token, semantic role, canonical `materials/<name>.vtf` request identity, and remaining budgets. It returns VTF metadata, decoded-plane availability, and provenance or `Missing`.
- A material dependency callback with the equivalent contract for selected material references and canonical `materials/<name>.vmt` identities. It returns resolved VMT input plus provenance or `Missing`.
- One evaluation context containing absolute presentation time, frame interval, deterministic random samples, entity binding identity, immutable generic entity presentation values, and an optional game-owned proxy-input snapshot. Omitted required fields are `Missing`; wall-clock time and ambient process state are never read.
- Caller-supplied VTF metadata manifests for model texture bindings. Each manifest contains depth, mip/frame/face topology, exact Source sampling state, and the complete ordered high-resolution subresource identities produced by the VTF owner; decoded-plane validation supplies the same identities plus nonzero sample lengths.
- A required limits record containing `maxParameters`, `maxDependencies`, `maxDependencyDepth`, `maxSelectionSteps`, `maxProxies`, `maxProxyArguments`, `maxEvaluationSteps`, `maxReferenceBytes`, `maxOwnedBytes`, and `maxDiagnostics`. The package defines no unbounded mode.
- Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`: `src/public/materialsystem/{IShader.h,imaterial.h,imaterialvar.h,imaterialproxy.h,imaterialproxyfactory.h,imaterialsystem.h}`, `src/public/shaderlib/{BaseShader.h,cshader.h}`, `src/materialsystem/stdshaders/{stdshader_dx9_inc.vpc,*.cpp}`, and TF2 client build and proxy sources under `src/game/client/{client_base.vpc,client_econ_base.vpc,client_tf.vpc,tf/**}` and `src/game/shared/{econ,tf}/**`.
- Exact archive indexes, VMT bytes, VTF metadata, shader-registry captures, and proxy-registry captures for one declared content build of each target game. These inputs are Missing.

## Outputs

- An immutable `ResolvedMaterial` containing canonical identity and provenance; selected shader and selection trace; ordered typed variables with source/default/proxy origin; author flags, defined masks, derived state flags, and per-entry coverage classifications.
- Ordered `TextureDependency` and `MaterialDependency` records containing exact source token, semantic role, canonical request identity, returned provenance, and dependency edge.
- Runtime-neutral static state containing lighting model, blend equation and factors, alpha test, culling, depth test/write/function, polygon-offset role, fog participation, vertex-input requirements, color and alpha modulation, and framebuffer-input requirements.
- Typed feature state for base textures, transforms, frames, detail layers, bump and normal maps, self illumination, environment mapping, phong, water, sky, and decals.
- Typed `VertexLitGeneric`, `EyeRefract`, and legacy `Eyes` model state; model-only texture requests; vertex/lighting/camera/Studio-eye requirements; authored texture bindings; and ordered game-input proxy effects.
- An immutable `MaterialEvaluationProgram` containing ordered proxy operations and their declared input dependencies. Evaluating it is pure over `ResolvedMaterial` plus one evaluation context and returns `EvaluatedMaterialState` plus a step trace; it never mutates shared material state.
- Stable diagnostics containing coverage classification, code, material identity, VMT source span, shader or proxy identity, parameter or argument identity, dependency chain, evaluation step, and no substitute material.

## Invariants

- Resolution and evaluation are deterministic over supplied documents, registries, dependency results, environments, contexts, and limits. They perform no filesystem access, provider selection, archive scan, network request, installation discovery, GPU query, wall-clock read, or game-state mutation.
- Shader, parameter, flag, and proxy lookup ignores ASCII case while retaining source spelling and registry identity. No unknown identity selects a similarly named shader, parameter, proxy, or material.
- Shader-declared selection, material-condition selection, and `$fallbackmaterial` are successful data-selection operations only when every selected edge resolves. Unknown, cyclic, missing, malformed, or over-limit selection returns its exact classification and never substitutes an error material or second implementation.
- Explicit active values are parsed before shader initialization. Shader-declared initialization then supplies only values that remain undefined. Every output variable records whether it came from VMT syntax, a selected override block, a declared initializer, a derived flag, or a proxy step.
- Unknown variables remain ordered typed variables because a later proxy can reference them. They cannot affect static material state until an accepted parameter or proxy contract assigns semantics.
- Texture and material references retain their exact source tokens. Material constructs one canonical dependency request per active external reference; Content alone validates provider eligibility and selects bytes.
- Render targets, environment lookups, and other accepted non-file bindings are typed reference kinds. They never generate guessed file requests.
- Texture frames are zero-based. VTF supplies frame count and decoded planes; Material owns static frame parameters and proxy-driven frame selection. A frame outside the supplied count is `Malformed` unless the accepted proxy contract explicitly wraps or clamps it.
- Texture transforms are finite 4×4 matrices with an exact source lexeme and operation trace. Center, scale, rotation, and translation order is fixed by the transform contract; malformed or non-finite transforms never become identity transforms.
- Proxies initialize and evaluate in VMT source order. Step `n` reads the variable state produced by step `n-1`; repeated proxy identities remain repeated operations. Every declaration records an initialized operation or an explicit classified no-operation disposition, and later declarations retain their source positions.
- Time-dependent proxies use supplied absolute presentation time and frame interval. Entity-dependent proxies use the supplied entity snapshot. Game-dependent proxies consume only a game-owned typed input. Random proxies consume supplied samples. Missing context never becomes zero, local player state, or process randomness.
- `ResolvedMaterial` and `EvaluatedMaterialState` describe semantics and required presentation work. They contain no browser object, GPU handle, shader module, draw command, scene mutation, or resource-lifetime policy.
- Every count, depth, string byte length, dependency, selection step, evaluation step, allocation, callback, and diagnostic is checked against the limits record before use.
- Source 2 shaders, KV3, compiled material resources, compatibility layers, legacy output schemas, and fallback implementations never enter this package.

## Ownership Exclusions

- `packages/formats/vmt` owns VMT/KeyValues structure, source spans, patch dependency requests, and patch composition. Material consumes one effective document and never reparses text or re-applies patches.
- `packages/formats/vtf` owns VTF metadata, frames, faces, slices, formats, resources, and decoded planes. Material owns reference role, color/normal interpretation, and frame-selection semantics.
- `packages/content` owns logical-path validation, provider order, exact byte retrieval, active-map PAK precedence, caching, and provenance acquisition. Material owns only reference-token classification and dependency requests.
- `packages/presentation/rendering` owns GPU shaders, resource lifetime, texture upload and sampling, draw sorting, pass scheduling, reflection and refraction targets, draw policy, and pixels. Material owns the runtime-neutral state those operations consume.
- `packages/world/map` owns map-wide sky-face assembly, water-surface and volume association, decal geometry association, lightmap association, and canonical runtime descriptors.
- `games/tf2`, `games/css`, and `games/csgo` own game-state derivation, game-specific effect selection, and typed values supplied to game-dependent proxy operations. Material owns proxy ordering and application of supplied values to material variables.
- `packages/presentation/particle` owns particle sequence use and particle frame selection. `packages/world/material` owns only material-level texture animation declared by a material proxy.
- Applications own product configuration and presentation-time sources. Tools own orchestration and inventory generation.

## Behavior Families

### Configured alpha, translucency, SpriteCard, and Water checkpoint

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Alpha-bearing base textures remain independently classified as opacity, alpha-test, self-illumination, environment-mask, Phong-mask, or tint-mask inputs. Alpha test uses the shader-qualified pass comparison and reference; alpha-zero RGB never replaces a discarded fragment. | Static state emits typed alpha ownership and one explicit fragment-discard requirement. `LightmappedGeneric`, `UnlitGeneric`, `VertexLitGeneric`, decals, glass, and the configured fence retain their exact blend, depth, cull, polygon-offset, queue, and alpha-modulation state. | [`inventories/jump-beef-alpha-water.md`](inventories/jump-beef-alpha-water.md) fixes every configured alpha-bearing identity. Semantic vectors cover `GEQUAL 0.35`, the `GEQUAL 0.7` default, source-alpha blending, mask ownership, and zero-alpha discard. | Ready |
| Every active texture role retains its initial frame, transform, proxy-mutation facts, color read, and exact caller-supplied VTF sampling and authored subresource chain. A selected frame outside the supplied frame count and any absent authored mip/frame/face/slice fail. | Material emits one ordered texture-use record per request and binds it only to a complete `TextureMetadataManifest`; no generated mip, substituted texture, inferred render target, or decoded-pixel fallback exists. | Fence, all 13 present mark textures, glass, 55 model materials/71 model VTFs, 12 particle materials/10 particle VTFs, and both Water roots are fixed in the configured inventory. Missing, out-of-range, duplicate, and incomplete-plane vectors fail. | Ready |
| `SpriteCard` forces two-sided source-alpha or additive blending, disables depth writes and destination-alpha writes, and discards shader-output alpha not greater than `0.01` unless its authored add-self/base-2 path disables that test. Its shader initializers, depth-feather dependency, frame blend, dual sequence, and screen-orientation inputs remain explicit. | `ParticleMaterialState` with `ParticleMaterialShader::SpriteCard` exposes the complete encountered PC state and required viewport, camera-matrix, scene-depth, and authored-plane inputs. Missing process-selected depth-feather default remains a named input instead of a browser default. | Eight exact SpriteCard and four mesh-sprite VMT vectors reproduce five additive and seven source-alpha materials; every configured SpriteCard has dual sequence disabled. | Ready |
| PC shader-model-90 Water distinguishes LDR `Water_DX90` from HDR `Water_DX9_HDR`; only defined texture variables bind. Shader initialization and type initialization remain distinct from declaration metadata defaults. Water retains base, normal, environment, reflection, refraction, linked beneath/overlay materials, frames, transforms, scale, depth/time, amounts/tints, Fresnel, fog, cheap/expensive state, and ordered proxy writes. | One complete Water output exposes optional bindings without constructing declaration-default textures, effective values with `Authored`, `ShaderInitializer`, or `TypeInitializer` origin, required authored planes/lightmap/environment/framebuffers/view/controller/time inputs, and evaluated normal-frame/transform/LOD state. | Exact composed surface and beneath VMTs reproduce seven- and three-operation proxy programs, 60 authored normal frames, surface reflection+refraction, beneath-only refraction, above/below state, fog, cubemap, and WaterLOD `1000..2000`; missing bindings remain absent. | Ready |

### StudioModel shader and stock TF2 viewmodel producer checkpoint

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| `VertexLitGeneric` model materials expose base/detail/bump/environment inputs plus half-Lambert, self illumination, Phong mask/exponent/texture/factor/tint/boost/Fresnel/warp, rim exponent/boost/mask, tangent, cloak, sheen, color, alpha, cull, depth, and fog state without reducing the shader to a base texture. | `ModelMaterialState::VertexLitGeneric` emits every listed target control and separates model-only texture roles from world roles. Generic static state remains the blend/depth/cull/fog authority. | All configured frog, locker, player, projectile, hand, and launcher VMTs resolve; fixed Phong/rim/self-illumination vectors pass. | Ready |
| `EyeRefract` exposes linear cornea and lightwarp inputs; sRGB iris, environment, and ambient-occlusion inputs; effective helper defaults; authored scalar/vector controls; ambient/local-light and camera requirements; and StudioModel-supplied eye-origin/iris-row requirements. | `ModelMaterialState::EyeRefract` emits the complete target record and model texture requests while generic shader classification remains registry-blocked. | Exact Soldier left/right and Demoman right-eye VMTs pass; three target eye materials are fixed in package evidence. | Ready |
| Target model proxies retain VMT order. Generic `AnimatedTexture`, `Sine`, `Equals`, `Multiply`, `LessOrEqual`, and `SelectFirstIfNonZero` evaluate locally; `BurnLevel`, `InvulnLevel`, `ItemTintColor`, `ModelGlowColor`, `StickybombGlowColor`, `YellowLevel`, `AnimatedWeaponSheen`, `WeaponSkin`, `spy_invis`, `invis`, and `weapon_invis` consume explicit typed game-owned inputs and apply only their declared writes. Missing game input remains `Missing`. | All 17 encountered identities compile to typed ordered operations. `ModelProxyInputs` supplies values/effects; absent or non-finite inputs fail without a write. | The 55-material occurrence audit has exactly the 17 listed handled identities and zero malformed/unsupported identities; fixed generic, game-input, stock, enabled-sheen, weapon-skin, and missing-input traces pass. | Ready |
| Every source model-texture binding joins its semantic color/data role to exact VTF metadata, wrap/min/mag/mip filtering, selected anisotropy level, and the complete authored high-resolution `(mip, frame, face, slice)` sequence. A mipmapped binding with any absent authored subresource is `Missing`; no generated-mip output exists. | `TextureMetadataManifest`, `bind_authored_texture`, `bind_authored_model_texture`, and `validate_authored_planes` require one ordered dimensioned nonempty plane per declared identity, require anisotropy level `1` for non-anisotropic filters and at least `2` for anisotropic filters, and expose no generated-mip state. | 55 configured model materials resolve 71 unique VTFs; complete VMT/VTF hashes and decoded authored chains produce SHA-256 `05c7869e…17033`; missing/duplicate/mismatched vectors fail. | Ready |
| Current `VertexLitGeneric`, `Eyes`, and `EyeRefract` draw state applies initialized base-alpha ownership, self-illumination/environment-mask alpha conflicts, alpha test, current material alpha modulation, and strict `0 < cloakFactor < 1` translucency from explicit current values produced by proxy evaluation. | `model_draw_state` consumes exact base-texture alpha and current runtime values, emits effective static depth/blend/alpha state, current opaque/translucent membership, and `None`, `Potential`, or `Current` framebuffer-input state. Entity FX blend remains a viewmodel eligibility/renderable input. | Fixed opaque, translucent, alpha-tested, self-illumination, alpha-modulated, cloak endpoint/interior, sheen, missing-cloak, and non-finite runtime vectors pass; 55 exact visible materials produce draw-state hash `12770b45…2014d`. | Ready |
| Model draw output enumerates every required ambient cube, local-light array, camera position, Studio eye rows/origin, local environment, current framebuffer, authored texture-plane, and game-proxy value input. Omission remains a named missing input and never selects base-texture-only shading. | One ordered input-requirement set and availability comparison accompany each typed model shader state; proxy evaluation retains its existing typed missing-game-input failures. | Exact 55-material vectors cover hands, launchers, player eyes, self illumination, Phong/lightwarp/rim/environment, eight alpha-tested materials, five current-framebuffer materials, authored planes, and all 17 proxy identities. | Ready |

### `jump_beef` world-surface parity checkpoint

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| LightmappedGeneric, Water, and Sky assign every target texture role its exact sRGB, linear, compressed-HDR, normal-data, or format-dependent read intent. | Every request has immutable `TextureColorRead`; `active_textures` retains every role independently from the primary profile texture. | Exact target material/texture inventory plus base/detail/bump/environment/water/sky role vectors. | Ready |
| Author flags and texture-alpha facts determine exact blend factors, alpha test/reference, culling, depth test/write/function, decal offset, fog mode, wireframe, queue translucency, and vertex color/alpha requirements. | `static_state` consumes selected Material plus explicit VTF alpha facts and emits the complete runtime-neutral state without pixel/GPU inference. | Translucent decal, `GEQUAL 0.35` alpha-tested fence, opaque, additive, sky ignore-Z/no-fog, integer flag conversion, and missing-alpha vectors pass. | Ready |
| Detail, bump/SSBump, environment-map, water, and decal parameters retain typed finite values and exact defaults required by target world surfaces. | Typed detail, bump, environment, Water, and decal records coexist with ordered raw parameters and exact requests. | [`../map/inventories/jump-beef-surfaces.md`](../map/inventories/jump-beef-surfaces.md), malformed scalar/vector vectors, and exact native closure compilation. | Ready |
| AnimatedTexture, Sine, Equals, TextureTransform, TextureScroll, and WaterLOD declarations compile in source order and evaluate only from supplied time, frame interval, texture frame counts, variables, and water-LOD values. | `ProxyProgram` retains handled/malformed/unsupported entries; pure evaluation emits ordered writes and trace with no ambient reads. | Seven-step target-surface-water program, malformed/unsupported no-operation, frame-wrap, sine, component-write, transform, and water-LOD vectors pass. | Ready |

### World environment output checkpoint

| Source behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| `Water` parameters select only defined normal/environment/framebuffer inputs, amounts/tints, fog, above-water state, cheap-water state, and linked materials. Declaration metadata defaults never become active bindings. | `WaterMaterialOutput` retains optional VTF/VMT requests, exact origins for effective PC values, force-cheap/force-expensive conflict, and complete input requirements. Undefined `WaterState` texture slots carry no token/path and `is_defined=false`; invalid values fail before output. | Default/explicit/missing/built-in/material/color/boolean/proxy vectors pass and Map exact evidence joins the two target water materials to all 16 profile-qualified surfaces. | Ready |
| Sky and cubemap consumers require profile-specific selected texture roles while decal projection requires material scale and receiver suppression facts. | Existing profile-selected Sky roles remain exact; `DecalState` adds positive finite scale plus alpha-test and no-decal receiver facts consumed by Map without geometry ownership. | LDR/HDR/incomplete Sky, malformed/zero decal scale, byte/float color, and exact 39-decal occurrence evidence pass. | Ready |

The active migration checkpoint implements immutable semantic projection from composed VMT trees: explicit shader-family classification, ordered scalar parameter and proxy syntax, Source boolean/float flag inputs, surface properties, canonical exact VTF dependency identities and roles, built-in environment/render-target dispositions, and typed unsupported shader/proxy state required by `jump_beef`.

The active HDR compiler profile checkpoint evaluates one explicit PC LDR or integer-HDR environment before dependency requests. Material owns conditional-key and shader-override selection, `Sky` implementation selection, and exact `$hdrbasetexture`, one-file compressed-HDR, or three-file compressed-HDR texture roles. A missing selected role fails; Material never substitutes `$basetexture` for a required HDR texture.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Resolve exactly one composed non-`Patch` VMT root with its canonical identity and provenance, and reject a missing, malformed, or partial effective document before semantic work. | `resolve` consumes only a complete VMT effective object and returns one immutable semantic projection. Canonical identity/provenance remain in the compiler dependency record rather than this value. | Ordinary/composed VMT tests pass; full failure/provenance integration remains absent. | Partial |
| Evaluate material-condition-prefixed variables and shader-selection override blocks against one explicit PC environment in target order while retaining every selected and unselected origin. | `SelectionEnvironment` fixes HDR mode, shader model, pixel-shader-2_b, sRGB blending, low-fill, editor, and model-use inputs. Active condition-prefixed values replace the first same-name value, inactive values remain in `selection_trace`, exact environment override blocks apply in fixed priority, and unknown conditions fail. Full target registry integration remains absent. | LDR/HDR/negation/replacement/inactive and unknown-condition unit vectors pass; exact HDR world/model/sky composition runs in native source bundling and WASM. | Partial |
| Resolve every registered shader and alias without ASCII case, distinguish aliases from implementations, and classify every unregistered root as `Unknown`. | Seven migration-required families are classified case-insensitively; every other root is explicit `Unsupported` pending the generated registry. | Synthetic LightmappedGeneric and exact map LightmappedGeneric/Water roots pass; complete registry remains blocked. | Partial |
| Follow shader-declared selection and material-reference selection in exact order, reinitialize parameters for each selected shader, detect canonical cycles, and return no substitute on failure. | No shader or material selection graph exists. | Selection-graph trace comparison covering zero and multiple edges, selected override blocks, `$fallbackmaterial`, cycles, missing references, and every limit. | Not started |
| Build each selected shader's exact ordered parameter registry, including 13 standard slots, shader-local additions, overrides, declared metadata defaults, and effective initialization defaults. | The candidate parameter inventory contains 484 identities; exact target effective defaults and content-build occurrence sets are Missing. | Parameter-registry capture comparison by shader identity, index, type, declared default, initialized value, edit flag, and selected environment. | Blocked |
| Apply active selected VMT entries, duplicate handling, overrides, undefined slots, and initialization defaults in target order while recording one origin per final variable. | Ordered scalar entries remain complete while first case-insensitive values provide semantic inputs. Registry defaults/origins remain absent. | Repeat and case vectors pass through VMT/Material integration. | Partial |
| Parse accepted scalar lexemes into exact integer, boolean, float, vector-2/3/4, color, matrix, matrix-4×2, texture, material, string, FourCC, and undefined material-variable forms without locale dependence. | No typed material-value model exists. | Cross-runtime lexical vectors compare type, components, binary32 values, matrix entries, retained lexeme, and exact malformed diagnostics. | Not started |
| Preserve a structurally valid variable absent from the selected registry as an ordered variable, classify its semantic effect, and allow accepted proxies to reference it without treating it as a known shader parameter. | Every scalar remains ordered in `parameters` and first lookup; no unknown scalar is discarded. Per-variable semantic classification remains absent. | Unknown scalar retention passes. | Partial |
| Map every accepted author/runtime flag and derived-state flag to exact bit, defined-mask behavior, precedence, and runtime-neutral effect. | Target world flags use Source integer conversion and drive typed state; the complete 50-item registry, defined masks, duplicate diagnostics, and all shader-forced mutations remain incomplete. | Target isolated/combination vectors plus future generated registry comparison. | Partial |
| Convert each active external texture token to one exact canonical VTF request, retain role and provenance, and expose texture metadata needed for alpha, frame, color, normal, and cubemap semantics. | Seventeen roles include base plus HDR base, one-file compressed HDR, and three-file compressed HDR inputs. Paths normalize separators, preserve source spelling, add only required prefixes/extensions, reject traversal, and expose the profile-selected role set. Provenance and VTF metadata join occur in the compiler. | Existing dependency vectors plus complete/incomplete HDR role-set tests pass; exact `jump_beef` selects six `$hdrcompressedtexture` VTFs at parsed format code 12 and dimensions `512×256` four times, `512×512`, and `4×4`. | Partial |
| Convert each active material token to one exact canonical VMT request and classify accepted render-target, environment-lookup, and procedural bindings without issuing file requests for them. | `env_cubemap` and `_rt_*` produce typed built-ins with no file request. External material-valued parameters are not yet implemented. | Built-in environment/render-target vectors pass. | Partial |
| Parse transform strings and matrices into exact finite matrices, preserve source state, and compose static and proxy-written transforms in declared operation order. | Target `TextureTransform` and `TextureScroll` proxy writes emit finite 4×4 matrices in source order. Static transform-string parsing remains absent. | Proxy center/scale/rotation/translation and scroll vectors pass; the complete static matrix remains required. | Partial |
| Select static and evaluated texture frames from VTF frame counts with exact zero-based wrap, clamp, start-time, and missing-context behavior declared by the owning parameter or proxy. | Target `AnimatedTexture` uses supplied absolute time/frame interval/count with wrap/no-wrap behavior. Static frames and entity start time remain absent. | Target frame-wrap/no-wrap and missing/zero-count vectors pass; complete static/entity matrix remains required. | Partial |
| Instantiate each proxy declaration through the ASCII-insensitive registry, validate its complete argument subtree and referenced variables, and retain repeated declarations in source order. | The candidate proxy inventory contains 84 SDK TF2-client registrations; exact target-runtime registrations and content occurrences are Missing. | Generated proxy-registry and initialization comparison over exact target declarations, argument defaults, variable lookup, repeated names, empty blocks, and failure cases. | Blocked |
| Evaluate proxy operations in source order from immutable base state, making every write visible to the next operation and returning a complete step trace. | `ProxyProgram` evaluates every handled target declaration in order and emits each write/effect. The complete generated registry remains absent. | Seven-step water and eight-step stock-launcher dependency traces pass. | Partial |
| Evaluate arithmetic, copy, comparison, clamp, wrap, fractional, integer, exponential, matrix, and selection proxies with exact scalar/vector coercion and exceptional-value rules. | `Equals`, `Multiply`, `LessOrEqual`, `SelectFirstIfNonZero`, sine, transform, and scroll are handled for target types. Other registry arithmetic families remain absent. | Target scalar/vector/component/matrix and invalid-type traces pass; complete truth tables remain required. | Partial |
| Evaluate current-time, sine, linear-ramp, texture-scroll, animated-texture, animated-offset, and animated-entity-texture proxies from supplied absolute time and frame interval. | `Sine`, `TextureScroll`, and `AnimatedTexture` consume explicit time; weapon sheen consumes explicit game-owned frame/state values. Other time families remain absent. | Target fixed timelines and wrap boundaries pass; complete registry timelines remain required. | Partial |
| Evaluate entity- and game-dependent proxy operations only from typed snapshots, with game modules owning derivation of TF2, CS:S, or legacy CS:GO values. | `ModelProxyInputs` supplies exact target invisibility, glow, yellow-level, sheen, and weapon-skin values without game-state reads. Other registered entity/game proxies and game adapters remain absent. | Target stock/default/enabled/missing model-input traces pass; the full snapshot matrix remains required. | Partial |
| Evaluate random, view, shadow, post-process, water-LOD, world-dimension, and external-image proxies only from explicitly supplied deterministic inputs. | `WaterLOD` and all target game-dependent model inputs are explicit. Random/view/shadow/post-process/world-dimension/external-image families remain absent. | Water/model missing-input and supplied-value traces pass. | Partial |
| Classify unknown proxy names as `Unknown`, known but unavailable semantics as `Unsupported`, invalid declarations as `Malformed`, and absent required context as `Missing`; record the declaration as a no-operation disposition and continue later source-ordered declarations when the proxy-qualified target contract does so. | Current programs distinguish handled, malformed, and unsupported declarations, retain no-operation trace entries, and fail missing evaluation inputs. `Unknown` registry identity and full continuation policy remain absent. | Target malformed/unsupported/missing and later-step traces pass; complete registry mutation matrix remains required. | Partial |
| Derive opaque versus translucent state from selected shader, flags, alpha modulation, vertex alpha, texture alpha metadata, self-illumination use, environment-mask use, and opaque-texture override. | `static_state` handles the complete target world/decal cross-product from explicit VTF alpha facts; broader shader families remain incomplete. | Target alpha-source/conflict vectors and future full registry captures. | Partial |
| Derive alpha-test enablement, comparison function, reference default or explicit value, alpha-source ownership, and interaction with translucency and blending. | Target LightmappedGeneric emits `GEQUAL`, default `0.7` or positive explicit reference, suppresses alpha blending, and retains depth writes unless another state disables them. | Exact fence `0.35`, default, self-illum/base-alpha conflict, and blend vectors pass; broader shaders remain incomplete. | Partial |
| Derive culling, depth test, depth write, depth function, polygon-offset role, fog participation, and wireframe/no-draw state from selected shader and flags. | Runtime-neutral target state covers no-cull, ignore-Z, z-nearer, decal, no-fog, additive fog, wireframe, and no-draw. Rendering consumption remains separately owned. | Isolated target state vectors pass; complete shader registry combinations remain required. | Partial |
| Derive exact disabled, replace, alpha, additive, blend-add, premultiplied, and shader-specific blend equations and factors, including depth-write consequences. | Target normal and additive states emit disabled, source-alpha/one-minus-source-alpha, source-alpha/one, or one/one with exact depth-write consequences. Other shader-specific and premultiplied modes remain absent. | Fixed target blend vectors pass; broader arithmetic/state captures remain required. | Partial |
| Classify each shader as unlit, vertex-lit, lightmapped, bumped-lightmapped, or shader-specific lighting and emit exact vertex, tangent, lightmap, baked-lighting, and flashlight requirements. | Target static state distinguishes unlit, vertex-lit, lightmapped, bumped-lightmapped, Water, Sky, and unsupported lighting identity. Complete input requirements remain incomplete. | Target world shader vectors pass; broader registry/state capture remains required. | Partial |
| Resolve self-illumination enablement, source mask, tint, fresnel controls, alpha-channel ownership, and conflicts with environment masking and alpha test. | Target model state distinguishes base alpha, separate texture, environment-mask alpha, and Fresnel sources with tint/ranges; generic alpha-test suppression is retained. Broader shader conflict evidence remains incomplete. | Target frog/item vectors pass; full feature cross-product remains required. | Partial |
| Resolve environment-map reference kind, frame, mask source, mask transform, tint, contrast, saturation, fresnel, camera/sphere space, and cubemap requirement. | `EnvironmentMapState` retains target environment/mask/tint/contrast/saturation; model state adds camera/tangent requirements. Frames, transforms, complete mask conflicts, sphere space, and cubemap binding remain incomplete. | Target world/model environment vectors pass; broader feature cross-product remains required. | Partial |
| Resolve detail texture reference, frame, transform or inherited transform, scale, tint, blend factor, and every encountered shader-qualified blend mode. | Target detail records retain reference, scalar-or-two-axis scale, tint, blend factor/mode, and exact linear-versus-sRGB read intent. Configured `wall028.vmt` preserves `[1.1 2.3]`; malformed cardinality, non-finite values and modes outside 0–11 fail. Frames, transforms, and unencountered execution modes remain incomplete. | Fixed scalar/vector/malformed vectors plus configured wall/detail dependency closure and runtime rendering pass. | Partial |
| Resolve bump, normal, secondary bump, bump mask, SSBump, transforms, frames, tangent requirements, and alpha-channel use without reconstructing pixels in Material. | Target primary bump/normal and SSBump records retain linear read intent and select bumped-lightmapped state. Secondary maps, masks, transforms, frames, and complete tangent/alpha requirements remain incomplete. | Exact wall SSBump and water normal inputs plus fixed vectors pass. | Partial |
| Resolve phong enablement, exponent scalar or texture, factor, tint, boost, fresnel ranges, warp texture, albedo tint, base-alpha mask, inversion, and rim-light interaction. | Target model state emits every listed control, packed Fresnel ranges, normal/base/no-mask source, and rim dependency on Phong plus exponent-texture alpha. Full registry coverage remains incomplete. | Exact locker and launcher vectors plus 55-material target closure pass. | Partial |
| Resolve Water shader aliases and parameters into normal/flow/base/environment references, reflection/refraction amounts and tints, fog, above-water state, cheap/expensive selection inputs, scroll state, and ordered animation program. | `WaterMaterialOutput` completes the configured PC shader-model-90 LDR/HDR family, keeps absent base/flow/environment/reflection bindings absent, and evaluates the exact proxy programs. Lower shader-model Water families and all-game occurrences remain outside this checkpoint. | Exact target joins, 540 authored normal subresources, seven/three-step evaluated proxy traces, and synthetic missing/default/state vectors pass. | Partial |
| Resolve Sky shader selection, LDR/HDR texture choice, tint, transform, ignore-depth state, and six face-material descriptions without assembling sky geometry. | `Sky` selects explicit PC LDR or HDR implementation identity. HDR chooses one-file compressed, complete three-file compressed, then HDR-base roles and never `$basetexture`; incomplete selected role sets fail. Tint, transform, depth state, and general sky families remain incomplete. | Synthetic LDR/HDR/missing texture-role vectors pass. Exact `jump_beef` resolves six ordered `Sky` HDR materials and rejects a same-length missing selected VMT mutation with WASM error `7`. | Partial |
| Resolve decal material flags, shader, blend, alpha, depth-write, culling, polygon-offset role, color modulation, and suppress-decal behavior without owning decal geometry or draw order. | Target decal scale/receiver state plus source-alpha blend, no depth write, culling, decal polygon offset, vertex color/alpha, and queue state are implemented. General color modulation remains incomplete. | Exact 13 present/one missing target materials, static state, fractional scale, and projection vectors pass. | Partial |
| Enforce all parameter, dependency, depth, selection, proxy, argument, evaluation, reference-byte, owned-byte, and diagnostic limits; emit one canonical output consumed by map, games, tools, and rendering with no duplicate semantic resolver. | No bounded resolver, evaluator, or consumer integration exists. | Boundary vectors at `limit-1`, `limit`, and `limit+1`, deterministic repeated-run comparison, allocation measurements, and repository-wide producer/consumer audit. | Not started |
| Generate exact shader, parameter, flag, and proxy inventories from the pinned registries and exact target-game archive indexes, then classify every indexed VMT occurrence. | No checked-in generator exists; exact target content builds, registry captures, and archive indexes are Missing. | Two byte-identical clean-work-directory generations followed by an occurrence audit requiring every shader, parameter, flag, proxy, default, and semantic disposition to map to one accepted inventory item. | Blocked |

## Generated Inventories

No generated Material inventory is accepted. Accepted item count: 0.

| Output | Authority identity | Authority revision | Generator command | Candidate items |
|---|---|---|---|---:|
| [`inventories/shaders.md`](inventories/shaders.md) | Official SDK PC standard-shader build registry, target-game shader-registry captures, and exact indexed VMT roots | SDK `88fa198f...`; target captures and content builds Missing | Missing | 130 |
| [`inventories/parameters.md`](inventories/parameters.md) | Official shader parameter declarations and material flag interfaces, target effective registries, and exact indexed VMT entries | SDK `88fa198f...`; target captures and content builds Missing | Missing | 534: 484 parameters and 50 flags |
| [`inventories/proxies.md`](inventories/proxies.md) | Official TF2 client build inputs and proxy registrations, target proxy-registry captures, and exact indexed VMT proxy declarations | SDK `88fa198f...`; target captures and content builds Missing | Missing | 84 |

Current candidate inventory count: 748. Current generated and accepted inventory count: 0.

The future generator must consume exact provider plans and archive indexes, resolve only indexed VMT/VTF identities, capture the declared runtime registries under a fixed PC environment, retain per-game content-build occurrence counts and source hashes, and fail on every unclassified, malformed, unknown, unsupported, or missing entry rather than omitting it.

## Exit Criteria

Material is Complete only when all of these predicates pass:

- All 50 Behavior Families rows are Ready.
- The shader, parameter/flag, and proxy inventories are generated, current, Accepted, and cover exact TF2, CS:S, and legacy Source 1 CS:GO content builds with no unclassified VMT occurrence.
- Every registered shader and alias has one selected semantic contract; every effective parameter has one type, declared default, effective initialization result, origin, and coverage classification.
- Every author/runtime flag and derived-state flag has exact isolated and combination evidence.
- Every encountered proxy has exact initialization, input-dependency, ordering, mutation, and failure evidence. Every game-dependent proxy is integrated through its game-owned typed input.
- Fixed vectors cover conditions, shader and material selection, dependencies, cycles, transforms, frames, proxies, alpha, depth, culling, blending, lighting, self illumination, environment mapping, detail, bump, phong, water, sky, decals, and every limit.
- VMT, VTF, Content, Map, TF2, CS:S, legacy CS:GO, Rendering, Particle, and tools consume the sole current Material interface; no embedded material resolver, direct VMT interpretation, placeholder material, compatibility layer, or legacy artifact reader remains.
- No required declared-content material, shader, parameter, flag, proxy, reference, or state remains `Unsupported`, `Unknown`, `Missing`, `Malformed`, `Partial`, or `Blocked`.

## Blockers

- **Exact target content:** the configured TF2 build-`24207079` misc/texture archive indexes and the current `jump_beef` closure are available. The local configuration contract has no CS:S or legacy Source 1 CS:GO root, so their exact build/index occurrences remain Missing.
- **Target registries:** the official SDK source produces finite candidate shader, parameter, flag, and TF2 proxy sets, but no checked-in operation captures and hashes the exact registries for the declared retail content builds. Effective parameter defaults and build-conditioned registrations cannot be accepted from source candidates alone. Checked the SDK build inputs, public registry interfaces, assigned inventories, and `tools/`.
- **Inventory generator:** no checked-in `tools/playsrc` command emits any assigned Material inventory. Production implementation is outside this roadmap-research checkpoint. Checked `tools/`, root manifests, and the complete assigned Material tree.
- **Semantic prerequisites:** VMT, VTF, and Content implementations expose current composition, metadata/plane, sampling, and exact-resolution interfaces, but their denominators remain Not accepted. Material can execute the configured TF2 checkpoint without claiming all-game completion.
- **Game-dependent proxy integration:** TF2, CS:S, and legacy Source 1 CS:GO typed proxy-input inventories are not accepted. Material can define and verify the input/write seam; each game remains responsible for deriving runtime values.
