# Entity Roadmap

[`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines delivery statuses, coverage classifications, inventory, and completion terms.

## Completion Denominator

This leaf roadmap contains exactly 28 Behavior Families rows. [`inventories/generic-behaviors.md`](inventories/generic-behaviors.md) contains 72 candidate generic behavior items and contributes 0 denominator items until a checked-in generator emits it and a denominator review accepts this roadmap. The current completion denominator is therefore the 28 behavior rows. Acceptance of all 72 inventory items would make the denominator 100 items.

The denominator is Not accepted. It covers runtime-neutral Source entity-lump syntax, identity, keyvalues, handles, references, lifecycle, hierarchy, I/O, templates, generic filter composition, collision-supplied trigger contacts, generic logic primitives, snapshots, deterministic ordering, and bounds. It contains no game classname inventory.

## Inputs

- Immutable BSP entity-lump bytes with map provenance and source byte range. The sequence may end in exactly one NUL byte; a NUL at any earlier position is Malformed.
- A selected-game class-binding registry. Each encountered classname maps to exactly one generic behavior composition, one game-owned handler, `Intentionally inert`, `Unsupported`, or `Unknown`; the Entity package never infers behavior from spelling.
- A fixed simulation tick, positive tick interval, monotonic current tick, and deterministic random stream with serializable state for random timer intervals and case selection.
- Ordered direct-input, lifecycle, and collision-contact batches. Every item carries a producer sequence, target handle or target expression, typed parameter, activator handle, caller handle, and output-action identity when applicable.
- A caller-supplied attachment-transform provider keyed by parent handle and attachment identity. Missing attachments return `Missing`; Entity does not derive animated poses.
- A required limits record containing `maxEntityLumpBytes`, `maxEntities`, `maxKeyValues`, `maxKeyValuesPerEntity`, `maxHierarchyDepth`, `maxOutputActions`, `maxQueuedEvents`, `maxEventsPerTick`, `maxTemplateInstances`, `maxTriggerContacts`, `maxSnapshotBytes`, and `maxDiagnostics`. The package defines no unbounded mode. `maxEntities` cannot exceed 8,192; one entity token cannot exceed 2,047 bytes; one output-action field cannot exceed 255 bytes.
- Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`: `src/public/{basehandle.h,const.h,datamap.h}`, `src/game/shared/{baseentity_shared.cpp,ehandle.h,entitylist_base.h,entitylist_base.cpp,mapentities_shared.h,mapentities_shared.cpp}`, and `src/game/server/{baseentity.h,baseentity.cpp,cbase.cpp,entitylist.h,entitylist.cpp,mapentities.h,mapentities.cpp,variant_t.h,entityinput.h,entityoutput.h,eventqueue.h,hierarchy.h,hierarchy.cpp,TemplateEntities.h,TemplateEntities.cpp,point_template.h,point_template.cpp,filters.h,filters.cpp,triggers.h,triggers.cpp,logicrelay.h,logicrelay.cpp,logicentities.cpp,gameinterface.cpp,saverestore_gamedll.cpp}`.

## Outputs

- An immutable `EntitySyntaxDocument` retaining the complete source byte sequence, ordered entity blocks, ordered repeated key/value token bytes, quoted or unquoted form, semantic key identity, and half-open byte spans.
- An immutable ordered entity-definition set containing source ordinal, classname, targetname, typed field projections, output actions, parent and attachment requests, template membership, class binding, provenance, and one coverage classification per record and field.
- One `EntityWorld` containing live slot/generation handles, creation order, lifecycle state, references, parent/child links, local and world transforms, output-action state, queued events, template state, filter graph, trigger-contact sets, generic logic state, and deterministic random-stream state.
- An ordered `EntityTransitionBatch` containing accepted inputs, rejected inputs, emitted outputs, scheduled or cancelled events, lifecycle changes, reference changes, generic effect requests, diagnostics, and the next entity state.
- An `EntitySnapshot` sufficient to restore every Entity-owned state bit and ordering key against the same source identity and class-binding registry identity.
- Diagnostics containing coverage classification, stable code, map identity, entity source ordinal, handle when allocated, key/value or action span, tick, producer sequence, target expression, input identity, violated bound or invariant, and causal dependency result.

## Invariants

- Entity-lump strings are Source byte tokens, not inferred UTF-8. ASCII-insensitive operations fold only `A` through `Z`; stored bytes and spelling never change.
- Entity blocks, key/value pairs, repeated keys, output declarations, and entities preserve source order. A map or object keyed by field name is never the canonical representation.
- Semantic lookup returns the first ASCII-insensitive key match. Raw duplicate suffixes, trailing key bytes, and every later duplicate remain observable even when a class binding derives a normalized semantic key.
- A live handle is a slot plus generation. Removing an entity invalidates every prior handle before that slot can identify another entity. Targetnames and classnames are not handles.
- Exact targetname resolution precedes classname fallback. Valid wildcard syntax is either an exact expression or one trailing `*`; results follow live creation order. Context selectors resolve at dispatch time from the event's searching entity, activator, and caller.
- Parent links form an acyclic forest. Reparenting and detaching preserve the child's world transform unless an attachment-snap input explicitly selects zero local offset. Descendant world transforms are derived once from parent world transform and child local transform.
- Input names are ASCII-insensitive. Every dispatch retains target, input, typed parameter, activator, caller, output-action identity, tick, and sequence through success or failure.
- Output actions fire in the target's observable reverse declaration order. Equal-due-time queue entries preserve enqueue order. Finite fire counts are consumed when an action emits an event, whether or not dispatch later finds a target or accepted input.
- Servicing one tick consumes every due event, including a newly enqueued event already due in that tick, until the queue is quiescent or `maxEventsPerTick` is reached. Reaching the bound returns `BoundExceeded` with the unprocessed queue intact.
- Removing an entity cannot cancel a name-targeted event merely because it supplied that name when the event was queued. A direct-handle target becomes stale; target expressions resolve against the live world at dispatch.
- A classname without an accepted class binding never receives guessed generic behavior. Its record and keyvalues remain available with exactly one coverage classification.
- Collision produces contacts and owns overlap geometry. Entity owns filter evaluation, the accepted contact set, first/last-contact transitions, trigger I/O, and contact-state snapshots.
- Every count, source span, allocation, traversal, recursion step, queued event, emitted transition, contact, template instance, diagnostic, and snapshot byte is checked against the required limits before mutation or allocation.

## Ownership Exclusions

- `packages/formats/bsp` owns the entity-lump byte range and BSP container integrity. This package owns tokenization and semantic entity records after receiving those exact bytes.
- `packages/world/map` owns map-wide assembly and immutable references to geometry, brush models, collision, visibility, and environment data. Entity owns no duplicate map representation.
- `games/<game>` owns every game classname assignment, game-specific key and input meaning, player/team/class predicates, damage, teleport, spawn, objective, item, weapon, and rules behavior. A game maps each classname to generic Entity primitives or to one game-owned handler.
- `packages/runtime/movement` owns mover trajectories, velocity, blocking, stepping, and carried-subject motion. Entity may dispatch and record movement requests and completion inputs but cannot compute movement.
- `packages/runtime/physics` owns rigid bodies, constraints, forces, mass, and physics integration. `packages/world/collision` owns shapes, traces, sweeps, overlaps, and contact production.
- `packages/runtime/simulation` owns the gameplay tick and phase schedule. Entity owns deterministic ordering only inside the Entity phase invoked by Simulation.
- `packages/runtime/networking` owns wire entity indexes, replicated-handle encoding, snapshots on transports, and prediction transport. Generic slot/generation identity remains Entity-owned.
- `packages/runtime/replay` owns recorded-state advancement. It may consume Entity snapshots but cannot advance Entity gameplay state.
- `packages/presentation/rendering`, `packages/presentation/particle`, and `packages/presentation/audio` consume Entity state and effects without becoming lifecycle, hierarchy, or I/O authorities.
- Studio-model and game animation owners produce attachment transforms. Entity only composes an explicitly supplied attachment transform.
- `packages/formats/keyvalues` does not parse BSP entity blocks. Entity-lump anonymous blocks and ordered key/value pairs are a distinct syntax contract.
- `tools/playsrc` owns command orchestration and inventory generation. Applications and tools consume the Entity interface and cannot embed another entity graph, queue, hierarchy, or trigger authority.

## Behavior Families

The active migration checkpoint implements bounded BSP entity-text tokenization, ordered duplicate-preserving entities and key/value pairs, first-field structural views, brush-model identities, and ordered parsed/malformed Source output connections required by `jump_beef`. Runtime class behavior and I/O scheduling remain subsequent Entity checkpoints.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Entity-lump tokenization accepts bytes `0x01` through `0x20` as whitespace and one optional final NUL terminator, skips `//` comments, reads raw quoted or unquoted tokens without escape decoding, and requires a flat sequence of `{` plus key/value pairs plus `}`. It rejects unterminated quotes, missing values or braces, nested blocks, interior NUL, trailing data after NUL, and tokens above 2,047 bytes without prefix parsing. | The Rust parser implements the flat grammar, source ranges, comments, quoted/unquoted bytes, full whitespace range, terminal NUL, and whole-input rejection. Its caller-selected token limit defaults above 2,047 pending denominator acceptance. | Structural, malformed, repeat, comment, and limit vectors pass; exact SDK differential remains absent. | Partial |
| Each parsed entity receives its source ordinal as immutable definition identity and retains ordered repeated key/value bytes. `classname` is required for instantiation; `targetname` is optional and mutable at runtime; first-match semantic summaries never replace source pairs. | Every entity and pair retains source order/ranges; first case-insensitive classname/model/targetname/parentname/spawnflags views and `*N` identities remain additional fields. | Synthetic repeats and exact `jump_beef` counts of 361 entities and 3,674 pairs pass. | Ready |
| Class bindings project Source strings into `void`, boolean, signed 32-bit integer, binary32 float, string bytes, vector, position vector, RGBA8, or entity handle without changing source tokens; malformed and unsupported projections retain the original pair and exact classification. | No typed entity-value domain exists. | **Typed-field vector comparison:** compare target and playsrc type tags, binary32 bits, integer values, vectors, colors, handles, retained tokens, and malformed boundaries for every accepted field type. | Not started |
| The selected-game class-binding registry assigns every classname exactly once to a generic composition, game handler, `Intentionally inert`, `Unsupported`, or `Unknown`; no spelling heuristic creates behavior. | No class-binding interface or coverage audit exists. | **Registry partition audit:** synthetic and fixed-map registries must assign every encountered classname once, reject duplicates and omissions, and retain exact `Handled`, `Intentionally inert`, `Unsupported`, or `Unknown` results. | Not started |
| The live entity list allocates at most 8,192 slot/generation handles, iterates live entities in creation order, invalidates stale handles on removal, and cannot resolve a reused slot through an older generation. | No entity list or handle implementation exists. | **Allocation-trace comparison:** scripted create/remove/reuse schedules compare live order, slot/generation equality, stale dereference results, capacity failure, and listener event order with the declared handle contract. | Not started |
| Target resolution handles exact names, one trailing-`*` ASCII-insensitive prefix wildcard, duplicate targetnames, `!self`, `!caller`, and `!activator`; one registered external resolver supplies player, visibility, or picker selectors. Name matches return all live entities in creation order; only zero name matches permit classname fallback. | No target resolver exists. | **Resolver-table comparison:** fixed worlds enumerate exact, case, wildcard, duplicate, contextual, external, missing, and classname-fallback queries and compare ordered handle lists with SDK entity-list queries. | Not started |
| Map construction creates the world record first, resolves template builders before captured prototypes, computes parent depth, and spawns remaining entities parent-before-child by depth, descending binding-supplied spawn priority, then source ordinal. A missing or unsupported class binding produces a classified record and never a partially spawned entity. | No map-entity construction path exists. | **Spawn-trace comparison:** instrument fixed parent, template, priority, unsupported, and equal-depth maps; compare create, key projection, parent setup, spawn, failure, and removal traces in order. | Not started |
| Activation begins only after every non-template entity in the construction batch has either spawned or failed. Successful entities activate once in spawn order and resolve activation-time references against the complete live batch. | No activation phase exists. | **Lifecycle-trace comparison:** fixed cross-reference and failure maps compare activation count, order, visible entity set, and resolved or missing references with an instrumented target run. | Not started |
| Removal marks an entity and its remaining descendants for deletion, emits removal notifications while state is still readable, clears target lookup participation, cancels direct events owned by removed internal operations, commits storage release at the phase boundary, and invalidates handles before reuse. Repeated removal is inert. | No deletion lifecycle exists. | **Deletion-schedule comparison:** remove, kill, hierarchy-kill, self-remove-during-input, and clear-world vectors compare notifications, lookup visibility, descendant order, queued-event survival, handle invalidation, and final live order. | Not started |
| Parent resolution uses the first creation-order name match, reports additional matches, rejects self-parenting and cycles, preserves world origin and orientation across reparent and detach, and propagates parent transforms through every descendant in parent-before-child order. | No hierarchy implementation exists. | **Transform-trace comparison:** fixed multi-level hierarchies compare parent handles, local transforms, world matrices, ambiguity diagnostics, cycle rejection, and reparent/detach results; axis-aligned cases require exact bits, while general rotations permit at most 4 ULP per binary32 component and absolute error `0.000001` when the target magnitude is below `0.00001`. | Not started |
| A parent request may include one attachment identity. Attachment setup is deferred until the parent has spawned; snap mode sets zero local position and orientation, maintain-offset mode preserves world transform, missing attachment data fails without changing the prior parent, and later parent attachment transforms propagate to descendants. | No attachment-parent contract exists. | **Attachment-provider comparison:** a deterministic provider drives missing, deferred, snap, maintain-offset, animated-parent, reparent, and detach vectors; compare calls, local/world transforms, diagnostics, and unchanged-state failures. | Not started |
| Input dispatch performs ASCII-insensitive lookup in the selected binding, then either invokes one handler or writes one declared input field. The complete input tuple reaches that operation synchronously and produces one accepted or rejected dispatch result. | No generic input registry or dispatcher exists. | **Dispatch-trace comparison:** inherited and composed bindings receive fixed input tuples; compare selected handler/field, parameter, activator, caller, output identity, synchronous nested outputs, state mutation, and result code exactly. | Not started |
| Parameter conversion permits identity, any value to `void`, pass-through to variant input, integer to float or boolean, float to truncating integer or boolean, string to integer, float, boolean, vector, color, or target handle, handle to targetname string, and empty `void` to empty string. Every other conversion fails without invoking the handler. | No variant or conversion matrix exists. | **Conversion-matrix comparison:** every source/target type pair uses zero, boundary, overflow, malformed, non-finite, missing-handle, and mixed-case-name vectors; compare success, target tag/value, binary32 bits, resolver calls, and unchanged-state failure. | Not started |
| An output action uses the escape byte delimiter when present and comma otherwise and contains exactly target, input, parameter, delay, and fire-count fields. Target is nonempty; empty input defaults to `Use`; empty parameter inherits the output value; empty delay is zero and another delay must be finite binary32; empty count, `0`, or `-1` means always, and a positive signed-32 count is finite. Every other shape is Malformed and unschedulable. | Ordered pair views parse both delimiters, defaults, finite delays, canonical unlimited count, and malformed dispositions without scheduling. Parameter inheritance remains a runtime emission responsibility. | Synthetic parsed/malformed vectors and exact `jump_beef` counts of 66 parsed plus one malformed action pass. | Partial |
| Output declarations receive stable action identities and emit in reverse source declaration order. Each emission snapshots activator and caller, chooses inherited value or string override, schedules inherited values at action delay plus call-site delay and overrides at action delay alone, decrements a finite count immediately, and removes the action at zero. | No output state or firing behavior exists. | **Emission-trace comparison:** repeated declarations, mixed delays, overrides, finite counts, nested firing, missing callers, and post-emission cancellation compare action identities, event enqueue order, values, contexts, and remaining action state with SDK output traces. | Not started |
| The event queue orders by due simulation time and then enqueue sequence, services events at the first Entity phase whose time is at or after due time, and processes zero or negative delay plus newly enqueued due events in the same phase. Delay-to-tick conversion is ceiling division under the supplied tick interval. | No event queue exists. | **Queue-trace comparison:** fixed delays around tick boundaries, equal times, negative and zero delay, nested enqueue, and long chains compare due tick, enqueue sequence, dispatch sequence, and residual queue exactly. | Not started |
| Queue operations cancel caller-owned events whose live caller handle, targetname, and classname still match, or direct-target events whose input begins with the requested case-sensitive byte prefix; the pending query uses the same direct-target prefix rule. A missing target or unknown input consumes only that event, emits a bounded diagnostic, and does not stop later events. | No cancellation, pending query, or I/O failure model exists. | **Fault-trace comparison:** caller cancellation, target/input prefix cancellation, stale handles, removed/recreated names, missing targets, class fallback, unknown inputs, and mixed good/bad queues compare cancelled identities, diagnostics, accepted later events, and final queue. | Not started |
| Template composition selects up to 16 target expressions, captures every matching definition and transform, optionally removes prototypes, stores member transforms relative to the template, uniquifies member targetnames and internal references per instance unless names are preserved, and constructs each instance through the same parent-aware spawn and activation pipeline. | No template representation or instancer exists. | **Instance-graph comparison:** fixed single, grouped, duplicated-name, internal-I/O, parented, preserve-name, missing-member, failed-member, and repeated-instance templates compare definitions, rewritten references, transforms, lifecycle order, outputs, and failure atomicity. | Not started |
| Generic filters evaluate a caller/subject predicate, optional negation, targetname or classname matching, and up to five child filters with AND or OR short-circuit semantics. Missing, wrong-kind, cyclic, and over-depth child references fail closed; `TestActivator` emits exactly one pass or fail output with the tested activator. | No filter graph or evaluator exists. | **Truth-table comparison:** name/class wildcard, negation, empty/non-empty AND/OR, short-circuit, five-child, missing, wrong-kind, cycle, depth, and activator propagation vectors compare result, child evaluation trace, and output event. | Not started |
| Collision-supplied trigger contact inputs are accepted only while enabled and after filter success. A start event records one unique contact, fires per-contact start output, and fires start-all only on the empty-to-nonempty transition; an end event for a recorded contact fires end and end-all only on the nonempty-to-empty transition. Disable rejects new starts while collision-supplied ends complete tracked contacts; disable-and-end-touch ends contacts in reverse insertion order before disabling; enabled touch-test emits touching or not-touching from current set cardinality. | No trigger-contact state exists. | **Contact-trace comparison:** ordered duplicate starts, stays, ends, stale subjects, filter changes, first/last contacts, disable, explicit flush, touch-test, simultaneous contacts, deletion, and restored contacts compare contact sets and I/O traces with target trigger captures. | Not started |
| The generic relay primitive supports enable, disable, toggle, trigger, cancel-pending, spawn output, remove-on-fire, fast retrigger, and a retrigger lock lasting through the greatest remaining output delay plus 0.001 seconds. | No relay primitive exists. | **Relay state-trace comparison:** fixed disabled, spawn, remove, immediate, delayed, finite-action, fast-retrigger, blocked-retrigger, and cancellation schedules compare state, queue, output order, and removal tick. | Not started |
| The generic timer primitive enforces a 0.01-second minimum fixed interval, supports deterministic random intervals between supplied bounds, regular or alternating high/low outputs, enable, disable, toggle, immediate fire, refire-time change, reset, add, and subtract without scheduling before the current tick. | No timer primitive exists. | **Seeded timer-trace comparison:** fixed and random timers run under recorded seeds across every input and tick boundary; compare random draws, next due time, alternating state, output trace, disable behavior, and snapshot state. | Not started |
| The generic counter primitive stores binary32 value, ordered minimum and maximum, disabled state, and independent hit-min/hit-max latches; add, subtract, multiply, divide, set, silent set, limit changes, and get-value apply clamping and emit threshold/output events in target order. Division by zero retains the value and still performs the declared output update. | No counter primitive exists. | **Numeric state-trace comparison:** binary32 boundary, swapped limits, unclamped zero/zero limits, repeated threshold crossings, divide-by-zero, disabled, silent-set, and get-value vectors compare bits, latch state, and emitted events. | Not started |
| The generic case primitive stores 16 ordered case strings, matches input ASCII-insensitively with first-match semantics, emits the typed input through default on no match, selects only connected cases for random choice, and shuffles each connected case once while avoiding a consecutive repeat across batches when more than one case exists. | No case primitive exists. | **Seeded case-trace comparison:** duplicate values, sparse cases, mixed case, no match, no connected output, random selection, complete shuffle batches, and restored RNG state compare selected output, default value, shuffle state, and random draws. | Not started |
| Entity snapshots preserve source identity, registry identity, current tick, handle slots and generations, lifecycle and hierarchy state, mutable names and typed fields, output values/actions/counts, event due times and sequence, templates, filter links, contacts, generic logic state, and random state. Restore reproduces the next transition exactly or fails without partial state. | No snapshot or restore contract exists. | **Round-trip continuation comparison:** snapshot at every phase boundary, restore into a fresh world, then compare byte-identical canonical snapshots and transition traces for fixed continuations; source or registry mismatch must fail atomically. | Not started |
| Inside each Simulation-owned tick invocation, Entity applies ordered external lifecycle, contact, and direct-input records, advances due generic scheduled primitives, drains due I/O to quiescence, and commits deferred deletions. Every emitted record receives one monotonic sequence; repeated runs with identical inputs produce identical state and traces. | No Entity phase integration exists. | **Phase-log comparison:** controlled target captures and playsrc runs compare producer sequence, generic primitive firing, nested I/O, deletion visibility, end-of-phase live set, and repeated-run byte identity. | Not started |
| All parser and runtime limits fail before over-bound allocation or mutation. Entity count 8,193, token byte 2,048, template member 17, filter child 6, case 17, and every caller limit plus one return the declared bounded failure while preserving prior authoritative state. | No bounded implementation exists. | **Boundary and resource comparison:** run every fixed and caller limit at `limit-1`, `limit`, and `limit+1`; compare exact error fields, allocator counters, queue/contact/snapshot sizes, prior-state equality, and bounded execution time. | Not started |
| Every accepted generic behavior has one current inventory identity and one Entity implementation; every map compiler, game, Simulation, Collision, Movement, Physics, Networking, Replay, and presentation consumer uses that interface without an embedded parser, graph, queue, hierarchy, trigger state, fallback, or legacy path. | The candidate inventory has 72 rows and 0 accepted items; no implementation or integration exists. | **Regeneration and authority audit:** regenerate the inventory twice for byte identity and exact count, then audit all named producers and consumers for one current interface and no duplicate Entity authority. | Not started |

## Generated Inventories

No generated inventory is accepted. Accepted item count: 0.

| Output | Authority identity | Authority revision | Generator command | Candidate items | Accepted items |
|---|---|---|---|---:|---:|
| [`inventories/generic-behaviors.md`](inventories/generic-behaviors.md) | Source SDK base entity, map-entity, entity-list, handle, input, output, event-queue, hierarchy, template, filter, trigger, generic logic, and save/restore contracts; playsrc Owner Registry | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; reviewed playsrc commit Missing | Missing | 72 | 0 |

The future generator is owned by `tools/playsrc`. It must consume a checked manifest of the fixed SDK contract paths and the current playsrc Owner Registry, emit all 72 stable identities in category and numeric order, retain every unsupported or unknown discovery, and fail on a duplicate identity, missing contract path, unassigned adjacent owner, changed fixed bound, or unclassified behavior. No command name is declared before that implementation exists.

## Exit Criteria

Entity is Complete only when all of these predicates pass:

- All 28 Behavior Families rows are Ready.
- The generic-behavior inventory is generated, current, Accepted, and contains exactly 72 accepted items with no required item `Unsupported`, `Unknown`, `Missing`, or Blocked.
- Entity-lump syntax, identity, typed values, handles, lifecycle, target resolution, hierarchy, attachments, I/O, templates, filters, trigger contacts, relay, timer, counter, case, snapshot, phase-order, and bound evidence satisfies the roadmap contract.
- Every accepted game class registry maps each encountered classname exactly once to a generic composition, game handler, `Intentionally inert`, `Unsupported`, or `Unknown`, and no game classname enters the generic inventory.
- Map, game, Simulation, Collision, Movement, Physics, Networking, Replay, rendering, particle, audio, application, and tool integrations consume one current Entity interface and preserve their ownership exclusions.
- No embedded entity-lump parser, duplicate entity graph, alternate handle table, event queue, hierarchy, trigger-contact state, fallback, compatibility layer, legacy path, or stale generated inventory remains.
- Every encountered entity, key/value, reference, action, input, contact, template member, filter edge, and class binding has exactly one coverage classification, and no required behavior remains `Unsupported`, `Unknown`, `Missing`, Partial, or Blocked.

## Blockers

No delivery blocker is recorded. The inventory generator, denominator review, implementation, integration, and evidence are Not started, and each has an available next action.
