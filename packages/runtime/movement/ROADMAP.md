# Movement Roadmap

[`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines delivery status, coverage classification, inventory, and completion terms.

## Completion Denominator

This leaf roadmap contains exactly 35 behavior rows. The candidate inventory contains 101 generic movement-mode, toss-response, configuration, policy-input, limit, threshold, factor, distance, and timer items in [`inventories/modes-and-parameters.md`](inventories/modes-and-parameters.md). It contributes 0 denominator items until a checked-in generator emits it and a denominator review accepts it. The current completion denominator is therefore 35 behavior rows. Acceptance of all 101 candidate inventory items without a roadmap change would make the denominator 136 items.

The denominator covers generic Source player movement for one simulation tick: canonical movement-command interpretation; button transitions; command and absolute view angles; wish velocity; effective speed; constraints; standing, crouched, and observer hull selection; duck transitions; ground and surface classification; ground identity; friction; ground and air acceleration; jump latching; gravity; falling and landing; base velocity; conveyors; linear and rotational moving-ground carry; steps; slopes; walls; crease motion; stuck recovery; initial overlap; water levels, currents, swimming, and ledge exit; ladders; noclip; generic observer modes; toss motion; collision-query requirements; deterministic authority/prediction output; numerical policy; and classified failure. Game-selected values and game-specific movement decisions remain outside this denominator.

The denominator is Not accepted. The candidate inventory has no generator or review record; the current collision query contract is not accepted; deterministic arithmetic and parity tolerances are undecided; stuck-check cadence combines command and wall-clock inputs in the target; and two toss collision-response values lack a retained observable target comparison.

## Inputs

- One immutable prior `MovementState`: stable player identity; Source-space origin, velocity, base velocity, local and command angles; move type and toss collision-response type; standing, crouched, transition, ladder, ground, water, falling, and stuck state; prior movement-button bits and prior forward command; ground identity and its immutable transform and linear/angular velocity; surface identity and movement properties; movement timers; model scale; health/alive and movement-enable predicates; and the collision snapshot identity used by the step.
- One canonical `MovementCommand`: non-negative safe command number and tick count; finite command and absolute view angles; finite forward, side, and up movement; movement-relevant button bits; and a first-execution marker. Networking owns decoding, ordering, acknowledgement, and transport. Simulation owns command admission and tick scheduling. The selected game owns command changes caused by classes, conditions, items, weapons, teams, and game rules before the generic step.
- One immutable `MovementConfiguration` containing every accepted movement-owned item in [`inventories/modes-and-parameters.md`](inventories/modes-and-parameters.md). Game-owned hull values, view offsets, effective maximum speed, air-speed cap, collision policy, jump policy, and movement modifiers enter through explicit validated values or pure policy results; Movement owns their generic consumption order.
- One positive finite fixed tick interval, one non-negative safe simulation tick, and one immutable simulation-time value. A movement step never reads render time, process time, network arrival time, or an ambient mutable cvar registry.
- One immutable collision snapshot and pure query interface supplied by `packages/world/collision`: exact point-contents queries; zero-motion standing/crouched/observer hull tests; swept hull traces; world/entity hit identity; fraction; end position; valid plane normal; surface identity and properties; contents; `startSolid`; `allSolid`; and the snapshot identity on every result.
- One ordered mover input supplied through entity/simulation coordination: requested old and new transforms, linear and angular velocity, hierarchy identity, blocking policy, and collision snapshot revisions before and after the request. Movement does not advance the mover clock.
- Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`: `src/public/const.h`; `src/game/shared/igamemovement.h`, `gamemovement.{h,cpp}`, `imovehelper.h`, `movevars_shared.{h,cpp}`, `usercmd.{h,cpp}`, `in_buttons.h`, `shareddefs.h`, and `baseplayer_shared.{h,cpp}`; `src/game/server/player_command.cpp`, `player.cpp`, and `physics_main.cpp`; and `src/game/client/prediction.{h,cpp}`.
- The same SDK commit's `src/game/shared/tf/tf_gamemovement.cpp` and `tf_gamerules.cpp`, `src/game/server/tf/tf_playermove.cpp` and `tf_pushentity.cpp`, and `src/game/client/tf/tf_prediction.cpp` establish the generic/TF2 ownership seam. TF2 rows belong to `games/tf2/ROADMAP.md`.

## Outputs

- One immutable `MovementStepResult` containing the complete next `MovementState`: origin; velocity; base velocity; local and command angles; move mode; selected hull state; crouch fraction and timers; ground identity and surface state; ladder normal; water level and contents; water-jump state; fall velocity; stuck-sequence state; and prior-button state for the next command.
- Exact movement observables equivalent to the generic move-data outputs: accumulated wish velocity, jump velocity, and climbed step height.
- One stable ordered contact list. Each contact contains the collision snapshot identity, hit identity, trace result, impact velocity before clipping, and first-contact order; duplicate hit identities have one declared disposition.
- One stable ordered movement-event list containing only generic facts: jumped, landed with pre-impact speed, entered or left water, changed ground, climbed a step, attached to or detached from a ladder, became trapped, recovered from initial overlap, and requested a footstep or impact presentation. Games decide damage, class state, animation meaning, and game events; presentation and audio consume effect requests.
- One mover result containing accepted player displacement, resulting support/base velocity, blocker identity when blocked, and exactly one `moved`, `blocked`, or `crushed` classification. Entity/simulation owns mover acceptance, rollback, I/O, and damage requests.
- A structured failure containing operation, simulation tick, command number, player identity, collision snapshot identity, stable input field or query identity, violated invariant, offending value, configured limit, and exactly one `Malformed`, `Unsupported`, `Unknown`, or `Missing` classification. Failure publishes no next authoritative movement state.

## Invariants

- All positions, velocities, accelerations, hull bounds, distances, and plane normals use Source XYZ coordinates and inches. Angles use degrees. Tick durations and configuration units are explicit; millisecond timers are never compared directly with seconds.
- One step is a pure deterministic transition over the declared command, prior state, configuration, mover inputs, and one immutable collision snapshot. Authority and prediction invoke the same implementation and receive byte-identical serialized results for identical inputs on an accepted arithmetic profile.
- Command ingestion derives pressed and released bits from current and prior button sets exactly once. A held jump cannot create another ground jump until release; a failed generic air attempt and a game-rejected attempt retain their separately declared latch outcomes.
- Command view angles produce the forward/right/up basis used by movement. Absolute view angles are retained separately for parented state. Body/local angle output, view roll, and game-owned angle restrictions never overwrite the command basis retroactively within the same step.
- Movement applies speed selection in one declared order: game/player maximum, client maximum when nonzero, surface maximum-speed factor, outward movement-constraint factor, command-vector clamp, mode-specific wish-speed clamp, and game-owned post-policy where declared. A selected game cannot bypass generic finite-value or maximum-velocity validation.
- Standing, crouched, and observer hulls retain their supplied mins/maxs and model scale. Ducking changes collision hull, origin, view offset, timers, and command scaling as one state transition; a failed unduck retains the crouched hull and a deterministic retry state.
- Ground exists only after a movement-filtered hull trace returns an accepted hit with a standable plane. The full hull is checked before the four declared quadrant hulls. Ground identity, surface identity, surface friction, and base-velocity transition are changed atomically.
- Ground friction is applied before adding base velocity. Ground and air acceleration project current velocity onto normalized wish direction, add no speed when the projected speed already meets the applicable wish speed, and apply surface friction only where the selected generic formula requires it.
- Gravity uses the accepted effective gravity and player scale. Walk movement applies the two half-tick gravity contributions around horizontal/air movement; water-jump and mode rules explicitly suppress or replace gravity. Every gravity path is followed by maximum-velocity validation.
- Collision resolution performs no more than the accepted bump and clipping-plane limits. It consumes the remaining tick fraction after each hit, clips against every accumulated plane, uses the normalized cross product only for an exactly two-plane crease, and stops rather than moving opposite the primal velocity.
- Step movement evaluates low and high routes from the same prior origin and velocity. Only a route ending on standable ground is eligible. The accepted route-selection tie rule and retained vertical-velocity component are observable and game specialization may replace them only in the selected game.
- `startSolid` invokes the accepted deterministic recovery sequence or returns a trapped result. `allSolid` immediately prevents authoritative displacement and zeroes movement velocity according to the active mode. Neither flag is inferred from a miss or broad-phase overlap.
- Water level is exactly not-in-water, feet, waist, or eyes and is derived from declared hull-relative samples. Water contents and current bits remain unsigned 32-bit values. Current contribution, water friction, swim wish velocity, jump-up velocity, and ledge-exit state are applied exactly once per tick.
- Ladders attach only after a movement-filtered hull query identifies ladder contents or a climbable surface within the accepted distance. Ladder motion, jump-away, gravity suppression, and detach state are deterministic; the selected game may disable ladders or supply specialized policy.
- Noclip displacement invokes no collision query; the common prepass can still run target-required water and ground categorization. Observer follow modes copy one supplied target snapshot; fixed modes do not advance position; roaming selects clipped or noclip behavior from explicit configuration. Toss modes never become rigid-body simulation.
- Generic movement emits facts and requests but never mutates health, weapons, classes, conditions, objectives, rulesets, audio, particles, rendering, or mover entity state. Falling, drowning, crushing, and impact damage are selected-game or simulation consumers of movement output.
- Query caches, allocation addresses, worker schedules, broad-phase order, rendering frames, and prediction replay count never affect public movement output. A cache hit and miss over the same collision snapshot produce identical state, contacts, and events.
- Every scalar, vector, angle, bitset, timer, count, identity, hull, transform, query result, and intermediate arithmetic result is finite and within accepted limits before it can enter authoritative output. No fallback movement path, collision sampler, compatibility schema, or legacy state is permitted.

## Ownership Exclusions

- Networking owns user-command encoding/decoding, transport, acknowledgement, queues, backpressure, snapshots, and reconciliation. Simulation owns command admission, fixed-tick scheduling, pre/post-game ordering, state publication, and gameplay authority. Movement interprets exactly one admitted command for one scheduled tick.
- `packages/world/collision` owns collision geometry, contents/mask values, filtering execution, point contents, hull sweeps, overlaps, hit selection, trace fields, and query acceleration. Movement chooses query sequence and interprets returned movement facts without redefining collision.
- `packages/world/entity` owns entity state, parenting, mover requests, mover lifecycle, trigger/touch lifecycle, I/O, rollback publication, and crush dispatch. Movement owns player displacement induced by accepted linear/rotational mover input, support carry, base velocity, and the player-side blocker result.
- `packages/runtime/physics` owns rigid bodies, persistent contacts, constraints, sleeping, restitution, and solver response. Player walk, ladder, water, noclip, observer, and toss movement do not become rigid bodies.
- Each game owns hull and view values, maximum-speed derivation, collision masks/groups, air-speed-cap overrides, jump eligibility and impulse overrides, class/condition/item/weapon movement, fall and drowning damage, game events, and movement differences. TF2 owns air dashes, bunnyhop cap, backward-speed clamp, parachutes, grappling hooks, charges, stuns, taunts, karts, ghosts, lost footing, TF2 duck restrictions, team/buildable collision, disabled ladders, TF2 water sampling, and escort pusher policy.
- Each game-owned ruleset owns mode lifecycle and restrictions, including whether a TF2 ruleset selects escort pusher behavior, noclip permission, observer permission, spawn movement locks, or course/timer interaction.
- Material owns surface-property meaning. Movement consumes validated surface friction, maximum-speed factor, jump factor, material identity, and climbable state. Audio/presentation owns actual footstep, splash, landing, and impact effects.
- Weapons, explosions, trigger pushes, teleports, and game abilities own the impulses they request. Movement consumes explicit base-velocity or impulse inputs at the declared tick boundary and cannot originate those game decisions.
- Source 2 movement, vehicle driving, NPC locomotion, animation root motion, camera smoothing, render interpolation, replay resimulation, and product input bindings are excluded.

## Behavior Families

The selected jump-movement checkpoint covers the complete target-reachable command, ground, air, jump, gravity, fall/land, clip, slope, stair, initial-overlap, crouch, and noclip vectors required by local `jump_beef`. Its retained schedules cross forward/side/diagonal acceleration, release friction, five air-strafe yaw deltas, speed caps, jump press/hold/release and repeated hops, standable/steep slopes, low/18-unit/over-height steps, walls/corners/two-plane creases/seams, deterministic trapped recovery, grounded/airborne/reversed/blocked crouch timelines, pitched noclip acceleration/direct/friction/speed-button boundaries, accepted mode-state dispositions, exact snapshot bytes, 1,024 repeats, and the accepted spawn/browser-command trace. Water, ladders, observers, toss movement, and mover carry remain in their existing rows and are not part of this checkpoint.

The active migration checkpoint implements one fixed-tick, version-1 Source-space Movement state/result for target-reachable walk and noclip: command basis and three-axis inputs, maximum-speed command clamp, support identity/plane/friction, ground and 30-unit-capped air acceleration, TF2 policy values, jump latch/bunnyhop cap/split gravity/fall/landing, reversible timed crouch and blocked retry, four-bump/five-plane clipping, normalized crease motion, slopes/walls, TF2 high-first 18-unit steps, deterministic 54-offset initial-overlap recovery, accepted mode dispositions, complete query/contact/event output, and exact snapshot bytes. Water, ladders, observer/toss modes, moving support/base velocity, generic non-TF2 step policy integration, and Simulation/browser adapters remain outside this checkpoint.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Every generic movement mode and movement-owned parameter has one stable identity, target disposition, supplied-value owner, and numeric contract in [`inventories/modes-and-parameters.md`](inventories/modes-and-parameters.md). | The 101-item inventory is manually derived, has no generator, and is Not accepted. | **Generated-inventory audit:** expand the pinned SDK declarations and generic/game seams, compare stable identities, values, units, defaults, dispositions, exclusions, and exact count, and require no unclassified declaration. | Blocked |
| One admitted command is converted into one finite movement input without consuming weapon selection, random seed, mouse deltas, transport metadata, or a render-frame delta. | `StepInput` admits one command number, finite forward/side/up and pitch/yaw values, jump/duck/speed state, and optional mode request; classified validation fails before transition. Simulation admission remains unintegrated. | Fixed finite/boundary and repeated authority/speculation vectors pass. | Partial |
| Current and prior movement-button sets produce exact changed, pressed, released, held, and next-prior sets, including jump and duck latches. | Versioned state retains prior jump/duck, successful and airborne-failed jump latches until release, duck press/release/reversal, and blocked retry. Other movement-relevant bits remain absent. | Jump press/hold/release/repeated-hop and crouch reversal timelines pass. | Partial |
| Command view angles and parent-relative absolute view angles produce the exact movement basis, normalized yaw handling, body/local angle output, and roll response for each eligible mode. | Binary32 pitch/yaw produce the Source forward/right/up basis; walk flattens/renormalizes it and noclip retains pitch. Absolute parent angles, body output, and roll response remain absent. | Axis, five-yaw air-strafe, side/diagonal, and pitched-noclip vectors pass. | Partial |
| Forward, side, and up commands produce wish direction/speed, effective maximum speed, and outward constraint scaling in the declared order; the selected game supplies game-specific caps and restrictions. | Walk clamps the three-axis command before crouch crop, then derives normalized horizontal wish direction/speed; noclip consumes all three axes and speed-button factor. Client/surface maximum factors and annular constraints remain absent. | Cardinal/diagonal, crouch-crop, class-cap, and speed-button vectors pass. | Partial |
| Standing, crouched, and observer movement select the supplied game-owned hull and view values, preserve asymmetric bounds, and apply model scale exactly once. | Policy-supplied asymmetric standing/crouched hulls and views drive every selected query and snapshot. Observer hulls and model scale remain absent. | Ground/air switch and standing-clearance vectors pass with TF2 82/62 hulls and 68/45 views. | Partial |
| Duck press, hold, release, reversal, grounded completion, airborne completion, duck-jump, blocked unduck, retry, origin shift, view interpolation, and one-time ground command crop produce one exact next crouch state. | One state machine implements all listed target-reachable transitions, binary32 timer/fraction/view state, hull-top-preserving air origin shifts, standing-hull clearance, blocked retry, and post-command-clamp one-third crop. Generic water/observer restrictions remain absent. | Fixed ground/air/reversal/duck-jump/ceiling-unblock timelines pass per tick. | Partial |
| Ground categorization rejects rapid upward motion and ladders, checks the full hull then four quadrants, accepts only standable planes, and applies the accepted ground snap rule. | Walk rejects upward speed above 250, probes full then ordered quadrant hulls, accepts normal Z at least 0.7, extends retained-ground probes by step height, and records exact queries. Ladder/water paths remain absent. | Flat, steep-full/standable-quadrant, standable-ramp, jump, and no-hit vectors pass. | Partial |
| Ground transitions atomically update ground identity, surface identity, surface friction, vertical velocity, touch output, and support-relative base velocity; fall/landing state follows in the same tick's declared order. | State atomically retains support identity, plane, supplied surface friction, fall speed, and ordered ground/landing events. Surface identity and moving-support/base-velocity transfer remain absent. | Surface-factor, support change, jump departure, ramp landing, and repeated-hop vectors pass. | Partial |
| Conveyor and explicit base-velocity inputs are combined once, excluded from ground friction, included during displacement, transferred on ground enter/leave, and cleared or retained by the declared rule. | No base-velocity or conveyor behavior exists. | **Base-velocity sequence comparison:** fixed standing, walking, jumping, airborne, water, ladder, and toss sequences over each axis compare origin, local velocity, base velocity, and ground identity per tick. | Not started |
| Accepted linear and rotational mover transforms carry supported players, push intersecting players through collision, classify blockers, preserve one snapshot revision, and return mover acceptance data without advancing entity state. | No moving-ground carry or player-side pusher resolution exists. | **Mover differential capture:** compare static support, translation, each rotation axis, hierarchy, side contact, ceiling blockage, rollback, and game-policy cases against SDK generic pusher behavior; retain requested transforms, traces, player state, and mover result. | Not started |
| Ground friction uses horizontal speed, stop speed, configured friction, and surface friction before base velocity and preserves direction while reducing magnitude. | Stop-speed friction consumes supplied surface friction before acceleration and preserves direction; moving base velocity remains absent. | Release, stop-speed, high-speed, zero-speed, and half-surface-friction vectors pass. | Partial |
| Ground acceleration adds only the missing projected speed along normalized wish direction and preserves non-wish velocity components before declared ground speed clamps. | Projected binary32 acceleration, class maximum clamp, orthogonal preservation, and TF2 backward policy execute in Movement. | Forward/side/diagonal/aligned/cap/backward vectors pass. | Ready |
| Air acceleration uses uncapped wish speed for acceleration magnitude, the effective generic/game-owned air-speed cap for projected add speed, and no ground friction. | The projected add speed uses the policy cap while acceleration magnitude uses uncapped maximum-speed-clamped wish speed; no ground friction runs. | Exact 0/45/90/135/180-degree vectors and jump schedules pass. | Ready |
| Generic ground jump eligibility, surface jump factor, jump impulse application, half-gravity, ground detachment, base-velocity inheritance, output jump velocity, and release latch occur once; game-owned jump policy can replace eligibility or impulse without duplicating generic state. | Eligibility, policy impulse, surface factor, TF2 bunnyhop cap, detachment, three observable half-gravity applications on the jump tick, output, and latch execute once. Base/support velocity inheritance remains absent. | Exact press/hold/release/duck-block/repeated-hop and `jump_beef` traces pass. | Partial |
| Effective gravity and player gravity scale advance walk and fly-gravity modes with the declared half-step/full-step ordering, vertical base-velocity treatment, suppression rules, and per-axis maximum velocity. | Walk split gravity and symmetric per-axis maximum velocity execute around movement and the jump specialization. Player scale, base Z, water suppression, and fly-gravity remain absent. | Ground, jump, fall, landing, repeated-air, and maximum-bound vectors pass. | Partial |
| Falling records the greatest applicable downward speed; landing adjusts for water, floating or descending ground, emits one landing fact, clears fall state, and leaves damage/effects to game consumers. | Walk state retains greatest downward speed, emits one landing fact, and clears it. Water and moving-support adjustments remain absent. | Fixed airborne-to-flat/ramp landing and repeated-hop timelines pass. | Partial |
| Unobstructed walk movement clamps command wish speed, accelerates, adds base velocity, advances horizontal position, removes base velocity, snaps to valid ground, and stops sub-unit total motion according to the accepted rule. | Fixed command basis, speed clamp, acceleration, collision displacement, and ground reprobe are implemented. Base velocity and exact stop/snap policies remain absent. | Fixed floor forward movement passes. | Partial |
| Obstructed movement consumes at most four bumps and five planes, advances by remaining tick time, clips floor/wall velocity, resolves exactly two-plane creases, stops on excess planes or reversal, and records stable contacts. | One bounded implementation performs all listed clipping behavior, endpoint tests, normalized creases, stable contact order, and trapped output. Duplicate-hit disposition beyond the selected scenes remains unclassified. | Wall, corner/crease, ramp, step, all-solid, and 1,024-repeat vectors pass. | Partial |
| Ramps, shallow/steep slopes, diagonal walls, convex/brush seams, thin solids, and maximum-velocity one-tick motion remain non-tunnelling and preserve the standable/wall distinction. | Arbitrary normalized planes retain the 0.7 standable distinction and clip through the same bounded sweep path; broader Collision geometry evidence remains blocked by its roadmap. | Standable ramp, steep full-hull/quadrant, orthogonal walls/corner, and step scenes pass. | Partial |
| Step movement evaluates low and high routes from one prior state, respects auto-movement and configured step height, rejects non-standable high routes, applies the accepted horizontal-distance tie rule, and accumulates positive climbed height. | Policy selects generic furthest-horizontal or TF2 high-first routes. Both use height plus 1/32 inch, reject blocked/all-solid/unchanged/non-standable high routes, and emit positive height. Auto-movement eligibility remains absent. | Flat, 18-unit, and 19-unit blocked-step vectors pass. | Partial |
| Initial overlap checks run at the declared cadence and try the exact bounded recovery sequence without random, render-time, or allocation-order inputs. | No stuck detection or recovery exists; the target mixes command-based intervals with a wall-clock throttle and no deterministic mapping has been accepted. | **Stuck cadence capture:** retain client/server command number, simulation tick, elapsed time, 54 candidate offsets, collision outputs, chosen recovery, contacts, and final state; compare a proposed tick-derived schedule before acceptance. | Blocked |
| `startSolid` without recovery and `allSolid` during movement, steps, water, ladders, and toss modes produce categorical trapped/displacement/velocity outcomes and never publish an embedded successful endpoint. | Walk tests initial/endpoint overlap, attempts the declared deterministic 54 offsets, and returns recovered or trapped with zero movement velocity; water/ladder/toss dispositions remain absent. | Fixed offset-18 recovery and all-54 trapped vectors pass. | Partial |
| Hull-relative samples classify exactly four water levels, retain water/slime/current contents, detect entry/exit, and add each directional current to base velocity once with depth scaling. | No water classification or current behavior exists. | **Water-sample grid comparison:** fixed brush volumes and boundaries compare sample positions, contents, level/type, current vector, base velocity, entry time, and transition events exactly. | Not started |
| Waist/eye water movement derives three-dimensional wish velocity from command view, applies idle sinking, jump-up intent, maximum and water-wish scaling, water friction/acceleration, base velocity, collision, stepping, and recategorization in declared order. | No water movement exists. | **Swim timeline comparison:** cardinal/diagonal/pitched input, idle, jump, water/slime, each water level, moving support, step, collision, and exit sequences compare all state and query outputs per tick. | Not started |
| Generic water ledge exit checks waist obstruction, eye clearance, standable landing, approach direction, downward-speed gate, and timers before applying horizontal push and upward velocity; release or loss of water ends the state exactly once. | No water-jump or ledge-exit state exists. | **Ledge-exit decision table:** cross every eligibility predicate plus timer boundaries and obstruction heights; compare traces, latch, velocity, flags, timers, water state, and termination tick. | Not started |
| Generic ladder movement detects ladder contents or climbable surfaces, attaches within distance, maps command basis onto the ladder plane, handles floor departure and jump-away, suppresses gravity, and detaches when contact is lost or the game disables ladders. | No ladder behavior exists. | **Ladder scene comparison:** vertical/sloped ladders, each approach/view angle, ground contact, lateral input, idle, top/bottom, jump-away, lost contact, and disabled-game policy compare mode, normal, velocity, queries, and events. | Not started |
| Noclip derives three-axis wish velocity from command view, applies configured acceleration/friction or direct velocity, clamps speed, advances without displacement collision, and handles the speed button and negative no-acceleration policy exactly. | Noclip implements the complete listed motion and accepted preserve/reset mode transitions; it issues zero collision queries. The TF2 browser command adapter does not yet supply up/speed/mode fields. | Exact pitched, speed-button, positive/zero/negative acceleration, maximum, transition, no-query, and repeated vectors pass. | Partial |
| Generic observer follow modes copy one supplied target snapshot; fixed/death modes do not advance; roaming selects clipped or noclip motion, observer factors, friction, and speed-button behavior. | No observer movement exists. | **Observer-mode matrix:** every accepted observer mode, target present/missing, clipped/noclip selection, movement command, and speed button compare origin, velocity, angles, collision queries, and classified errors. | Not started |
| Fly and fly-gravity toss movement accelerate from full view basis, apply optional gravity/base velocity, sweep once, classify all-solid, and apply each accepted toss collision-response value. | No toss movement exists; official declarations expose slide and custom values whose complete observable player response is not established by the generic handler, and no retained controlled target comparison exists. | **Toss response capture:** cross fly/fly-gravity with default, bounce, slide, and custom responses over floor/wall/ceiling/all-solid hits; retain target origin, velocity, ground, contacts, and callback effects before selecting a pure policy contract. | Blocked |
| Wish velocity, jump velocity, step height, contacts, ground/water/ladder/mover transitions, and generic effect requests are emitted in stable order and do not directly execute game, entity, damage, audio, or presentation behavior. | `StepResult` emits state, wish/jump/step values, contacts, ground/crouch/mode/jump/land/step/recovery events, and complete queries without game/presentation mutation. Water/ladder/mover outputs remain absent. | Compound movement tests compare complete results and snapshots. | Partial |
| Every movement query uses the selected hull, game-supplied movement policy, exact collision snapshot, stable ignored player identity, and complete trace fields; no mesh sampler, render ray, fallback box, or alternate query authority exists. | The collision roadmap has no accepted query/result inventory or implementation, so the current movement collision adapter cannot be implemented or integrated. | **Collision-transcript integration:** compare ordered point/hull requests and complete returned records for every behavior vector, then repository-audit one collision authority and no fallback or duplicate sampler. | Blocked |
| Finite arithmetic, source constants, thresholds, limits, target-reachable correction behavior, malformed inputs, unsupported modes, missing identities, and query failures have exact outcomes and field-specific parity tolerances. | Selected walk/noclip inputs and trace results validate binary32 finiteness, command/coordinate/velocity/tick/hull/fraction/normal/water bounds, and return command number, operation, classification, field, offending bits, and limit bits without output. The accepted cross-platform arithmetic, target correction, all-mode failure, and tolerance contracts remain absent. | Non-finite command and deterministic failure vectors pass; the complete boundary matrix remains blocked. | Blocked |
| Repeated authoritative and predicted execution from identical state, command, configuration, mover input, and collision snapshot produces identical next state, contacts, events, query transcript, and error; resimulation count cannot change output. | One pure `step` implementation and version-1 binary32 snapshot produce exact bytes for identical selected inputs. Accepted multi-platform arithmetic and external prediction adapters remain blocked. | Authority/speculation equality plus 1,024 exact repeated result/snapshot comparisons pass locally. | Blocked |
| Simulation, networking prediction, games, rulesets, entities, collision, physics, replay interpretation, tools, and inspectors consume one current Movement interface without embedded movement math, duplicate state authority, fallback, compatibility layer, legacy path, or stale inventory. | TF2 session state, class policy, combat impulses, teleports, and player snapshots consume one Movement transition. The existing TF2 WASM/browser adapter still omits complete Movement snapshot and noclip/up/speed command fields; other named consumers remain absent. | TF2 native and existing WASM tests pass; Stage 2 seam is declared in TF2 and Movement documentation. | Partial |

## Generated Inventories

No generated Movement inventory is accepted. Accepted inventory item count: 0.

| Output | Authority identity | Authority revision | Generator command | Candidate items | Accepted items |
|---|---|---|---|---:|---:|
| [`inventories/modes-and-parameters.md`](inventories/modes-and-parameters.md) | SDK generic movement modes, toss responses, shared movement variables, move data, game-supplied policy seams, constants, limits, thresholds, factors, distances, timers, and TF2 owner overrides | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a` | Missing | 101 | 0 |

Current manually derived candidate count: 101. Current generated count: 0.

The future generator is owned by `tools/playsrc`. It must consume a retained SDK authority snapshot at the recorded revision plus the accepted root owner registry; emit the assigned inventory in stable-identity order; retain every intentionally inert, unsupported, malformed, unknown, and missing classification; identify the supplied-value owner separately from the generic consumer; and fail on an unclassified movement mode, shared variable, policy input, limit, threshold, factor, distance, timer, or game override. No command name is declared before that implementation exists.

## Exit Criteria

The Movement package is Complete only when all of these predicates pass:

- All 35 behavior rows are Ready.
- [`inventories/modes-and-parameters.md`](inventories/modes-and-parameters.md) has an Accepted denominator review with current authority revision, generator command, output path, exact item count, reviewer identity, review date, and reviewed commit.
- Every accepted generic movement mode, parameter, policy seam, threshold, limit, timer, and failure state has one implementation and one coverage classification; every game-owned value and override is excluded from generic ownership and integrated through the declared seam.
- The arithmetic profile, platform set, stuck cadence, target-reachable correction behavior, toss response contract, and field-specific parity tolerances are accepted and retained with their evidence.
- Every fixed command/state/configuration/collision vector matches target movement state, contacts, events, queries, and classified failures under its declared comparison predicate.
- Authority and prediction use the same Movement implementation and produce byte-identical serialized output for identical inputs across repeated and concurrent execution on every accepted platform.
- Collision, Entity, Simulation, Networking, Physics, each selected game and ruleset, replay interpretation, tools, and inspectors use the current interfaces with no duplicate movement authority, mesh sampler, fallback, compatibility layer, legacy path, or stale inventory.
- Movement has no dependency on a game, ruleset, application, service, renderer, audio system, network transport, or replay authority; game differences enter only through declared values and pure policy results.
- No required item remains Blocked, Unsupported, Unknown, Missing, Malformed, or Partial.

## Blockers

- **Inventory generator:** no checked-in command emits [`inventories/modes-and-parameters.md`](inventories/modes-and-parameters.md). Checked `tools/`, root manifests, and the current Movement package tree.
- **Collision interface:** the Collision roadmap and both candidate inventories are Not accepted and contain unresolved query/result behavior. Movement cannot integrate exact point contents, hull sweeps, initial overlap, equal-hit identity, moving snapshots, or error propagation through a current interface. Checked [`../../world/collision/ROADMAP.md`](../../world/collision/ROADMAP.md), its inventories, and the current package trees.
- **Stuck cadence:** generic movement combines command-number intervals with a process-time minimum interval and distinct client/server recovery paths. No accepted deterministic tick mapping or retained command-by-command target capture exists. Checked SDK `gamemovement.cpp`, server `player_command.cpp`, client `prediction.cpp`, and current movement evidence.
- **Toss response contract:** `MOVECOLLIDE_FLY_SLIDE` and `MOVECOLLIDE_FLY_CUSTOM` are declared, but the complete observable generic player response is not established by the generic toss handler and no controlled target capture is retained. Checked SDK `const.h`, `gamemovement.cpp`, move-helper interfaces, and game specializations.
- **Arithmetic and tolerances:** no accepted decision names the binary arithmetic profile, platform set, treatment of fused operations, target-reachable non-finite correction contract, or per-field parity tolerance for origin, velocity, angle, fraction-derived displacement, crouch interpolation, mover transforms, and timers. Checked the SDK files named in Inputs, `TERMINOLOGY.md`, the roadmap contract, and current package evidence.
