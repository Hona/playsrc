# Physics Roadmap

[`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines delivery statuses, coverage classifications, inventory, evidence, and Complete.

## Completion Denominator

This leaf roadmap contains exactly 41 behavior rows. [`inventories/body-and-constraint-families.md`](inventories/body-and-constraint-families.md) contains 30 candidate body-motion, body-shape, body-role, relationship, constraint, controller, and articulated-assembly items. Those candidates contribute 0 denominator items until a checked-in generator emits the inventory and a denominator review accepts this roadmap. The current completion denominator is therefore 41 behavior rows. Acceptance of all 30 candidate inventory items without a roadmap change would make the denominator 71 items.

The denominator covers isolated physics environments; bounded body and relationship lifecycle; static, kinematic, motion-disabled, and dynamic rigid-body state; transforms; velocity; mass; inertia; gravity; damping; aerodynamic drag; force, impulse, and torque application; activation and sleeping; body-pair selection; broad-phase synchronization; continuous collision; persistent contacts and manifolds; physical surface response; penetration recovery; stacking; the accepted constraint and controller families; moving geometry; fluids; bone followers; ragdoll state; breakable state; physical callbacks; fixed-tick advancement; deterministic ordering; numerical and resource bounds; single-writer concurrency; continuation snapshots; and gameplay/entity seams.

The denominator is Not accepted. The generated inventory command and exact target-game occurrence registries are Missing; persistent-contact ownership is unresolved at the collision boundary; the physical surface-property producer is not declared by its adjacent owner; BSP, PHY, Collision, and Physics disagree about collision-payload and shape production; and numeric performance budgets and platform profiles have not been selected.

## Inputs

- One environment definition containing a stable environment identity, positive fixed tick interval, Source-space gravity vector, non-negative air density, arithmetic profile, surface-registry identity, collision-world identity, and performance profile. The TF2 server comparison profile uses its declared simulation tick and the public server setting of 10 maximum collisions per object per timestep; no caller can substitute render-frame duration.
- Immutable canonical collision shapes and stateless geometric query results supplied by `packages/world/collision`. Each polygon convex exposes stable shape/solid/convex identity, exact authored binary32 internal-space points, complete authored 16-byte triangle records, all three packed directed-edge words in authored order, source association, local transform, bounds, contents, and physical-surface identity. Missing authored topology fails explicitly; Physics never parses BSP, PHY, StudioModel, or collision-shape bytes and never reconstructs lost directed links.
- Ordered body definitions. Each definition supplies a stable body identity; one accepted motion class and shape family from [`inventories/body-and-constraint-families.md`](inventories/body-and-constraint-families.md); transform; linear and local-axis angular velocity; mass; local inertia; local center of mass; volume; gravity, drag, motion, collision, and trigger state; physical-surface identity; contents and generic pair-filter keys; and an opaque owner identity used only in emitted events.
- Ordered relationship definitions for pair exclusions, indexed collision sets, springs, constraints, constraint groups, controllers, fluids, bone followers, ragdolls, and breakable fragment sets. Every body reference names a body in the same environment or the operation is `Missing`.
- One ordered command batch per physics tick. Commands can create, destroy, or transfer bodies and relationships; set transforms, velocities, mass properties, materials, flags, and controller targets; apply center or offset impulses and torque impulses; wake or sleep bodies; change pair policy; activate or deactivate constraints; and request a supplied fragment transition. Every command carries tick, producer sequence, operation identity, and stable target identity.
- An owned game collision policy supplies pair filtering, penetration and solver-budget decisions in declared order. Its state is cloned and restored with the physical transaction, rather than shared through a mutable callback pointer. Deferred gameplay work remains game-owned; callbacks cannot reenter world mutation.
- Resolved physical surface records containing exact identity, friction, elasticity, density, thickness, and dampening. The adjacent public owner for parsing and resolving these records is unresolved. Physics applies the records and never owns audio, particle, decal, or game-material effects.
- Ordered kinematic transforms, shadow targets, bone transforms, vehicle controls, and ragdoll activation inputs supplied by their owning Entity, Movement, StudioModel, or game integration. Physics does not derive animation, mover trajectories, player commands, or game intent.
- A required limits record containing `maxEnvironments`, `maxBodies`, `maxStaticBodies`, `maxKinematicBodies`, `maxDynamicBodies`, `maxBodyShapeReferences`, `maxPairExclusions`, `maxCollisionSetElements`, `maxConstraints`, `maxConstraintGroups`, `maxSprings`, `maxControllers`, `maxFluids`, `maxBoneFollowers`, `maxRagdollBodies`, `maxFragmentsPerBreak`, `maxCommandsPerTick`, `maxCandidatePairs`, `maxNarrowPhasePairs`, `maxPersistentContacts`, `maxManifolds`, `maxPointsPerManifold`, `maxSolverIslands`, `maxBodiesPerIsland`, `maxSolverIterations`, `maxContinuousIterations`, `maxPenetrationIterations`, `maxSubsteps`, `maxEventsPerTick`, `maxSnapshotBytes`, `maxScratchBytes`, and `maxConcurrentReaders`. Every field is a non-negative integer and no operation has an unbounded mode.
- Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`: `src/public/vphysics_interface.h`; `src/public/vphysics/{constraints.h,friction.h,performance.h,player_controller.h,vehicles.h,collision_set.h,object_hash.h}`; `src/game/shared/{baseentity_shared.cpp,physics_shared.h,physics_shared.cpp,ragdoll_shared.h,ragdoll_shared.cpp,props_shared.h}`; and `src/game/server/{physics.h,physics.cpp,physics_collisionevent.h,physconstraint.h,physconstraint.cpp,physics_bone_follower.h,physics_bone_follower.cpp,props.h,props.cpp,physics_impact_damage.h,physics_impact_damage.cpp}`. TF2 integration also uses the public TF2 grenade, prop, and collision-group consumers at that commit.
- Configured TF2 build `24245096`, patch `10828683`: Win64 `VPhysics031` binary SHA-256 `7994e08ecd78af24a5980a8522068301523b23a3c326ce30f779dde0daf1d2b8`; `models/weapons/w_models/w_stickybomb.phy` SHA-256 `7e70a4a90eca8bb74aafddf01bdbd755532aa1229397f39f9121be792143ce57`; `models/props_gameplay/resupply_locker.phy` SHA-256 `c3ff7d83b9bf5cbab075ae814e3348194a5cb8e08f2092b89419bbad11b48a03`; `jump_beef` BSP SHA-256 `b2e22010b56aa03387c76396a55f2fb83cdeb72a9562ed16cfb656a747e58959`; and exact indexed surface-property manifest, base, HL2, and TF2 documents. Controlled fixed-input module observations establish arithmetic width, compact-feature topology, body response, persistent contacts, callback order, and continuation; a module hash alone does not establish behavior.
- Valve Developer Community `VPhysics` revision `505683`, dated 2026-05-15, is a public inventory lead for collision-model sources, QPhysics/VPhysics ownership, initialization modes, units, interfaces, TF2-branch arithmetic history, and the reported post-2025-12-23 collision fix. Valve Source SDK declarations and callers above govern every exposed contract; configured-build probes must establish arithmetic and fixed collision behavior before numeric parity claims.
- One fixed physical-prop target naming exact model bytes, body mode, transform, surfaces, force vector and point, tick interval, and queried state. This input is Missing: the declared `jump_beef` prop set contains no selected `prop_physics` trace subject.
- Controlled target observations for fixed body, contact, constraint, fluid, controller, ragdoll, bone-follower, break, event, and tick scenarios. Each capture supplies identical initial state, command sequence, tick interval, physical surfaces, and collision geometry.

## Outputs

- One `PhysicsWorld` per environment containing monotonic world revision, simulation tick and time, stable body and relationship tables, broad-phase synchronization revision, persistent contact/manifold state, solver-island state, controller state, fluid membership, sleeping state, and next event sequence.
- One ordered `PhysicsTransitionBatch` per completed tick containing created, changed, transferred, sleeping, waking, and destroyed identities; constraint state changes; contact transitions; body transforms and velocities; and the resulting world revision. A failed tick publishes no partial transition batch.
- A body-state record containing motion class, shape identity, transform, linear velocity, local-axis angular velocity, mass and inverse mass, local inertia and inverse inertia, center of mass, volume, energy, material identity, enabled flags, sleep state, and current environment identity.
- A persistent-contact snapshot containing stable body pair, manifold identity, ordered points, oriented normal, penetration depth, normal force, absorbed energy, friction coefficient, surface identities, lifetime state, and valid tick. Stateless geometric contacts remain Collision output and never become solver state without Physics admission.
- Ordered physical events for matched pre/post collision observations, contact start/end, friction, fluid enter/leave, trigger enter/leave, wake/sleep, constraint break, body transfer, and fragment transition. Collision events include pre/post linear and angular velocities, contact point and normal, relative contact speed, collision speed, surfaces, and solved impulse data required by game-owned damage policy.
- An immutable `PhysicsSnapshot` sufficient to restore every Physics-owned body, relationship, contact, controller, fluid, sleep, ordering, and fixed-tick state bit and reproduce the next transition against the same collision world, surface registry, arithmetic profile, and limits. Physics defines no file, wire, or compatibility encoding for this value.
- A structured error naming environment, tick, producer sequence, operation, stable target identities, offending scalar or count, configured limit, violated invariant, and exactly one `Malformed`, `Unsupported`, `Unknown`, or `Missing` classification.

## Invariants

- Public position, distance, velocity, force impulse, and torque impulse units follow `vphysics_interface.h`: Source XYZ inches, seconds, kilograms, degrees, kilograms-inches-per-second, and kilograms-degrees-per-second. Angular velocity is stored relative to body-local axes; world-space contact velocity is derived from transform, center velocity, and local angular velocity.
- Initial body mass is in `[0.1, 50000]` kilograms. A runtime mass mutation accepts only finite positive mass and caps the stored result at `50000`; it never silently creates a static body. Static state, motion-disabled state, and zero inverse mass are distinct observable states.
- Static bodies never integrate. Kinematic bodies follow supplied transforms and expose derived surface velocity without accepting force-driven displacement. Dynamic bodies integrate while motion is enabled. Disabling dynamic motion retains dynamic identity, transform, velocity, mass, contacts, and constraints and does not reclassify the body as static.
- One stable identity names each environment, body, shape reference, constraint, spring, controller, fluid, contact, manifold, and event for its complete lifetime. Allocation addresses, container slots, broad-phase nodes, worker schedules, and contact-cache slots never become identity or tie-break inputs.
- A body command batch is validated in full before mutation. Commands then apply by producer sequence. A command targeting an identity destroyed by an earlier command is `Missing`; duplicate creation is `Malformed`; a failed command prevents tick publication and leaves the prior authoritative world unchanged.
- A tick synchronizes every accepted transform exactly once, produces stable body-pair candidates, admits or retires contacts, solves connected islands, integrates dynamic state, updates sleeping state, and emits one final immutable snapshot. Render frames never advance or partially observe these phases.
- Broad phase may return false positives and cannot omit a pair whose exhaustive accepted-shape test produces a required contact or continuous hit. Narrow-phase geometry remains Collision-owned; Physics owns candidate consumption, persistent manifold lifetime, response, and solver ordering.
- Pair filtering applies environment equality, enabled collision state, body role, explicit pair exclusion, indexed collision-set result, contents intersection, generic pair keys, game-owned pure predicate, and pre-solve disposition in that order. Rejection retires existing contacts deterministically before event publication.
- Persistent contacts are keyed by stable body pair and stable geometric features. Point replacement, contact start/end, normal orientation, warm state, and friction state are deterministic under the declared arithmetic profile. Pure geometric overlap alone is not a persistent contact event.
- Physical pair friction is `clamp(surfaceA.friction × surfaceB.friction, 0, 1)`. The configured static-floor sticky response matrix establishes restitution speed ratios of `0.19998794794082642`, `0.5`, `sqrt(0.5)`, and `1` for elasticity products `0`, `0.25`, `0.5`, and `1`: nonzero products use `sqrt(surfaceA.elasticity × surfaceB.elasticity)`, while zero follows the partial-push crossing and minimum-separation rules below. Density, thickness, and dampening retain their supplied units and affect only their declared rigid-body, fluid, or contact calculation. Unknown or Missing surface identity cannot become a default surface inside Physics.
- The selected impact response owns both complete endpoint states, authored body-local contact offsets, orientation matrices, inverse masses/inertias, and pre/post velocities. It constructs a cone with tangent `pairFriction × (1 + sqrt(pairElasticity))`; evaluates cone cosine as binary32 `1 - angle²/2 + angle⁴/24` and sine as `cosine × tangent`; and applies at most 100 fixed partial point impulses. Each partial impulse uses widened binary32 `0.20000000298023224` times initial approaching point speed and the two endpoints' selected direction-response mass. Static endpoints use a `100000` response-mass multiplier. Point velocity and cone direction are recomputed after every push. Nonzero elasticity applies one normal unit probe, reverses that probe through binary32 subtraction, then applies the normal impulse producing `sqrt(pairElasticity) × initialApproachingNormalSpeed`; dynamic opposing endpoints apply the exact final minimum-separation correction. Every ordered push retains scalar, direction, pre-push relative velocity, post-push dynamic linear/angular velocity, and exact applied component deltas. Whole-body kinetic-energy interpolation is not the target contract. The configured compact-convex/plane contact distance is `0.2499 in`.
- Force and torque inputs are impulses unless the selected motion-controller result explicitly declares acceleration or force integrated over the current fixed tick. Center impulses change linear velocity; offset impulses also change local angular velocity about the current center of mass; static, kinematic, and motion-disabled bodies ignore force-driven displacement without consuming a different body's impulse.
- Sleeping removes a dynamic body from integration but not from identity, pair policy, contacts, constraints, fluids, or snapshots. Explicit wake, accepted nonzero velocity or impulse, enabled motion, active controller target, moving contact, constraint activation, or fluid wake request follows the accepted wake matrix. Wake/sleep events are edge-triggered once per state change.
- Continuous collision uses supplied relative-motion casts for accepted fast body/world and body/body pairs. Look-ahead, substep, collision-count, and iteration limits come from the selected performance profile; reaching a bound returns a classified result and never tunnels by switching to a discrete-only fallback.
- Penetration recovery is a bounded solver operation selected by one pure penetration disposition. Recovery cannot teleport an unrelated body, change game collision policy, create damage, or suppress a retained penetration diagnostic. Exhausting the accepted iteration or correction bound leaves the prior published tick authoritative.
- Constraint endpoints belong to one environment. Destroying or transferring an endpoint deterministically deactivates or transfers every affected relationship before the body changes environment. Constraint break thresholds of zero mean unbreakable; one threshold crossing produces one inactive state and one break event.
- Ragdoll physical state contains at most 24 body elements. Its body order, parent references, collision-set choices, three-axis joint state, animated-friction input, sleep result, and solved transforms remain Physics state; skeleton pose production, death policy, retention policy, and pixels remain external.
- A fragment transition is atomic: retire the source body's contacts and relationships, remove it, create the supplied ordered fragment bodies, apply supplied inherited and burst impulses, and emit one transition. Physics never chooses whether an entity breaks, which fragments exist, damage amount, health, model, effect, or sound.
- Event ordering is `(tick, substep, event-kind rank, lower body identity, higher body identity, relationship identity, contact identity, event sequence)`. Matched pre/post collision observations share one contact-attempt identity. Consumer commands caused by emitted events enter the next declared Physics command phase; reentrant world mutation is prohibited.
- Mutable world advancement is single-writer. Concurrent calls that create, mutate, transfer, or step the same environment fail with `InvalidPhase`. Immutable completed snapshots support up to `maxConcurrentReaders` readers and produce bit-identical ordered reads without serializing those readers through the mutable world.
- Every scalar is finite before use. Every count, product, sum, transform, inverse, allocation, candidate visit, narrow-phase request, contact point, island, solver iteration, event, snapshot byte, and scratch byte is checked against fixed numeric rules and the required limits before mutation or allocation.
- Physics snapshots contain only current-contract structured state. Simulation, Networking, Replay, tools, and applications may encode or transport them but cannot redefine Physics state, retain a compatibility schema, or restore a snapshot against different collision, surface, arithmetic, or limits identities.

## Ownership Exclusions

- BSP headers, lumps, model-block framing, and exact bytes belong to `packages/formats/bsp`. PHY headers, collision payloads, convex records, physical keydata syntax, and decoded runtime-neutral records belong to `packages/formats/phy`.
- Immutable collision-shape construction, contents and masks, support queries, ray and hull casts, overlaps, stateless geometric contacts, broad-phase query structures, and narrow phase belong to `packages/world/collision`. Physics owns only dynamic body state, persistent contacts/manifolds, physical response, and solver integration over those results.
- Physical surface-record parsing, inheritance, name resolution, and registry provenance require an adjacent public owner. Physics owns application of supplied friction, elasticity, density, thickness, and dampening. Audio, particle, decal, movement, and game meanings of the same surface remain with their owners.
- Entity identity, classname behavior, lifecycle, hierarchy, I/O, trigger lifecycle, mover request/completion, break decision, fragment selection, and forwarding an event to an owning entity belong to `packages/world/entity` and the selected game.
- Player command movement, hull selection, stairs, ground state, water level, base velocity, mover trajectory, push rollback, blocker choice, and carried-player displacement belong to `packages/runtime/movement` and the selected game. Physics applies only supplied controller targets and physical responses.
- Simulation owns the gameplay tick and cross-module phase schedule. Physics owns deterministic ordering only inside one Simulation-invoked Physics phase and cannot advance gameplay authority independently.
- TF2, CS:S, and legacy Source 1 CS:GO own collision-group extensions and pair decisions, damage and health policy, prop and projectile behavior, ragdoll creation/retention policy, vehicle control meaning, and game-specific callback responses. Physics invokes typed pure inputs and emits physical facts.
- StudioModel and the selected animation owner produce skeleton and bone transforms. Physics advances ragdoll and bone-follower bodies but does not parse models, evaluate animation, or produce a render pose.
- Rendering owns ragdoll, prop, fragment, and bone-follower presentation. Audio and Particle own impact, scrape, break, strain, splash, and effect selection. Physics emits no pixel, GPU, audio, particle, or decal work.
- Networking and Replay own wire and recorded-state interpretation. Asset Store owns durable artifacts. Tools own inventory generation, captures, benchmarks, and inspection. Applications cannot embed another rigid-body solver.
- Persistent save-game file formats are excluded from the current Active targets. A future accepted owner requirement must add any such serialization behavior to this denominator before implementation.

## Behavior Families

`source_shape_bounds` computes model bounds from authored physical points, the Source angle basis, and ordered support selection. All 160 configured sticky, grenade, locker and Soldier-solid bounds match the target bit-for-bit across eight transforms. `ShapeCastModel` matches 5,760 physical point/hull traces both directly and through Collision's scene dispatcher. TF2 publishes all 17 configured Soldier followers into scene collision with separate collider identities, parent hit reporting, pose invalidation and removal checks. Ordinary parsed vertex bounds and the former physical-shape SAT tracer are not substituted. Full gameplay and performance acceptance remain in progress.

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
| --- | --- | --- | --- |
| Per-object movement ranges retain separate total/center travel, predicted boundary travel, ordered distance listeners, renewal keys, callback budgets, and periodic value resets. | `MovementRange` owns the clock, listener slots/reuse, due checks, renewal/budget protocol, and reset notifications. Each environment body now advances and snapshots its range from retained motion phases. Object-pair subscriptions remain unfinished. | **5,623/5,623** native clock/listener states, **2,744** advances, **4,854** callbacks, **24** resets, **129** mutations, six budget exhaustion runs, and **3,217** cloned operations. Environment-owned free-body clocks and restored simulations additionally match **80/80** submissions without injected core/clock state. | Partial |
| Ordinary object-pair rechecks filter spatial candidates, retain endpoint orientation and per-body link order, retire rejected pairs, then create new pairs. | `ObjectPairGraph` owns bounded link reconciliation and disconnect. Peer removal fills the vacated slot with the last link; the active endpoint retains its separately reordered prefix. Phantom-specific admission and automatic environment subscriptions remain separate work. | **94/94** native recheck/disconnect operations, both endpoint lists, **321** ordered game-filter calls, **26** creations, **38** retirements, **77** nonempty prior graphs, and cloned continuations. | Partial |
| Distant convex pairs leave continuous-time tracking for movement-distance listeners, with endpoint-specific allowances and retained rotational travel. | `PairResidence` retains the native distance-band decision and moving-endpoint shares. `MovementRangeClock` owns keys and rotational-travel projection. Full child-pair transitions/subscriptions remain unfinished. | **63/63** native decisions, including **45** deferred pairs (**39** both-moving), **45/45** stored key pairs and rotational-travel sums. Strict threshold/conversion-disabled branches have unit tests. | Partial |
| Movement-listener expiry can update a retained separating-plane estimate and renew both endpoints, or require full feature minimization. | `MovementPairHint` owns projected-core/rotational-travel arithmetic, the strict renewal threshold, changed hint words, and endpoint allowances. Missing hints require recalculation. | **42/42** native callbacks (**31** with rotation), **19/19** renewed hint/key sets, **23** recalculations, and cloned continuations. Forced/invalid-hint, reset-shift, and threshold boundary cases have instruction-grounded tests. | Partial |
| Interior feature recovery starts from authored opposing-face metadata and traverses nonpositive directed region tests without revisiting faces. | `FeatureTopology` owns exact region checks and directed face traversal. Ordinary feature walking now consumes that same checked region arithmetic; its obsolete widened-edge/no-bias calculation is removed. Intrusion/retry state and body recovery impulse still require integration. | **9,108/9,108** native returned edges/visit sequences, **18,216/18,216** region outputs, **112,779** visited edges, maximum path **44**; cloned calls and invalid-input/link guards pass. All **308** pair queries and retained motion/owner/manifold gates remain accepted. | Partial |
| Finite segment metrics and near-parallel region selection determine feature transitions when more than one face boundary is violated. | `SegmentPairMetric` and shared topology region checks replace unsupported fallthrough and obsolete point/segment formulas. | **9,108** exact point-region/distance results and **72** segment-pair region/distance results. | Partial |
| Retained closest-feature pairs preserve selected-side state, skip repeated environment codes, retain partial geometry on intrusion, detect cycles and retry authored interior recovery. | `ClosestFeaturePair` shares primitive geometry with ordinary queries, distinguishes ordinary/invalid rechecks, and preserves snapshot state. Selected-triangle pair dispatch uses ordered feature metrics, not an all-convex fallback. | **307/307** native calls across **55** lifetimes, **95** cache hits, **1,030** primary entries, **five** intrusion results; **24** direct triangle-pair queries. Complete environment residence and recovery lifetime remain unfinished. | Partial |
| Default overlap recovery uses the immovable hull's selected plane or a paired mass-scaled push, with phase-specific velocity tests and queued angular changes. | `recover_overlap` owns the response and shares internal impulse accumulation. Replace fixture-specific fixed recovery ticks and integrate callback/event ordering. | **Five** native responses and speed values, **three** evaluated plane/direction results. Broader moving-pair and world-lifetime acceptance is still required. | Partial |
| Default acceleration applies damping before consuming controller-queued impulses, then adds gravity. | `MotionProfile::accelerate` owns that commit point, clears the queue after a complete update and skips pinned bodies before evaluation. | **541/541** native core updates in **467** calls, including **26** nonzero queued inputs and **57** pinned no-ops. Existing **491** body-frame states and **86** settling phases remain exact. | Partial |
| Waking a physically static object is a no-op, not a motion-enable error. | Preserve the object's state and pending-wake list; dynamic activation/island ordering remains separate work. | **Three** native static wake calls retain flags, pending count and asleep state. Environment snapshot equality guards the no-op. | Partial |
| Simulation islands own core order, controller rosters, active/dormant lists, dependent-controller fusion and connectivity splits. | Integrate one ordered island owner into environment creation, wake, controller dispatch and snapshots; preserve stable removal, phase traversal and multi-component extraction order. Dynamic impact propagation and multi-core sleep remain under differential verification. | Native lifecycle mutations and complete ordered graph snapshots, followed by environment-driven motion/contact histories. | In progress |

The environment now owns spatial subscriptions, retained convex pairs, invalid-overlap retries, contact controller ordering, quiet-state references and sleep. Without caller pair admission, **11/11** polygon first contacts and **14** restored boundaries match. All **43** retained body histories match **2752/2752** complete published states and restored submissions, including resting, tangential, spinning and multiple-contact trajectories. Retained-coordinate clamping rearms feature checks and accumulates absorbed energy; six native single-contact clamp calls include two rechecks.
The autonomous path also matches **6048** ordered friction-contact queries, eight friction events, **168** owner banks/slot vectors/tangent admissions, and all retained manifold fields (**488** directed edges and frames, **480** coordinate pairs, **360** forces). All **208** states of the thirteen finite-contact fixtures now match exactly, including rotated, grazing and moving-obstacle cases. Child events re-minimize before collision admission; physical-object angle bases retain `AngleVectors` arithmetic. General impact graphs, command lifecycle, recursive hulls and game integration remain required.

The one-active-row normal shortcut is removed. Dense elimination matches **60** native calls; incremental contact factorization matches **150** operations; the complementarity search matches **180** cold/warm calls, including **64** multi-active results and **14** completed failures. Retained-active validation and full solve selection preserve **1780** captured contact systems and **4427** solution words. Coupled contact lifecycle, dynamic impact propagation and full gameplay integration remain required; numerical helper acceptance is not solver completion.

`SpatialIndex` preserves native overlapping-cell membership, parent/child order, root expansion/reuse, stable removal, ordered sphere candidates, and empty-chain pruning. **308/308** native tree snapshots, **150/150** candidate lists, and **25,852** node observations match, including cloned continuations, tangency boundaries, absent removals, and root reuse. Insertions stage only changed nodes before atomic commit, not a full-tree copy. Environment movement-range callbacks, collision filtering, and object connector/manifold lifecycle remain unfinished.

The environment owns Source center-force, offset-force, and torque commands plus both offset queries. **90/90** native active/pending vectors, flags, published poses, and published velocities match across fresh, awake, sleeping, pinned, and static bodies; **10/10** query pairs and **90/90** restored operations match. Zero force wakes movable bodies; static/pinned application is a no-op. Offset work uses the current integration frame, not the next/presentation pose. Simulation clipping uses the common native limit owner, and isolated pin/unpin preserves retained phases. Colliding filter/contact rechecks and full island wake propagation remain unfinished.

`SpatialCell::fit` uses binary32-rounded floor/ceiling rasterization and the native optional one-level expansion. **1,920/1,920** native fits match all cell coordinates, raster/size levels, and selected radius, including **1,181** expansions and **355** rejected expansions. Unsupported power-table scales and integer-conversion domains fail explicitly. This does not implement the object spatial tree, membership/candidate order, or automatic pair admission.

Physics topology retains authored face metadata alongside directed-edge flags. Recursive pairs now own lazy subtree refinement, ordered children, propagated descendant counts, movement subscriptions and retirement. **12/12** isolated decisions remain exact. Twelve autonomous worlds over the six actual enclosing `jump_beef` submodels additionally match **768 body states and restored submissions, 913 complete pair-lifetime records, 285 ordered minimizer calls, 804 callbacks and 10 contact rows**. Snapshots validate parent links, child order, counts and residence; the broader solver and gameplay gates remain required.

`query_hull_pairs` projects opposing core centers with native elapsed-time rounding, transforms into cached object frames, and preserves radius/scan-range widths. `HullPairSet` reconciles retained-prefix swaps, reverse candidate creation/retirement, and reverse insertion. **56/56** native candidate lists, final pair lists, and ordered changes match, including **48** retained-list calls, **eight** preselected-hull calls, **43** creations, and **35** retirements. Cloned reconciliation and invalid-input atomicity pass. Pair-list computation does not yet own environment collision events, recursive pair lifetime, or manifolds/islands.

`CollisionSearchRanges` derives world and paired search ranges from retained speeds/radii, configured lookahead, and the actual timestep. **96/96** paired ranges and **192/192** world ranges match native calls over three timesteps and two lookahead settings. Distinct distance/time floors, radius caps, and ordered speed sharing are preserved. The environment owns the validated policy and rebuilds it on atomic restore. Pair discovery and reconciliation remain unfinished.

`HullHierarchy` borrows the immutable authored shape and preserves root-hull admission, left-before-right visitation, sphere and quantized-box comparisons, and enclosure refinement from its children. **19,030/19,030** native ordered queries match, including **792** enclosure refinements, **10,172** nonempty results, and **39,693** returned hulls. Root selection and complete terminal order match for **127/127** configured surfaces. Queries retain enclosing hulls as candidates rather than flattening them. Body-pair discovery, pair reconciliation, and autonomous world lifetime remain unfinished.

Physics can compile feature topology for both authored terminal pieces and enclosing hull references from Collision. The configured handoff preserves **829** tree nodes, **478** terminal pieces, and **six** enclosing hulls, with **484/484** valid Physics topologies and **127** retained snapshot shape leases. The hierarchy is not flattened or regenerated; runtime hierarchical candidate selection/refinement and autonomous world-pair lifetime remain unfinished.

`ManifoldContact` now retains binary32 normal force directly. Normal solves publish their force result once; tangent bounds and Source-unit force queries consume that same value without reconstructing it from a stored impulse. Tangent solving owns its energy transition and preserves the strict minimum-impulse early exit without clearing prior energy. **79/79** complete both-moving tangent controller outputs and energy updates match, while all **43** strict histories, **360** retained force comparisons, and **90** sleeping force publications remain accepted. Automatic contact lifetime and world/island integration remain unfinished.

`ManifoldContact::refresh` now owns both synchronized core offsets and transports retained coordinates with relative point velocity. **550/550** native refreshes match coordinates, frame, point, and offsets, including **210** both-moving observations, cloned replay, and atomic invalid-pose rejection. Admission response weights combine binary64 virtual masses before binary32 publication: **96/96** calls match, including **17** both-moving weights. Contact admission/retirement cadence, persistent normal-pressure storage, and autonomous world/island ownership remain unfinished.

`TangentAssembly` now combines both endpoints into one two-axis response and signed relative-velocity vector. `AssembledTangentSystem` and `ManifoldContact::solve_tangent` return both updated bodies using their own mass/inertia and opposite impulses. **79/79** native both-moving matrices, inverses, velocities, RHS vectors, impulses, and applications match, while all **1,899** original static-contact comparisons and **43** strict histories remain accepted. The static tangent gate now compares bit patterns, including signed zeros. Two-endpoint manifold admission/refresh, coefficient lifetime, and autonomous world integration remain unfinished.

`CollisionRequest` combines both rotational contributions in native order; `CollisionCorrection` replaces the one-body correction with the shared response and exact impulse rounding. `CollisionMotionCommit` preserves active/queued velocity storage, delayed endpoint selection, and collision counters. **268/268** requests, **93/93** correction calls, and **93/93** commits match, including **17** delayed impulses. All **3/3** configured both-moving first contacts now match times, geometry, before/after active and queued velocities, and applied endpoints, with **6/6** restored phase continuations. The isolated paired-response minimum-speed shortcut is deleted; clipping runs after correction. Automatic world/manifold lifetime, collision-limit freeze policy, and full gameplay acceptance remain unfinished.

`schedule_convex_pair` replaces the vertex/face-only queue contract. Queued pair events retain ordered body/convex identities and selected features rather than a predicted collision placeholder. Dispatch pins both cached poses before core synchronization, re-minimizes the selected pair, and projects the complete binding. **11/11** configured first contacts across all four polygon families match exact dispatch times, feature bindings, incoming/outgoing velocities, points, normals, and offsets; **14/14** restored phase continuations match. Reversed inputs, derived-time ownership, atomic invalid-snapshot rejection, and body-removal cancellation are covered. Coupled moving-body correction, automatic world-pair lifetime, penetration recovery, and full lifecycle acceptance remain unfinished.

`ConvexPairQuery` owns feature minimization, Source endpoint ordering, family-specific separation, motion admission, and event selection in one bounded call. **308/308** native windows match selected features/order, separation/normal, speed bounds, and exact unqueued event times. `VertexFaceSeparation` independently matches **641/641** plane publications, including **14** moving-face observations; authored face bindings retain widened edge differences and the regularized inverse-area scale. `FeatureSelection` keeps endpoint order instead of discarding it. Environment-owned broad phase and persistent pair/world advancement remain unfinished.

Each complete feature query owns its **21-cell** raster transform cache shared across collision and sector searches. A cell retains its first projected pose; refinement uses uncached object projections and both retained bracket values. `ContinuousSample` distinguishes those two sample paths. This preserves exact roots without loosening queued-time or trajectory comparisons; the environment's separate shared transform pool remains unchanged.

`ContactFeatureBinding` owns immutable feature identity, authored face scale, and one-time checks. Its `ContactSurface` output covers all four polygon contact families. **340/340** native geometry observations match: **300** vertex/face, **7** vertex/edge, **9** vertex/vertex, and **24** edge/edge, including exact points, normals, raw tangents, gaps, checks, and retirement flags. Binding restore and invalid-input atomicity pass for all observations. `ManifoldContact::refresh` now owns the binding, with no independent vertex-only projection or duplicate fixed-edge identity. All **43** retained histories and **2,064** restored continuations remain exact in the declared published-state gate. Generic world/manifold ownership and full lifecycle acceptance remain unfinished.

`VertexPairEventQuery` retains the signed, scaled initial-plane collision scalar and both endpoints' ordered vertex/edge sectors. Safe margins depend on the shortened horizon, separation, authored radius, and edge length. `VertexPairSeparation` preserves the distinct minimized normal and rejects coincident vertices at the established resolution. Configured box-corner probes match **25/25** collision scalars, **93/93** sector scalars, and **4/4** complete windows (three collisions and one sector transition), including translated placement. Public world/manifold ownership and complete vertex-contact lifecycle integration remain unfinished.

`VertexEdgeEventQuery` owns virtual-plane collision search, both adjacent face transitions, and the ordered neighboring-edge sector search with shrinking horizons. `VertexEdgeGeometry`, `VertexEdgeCollision`, and `VertexEdgeSeparation` preserve distinct point/direction widths, rounded perpendiculars, and normalization. Configured verification passes **49/49** collision scalars, **1,230/1,230** sector scalars, and **5/5** complete event windows. Expanded edge verification passes **24/24** contact geometries and authored handoffs plus **61/61** separation planes, including reversed endpoint ordering. Repeated-contact advancement no longer uses the old all-vertex search; **3,379** selected-pair queries complete without unsupported-query errors while retaining all **43** exact histories. Vertex/vertex event selection, generic world ownership, and complete contact lifecycle acceptance remain unfinished.

Finite edge contact projection and its distinct minimized separating-plane calculation preserve **24/24** configured PHY/cache handoffs, **2/2** selected frames, and **63/63** separation planes. The original five native edge observations are also replayed through the compiled-shape binding rather than a separate raw-segment interface. All **43 × 64 histories (2,752 states)** match published positions, linear/angular velocities, and rotations exactly, with **2,064** restored continuations. The fixture still owns orchestration; autonomous colliding-world/manifold ownership and full acceptance are unfinished.

`MotionActivity` owns the simulation group's strict one-meter-per-second fast flag and retained contact-energy-reset state. Classification precedes controllers; a fast-to-slow transition also resets quiet-reference clocks. **118/118** native group transitions over **207** core velocities match, including quiet resets and the reset flag retained across subsequent fast frames. Correct transition handling restores the complete 50-inch/second contact history; generic island membership remains unfinished.

`VertexFaceSectorQuery` evaluates the authored outgoing fan in order and shortens each later search horizon after an accepted root. Sector traversal may sample the final raster cell past the horizon; transition dispatch still uses the fixed-boundary `ContinuousEventClock`, including after an earlier collision. `ContinuousFeatureWindow` now requires that clock instead of inventing a post-impact time base. These fixes preserve **42** exact first contacts and raise strict persistence to **42 complete histories / 2,742 of 2,752 exact states**. The remaining 30 ms history requires a genuine finite-floor edge contact; it is not a plane-contact approximation.

`MotionProfile::settling` adds binary32 `0.1` to linear and angular damping coefficients before the usual damping/gravity phase; **86/86** native slow-body phases match. Contact transport observes pre-damping velocity. Removing initial-tangent suppression and correcting first-phase readiness restores the five resting-height histories. Persistence now compares every frame, including sleeping publication: **38** complete 64-tick histories, **90/90** sleeping contact forces, all five native sleep ticks, and **2,064/2,064** restored continuations. The denominator expands from 2,662 awake states to **2,752 total states**. Automatic island-state admission and generic colliding-world integration remain unfinished.

`SingleContactNormal` derives synchronized offsets and Jacobians from the retained world point and current mass-center transforms, then computes response mass, closing velocity, impulse, force, and both endpoint updates. `ManifoldContact::solve_normal` owns the retained normal impulse and shares world-point synchronization with contact refresh. **484/484** native positive solves match, including **29** two-moving-body solves and **513/513** endpoint offsets, Jacobians, and velocity updates. Deterministic tests cover separating/no-push and invalid-input branches. Removing authored-offset substitution and preserving absolute remaining-phase durations raises strict persistence from **23 to 33** complete 64-tick histories without losing an accepted history. Full manifold admission/retirement and colliding-world integration remain unfinished.

`ContactTolerances` derives collision distances, contact-retention ranges, separating-speed floors, and sector margins from the configured Source tolerance calculation. Gravity length widens converted binary32 components before squaring. **225/225** native tolerance/table values across three gravity inputs and **9,224/9,224** live pair collision/margin values match. `ConvexContactPair` accepts no caller-injected distance or sector policy; the environment supplies it. All **42** original automatic first-contact outputs remain exact. Retained manifold/keeper policy consumption is unfinished.

Continuous vertex/face pair selection uses shared cached transforms at the current event time and pins both endpoints during minimization. Start-time sector tests retain the cached pose while later search samples project the motion phase. Dispatch acquires both cached poses before collision synchronization; errors preserve cache/queue state and successful queries release both pins. All **42/42** first-contact dispatch poses, cached quaternions, and time codes match, with existing event times, velocities, points, normals, offsets, and restored continuations unchanged. Other feature families and complete retained pair/manifold advancement remain unfinished.

`TransformCache` replaces unused per-body retention with a shared **256-slot** owner. It preserves rotating allocation, pinned-slot exclusion, eviction backlink cleanup, inactive retention, signed increasing time-code refresh, and allocation/payload snapshots. Target comparisons cover **260** allocations, **67,340** owner links, **10** refresh queries, **four** pin/release operations, and **275** restored operations. Public environments additionally match **491** complete cached transforms and allocation observations, including offset centers and explicit sleep/wake. Exhaustion and invalid projection fail atomically. `cached_transform`, vertex/face event selection/dispatch, and environment snapshots use this owner; persistent-pair and manifold queries still need complete lifecycle integration.

Object poses preserve separate core mass-center and object coordinates, the strict squared `1e-8` shift threshold, placement normalization, matrix composition ordering, and post-rebase quaternion construction. Initial core orientations match **51/51** native inputs; the original **204/204** internal core positions and orientations now match in addition to published states. The expanded body-state gate covers **56** environments, **469** Source-space states, **220** core states, **456** time codes, **228** restored simulation continuations, and **eight** queued/additive/instantaneous velocity commands. Velocity queries sum live and queued vectors; boundary advancement commits queued changes once. Cache time codes start at one and advance even for equal-time publication. Complete contact-cache, manifold, and island lifecycle integration remains unfinished.

Close-contact scheduling runs selected-feature collision/re-approach and sector selection before applying queue timing. Sub-microsecond candidates distinguish immediate, short collision, and long refresh/feature delays; adjusted candidates at the next integration boundary are excluded. All **53** captured short-collision timing adjustments match; deterministic mode tests cover immediate, long, minimum-gap, and terminal-equality branches. `PhysicalShape` derives inverse diameter, and `SectorThreshold` scales actual separation against the collision distance: **9,224/9,224** inverse diameters and **4,905/4,905** native sector thresholds match. The environment no longer accepts a supplied sector threshold or manufactures an event solely because the distance is close. Search-only sampling admits one additional 5 ms raster cell without extending live publication. First-contact and accepted persistence gates remain unchanged; automatic pair/manifold lifetime remains unfinished.

Owned motion phases retain linear-speed and rotation-envelope bounds once per phase. `CollisionMotionBounds` derives directional approach from separately widened binary32 projections, perpendicular angular deviation, worst-case pair speed, angular-sector bounds, and binary32-remaining-time admission. All **4,612** native pair bounds, including **31** with both endpoints moving, and **9,224** endpoint speeds match exactly. Vertex/face scheduling consumes these owned bounds and removes caller-provided maximum-speed values; all **42** generated first contacts and **43** restored phase continuations remain exact. Nonapproaching and unreachable pairs produce no event. Moving opposing geometry and complete pair/manifold lifetime remain unfinished.

`RotationEnvelope` derives its world-space axis from the per-phase quaternion increment and retained core basis, not from a normalized angular-velocity vector. Its speed preserves the exact five-refinement reciprocal root, binary32 upper-angle polynomial, timestep reciprocal, authored deviation radius, and `1e-19` squared-increment threshold. All **3,770/3,770** native phase increments, world-axis vectors, angular-speed words, and surface-speed words match. `CoreOrientation::predicted_angular_speed` and the live persistent-contact deviation calculations consume the same producer; event-plane normalization shares the arithmetic owner while retaining its distinct four-refinement contract. Reconstructing the retained basis from the phase quaternion matches **3,765/3,770** inputs; the five differing bases occur on zero-angular-speed freeze transitions, where the envelope emits its constant axis without reading the basis. Full automatic world-bound ownership remains unfinished.

The environment interleaves fixed integration boundaries with derived collisions in one newest-first equal-key queue, rebases keys only at integration boundaries, applies each body's velocity controllers once per boundary, retains exact quaternion endpoints and projection velocity across publication, and publishes at the clock's actual submission terminal. Explicitly supplied phases follow the same queue and boundary order. Snapshot/restore retains the fixed-boundary event and all partially consumed body phases; pause and failed advancement preserve the entire prior environment. The submission-count clock, duplicate phase-preparation path, external publication timestep, and collision-only queue accessor were removed. All **6,528** three-query clock samples and **3,264** restored transitions also pass through isolated empty environments; populated free-motion environments additionally match **408/408** clock publications and preserve **204/204** exact body states. Three explicit-sleep schedules distinguish pending activation from active fresh/warmed phases: **22/22** exact state samples and **14/14** restored transitions pass. Automatic sleep admission and contact-island wake propagation remain unfinished.

`FixedStepClock` retains current simulation time separately from the last and next integration boundaries. Its first fixed-step call crosses the zero-time boundary and the following boundary; later calls cross one boundary. The publication terminal is the last integration boundary plus the binary32 timestep/look-ahead product. All **6,528/6,528** native `GetSimulationTime`, `GetNextFrameTime`, and `GetSimulationTimestep` observations match across **135** fresh/warmed, free-motion, collision, persistence, and penetration-recovery schedules; all **3,264/3,264** cloned continuations match. The initial next-frame value retains the creation default independently of the subsequently configured timestep.

`simulate(duration)` accepts Source submission durations independently of the fixed integration timestep, preserving skipped/clamped submissions and the persistent transition to duration scheduling. `reset_simulation_clock` rebases active phases, controllers and queued events without incorrectly shifting dormant ranges or contact-pair history. Reset and duration tests preserve 120 exact clock publications. `set_position` and `set_position_matrix` preserve teleport collision toggles, sleeping/pinned/static behavior, current-time overlap impulses and movement-range accounting. The combined ten-world gate checks **720 body states, 1267 contact queries, 794 callbacks, 12 immediate command states/replays, 144 range states and 400 restored submissions**.

Creation now preserves the supplied inertia factor and rotational inertia limit, normalizes creation mass/factor bounds, and applies final inertia bounds before deriving inverse inertia and drag. `set_simulation_timestep` changes future integration spacing without rescheduling the pending boundary; optimized submission may consequently repeat or move current time backward. Four creation-parameter worlds and one timestep-change world extend the gate to **23 exact initial mass/inertia states, 920 body states, 1393 contacts, 1079 callbacks, 160 clock publications and 600 restored submissions**. These remain derivation checks, not full original-campaign or gameplay acceptance.

The original 75-environment derivation replay now checks **2,783 body publications, 888 friction queries, 2,565 callbacks and 1,525 restored submissions**, including both reported door-impact worlds and the rigid-object save/reconstruction case. Anisotropic angular integration belongs to fixed boundaries, not collision remainder steps. Callback output retains point velocities and exact endpoint ordering; destroying an object detaches its callbacks before contact retirement. Getter-space object archives reconstruct collision resources and motion rather than cloning environment continuations. Extended archive metadata, cross-environment transfer and broader runtime API families remain unfinished; held-out and gameplay acceptance are still pending.

Delete-queue mode retains marked physical bodies separately from the visible object list and performs FIFO cleanup before the next submission. Snapshots include that pending ownership; replacing a saved object does not reuse a still-live object's identity. Deferred deletion and reconstruction are covered by targeted visibility, atomicity and continuation tests.

Core generations and recyclable storage identities are separate owners. Recent-contact timing follows storage reuse, while body, island and pair lifetimes retain unique generations. The bounded, snapshotted free list passes single-body and reversed-order two-body reconstruction/recontact comparisons. The expanded graph gate preserves **1,192 states, 1,461 contacts, 1,356 callbacks and 808 restored submissions**; held-out revalidation remains pending.

Configured water volumes use a separate fluid controller, not solid contact response. Seven native-comparison worlds cover entry/exit, submerged rotation, current, volume movement, pinning, sleep/wake, collision toggles and buoyancy overrides: **896 body publications, 896 clocks, 480 callbacks, 396 retained-pair records and 448 restored submissions** match exactly. All 278 observed volume/centroid, pressure/torque and complete controller evaluations match. Dormant intrusion ownership suppresses false exit/reentry notifications, and controller priority precedes gravity. The solid-only seeded corpus does not establish water gameplay parity; browser/gameplay validation is still pending.

Oversized-contact handling, both object-freeze decisions and emergency impact retirement are tested with three 96-body worlds and two opposing-surface worlds: **18,440 body/internal states, 55,379 contact queries, 61,607 policy calls and 200 restored submissions** match exactly. The conflicting-contact case reaches the bounded propagation limit and retires the exact originating pair, preserving contact state and continuation. Focused checks preserve 19 complete normal systems/solutions, 20 impact input/output records and 19 separation-speed computations. Held-out and gameplay acceptance remain required.

`ManifoldContact` preserves authored/fixed features, synchronized local offset, tangent frame, prior world point, retained tangent coordinates, response coefficient, prior tangent energy, update time, and normal impulse in one cloneable Physics-owned value. Its refresh projects through the supplied object cache, uses the current core basis for synchronized offsets and point velocity, and atomically commits binary32-elapsed friction transport. Its tangent operation owns assembly, impulse bounds, solve, and body application. The live differential uses these public operations and preserves all **480 retained-coordinate pairs**, **360 normal forces**, **168 owner/admission histories**, and **23 exact 64-tick histories**. Automatic first-contact dispatch now re-minimizes the authored pair at its stored event time before publishing current geometry: all **42/42** Source contact points, Source surface normals including signed zero, and synchronized local offsets match native callbacks. Environment-wide manifold admission, owner-bank lifetime, and phase orchestration remain unfinished.

The first-contact integration binds authored convex features to existing environment bodies. `advance_boundary` applies gravity/damping/drag/limits once; `schedule_convex_pair` derives event time, preserves close-contact delay, and processes queued sector transitions through authored re-minimization. The expanded polygon path preserves **42/42** original non-penetrating first contacts and **43/43** boundary-phase snapshot continuations without injected event times or offsets. The initially penetrating scenario remains unresolved in this integration and remains in the complete persistence denominator. `CollisionCorrection` also preserves **945/945** immutable post-impact fixtures. Full world-pair discovery, collision-limit freeze policy, penetration recovery, and persistent manifold advancement remain unfinished; missing behavior fails atomically rather than substituting a different feature family.

`EdgePairEventQuery` derives each authored edge direction with binary32 endpoint subtraction followed by binary64 normalization, computes signed skew-edge separation from separate transformed endpoint projections and a binary32-rounded reciprocal cross-product length, then checks parallelism and all four adjacent face transitions in fixed order. Later equal-time candidates replace earlier candidates. Exact target comparison matches **26/26** complete authored edge-pair geometry records and all **6/6** ordered edge-pair root-evaluation scalars, completing **9,604/9,604** captured continuous scalar evaluations across the implemented vertex/face, sector, and edge/edge kernels. Full automatic event-sequence/world integration remains unverified; no vertex/face formula substitutes for this pair family.

The active sticky-required checkpoint covers exactly one configured 134-point/264-triangle polygon convex; Collision-supplied compact points, authored triangle order, packed directed-edge links, shape center, inertia, and radius; static and supplied motion-disabled, kinematic, or dynamic contact bodies; 0.015-second fixed steps; transform, world linear velocity, body-local angular velocity, mass and shape inertia; gravity, damping, aerodynamic drag, continuous authored-feature traversal, bounded impacts, bounce, physical friction/elasticity, generated-contact primary/secondary banks, stable persistent owners, coupled normal and tangent response, retained friction coordinates, producer-order contact publication, movement-reference and shared-random sleep scheduling, sleep/wake, motion disable/enable, center/offset impulses, destroy, complete solver-owned snapshots, restore, rollback, limits, matched callback phases, deterministic continuation, and the producer/consumer rows below. Physics consumes decoded Collision topology and never parses PHY payloads. The compact convex producer seeds both feature descriptors from their first authored directed edge, performs bounded strict vertex/vertex, vertex/edge, vertex/face, and edge/edge transitions with binary32 face-region checks, and preserves the selected directed edge when an equivalent face has multiple authored tangent frames. Authored directed links, geometry bank membership, stable friction-owner identity, retained friction coordinates, force, and fixed-core clock are distinct state; historical feature identity, allocation addresses, and one point-owned contact list cannot substitute for them. It adds no denominator row. Configured-build observations cover deterministic free-motion matrices, face/edge/vertex/corner/thin/rotated/overlap contacts, the exact `jump_beef` world solid, one exact motion-disabled model PHY, zero/half/unit surface pairs, simultaneous-impact candidates, natural sleep, disable/enable, center/offset impulses, public issue 7426 long/short fixtures, and object serialization. Target continuation is reconstructed by replaying the complete checked command/history prefix; playsrc snapshots store every independently implemented continuation value and never depend on target object serialization. The current TF2 adapter also lacks enable-motion and force-at-point commands and can return only one optional contact per body step rather than zero-to-ten matched collision callbacks, independent sleep/wake edges, persistent-contact state, and post-simulation completion. Derivation, implementation, and differential correction remain active; a replacement engine, conventional sequential-impulse solver, learned transition, swept-box response, or trace-specific trajectory is not accepted evidence.

`PhysicsEnvironment::simulate` consumes typed scheduled body-pair collisions inside supplied complete motion phases. Each of **945 immutable native response fixtures** must traverse the public environment with exact impulse output. Additional live candidate calls exercise the same queue, terminal exclusion, restoration, and atomic failure checks; their current combined count is **2,734**, not an immutable target-case denominator. All **4,721** target motion-phase samples also match. `VertexFaceEventQuery` evaluates the selected authored vertex against its moving authored plane first, then each outgoing authored sector in previous/opposite order; each accepted sector root shortens the event horizon and later equal-time sectors replace earlier candidates. `EventPlane` uses widened point subtraction and four binary64 reciprocal-square-root refinements; `SectorDirection` uses binary32 point subtraction and squared-length rounding before its binary32 reciprocal. Exact configured arithmetic matches **11,023/11,023** event planes, **11,001/11,001** outgoing-sector normals/directions, and **9,598/9,598** ordered vertex/face and sector root-evaluation scalars; `EdgePairGeometry` supplies the remaining **6/6** edge/edge scalars. These geometry/scalar comparisons do not establish full event-sequence parity. Complete world pair discovery, adjacent feature-pair transitions, retained manifold advancement, and TF2 gameplay integration remain unfinished.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Every required body motion class, shape family, body role, pair relationship, constraint, controller, and articulated assembly has one stable item in [`inventories/body-and-constraint-families.md`](inventories/body-and-constraint-families.md). | The 30-item inventory is manually derived, has no generator, and is Not accepted. | **Generated-inventory audit:** regenerate from the pinned SDK declarations, current Owner Registry, accepted producer registries, and exact target-game occurrences; compare stable identities, count, order, ownership, and coverage classifications with no unclassified call site or content occurrence. | Blocked |
| Environments create, configure, reset, enumerate, isolate, pause, and destroy all owned state without sharing bodies, relationships, contacts, clocks, or mutable scratch. Fixed integration boundaries and collisions share ascending binary32 time keys with newest-first equal keys; event offsets rebase at integration boundaries, not submission terminals. | `PhysicsEnvironment` owns typed boundary/collision events, fixed integration clocks, variable-duration submission scheduling, clock reset, world-pair and manifold lifetimes, retained quaternion/translation phases, callback output, pause and atomic restore. Cross-environment transfer remains unfinished. | **6,528** original three-query clock samples, **3,264** restored empty-environment continuations, and **120** mixed-duration/active-reset/dormant-reset clock/body/callback submissions. Transfer requires its own target trace. | Partial |
| Body creation, destruction, and cross-environment transfer are atomic, bounded, identity-stable, and close every contact, relationship, controller, and event edge in declared order. | `PhysicsEnvironment::create_body` consumes one immutable Collision polygon shape, compiles all authored convex topologies plus physical shape exactly once, resolves Material/default identity, preserves source-space position/angles/velocity, mass, damping, drag and enable flags, and creates sleeping bodies in caller order. Duplicate identities, invalid scalar/shape inputs, and configured body-capacity overflow fail before mutation; explicit wake and destruction preserve environment isolation. All **51/51 configured bodies** start asleep, wake exactly once, and reproduce **204/204 target trajectories**. Cross-environment transfer and contact teardown remain unimplemented. | **Body lifecycle trace comparison:** fixed polygon and sphere bodies cover success, duplicate identity, Missing shape, destroy before/after step, destroy in an event-producing tick, endpoint destruction, transfer, and limit exhaustion; compare complete transition logs and snapshots. | Partial |
| Static, kinematic, motion-disabled dynamic, and force-driven dynamic bodies retain distinct motion, inverse-mass, collision, contact, wake, and transform semantics. | `BodyKind::{Static,Dynamic}` preserves static identity separately from dynamic motion enablement; creation starts sleeping, explicit wake/sleep operate on owned bodies, static motion is rejected, disabling dynamic motion clears both velocities, and integration skips static, sleeping, or motion-disabled bodies. Gravity and aerodynamic drag obey independent supplied per-body flags. Kinematic controllers, automatic sleep/wake propagation, and live contact-side effects remain unimplemented. | **Motion-class state matrix:** apply the same transforms, velocities, impulses, contacts, constraints, and controller targets to every class and compare accepted state changes, ignored operations, contact response, and classifications with controlled target observations. | Partial |
| Teleports, ordinary transform updates, center-of-mass offsets, local/world transforms, and body-to-shape transforms produce exact Source-space state without render-coordinate conversion. | `world/pose.rs` stages Source position/angle and matrix mutations, preserves angular/queued velocity, updates shifted core placement, collision eligibility and movement listeners, and does not invent wake events. Invalid commands reject atomically. Center-of-mass mutation remains unfinished. | Eight exact immediate command states and replays, 144 range/clock states, two sampled-frame overlap responses, and a complete two-body/40-submission trajectory; the original 204 free-motion states remain required. | Partial |
| Linear velocity is world-space; angular velocity is body-local degrees per second; set, instantaneous set, additive change, point velocity, and actual prior-tick motion are separately observable. | `RigidBody` stores world-linear internal meters/second and body-local internal radians/second; `published` converts to exact Source inches/second and body-local degrees/second, preserving the configured binary32 `0x42652ee1` degrees conversion, signed zero, look-ahead-clock position, and interpolated rotation. `PhysicsEnvironment::set_velocity` validates either optional vector atomically and rejects missing/static/motion-disabled owners. All **204/204 target position, linear, angular, quaternion, and matrix fields** match across gravity, drag, damping, yaw/pitch/roll, timestep, and clamp scenarios. Additive/point/implicit velocity and queued-versus-immediate setter differences remain unimplemented. | **Velocity-operation trace comparison:** axis and rotated-body vectors exercise every setter/query before and after impulses, contacts, teleport, sleep, and one tick; compare vectors and binary32 tolerances field by field. | Partial |
| Mass, inverse mass, local inertia, inverse inertia, center of mass, volume, density relation, authored angular surface-deviation radius, and runtime mutation follow the accepted bounds and recompute every dependent physical quantity once. | `rust/src/shape.rs` consumes Collision's immutable exact center/inertia/radius/deviation/drag fields and authored convex points, deriving binary32 mass-scaled local inertia, farthest-point core radius, authored bounding extents, exact linear/angular drag projection bases, and `surface_deviation_radius = binary32(binary32(radius × binary32(1/250)) × authoredDeviationByte)` without reopening a PHY payload. The configured sticky preserves exact deviation word `0x3d0415bc`; binary32 predicted angular speed multiplied by this radius reproduces every captured initial rotational collision-speed envelope. Generic Collision geometry with no authored physical source fields and invalid mass/drag/inertia fail before body creation. Runtime mass mutation, volume/density, other shape families, and live environment ownership remain unimplemented. | **Mass-property matrix comparison:** vary mass across creation minimum, ordinary values, runtime subminimum positive values, maximum, and over-maximum; vary signed/zero inertia components, center offsets, volume, shape scale, authored deviation byte, and motion-disabled state; compare stored properties, angular envelope, first-event times, response, drag basis output, and errors. | Blocked |
| Environment gravity plus per-body gravity enablement and linear/rotational damping advance dynamic velocities at the fixed tick while static, kinematic, sleeping, and gravity-disabled bodies retain their declared state. | `rust/src/motion.rs` owns binary64 linear damping with its exact `0.25` linear/exponential branch, binary32 body-local rotational damping, widened gravity integration, binary32 aerodynamic drag factors, target-width world-linear/body-local-angular limits, and fixed-step publication; `rust/src/orientation.rs` advances the exact body-local quaternion. The independent configured corpus matches 204/204 complete position, world-linear velocity, local-angular velocity, quaternion, and rotation-matrix states across gravity, anisotropic drag, damping, initial rotation, timestep boundaries, warmup, and velocity clamps. Body roles, explicit activation, and public live-environment integration remain unimplemented. | **Free-motion trajectory comparison:** fixed bodies run gravity-only, damping-only, combined, disabled, asleep, kinematic, and static trajectories for 1, 2, 64, and 1024 ticks; compare transforms and velocities at every tick within declared binary32 tolerance. | Blocked |
| Aerodynamic drag uses supplied air density, shape projection data, mass/inertia, linear and angular drag coefficients, orientation, and enable state without substituting an AABB when exact projection data is supplied. | `rust/src/motion.rs` consumes supplied authored linear/angular drag projection bases, current body-local linear velocity, body-local angular velocity, air density, configured drag coefficient, and fixed timestep; both negative factors saturate at `-1.0`, and each velocity update preserves target binary32 addition ordering. Axis-aligned linear/angular drag and the rotated asymmetric body pass all 204/204 complete configured free-motion states. Generic shape families and public live-environment integration remain unimplemented. | **Drag trajectory comparison:** sphere and anisotropic compounds at axis and rotated orientations vary air density, coefficients, mass, projection fractions, velocity, and angular velocity; compare per-tick deceleration, zero-density/disabled results, and wake state. | Blocked |
| Center and offset force impulses, torque impulses, velocity additions, and motion-controller forces produce mass- and inertia-correct linear/local-angular changes and obey velocity limits. | No force, impulse, or torque implementation exists. | **Impulse-response comparison:** center, six offset directions, pure torque, rotated inertia, repeated impulse, force-over-tick, static, kinematic, motion-disabled, and maximum-speed vectors compare linear/angular deltas, point velocity, wake events, and clamps. | Not started |
| Explicit activation, explicit sleep, automatic sleep, wake propagation, and edge-triggered wake/sleep events follow one accepted threshold, quiet-time, contact, constraint, controller, fluid, and mutation matrix. | `rust/src/sleep.rs` owns exact moving, pending-quiet, and terminal-quiet classification plus the shared-random sleep scheduler: 166/166 classifications, 2,574/2,574 countdown decrements, 161/161 random-state transitions, and 161/161 reset countdowns match configured observations. The environment's explicit isolated-body sleep preserves pending activation velocity or freezes an active phase at its retained origin/prior orientation and clears active velocity; **22/22** pending/fresh/warmed state samples and **14/14** restored transitions match. Automatic active-set admission, contact-island wake propagation, and edge-triggered events remain unresolved. | **Sleep/wake schedule comparison:** isolate every wake cause and non-cause, threshold-adjacent velocities, constrained islands, resting stacks, fluids, explicit sleep, and repeated commands; compare active identities, quiet state, event edges, and next-tick work. | Partial |
| Collision enablement, trigger conversion, contents, pair exclusions, indexed collision sets, filter rechecks, and contact rechecks update eligible pairs and retire invalid contacts without game policy inside Physics. | No body-pair policy implementation exists. | **Pair-policy decision-table comparison:** cross enabled state, solid/trigger/fluid roles, contents, pair exclusion, collision-set values, owner equality, game predicate, and mid-contact changes; compare predicate call order, contact lifetime, wakes, and events. | Not started |
| Physics synchronizes body transforms to Collision once per tick, consumes bounded stable pair candidates, and produces the same admitted pairs as exhaustive accepted-shape testing. | No physics broad-phase integration exists. | **Exhaustive-pair differential:** fixed small scenes compare broad-phase-backed candidate/admission output with all-pairs Collision queries after creation, motion, rotation, sleep, wake, transfer, and deletion; require no false negative and stable candidate counts. | Not started |
| Fast body/world and body/body motion uses continuous relative-motion queries, accepted look-ahead, and bounded substeps so thin solids and opposing fast bodies do not tunnel. A selected authored vertex owns its collision search; its binary64 distance and maximum speed choose `2 × (distance - threshold) / maximumSpeed`, truncate to binary32 0.005-second cells, retain the complete resulting bracket, and refine that same selected feature before considering adjacent authored-sector transitions. An initially penetrating feature uses the distinct real-surface threshold and triggers only when its distance decreases. | `ContinuousTraversal` owns binary64 selected-feature distance/deviation, binary32 0.005-second cell quantization, bounded 1–20-cell traversal, original multi-cell bracket preservation, optional binary32 initial-distance caching, initially overlapping reapproach, and exact `ContinuousRefinement` publication. `CollisionDeviation` owns the exact widened `sqrt(binary64(1.001f) - rotationProjection²) × surfaceSpeed + approachingLinearSpeed` envelope and rejects malformed/nonpositive inputs. Adaptive initial and persistent-contact traversal starts at the selected feature's authored sector transition when present and otherwise at the fixed phase boundary; this preserves the original complete multi-cell bracket in both cases. **Twenty-three complete 64-tick histories** now pass all four state fields exactly, including 10/20-inch-per-second sliding and 10/50/100/200/500/1,000-degree-per-second spin, with strict totals **2,095/2,662 positions**, **1,731/2,662 linear velocities**, **1,661/2,662 angular velocities**, and **1,639/2,662 rotations**. Complete authored adjacent-sector ownership, moving-body pairs, world brush candidates, generic trace integration, matched callbacks, and live queue consumption remain unimplemented. | **Time-of-impact trajectory comparison:** compare selected authored-feature identity, exact binary64 initial distance and maximum deviation, truncated cell count, complete lower/upper bracket, binary32 cached value, corrected midpoint/refinement trace, initial-overlap return, bounded 20-cell termination, exact dispatch key/time, and complete body state across thin world, moving kinematic, opposing dynamic, tangent, rotating, initial-overlap, translated, and 0.030-second schedules. | Partial |
| Persistent contacts and manifolds admit, update, query, merge, replace, and retire stable contact state separately from Collision's stateless geometric contacts. | `rust/` owns Collision-supplied exact binary32 compact points and 16-byte triangles; authored face order; reciprocal packed directed edges; ordered opposite/previous feature fans; bounded vertex/vertex, vertex/edge, vertex/face, and edge/edge transitions; binary32 face-region and signed-zero inclusion checks; binary32-scaled/binary64-projected authored face planes; synchronized world-to-body impact-offset round trips; bounded last-released-first retained owner slots; shared phase-knot contact projection; distance-and-angular collision requests; binary64 partial impulses; signed-zero-preserving friction cones; body-local angular limits; fixed-timestep friction bounds; reverse-owner retained clamps; authored-frame retained transport; parallel-normal redistribution; complete two-axis tangent-response assembly, reciprocal-determinant inversion, solve, and atomic velocity application; distance-conditioned normal impulses; nonnegative coupled normal systems; distinct synchronous and queued normal velocity application; and fixed-face/moving-world-edge tangent frames. Exact configured shapes validate 792/792 sticky and 36/36 obstacle directed edges, 4,874/4,874 authored fan candidates and scores, 4,100/4,100 successors, 168/168 ordered generated-contact banks, 168/168 retained owner-slot vectors, 480/480 retained-coordinate pairs, 360/360 persistent binary32 forces, 168/168 solve admissions, 488/488 fixed-edge/selector identities, 488/488 six-component owner-matrix tangent frames, 455/455 isolated normal impulses, 945/945 collision requests/ordered impulses/post-impact velocity vectors, 1,899/1,899 authored fixed-face and moving-edge tangent frames/velocities/response matrices/inverse matrices/right-hand sides/velocity applications, 1,742/1,742 unconstrained and 157/157 clamped tangent vectors, 8,629/8,629 retained updates, 914/914 reverse-owner clamps, 373/373 redistribution phases covering 1,058/1,058 owner coordinates, 4,427/4,427 normal Jacobians/right-hand sides/forces, 11,659/11,659 raw and normalized matrix entries, 1,780/1,780 scales and active rows, and 1,979/1,979 full snapshot continuations. The complete public live environment, remaining 0.010-/0.030-second and spin histories, wake/motion lifecycle, and root ownership registry remain unresolved. | **Contact-lifetime trace comparison:** fixed face, edge, vertex, compound, sliding, rolling, separating, filter-change, sleep, and deletion scenarios compare manifold identities, ordered points, normals, forces, energy, coefficients, start/end events, and friction-snapshot queries. | Blocked |
| Continuous events retain binary32 keys relative to the fixed-step boundary, newest-first equal keys, and strict terminal exclusion. Native sorted-event storage reserves its high allocator indices and admits at most 65,532 entries. | `ContinuousEventQueue` preserves fixed-base timing, cancellation/rekey/reset, and clone/restore with the corrected **65,532** total limit. The environment reserves one entry for its boundary, leaving at most **65,531** collision/feature entries. Ordered convex-pair dispatch and all **2,734** response/queue/restore/failure exercises, **42** original delay cases, and **43** strict persistence histories remain accepted. Autonomous world/manifold lifetime remains unfinished. | Exact native insertion/dispatch words, tie and terminal ordering, typed feature bindings, atomic failures, restored continuation, and allocator-limit boundary tests. | Partial |
| Every retained vertex/face contact projects its authored binary32 moving vertex through the already-built binary64 object matrix; owners sharing one cached transform observe the same state across core mutations. | `TransformCache` owns the environment's rotating 256-slot pool, pinned-slot exclusion, owner links, signed increasing time codes, inactive retention, and atomic projection. Vertex/face selection and dispatch consume its retained poses. Zero elapsed copies the core coordinates/quaternion; other samples retain binary32 elapsed/inverse step, exact interpolation, and binary64 projection. Complete manifold/island integration is unfinished. | **Cached contact-projection differential:** **4,721** first-build poses, **544** focused owner builds, **936** same-code transforms/world points, **2,808** point coordinates, **364** shared phases; **260** pool allocations, **67,340** owner links, **275** restored pool operations; **491** environment cached states and **42** first-contact dispatch transforms/time codes. Exhaustion, invalid projection, and invalid-query rollback tests preserve prior state. | Blocked |
| Multi-contact friction tracks each owner's prior dynamic tangent energy, accumulates only positive pair energy, exponentially damps retained pair energy, caps removal at one-tenth of the mutual kinetic potential, derives world-linear and body-local angular queued impulses for static/dynamic endpoints, and commits queued impulses before gravity in the following controller phase. | `rust/src/energy.rs` owns binary32 retained-owner tangent energy, positive/negative owner-energy transitions, binary64 environment decay, five-iteration normalized relative directions, body-local directional inertia, static-endpoint 10,000:1 effective mass, binary64 angular/translational mutual potentials, one-tenth removal cap, atomic queued linear/angular component writes, static/dynamic endpoint selection, and categorical invalid input. The configured differential matches 1,899/1,899 owner-energy outputs and every direction, speed, mass, inverse mass, potential, damping, removed energy, and six queued components across all 1,088 multi-contact controller invocations. The live differential preserves the one-meter-per-second fast-object exclusion and commits reductions after retained-owner refresh but before gravity; generic active-island ownership and the public live environment remain unimplemented. | **Mutual-energy differential:** compare every owner previous/new energy word, pair accumulator before/after damping, binary64 removed energy, six queued impulse components, commit ordering, static/dynamic endpoint response, zero/negative potential behavior, and all low-speed/sliding/spinning continuation states against the complete configured controller transcript. | Blocked |
| Supplied physical surfaces resolve per-feature material identity and apply exact friction, restitution, density, thickness, and dampening response, including runtime material changes and contact recomputation. | Material's immutable ordered `SurfacePropertyRegistry` retains all five authored binary32 physics fields, source-order base/default inheritance, reserved shadow insertion, and stable overrides: 100/100 configured names, 100/100 indices, and 500/500 physical-value words match the exact target. `rust/src/surface.rs` consumes those typed records, resolves invalid indices to the registered default, multiplies paired friction/elasticity in binary64, clamps each product to `[0,1]`, rounds the result once to binary32, and retains both endpoints' density/thickness/dampening: 24/24 configured cross-surface friction and restitution pairs match exactly. Every collision response, persistent impulse, friction bound, and live candidate now derives its coefficients from the same immutable Material → Physics seam; no hardcoded default pair remains. Per-feature material selection, contact dampening dynamics, runtime changes, and live-world consumption remain unimplemented. | **Surface-response matrix comparison:** one-field and pair cross-products over friction/elasticity boundaries, density/volume buoyancy, thickness, dampening, per-triangle selection, runtime material changes, and unknown identity compare coefficients, trajectories, contact state, and classified failure. | Blocked |
| Initial overlap and later penetration use bounded recovery that preserves stable pair identity, honors the pure penetration disposition, and either publishes a nonpenetrating state or fails the tick atomically. | No penetration recovery implementation exists. | **Penetration-depth ladder comparison:** nested static/dynamic, dynamic/dynamic, ragdoll self, kinematic intrusion, compound, disabled pair, and iteration-limit vectors compare correction, selected bodies, contact state, events, and prior-state preservation. | Not started |
| Resting stacks and connected contact islands preserve stable support, friction, energy, and sleep without order-dependent collapse, jitter growth, or contact-capacity loss. | No island solver or stable stacking implementation exists. | **Stack and island trajectory comparison:** 1, 2, 8, 32, and accepted-limit body stacks, pyramids, bridges, mixed masses, mixed surfaces, moving supports, and simultaneous impacts compare per-tick state, contact graph, sleep tick, and insertion-order permutations. | Not started |
| Fixed constraints preserve the supplied relative position and orientation, support activation/deactivation and break thresholds, and close cleanly when either endpoint changes lifecycle. | No fixed-constraint implementation exists. | **Fixed-joint trajectory comparison:** translated/rotated frames, world endpoint, unequal masses, inactive start, activation, teleport, impulse, force/torque break, endpoint transfer, and deletion compare relative transforms, impulses, break event, and final state. | Not started |
| Ball-socket constraints co-locate two local anchors while permitting free relative rotation; torque break input has no positional-anchor effect. | No ball-socket implementation exists. | **Ball-socket trajectory comparison:** offset anchors, rotations, world endpoint, impulses, mass ratios, force break, ignored torque-only break, activation, and endpoint lifecycle compare anchor error, velocities, constraint state, and events. | Not started |
| Hinge constraints preserve one world anchor and axis, enforce optional angular limits, friction or motor input, activation, and force/torque break response. | No hinge implementation exists. | **Hinge state-space comparison:** every principal and diagonal axis, free/limited/locked ranges, limit crossing, friction, positive/negative motor, world hinge, mass ratios, break thresholds, and timestep boundaries compare angle, anchor, speed, impulse, and event traces. | Not started |
| Sliding constraints preserve five locked degrees of freedom, apply optional linear limits, friction or motor input along one axis, and enforce shared break behavior. | No sliding-constraint implementation exists. | **Prismatic trajectory comparison:** axis transforms, no-limit equal bounds, bounded ranges, endpoint expansion, friction, positive/negative motor, world endpoint, impulse, break, and lifecycle vectors compare coordinate, orientation, velocity, impulse, and events. | Not started |
| Pulley constraints preserve the two local anchors, two world pulley points, total geared length, gear ratio, rigid/non-rigid minimum behavior, activation, and break response. | No pulley implementation exists. | **Pulley trajectory comparison:** equal/unequal gearing, rigid and slack states, moving anchors, world endpoints, mass ratios, impulses, break thresholds, and invalid lengths compare segment lengths, velocities, impulses, state, and events. | Not started |
| Length constraints preserve maximum anchor distance and optional minimum distance, including rigid equal-length mode, slack mode, activation, and break response. | No length-constraint implementation exists. | **Length-joint trajectory comparison:** taut, slack, rigid, compressed, extended, moving-anchor, world-endpoint, unequal-mass, break, and lifecycle vectors compare distance, correction, velocity, impulse, and event traces. | Not started |
| Ragdoll constraints preserve two constraint frames, optional translation lock, three angular limit/friction/motor axes, clockwise-limit interpretation, activation, and shared break behavior. | No ragdoll-constraint implementation exists. | **Ragdoll-joint matrix comparison:** isolate every axis limit, friction, motor, clockwise mode, free translation, locked translation, single-axis case, frame rotation, mass ratio, activation, and break threshold; compare relative pose, speed, impulse, and events. | Not started |
| Linear springs apply constant, natural length, absolute and relative damping, local/world anchors, stretch-only behavior, runtime parameter changes, sleep preservation, and endpoint lifecycle. | No spring implementation exists. | **Spring oscillator comparison:** compressed, stretched, equilibrium, damped, relative-damped, stretch-only, moving-anchor, runtime update, sleep epsilon, mass ratio, and endpoint deletion vectors compare endpoint positions, velocities, energy, wake state, and lifetime. | Not started |
| Constraint groups add declared iterations, track bounded error ticks/tolerance, activate connected relationships, expose/clear error state, and solve requested group penetration; shared active, motor, mass-scale, and break modifiers retain one meaning across families. | No constraint-group or shared modifier implementation exists. | **Constraint-group trace comparison:** single and connected mixed-family groups vary iterations, error tolerance/ticks, mass scale, inactive members, motors, simultaneous breaks, penetration request, clear, and endpoint deletion; compare solve state, errors, ordering, and events. | Not started |
| Motion controllers attach ordered dynamic bodies and apply one of local/global acceleration or local/global force at low, medium, or high priority once per fixed tick. | No motion-controller implementation exists. | **Controller-mode comparison:** every result mode, priority, rotated body, mass/inertia pair, attach/detach/duplicate, sleeping body, handler replacement, and destroyed body compare callback order, linear/angular deltas, wakes, and bounded lists. | Not started |
| Shadow controllers and kinematic moving geometry follow supplied position/orientation targets with translation/rotation permissions, speed/damping limits, arrival time, step-up, teleport distance, physical-control mode, and solved contact response. | The environment owns attachment, target updates, removal, controller ordering, contact recomputation, bounded observations and snapshot continuation. Authored radius replaces a vertex-derived estimate; core-only property changes preserve the separate aerodynamic cache. Full entity/bone-follower integration remains under verification; position teleportation is not a replacement controller. | **Moving-target trajectory comparison:** 26 worlds match 4,992 body states, 3,211 contact queries, 2,613 callbacks and 1,690 restored submissions, including all 17 configured Soldier solids, late attachment, temporary gravity, interrupted updates, all-axis targets and removal. TF2 resource compilation supplies all 17 follower shapes and exact creation parameters. Gameplay acceptance remains outstanding. | In progress |
| The player-contact controller follows Movement-supplied position/velocity targets, tracks supplied ground velocity, bounds push mass/speed, reports contact/frozen/last-impulse state, and never computes player movement. | No player-contact controller implementation exists. | **Player-controller seam comparison:** fixed Movement target streams cover ground/no-ground, step-up, target rejection, teleport threshold, pushable/unpushable mass and speed, multiple contact planes, missed update, and body replacement; compare physical body/contact outputs only. | Not started |
| Vehicle controllers implement the accepted physical-wheel, car-raycast, jetski-raycast, and airboat-raycast families from supplied controls and collision-query results without owning entity or game driving policy. | No vehicle-controller implementation exists. | **Vehicle-controller trace comparison:** one fixed parameter set per accepted family covers acceleration, braking, steering, boost, suspension, wheel/raycast contact, friction change, airborne state, engine disable, and fixed control timelines; compare body/wheel state and operating outputs. | Not started |
| Fluid volumes track body enter/leave, moving surface plane and current, per-body buoyancy ratio, density, pressure damping, viscosity, torque, aerodynamic selection, drag, and wake-all behavior. | No fluid controller, buoyancy, or drag-volume implementation exists. | **Fluid trajectory comparison:** fully, partly, and non-submerged sphere/convex bodies vary mass, volume, buoyancy ratio, density, current, surface orientation/motion, damping, viscosity, torque, drag opt-out, sleep, enter/leave, and limits; compare forces, trajectories, membership, and events. | Not started |
| Bone followers create one ordered kinematic physical body per supplied solid/bone association, consume authoritative bone transforms, preserve owner/bone identity in contacts, and destroy the complete set atomically. | No bone-follower physical state exists. | **Bone-follower integration comparison:** fixed one- and multi-solid models cover parent-bone association, transformed updates, missing bones, collision filtering, gravity opt-out, contact owner/bone metadata, sleep, and destruction; compare ordered bodies, contacts, and events. | Not started |
| Ragdoll assembly creates at most 24 ordered bodies, joint graph, collision set, animated-friction state, supplied activation impulse, self-collision policy, sleep state, and physical transforms without producing presentation state. | No ragdoll physical assembly exists. | **Ragdoll continuation comparison:** fixed 1-, 2-, and 24-body graphs cover creation, parent gaps, self-collision rules, activation, fixed/ragdoll joints, animation-derived initial velocity, impulses, animated friction timeline, penetration, sleep, snapshot restore, and destruction. | Not started |
| A supplied break request atomically replaces one body with ordered fragments, retires old contacts/relationships, applies inherited and supplied impulses, and reports exact physical transition facts without choosing damage or fragments. | No breakable-body transition exists. | **Fragment-transition comparison:** fixed single/multi-fragment requests vary source motion class, center offsets, inherited velocity, burst impulses, constraints, contacts, sleeping state, malformed fragments, capacity failure, and event order; compare atomic snapshots and consumer-visible events. | Not started |
| Collision, contact, friction, trigger, fluid, wake/sleep, constraint-break, transfer, and fragment callbacks expose complete physical facts in stable order; pure pre-solve responses are bounded and all consumer mutations are phase-deferred. | Bounded `PhysicsCallback` output retains collision, touch, friction, wake/sleep and submission-start notifications, including both endpoint states at callback time. Command notifications survive into the next submission; snapshots retain callback output and the bounded recent-pair collision clocks. Collision-filter and contact-point rechecks share one policy predicate. Game bindings, pre-solve mutations, triggers, fluids, constraints and transfer remain unfinished. | **521 exact ordered callback records** across six dynamic/contact/command worlds, alongside **520 body states, 1078 contact rows, 240 restored submissions and 10 ordered filter calls**. Additional policy and callback families require their own transcript comparison. | Partial |
| Simulation invokes Physics exactly once in `physics.integrate` after `entity.think` and before `contact.resolve`; one tick consumes one command batch, advances the configured tick interval, publishes one revision, and exposes no render-frame or partial-substep authority. | Simulation's 18-item candidate phase inventory declares the required placement and feedback boundary; no Physics implementation or integration exists. | **Cross-module phase-log comparison:** the checked Simulation schedule drives Entity, Movement, Physics, `contact.resolve`, game responses, deferred commands, and snapshot publication for fixed scenarios; compare ordered phase entries, state visibility, tick/time, and repeated output. | Not started |
| Every body, relationship, command, query, solve, event, and snapshot validates finite arithmetic and required limits; accepted numeric performance budgets pass on every declared browser/native profile without partial publication. | Fixed Source mass and default velocity/performance values are known, but product scenes, platform profiles, time/memory budgets, and several solver capacity values are undecided. | **Boundary and benchmark comparison:** run every scalar and count at `limit-1`, `limit`, and `limit+1`, plus fixed synthetic and declared-map scenes; compare classified errors, prior-state equality, owned/scratch bytes, candidate/solve counts, and tick-time quantiles with accepted numeric budgets. | Blocked |
| Fixed initial world, commands, collision/surface inputs, arithmetic profile, limits, and platform produce bit-identical ordered Physics snapshots and events across repeats; mutable advancement is single-writer and immutable reads are concurrency-safe. | No deterministic solver, platform profile, or concurrency evidence exists. | **Repeat and concurrency comparison:** run each vector 1,024 times sequentially, then read each completed snapshot through `1..maxConcurrentReaders` workers; compare canonical bytes, events, diagnostics, input hashes, and immutable-state hashes exactly. | Not started |
| Physics snapshot and atomic restore preserve every owned continuation value, reject dependency-identity mismatch, and reproduce the next transition without defining a persistent file or wire schema. | `EnvironmentSnapshot` retains complete fixed-step configuration/clock, ordered bodies, immutable Collision shape handles, Material registry identity, physical shape/topology, current/previous core position and quaternion, body-local/world velocity, sleeping/motion/gravity/drag/collision flags, retained object-transform caches, and the bounded continuous-event queue. Restore validates registry identity, duplicate identities, configured bounds, queue clock, finite body state, and rederived Collision shape properties before replacing any state. All **204/204 configured fresh-environment snapshot restorations** produce identical next-tick bodies, clocks, and queues. Persistent contact/island snapshots remain unimplemented. | **Round-trip continuation comparison:** snapshot before and after every phase boundary, restore into a fresh environment, continue fixed commands, and compare snapshots/events exactly; mutate each dependency identity and byte/count bound to require unchanged-state failure. | Partial |
| Map, Collision, Material, Entity, Movement, Simulation, Networking, Replay, all three game modules, tools, and presentation consumers use one current Physics interface without duplicate solvers, body stores, contact state, fallback boxes, compatibility schemas, or legacy paths. | The root-workspace `playsrc-physics` crate now owns one `PhysicsEnvironment`/`RigidBody` lifecycle, immutable Collision-authored shape/topology consumption, Material registry/surface-pair consumption, exact free-motion/controller/clamp publication, bounded event queue, transform cache, collision feature walking, contact/normal/tangent/energy response, sleep helpers, and atomic environment snapshots; the differential constructs and advances public environments directly rather than duplicating free-motion integration. Application/game/WASM integration and a complete colliding Physics world are explicitly outside this clean-room port checkpoint and remain tracked. | **Producer/consumer integration audit:** exercise every named seam through the current interface and search the repository for one physics authority, one body/contact state, no parser or shape ownership, no fallback, no compatibility layer, no legacy path, and no stale generated inventory. | Partial |

## Generated Inventories

No generated Physics inventory is accepted. Accepted inventory item count: 0.

| Output | Authority identity | Authority revision | Generator command | Candidate items | Accepted items |
|---|---|---|---|---:|---:|
| [`inventories/body-and-constraint-families.md`](inventories/body-and-constraint-families.md) | Official SDK VPhysics environment, body, relationship, constraint, controller, fluid, ragdoll, bone-follower, prop, and game-side integration declarations; current playsrc Owner Registry; exact target-game occurrence registries | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; current Owner Registry; accepted TF2, CS:S, and legacy Source 1 CS:GO occurrence registries Missing | Missing | 30 | 0 |

Current manually derived candidate count: 30. Current generated count: 0.

The future generator is owned by `tools/playsrc`. It must consume a checked manifest of the pinned SDK declarations, the current Owner Registry, accepted BSP/PHY/Collision/Material/StudioModel producer identities, and exact indexed target-game entity/model occurrences. It emits the assigned inventory in category and stable-identity order, retains every unsupported, unknown, malformed, missing, inert, or blocked occurrence, and fails on an unclassified body creation mode, shape family, body role, pair relationship, constraint, controller type, fluid, ragdoll, bone follower, breakable assembly, producer, or consumer. No command name is declared before implementation exists.

## Exit Criteria

Physics is Complete only when all of these predicates pass:

- All 41 behavior rows are Ready.
- The body-and-constraint inventory is generated, current, Accepted, contains exactly the reviewed item count, and has no required `Unsupported`, `Unknown`, `Missing`, Malformed, or Blocked item.
- Every environment, body motion class, shape family, body role, relationship, constraint, controller, fluid, articulated assembly, callback, numerical boundary, and failure state satisfies its named evidence predicate.
- Broad-phase-backed pair admission equals exhaustive accepted-shape testing; continuous trajectories do not tunnel; penetration and solver-limit failure publishes no partial tick.
- Resting stacks, mixed constraints, ragdolls, bone followers, moving geometry, fluid bodies, and fragment transitions pass fixed target comparisons and deterministic continuation evidence.
- Every accepted surface identity has exact physical fields and per-feature selection; every pair-response combination and runtime material change satisfies its evidence predicate.
- The accepted Simulation phase schedule invokes exactly one Physics phase per gameplay tick and closes every event-to-command feedback path without reentrant mutation or duplicate authority.
- Accepted browser and native platform profiles pass their numeric tick-time, owned-memory, scratch-memory, body, pair, contact, manifold, island, solver, event, snapshot, and concurrent-reader budgets.
- Every named producer and consumer uses the current Physics interface; no duplicate solver, body store, contact store, parser, query engine, fallback shape, compatibility layer, legacy path, or stale generated inventory remains.
- Physics has no dependency on a game, ruleset, application, service, renderer, audio, particle, or product module; game-specific decisions enter only through declared typed inputs.

## Blockers

- **Active exact-solver derivation and result seam:** the configured derivation corpus is deterministic, but no current generic implementation matches its complete body, contact, friction, sleep, callback, and continuation outputs. Target state at any checkpoint is reproduced through complete history replay; lossy target object serialization is recorded behavior and is not a blocker. Continue equation, arithmetic-width, state, and phase derivation plus bounded static analysis until the generic Rust implementation passes derivation and sealed held-out vectors. The current TF2 result seam separately remains too small for enable motion, force at point, zero-to-ten matched collision results, persistent-contact facts, sleep/wake edges, and post-simulation completion.
- **Inventory generation and occurrences:** no checked-in command emits [`inventories/body-and-constraint-families.md`](inventories/body-and-constraint-families.md), and no accepted occurrence registry enumerates the body, constraint, controller, fluid, ragdoll, bone-follower, or breakable producers required by exact TF2, CS:S, and legacy Source 1 CS:GO content builds. Checked `tools/`, the current package/game roadmaps and READMEs, and the assigned Physics tree.
- **Contact ownership:** [`../../world/collision/README.md`](../../world/collision/README.md) owns contact queries while [`README.md`](README.md) owns contacts without distinguishing pure geometric output from persistent solver state. The Collision roadmap proposes the boundary, but the root Owner Registry does not record it. Collision must own stateless geometric contacts; Physics must own persistent manifold lifetime, response, friction state, and solver events.
- **Collision-shape producer:** the BSP roadmap assigns VPhysics payload decoding and shape construction to Physics, while the PHY and Collision roadmaps assign payload parsing to PHY and immutable query shapes to Collision. Collision's candidate shape inventory also lacks the analytic sphere family required by the public Physics body interface. Resolve the adjacent roadmaps so BSP frames model blocks, PHY parses supplied payloads, Collision constructs every immutable shape and stateless contact family, and Physics consumes them for body state and response.
- **Physical surface-property producer:** the Collision roadmap assigns surface-property meaning to Material, the PHY roadmap assigns physical response to Physics, and the Material roadmap does not enumerate surface-property scripts or resolved physical records. One adjacent owner must parse, compose, identify, and provide friction, elasticity, density, thickness, and dampening before the surface-response and integration rows can proceed.
- **Numeric and platform budgets:** no accepted decision names browser/native arithmetic profiles, fixed benchmark scenes, maximum tick time, owned and scratch memory, body/pair/contact/manifold/island capacities, solver iterations, snapshot bytes, or concurrent-reader targets. Checked Physics, Simulation, Networking, game, application, and tool READMEs and current roadmaps.
