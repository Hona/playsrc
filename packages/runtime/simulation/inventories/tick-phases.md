# Simulation Tick-Phase Inventory

Owner: [`../ROADMAP.md`](../ROADMAP.md)

State: Candidate; Not accepted.

| Field | Current value |
|---|---|
| Authority identity | Valve Source SDK 2013 game-system lifecycle, server game frame, player command, entity think, rigid-body integration, Entity I/O, prediction, and TF2 game scheduling contracts; playsrc Owner Registry and current runtime/game boundaries. |
| Authority revision | Source SDK commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`; playsrc commit `8ad1ca705dab2ec23c7d1deaf0ee84c5342357f9`; accepted participant declarations Missing. |
| Generator command | Missing. |
| Output path | `packages/runtime/simulation/inventories/tick-phases.md` |
| Item count | 18 candidate items; 0 accepted items. |

Simulation owns the phase order, barriers, and global record sequence. `Participant behavior owner` identifies the sole owner of work invoked in that phase and never transfers its domain behavior to Simulation. Every phase receives immutable input and returns a bounded transition batch; no participant mutates another owner's state directly.

| Order | Stable identity | Entry condition and frozen input | Required transition and observable output | Participant behavior owner | Current coverage |
|---:|---|---|---|---|---|
| 1 | `tick.begin` | Session is `Ready`; next tick, accepted command queues, participant manifest, prior complete snapshot, and starting random state are valid. | Set the in-progress tick to exactly prior tick plus one; freeze selected command, player, eligible-entity, participant, and random-state views; emit one tick-begin phase record. No participant domain callback runs. | Simulation | Unsupported |
| 2 | `lifecycle.pre-cleanup` | Tick-begin views are frozen; pending lifecycle work from map load or a committed paused transition is ordered by owner and request sequence. | Invoke bounded owner cleanup, invalidate completed stale handles, and produce the live starting view used by later phases. Work requested after this phase cannot enter the frozen current-tick entity list. | Entity and selected game; Simulation orders invocations | Unsupported |
| 3 | `systems.pre-entity` | Starting live view and phase participants are immutable; no player command has executed. | Invoke non-game, non-ruleset pre-entity participants in manifest order and collect bounded transition/event records. Later phases observe only committed outputs from this phase. | Declared world/runtime participants; Simulation orders invocations | Unsupported |
| 4 | `game.pre-entity` | Pre-entity system outputs are committed; selected game and ruleset identities match the composition. | Invoke game then game-owned ruleset pre-entity hooks in manifest order; collect round, objective, roster, bot, or game-state transitions without running player movement or generic domain algorithms. | Selected game and selected game-owned ruleset | Unsupported |
| 5 | `command.select` | Command queues are frozen for the in-progress tick; stable player order and immutable game command policy are available. | Select commands by player identity and command number; apply declared backup, repeated-last, neutral, null, or no-command dispositions; reject no already-admitted batch; emit one complete ordered command-selection record. | Simulation schedules; selected game owns payload and missing-command policy | Unsupported |
| 6 | `player.pre-move` | One selected command scope at a time contains player identity, command bytes, tick-base time, and command random seeds. | In stable player/command order, establish command scope and invoke game-owned input, weapon-selection, player pre-think, rules player-think, and due player-think work; emit bounded pre-move transitions. Command scope cannot leak to another command. | Selected game; Simulation owns command scope and order | Unsupported |
| 7 | `player.move` | The current command's pre-move transition and immutable movement input are complete. | Invoke Movement exactly once for the command, apply one bounded movement transition through the selected game adapter, and record input/output identities. No weapon, rigid-body, entity-I/O, or ruleset algorithm executes here. | Movement owns generic behavior; selected game owns game differences | Unsupported |
| 8 | `player.post-move` | The command's movement result is committed and command scope remains active. | Invoke ordered impact processing and game/player/weapon post-move work, update positive-time player tick base, collect events, then clear command scope even on command-owned rejection. | Selected game; Simulation owns scope teardown and order | Unsupported |
| 9 | `entity.think` | Every selected player command is complete; the eligible entity list remains the tick-begin frozen list minus identities made ineligible by declared deletion marking. | Invoke each retained live identity at most once in stable work-key order for due think and game-physics behavior. Game-owned projectile entities use this phase. Newly spawned identities cannot join this work list. | Entity schedules generic entities; selected game owns game entities and projectiles | Unsupported |
| 10 | `physics.integrate` | Entity-think transitions are committed; Physics receives one immutable body view and the exact tick interval. | Invoke Physics exactly once; return bounded body transitions, persistent-contact records, and solver events. Simulation performs no integration, manifold, response, constraint, or sleeping behavior. | Physics | Unsupported |
| 11 | `contact.resolve` | Ordered movement contacts, stateless Collision results, persistent Physics contacts, and current game/entity identities are available. | Resolve producer order, invoke game-owned pair/damage policy, and produce ordered Entity start/stay/end trigger/touch inputs plus bounded damage/effect requests. No geometry, manifold lifetime, or trigger-contact state is duplicated. | Collision, Physics, selected game, and Entity at their declared seams | Unsupported; Blocked on the Collision/Physics contact boundary |
| 12 | `systems.post-entity` | Physics/contact outputs are committed; game/ruleset post-entity hooks and Entity I/O have not run. | Invoke non-game, non-ruleset post-entity participants in manifest order and collect bounded transitions/events. Each output names its later consumer or is terminal diagnostic data. | Declared world/runtime participants; Simulation orders invocations | Unsupported |
| 13 | `game.post-entity` | All generic post-entity participant output is visible; due Entity I/O remains queued. | Invoke selected game then game-owned ruleset post-entity hooks in manifest order, including target-required second rule-think work, and collect bounded transitions/events. | Selected game and selected game-owned ruleset | Unsupported |
| 14 | `entity.service` | Ordered lifecycle, contact, direct-input, and game/rules output records for this tick are complete. | Invoke Entity once to apply external records, advance due generic primitives, drain due I/O including newly enqueued due events to quiescence or bound, and return ordered entity/lifecycle/event output. | Entity | Unsupported |
| 15 | `lifecycle.finalize` | Entity service is quiescent or has returned a terminal bound error; all spawn/delete requests carry owner and producer sequence. | On success, finalize spawn membership for later ticks, complete deletion lookup/storage/handle transitions, and emit the canonical live-set/lifecycle record. No identity enters an already completed phase. | Entity and selected game; Simulation orders cross-owner requests | Unsupported |
| 16 | `game.finalize` | Entity/lifecycle state is final for the tick and no due Entity event remains unprocessed. | Invoke selected game then ruleset end-of-tick invariants and finalize bounded gameplay events. A failure prevents snapshot and publication. | Selected game and selected game-owned ruleset | Unsupported |
| 17 | `snapshot.freeze` | Every mutable participant phase succeeded; command/event/random/lifecycle state is final and globally sequenced. | Serialize one complete canonical snapshot in participant-identity order, enforce snapshot/retention bounds, and optionally compute SHA-256. Nothing is published and no participant mutation is permitted during serialization. | Simulation composes; each participant owns its snapshot section | Unsupported |
| 18 | `tick.publish` | Complete snapshot bytes, optional hash, phase records, and event batch are validated within bounds. | Atomically publish the snapshot and events, increment transition ordinal, commit current tick, clear in-progress state, and return to `Ready`. Consumers can first observe this tick only after this phase succeeds. | Simulation | Unsupported |

## Generation Contract

The future checked-in generator must read a manifest pinning the authority revision and every contract path named by the owning roadmap, the current playsrc Owner Registry, and machine-readable declarations from every phase participant. It must emit these 18 identities in numeric order and reproduce this file byte-for-byte on repeated runs.

Generation fails on a missing or duplicate order or identity, item count other than 18, unknown participant, ownership cycle, dependency on a later phase, participant without a bounded input/output contract, emitted record without one later consumer or terminal disposition, or domain behavior assigned to Simulation. Failed generation retains every `Unsupported`, `Unknown`, `Malformed`, `Missing`, and Blocked discovery in diagnostics; it never silently omits an item.
