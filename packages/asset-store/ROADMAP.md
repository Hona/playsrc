# Asset Store Roadmap

[`../../TERMINOLOGY.md`](../../TERMINOLOGY.md) defines Object, CAS, asset graph, root manifest, Catalog, Channel, Asset store, CDN, Browser cache, delivery statuses, coverage classifications, and Complete. [`../../docs/roadmap-contract.md`](../../docs/roadmap-contract.md) defines denominator and evidence requirements.

## Completion Denominator

The completion denominator contains exactly the 41 Behavior Families rows below. [`inventories/object-kinds.md`](inventories/object-kinds.md) defines 7 draft immutable object-kind contracts; those items do not enter the denominator until a checked-in generator exists and the inventory passes the denominator review gate.

The denominator review state is Draft. No acceptance record exists.

## Inputs

- One validated `assetDir` identifying the durable local store root.
- An exact byte stream and an object descriptor with exactly `kind`, `mediaType`, `byteLength`, and `sha256`. `byteLength` is a canonical unsigned decimal string; `sha256` is lowercase 64-hex.
- RFC 8785 canonical UTF-8 JSON source roots, optional generated map roots, game roots, catalogs, and application roots using the exact shapes in the object-kind inventory. Every `references` value is a strictly sorted, duplicate-free object-descriptor array.
- A typed root descriptor containing `source-root`, `map-root`, `game-root`, or `application-root`, its SHA-256 content hash, and byte length.
- A channel mutation containing a channel name, expected prior channel-record revision or `Missing`, and one target root descriptor. The record is RFC 8785 canonical UTF-8 JSON shaped as `{channel,target}`; its revision is the SHA-256 hash of those exact bytes.
- A retained-root-set mutation containing its expected prior revision and the complete next sorted set of typed root descriptors. The set is RFC 8785 canonical UTF-8 JSON shaped as `{roots}`; its revision is the SHA-256 hash of those exact bytes.
- A remote-mirror adapter exposing exact reads, create-if-absent immutable writes, conditional mutable-record replacement, operation cancellation, and provider revision or capability identity.
- An immutable operation configuration containing numeric byte, object, graph, depth, queue, concurrency, retry, report, and elapsed-time bounds plus a cancellation signal.

## Outputs

- Object writes return the verified object descriptor and exactly one outcome: `Stored` or `AlreadyPresent`.
- Object reads return exact verified bytes and the same descriptor used to validate them.
- Root, catalog, retained-root-set, and channel writes return their immutable descriptor or mutable record revision only after durable publication.
- Reachability validation returns the root descriptor, a deterministic topological closure of unique descriptors, counts and byte totals by object kind, and one validation-record identity.
- Synchronization planning returns the target root, expected channel revision, sorted upload set, already-verified set, rejected conflicts, object and byte totals by kind, remote capability identity, and plan SHA-256.
- Publication returns a durable operation report containing every attempted immutable object, remote verification result, channel precondition and result, cancellation state, elapsed time, and exact failure.
- Failures use exactly these error kinds: `MalformedIdentity`, `MalformedRecord`, `MissingObject`, `MissingChannel`, `IntegrityFailure`, `ImmutableConflict`, `MissingReference`, `GraphCycle`, `ChannelPreconditionFailed`, `RetainedRootRequired`, `RemoteCapabilityUnavailable`, `BoundExceeded`, `Cancelled`, `IoFailure`, and `RemoteFailure`.

## Invariants

- An object identity is the lowercase SHA-256 digest of its exact bytes. Media type, semantic role, producer, filename, timestamps, and storage location never alter that identity.
- Root, catalog, channel, retained-root-set, plan, validation, and publication JSON uses RFC 8785 canonical UTF-8 bytes. Duplicate properties, non-I-JSON values, noncanonical bytes, and extra envelope properties are Malformed.
- Every immutable byte sequence, including an empty sequence and every root or catalog manifest, has one physical local path: `objects/sha256/<first-two-hash-hex>/<64-hash-hex>`. The remote mirror uses the same repository-relative key.
- Root and catalog bytes occupy the object path only. Typed root and catalog records never create a second copy of those bytes.
- A successful immutable write is durable and complete. A failed, cancelled, interrupted, or concurrent write exposes either the prior verified object or no object at the final path, never partial or replacement bytes.
- Existing bytes are reverified before `AlreadyPresent`. A hash or length mismatch is `IntegrityFailure`; the store never overwrites, repairs from another location, or returns those bytes.
- Source roots reference source objects. Map roots reference source objects, derived objects, and one source root. Game roots reference source roots, derived objects, and map roots. Catalogs reference source objects, source roots, optional map roots, and game roots. Application roots reference derived objects, source roots, map roots, game roots, and catalogs. Every other edge is Malformed.
- Every accepted root or catalog edge carries object kind, media type, byte length, and SHA-256. Duplicate descriptors, conflicting descriptors for one hash, self-reference, and graph cycles are Malformed.
- Reachability is computed from exact hash edges, independent of directory listings and producer filenames. Validation fails on every missing, corrupt, malformed, cyclic, over-bound, or disallowed reachable node.
- A channel target is retained and its complete closure is locally or remotely verified, as applicable, before the channel can point to it. Replacing a channel never removes the prior target from the retained-root set.
- Channel records are stored at `channels/<channel-name>.json`. A mutation succeeds only when its expected prior revision matches the current exact bytes; concurrent stale mutations fail without changing the record.
- Immutable object bytes and root-manifest bytes have no deletion operation in the current contract. Remote publication never issues object deletion. Channel records are the only mutable published resource.
- Publication uploads and verifies the complete immutable closure before changing a channel. Any failure before that final change leaves the prior channel revision and target unchanged.
- Plans, closures, and reports sort by object kind and SHA-256, deduplicate by SHA-256 for physical byte totals, and contain no source-root, work-directory, credential, or unconfigured machine path.
- The durable asset store contains explicitly published immutable raw Source objects and reproducible derived objects. The disposable Source cache contains local acquisition inputs and intermediates; one operation's work directory contains intermediates only; the CDN is a remote mirror only; and browser HTTP/IndexedDB caches are never validation or publication authority.

## Ownership Exclusions

- `packages/content` owns Source logical-path resolution, provider precedence, remote byte-range access, and the disposable local Source cache. Asset Store may retain exact provider bytes but never resolves a logical path or changes provider order.
- Format, world, runtime, presentation, and game compilers own compilation semantics, artifact media types, and the bytes they emit. The Asset Store verifies and stores those bytes without interpreting their domain semantics.
- `packages/world/map` owns a map's semantic identity and complete artifact dependency description. The Asset Store owns the map-root envelope, graph validation, identity, retention, storage, and publication.
- `games/<game>` owns each game root's game and content-build identity plus selected map and shared-artifact contents. The generic Asset Store owns no game-specific list.
- `apps/web/<product>` owns application-build identities, application-root contents, catalog entries, labels, ordering, and experience selection. The Asset Store owns catalog and application-root envelopes, immutable identity, graph edges, storage, and publication.
- `apps/services/assets` owns HTTP routing, range requests, cache headers, response metadata, and request observability for stored resources. `apps/services/api` owns API transport for catalog data; neither service changes asset-store truth.
- `tools/playsrc` owns local-configuration loading, per-operation work-directory lifecycle, command ordering, and release orchestration. The Asset Store owns the operations invoked by those commands.
- `infra` provisions and configures remote storage, CDN, credentials, routing, and environments. The Asset Store owns provider-neutral synchronization, verification, channel mutation, and rollback semantics.
- Browsers own their HTTP cache state. Cached responses never prove local or remote object existence or integrity.

## Behavior Families

The active migration checkpoints implement local SHA-256 descriptors and canonical object paths, atomic create-if-absent immutable writes, cancellation before commitment, verified warm reuse, exact reads, corruption refusal without repair, atomic exact channel records, browser immutable-object fetch verification with active-body cancellation, and a versioned IndexedDB derived-object cache under caller-owned SHA-256 keys. Local development publishes the exact source dependency bundle and its immutable provider-provenance/authoritative-absence ledger as separate objects. The active Cloudflare PoC checkpoint adds bounded checked-release synchronization to R2 through Wrangler with exact local and downloaded readback verification; root graph envelopes, conditional remote writes, Channels, and rollback remain subsequent checkpoints.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Derive one object identity as the lowercase 64-hex SHA-256 digest of the exact byte sequence, including empty bytes. | No implementation exists. | SHA-256 vectors covering empty, one-byte, binary, large streamed, and chunk-boundary-equivalent inputs compared with two independent implementations. | Not started |
| Verify declared byte length and SHA-256 while consuming input and reject either mismatch before final-path publication. | No implementation exists. | Fault vectors for short, long, bit-flipped, truncated, and post-read-changed inputs proving no final object appears. | Not started |
| Map every valid hash to `objects/sha256/<first-two-hash-hex>/<64-hash-hex>` beneath `assetDir` and reject every noncanonical identity before filesystem access. | No implementation exists. | Cross-platform path vectors instrumenting filesystem calls for lowercase, uppercase, wrong-length, separator, traversal, absolute, URI, control-character, and symlink-escape inputs. | Not started |
| Publish a local object atomically so crash, cancellation, quota exhaustion, short write, flush failure, and rename failure expose only a complete verified object or no final object. | No implementation exists. | Filesystem fault injection at every write and durability boundary followed by process restart and independent hash verification. | Not started |
| Converge concurrent writers of identical bytes and hash on one verified final object without lock leakage, temporary-file collision, or replacement. | No implementation exists. | Deterministic multi-process schedules covering simultaneous start, staggered completion, one cancelled writer, one failed writer, and repeated verified-read equality. | Not started |
| Treat an existing verified object as `AlreadyPresent` and an existing hash-path length or digest mismatch as `IntegrityFailure` without overwrite. | `putObject` and `verifyObject` stream existing bytes through bounded one-megabyte chunks, return `AlreadyPresent` only after exact length/SHA-256 verification, and reject mismatches without overwrite or a second full-object allocation. | Preseeded correct and corrupt objects compare outcome, exact errors, unchanged bytes, repeated verification, and absence of repair in [`tests/store.test.ts`](tests/store.test.ts). | Ready |
| Store one physical byte sequence per SHA-256 even when raw Source, derived, map, game, application, catalog, or producer references name it. | No implementation exists. | Multi-root fixtures with repeated source and derived objects comparing unique path count, inode-independent byte accounting, closure totals, and repeated publication writes. | Not started |
| Read exact bytes by descriptor, returning `MissingObject` for an absent path and `IntegrityFailure` for wrong length or hash. | `readObject` validates declared length and SHA-256 over the returned bytes; `verifyObject` performs the same classification without retaining object bytes. | Fixed valid, absent, and corrupt reads plus streaming verification compare returned bytes and exact errors in [`tests/store.test.ts`](tests/store.test.ts). Extended, replaced, unreadable, and empty vectors remain absent. | Partial |
| Ignore and remove only operation-owned stale temporary writes after success, failure, cancellation, or restart; never classify them as objects. | No implementation exists. | Crash-recovery vectors preseeded with current, foreign, expired, colliding, and locked temporary files, proving bounded cleanup and unchanged final objects. | Not started |
| Accept exactly the immutable object kinds and edge rules in the accepted generated object-kind inventory; retain unknown kinds as `Unknown` inventory entries and reject them at runtime. | No implementation or accepted inventory exists. | Regenerate the inventory twice, compare byte identity and count, then run one descriptor and edge vector per accepted and unknown kind. | Not started |
| Store one source root that maps each normalized logical path to exact provider provenance, decoded length/SHA-256, lifecycle roles, and either one complete immutable object or one bounded range in a deterministic encoded chunk; verify every encoded object, decoded entry, ordering rule, uniqueness rule, codec, offset, length, role, and numeric bound before publishing the root. | The Rust graph owner implements stable role/path-prefix chunks, one-entry large objects, deterministic identity/raw-DEFLATE selection, canonical root encoding, encoded/decoded/entry verification, sorted resource sets, and bounded batch decoding. TypeScript rejects noncanonical graph/catalog bytes, validates descriptors, and selects role sets. Provenance remains in the separately referenced ledger rather than each root entry; general nested-root traversal remains absent. | Unit vectors cover deterministic packing, one-entry change locality, cross-map chunk reuse, large separation, maximum-plus-one graph/chunk bounds, corruption, duplicate paths, native/WASM batch decoding, noncanonical JSON rejection, and a 100-entry catalog fixture. Real `jump_beef` produces 142 chunks and native/WASM gameplay parity. | Partial |
| Store an optional immutable map root whose identity names one map source plus compiler/configuration identities and whose sorted source/derived descriptors reproduce the compiled runtime representation; first load never requires this root. | No implementation exists. | Fixed direct-load and prepopulated-cache vectors comparing source fetches, derived keys, root bytes, first-load compilation, warm reuse, and malformed or stale manifests. | Not started |
| Store an immutable game root whose identity names one game and content build and whose closure contains exactly its selected map roots and shared game artifacts. | No implementation exists. | Two-content-build vectors comparing root identity, map and shared-artifact selection, cross-build separation, deduplication, and missing-map failure. | Not started |
| Store an immutable catalog envelope while preserving application-owned entries and validating every selected map source, optional map root, or game root without changing source, map, game, or derived-object truth. | No implementation exists. | Application catalog fixtures comparing exact retained `MapSource` bytes, allowed descriptors, optional cache roots, duplicate selection, missing target, and attempted truth override. | Not started |
| Store an immutable application root whose identity names one application build and whose closure contains its application artifacts, selected game or map roots, and catalogs. | No implementation exists. | Fixed application-build vectors comparing complete closure, catalog and game selection, shared-object reuse, and omission or cross-kind edge failures. | Not started |
| Validate every root and catalog descriptor's kind, media type, byte length, hash, strict ordering, uniqueness, and permitted edge before accepting the manifest. | No implementation exists. | Table vectors mutating each field independently plus duplicate-hash, conflicting-size, conflicting-media-type, unsorted, self-edge, and disallowed-edge cases. | Not started |
| Reject direct and transitive graph cycles without recursion overflow or partial validation state. | No implementation exists. | Synthetic self, two-node, long-cycle, shared-DAG, and maximum-depth-plus-one graphs compared for exact cycle identity and bounded resource use. | Not started |
| Compute one deterministic unique reachability closure from a root and report topological descriptors, counts, and physical byte totals by object kind. | No implementation exists. | Diamond, repeated-edge, disconnected-object, nested-root, catalog, and reordered-input graphs compared byte-for-byte across repeated runs. | Not started |
| Maintain one atomically revised retained-root set; adding or removing an entry never mutates a root, and every channel target remains retained. | No implementation exists. | Concurrent retained-set mutation schedules comparing expected revisions, sorted snapshots, channel constraints, and restart durability. | Not started |
| Prohibit deletion of immutable local or remote objects and reject removal of a retained root targeted by any channel. | No implementation exists. | Interface audit proving no object-delete capability plus mutation vectors for retained, channel-targeted, unreferenced, and unknown roots with unchanged bytes and remote call logs. | Not started |
| Validate an entire retained closure and emit reusable validation metadata only after every descriptor and exact byte sequence passes current checks. | No implementation exists. | Corruption matrix at root, catalog, nested root, shared object, and final leaf positions comparing complete failure reports and proving no partial-valid result. | Not started |
| Validate channel names against `[a-z0-9](?:[a-z0-9._-]*[a-z0-9])?` under the accepted byte bound, read one exact channel snapshot from `channels/<channel-name>.json`, and distinguish an absent channel from a malformed record. | No implementation exists. | Name and record vectors covering shortest and longest accepted tokens, case, separators, traversal, control bytes, absent records, and malformed target descriptors. | Not started |
| Require every proposed channel target to be a retained map, game, or application root with a complete verified closure in the target store. | No implementation exists. | Channel vectors for each root kind, unretained roots, missing or corrupt closure nodes, catalog-only targets, and stale validation metadata. | Not started |
| Change a channel atomically from one immutable root to another and return the new exact record revision without an observable missing or partial state. | No implementation exists. | Reader/writer schedules and crash injection comparing every observed snapshot with either the old or new complete record. | Not started |
| Reject concurrent channel mutations whose expected prior revision is absent or stale, leaving the winning revision unchanged. | No implementation exists. | Multi-process create, update, rollback, and identical-target races compared for one winner, deterministic precondition errors, and final bytes. | Not started |
| Drive local development exclusively from validated `assetDir` objects, retained roots, catalogs, and channels, without reading the Source cache, operation work directory, remote mirror, or browser cache as substitutes. | The exact local configuration exists and `dev jump_beef` verifies existing BSP, WASM, dependency bundle, UI bundle, and dependency ledger objects through bounded streams, reads Source-cache payloads only for absent Asset Store objects, and starts listeners only after every object verifies or installs. Retained roots, catalogs, channels, and excluded-store poisoning remain unimplemented. | Controlled local-development startup profiles compare existing-object verification, missing-object installation, command-to-ready time, process memory, exact descriptors, and clean shutdown. Excluded-store poisoning remains absent. | Partial |
| Browser derived-cache open, read, write, and remove operations terminate within 30 seconds; large immutable records use Blob-backed IndexedDB values without an additional full `ArrayBuffer` clone, verify exact length and SHA-256 on every read/write, and publish no readable key before transaction commit. | The version-2 browser cache rejects timed-out requests/transactions as `PersistenceUnavailable`, aborts timed-out transactions, stores exact Blob records, and retains create-if-absent convergence. Version-1 development cache data is not read. | A fake never-settling open request terminates at exactly 30,000 ms; malformed/corrupt Blob vectors fail verification; installed Edge commits and reads the exact 475,665,764-byte presentation plus 78,299,802-byte map under a 554,614,784-byte storage estimate. Explicit transaction-abort, quota, and concurrent large-Blob fault injection remain absent. | Partial |
| Roll back a local channel to a prior retained root without recompiling, copying, mutating, or deleting immutable bytes. | No implementation exists. | Three-root channel history vectors comparing pre/post object hashes, write-call logs, retained-set state, and exact restored channel target. | Not started |
| Refuse remote publication unless the configured adapter proves exact readback, create-if-absent immutable writes, conditional channel replacement, strong origin read-after-write behavior, and cancellation. | The Cloudflare PoC selects R2 and exact Wrangler upload/readback for objects no larger than 300,000,000 bytes. Wrangler exposes no conditional immutable put or conditional Channel replacement in this path, so complete adapter admission remains blocked. | Adapter conformance against the provisioned origin, including concurrent writes, stale conditions, immediate reads, partial uploads, cancellation, and capability-revision recording. | In progress |
| Build a deterministic synchronization plan by traversing the validated local target closure and classifying each remote key as verified, missing, or conflicting. | No implementation exists. | Fake-remote matrices and a controlled origin run comparing sorted plan bytes, plan SHA-256, unique counts, byte totals, and zero mutation during planning. | Not started |
| Verify existing remote immutable bytes by exact length and independently computed SHA-256; existence, listing, ETag, cache state, and unverified metadata never satisfy the check. | No implementation exists. | Remote fixtures with matching, same-length corrupt, truncated, extended, stale-edge-cache, misleading-ETag, and changed-metadata objects compared for exact classification. | Not started |
| Upload each missing immutable object with create-if-absent semantics so retries and concurrent publishers are idempotent and a conflicting existing key fails closed. | The R2 S3 adapter conditionally creates with `If-None-Match: *`, treats 412 only as a concurrent winner, verifies exact readback, performs zero warm writes, and rejects corrupt existing bytes. Timeout-after-commit and controlled live concurrency evidence remain absent. | Fake-adapter vectors cover cold upload, warm zero-write reuse, concurrent precondition loss, corrupt conflict, and missing readback. Controlled origin interruption and concurrency remain required. | Partial |
| Upload and verify referenced objects before their catalogs or roots, and upload and verify every immutable closure member before any channel mutation. | TF2 publication sorts immutable leaves by SHA-256, source roots next, and catalogs last; it has no Channel mutation capability. General DAG topological traversal remains absent. | Pure ordering vectors cover leaf/root/catalog permutations; the real 142-chunk closure is locally enumerated. Fault-at-depth and general nested-root evidence remain required. | Partial |
| Replace the remote channel with one conditional atomic write against the planned prior revision and verify the resulting exact target and revision. | No implementation exists. | Concurrent publisher and rollback schedules against a controlled origin comparing one winning revision, stale-precondition errors, and post-write readback. | Not started |
| Preserve the prior channel and durable operation state when publication is interrupted, then resume by replanning and reusing only reverified immutable objects. | No implementation exists. | Process termination before, during, and after object commits and before channel commit, followed by restart; compare channel bytes, resumed uploads, and operation records. | Not started |
| Declare publication successful only after full remote closure verification and final origin channel readback; a CDN or browser cache response cannot supply this evidence. | No implementation exists. | End-to-end publication with poisoned cache responses and exact origin reads, comparing every closure descriptor and final channel revision. | Not started |
| Roll back a remote channel to a prior retained and remotely verified root without rebuilding or uploading when its closure remains present. | No implementation exists. | Three-release publication and rollback vectors comparing zero immutable upload calls, conditional channel history, final target, and unchanged remote bytes. | Not started |
| Emit deterministically ordered local-validation, synchronization-plan, publication, interruption, and rollback reports with per-kind object counts, physical byte totals, outcomes, durations, and exact bounded errors. | No implementation exists. | Golden report vectors with reordered inputs, deduplicated objects, mixed outcomes, cancellation, redacted machine paths, exact equality for invariant fields, and explicit duration-field validation. | Not started |
| Apply bounded queues, concurrent reads and writes, in-flight bytes, retry attempts, elapsed time, and cancellation to local validation and remote synchronization without changing serial results. | No implementation exists. | Deterministic schedules at empty, saturated, cancellation, failure, retry, and recovery states comparing serial-equivalent plans and closures plus resource measurements. | Not started |
| Enforce accepted numeric maxima for object bytes, manifest bytes, descriptors per manifest, graph nodes and depth, channel-name bytes, retained roots, queued operations, concurrency, in-flight bytes, retries, report entries, and operation duration. | Exact browser, native, local-filesystem, and remote-adapter maxima have not been accepted. | Boundary vectors at every accepted maximum and maximum-plus-one, plus fixed-machine memory, descriptor, and elapsed-time measurements. | Blocked |
| Return the declared exact error kind, failing identity, operation, bound or precondition, and causal adapter error without fallback bytes, partial success, lower-priority store lookup, or implicit repair. | No implementation exists. | Exhaustive fault-injection matrix over local objects, manifests, graphs, retained roots, channels, remote planning, upload, verification, cancellation, and reports. | Not started |

## Generated Inventories

[`inventories/object-kinds.md`](inventories/object-kinds.md) contains 7 draft immutable object kinds and 0 accepted items. The future generator must consume machine-readable current-contract declarations, emit stable kind identities and permitted edge kinds, fail on duplicates or omissions, and reproduce this file byte-for-byte.

The generator command is Blocked because no checked-in generator exists and production implementation is prohibited during roadmap research.

## Exit Criteria

- All 41 Behavior Families rows are Ready.
- The object-kind inventory is generated by its checked-in command, records exactly 7 accepted items, and passes the denominator review gate.
- Fixed integration vectors cover artifact, map-root, game-root, catalog, and application-root objects through local storage, remote synchronization, channel publication, interruption, and rollback.
- Every compiler, game, application, catalog producer, tool, asset service, and remote adapter uses the same current object descriptor, root envelope, channel record, retained-root set, error, bound, and report contracts.
- Local and remote fault injection proves atomicity and byte integrity under concurrent writers, cancellation, process termination, storage exhaustion, network interruption, and stale channel revisions.
- The Source cache, operation work directory, durable asset store, remote CDN mirror, browser HTTP cache, and browser IndexedDB derived cache retain distinct lifecycle and authority. Equal immutable bytes may share SHA-256 while each descriptor preserves representation kind and provenance.
- No immutable object deletion, overwrite, fallback lookup, implicit repair, compatibility path, or mutable-root publication exists.
- No required value or behavior remains Unsupported, Unknown, Missing, Partial, or Blocked.

## Blockers

- **Remote adapter and environment:** no infrastructure definition selects a remote object-store adapter, capability revision, credentials binding, or isolated publication environment. Checked `ARCHITECTURE.md`, `infra/README.md`, `apps/services/assets/README.md`, AWS S3 conditional-write documentation, and Cloudflare R2 consistency and S3-compatibility documentation. A selected adapter must satisfy the roadmap contract rather than rely on provider existence checks or cache responses.
- **Numeric bounds:** exact accepted maxima for browser, native, local-filesystem, and remote publication operations are unresolved. Checked the playsrc architecture and terminology, RFC 8785 JSON Canonicalization Scheme, OCI Image Specification 1.1.1 descriptors and image layout, Bazel Remote Execution CAS contracts, and the public remote-provider documentation named above; these authorities permit implementation-specific limits and do not establish one playsrc bound set.
- **Inventory generator:** no checked-in command generates `inventories/object-kinds.md`; its 7 draft items therefore remain unaccepted.
