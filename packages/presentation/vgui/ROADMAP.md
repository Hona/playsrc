# VGUI Roadmap

[`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines delivery status, coverage classification, inventory, and completion terms.

## Completion Denominator

This leaf roadmap contains exactly 40 behavior rows. The five manually derived Candidate inventories contain 300 items and contribute 0 items until one checked-in generator emits each inventory and a denominator review accepts this roadmap. The current completion denominator is therefore 40 behavior rows. Acceptance of all current candidates would make the denominator 340 items.

The target is generic Source 1 VGUI behavior presented as framework-independent TypeScript through direct browser DOM and CSS. The application shell uses Preact, Vite, and Bun; Preact neither renders nor reconciles VGUI-owned DOM. The denominator is Not accepted because VGUI is absent from the root Owner Registry, an adjacent roadmap assigns VGUI semantics to applications, required format owners and TF2 inventories are unavailable, generators and numeric limits are Missing, and no review record exists.

Every behavior row is `Blocked` by the unresolved owner assignment. Research and roadmap definition do not change delivery status, and no implementation exists.

## Inputs

- One application-supplied `HTMLElement` root, one injected monotonic clock, one explicit CSS-pixel viewport with device-pixel ratio, one browser event adapter, one content resolver, one font and image adapter, one clipboard and system adapter, one accessibility configuration, one reduced-motion selection, one typed command sink, and one required limits record.
- Parsed KeyValues resource and scheme documents preserving repeated keys, ordering, conditions, composition provenance, and exact logical identities; parsed localization token tables; parsed HUD-animation manifests and scripts; decoded font and image inputs; and immutable game-owned control registrations.
- One ordered stream of lifecycle, viewport, resource-replacement, dialog-variable, animation-sequence, input, focus, popup, modal, and destroy operations. Every time-bearing operation uses the injected monotonic clock.
- Immutable gameplay or replay presentation state supplied only through game-owned bindings. VGUI receives display values and emits typed commands; it never receives authority-mutating callbacks.
- A limits record containing maximum resource bytes, decoded bytes, resource depth, resource nodes, panels, hierarchy depth, children per panel, schemes, colors, fonts, glyph candidates, borders, images, texture bytes, text code units, animation scripts, sequences, commands, active animations, delayed commands, queued messages, tick registrations, input contexts, popups, DOM nodes, listeners, observers, timers, diagnostics, and retained bytes.
- Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`: `src/public/vgui/{IVGui,IPanel,IClientPanel,IInput,IInputInternal,ISurface,IScheme,ILocalize,IImage,IBorder,ISystem,IHTML}.h`; `src/public/vgui_controls/{Panel,EditablePanel,BuildGroup,MessageMap,PanelAnimationVar,AnimationController,FocusNavGroup}.h`; the public headers and implementations for every generic control named by [`inventories/controls.md`](inventories/controls.md); and TF2 HUD consumers under `src/game/client`, `src/game/client/game_controls`, and `src/game/client/tf`.
- Configured TF2 build `10822003`: archive index `tf2_misc_dir.vpk` with SHA-256 `63f7db0d1c509e303ca9002fee9e3d805e9220ea5afdd639d8a6b68b8a3710b9`, 743 indexed VGUI `.res` identities, four indexed scheme identities, two indexed HUD-animation identities, 84 loose `resource/tf_*.txt` localization identities, and eight loose TrueType font identities.
- Fixed synthetic vectors, parsed-resource snapshots, browser DOM and accessibility snapshots, event traces, virtual-clock schedules, target captures, and integration records satisfying the roadmap evidence contract.

## Outputs

- One VGUI runtime containing stable panel identities, parent and ordered child state, signed z-order, popup order, local and effective visibility, enabled and input-interest state, local and absolute bounds, insets, clipping, layout invalidation, scheme identity, control state, focus, capture, modal state, tick registrations, queued messages, and animation queues.
- One VGUI-owned DOM subtree whose node order, CSS bounds, clipping, opacity, visibility, pointer eligibility, text, images, borders, focus state, accessibility state, and cleanup agree with the runtime state after each committed frame.
- Typed resource-application, scheme, localization, text-measurement, animation, input, focus, command, action-signal, system-request, and diagnostic results. A failed operation publishes no partial subtree or registry.
- Ordered typed requests for application navigation, clipboard, user configuration, cursor lock, audio, and game commands. The receiving owner decides policy and effect.
- An observation stream keyed by runtime, panel, resource, scheme, sequence, command, input context, frame, queue position, viewport revision, and error code without exposing mutable DOM as presentation truth.

## Invariants

- VGUI is one deep module. Applications mount, update, observe, and destroy it through one interface; controls do not expose browser listeners, CSS implementation, parser details, or mutable DOM as caller responsibilities.
- One live panel has one stable identity, at most one parent, one owned DOM node, and no hierarchy cycle. Reparenting removes the old edge before publishing the new edge.
- Sibling paint and hit-test order is signed z-order followed by stable tie order. Painting visits back to front; hit testing visits front to back. Popup order is separate from ordinary child order.
- Effective visibility requires local visibility from the panel through every ancestor. Invisible subtrees do not lay out from frame input, paint, receive input, remain focused, or retain capture.
- Enabled, mouse-input-enabled, and keyboard-input-enabled are independent. Disabled controls retain layout and paint state but expose disabled interaction and accessibility state.
- Source layout units are integer CSS pixels. Proportional conversion scales from 640×480 by viewport height and truncates toward zero. Device-pixel ratio changes raster selection and never changes CSS-pixel panel geometry by itself.
- Layout applies resource values, scheme values, parent geometry, anchors, sibling pins, and control-specific layout in the declared order. One resize or resource commit produces one downstream invalidation closure before paint.
- Non-popup clipping is the intersection of panel bounds and ancestor clips after insets. Popups escape parent clipping but remain clipped to the explicit VGUI workspace.
- Paint order is early border when declared, background, foreground, children, late border otherwise, then post-child foreground. Ancestor alpha multiplies each descendant's own alpha.
- Resource replacement validates the complete typed document, dependencies, factories, references, conditions, and limits before atomically replacing the resource-owned subtree. Missing or unknown items never create substitute controls.
- Schemes, localization tables, font registries, border registries, image registries, and animation script sets are immutable between atomic replacement commits. Controls hold stable handles that are invalidated on replacement or destroy.
- A leading `#` requests localization lookup. Dialog-variable substitution uses only caller-supplied typed values. Missing tokens and variables remain distinct `Missing` results and cannot silently become empty text.
- Animation advances only from the injected clock. Delayed commands use due time then script order. A sequence can mutate registered presentation variables and emit typed requests; it cannot call gameplay, replay, networking, audio, routing, or storage implementations directly.
- Input edge states last for exactly one VGUI frame. Mouse-over, mouse focus, mouse capture, calculated key focus, committed key focus, application modal surface, and modal subtree are distinct states.
- Focus loss is delivered before focus gain. Capture, focus, queued messages, tick signals, listeners, observers, timers, accessibility relationships, font references, image references, and DOM nodes are cleared when their owner is destroyed.
- Browser accessibility and reduced-motion modes are explicit inputs. Reduced motion changes visual interpolation sampling only; command delays, event order, state changes, and sequence completion time remain unchanged.
- Every count, offset, duration, scalar, allocation, lookup chain, traversal, queue insertion, DOM creation, texture upload, and emitted diagnostic is checked against integer range and the supplied limits before mutation.

## Ownership Exclusions

- `packages/formats/keyvalues` owns lossless KV1 syntax, directives, conditions, composition records, and diagnostics. VGUI owns control-resource and scheme semantics over parsed documents.
- The proposed localization, UI-script, and font format packages own localization-file, HUD-animation-text, and font-file parsing. `packages/formats/vtf` owns VTF bytes. VGUI owns lookup, binding, scheduling, measurement, and browser presentation over typed inputs.
- `packages/content` owns exact logical-path resolution, provider precedence, bytes, provenance, and caching. VGUI requests exact identities and never searches providers or the filesystem.
- `games/tf2` owns health, ammo, weapon selection, objectives, death notices, scoreboards, class and team menus, buildings, conditions, event bindings, game commands, custom control semantics, and immutable HUD presentation mappings. VGUI owns generic controls and their presentation runtime.
- `apps/web/*` own Preact shell composition, routes, navigation policy, uploads, settings policy, browser lifecycle, external-link policy, persistence adapters, and product UI that is not Source VGUI behavior.
- `packages/presentation/rendering` owns world and model pixels, Three.js, GPU resources, and camera presentation. Particle and Audio own their presentation; VGUI emits typed image, particle, and sound requests at those seams.
- Simulation and Replay remain the only gameplay and replay authorities. VGUI cannot mutate either authority, derive missing state, or execute a game command.
- Drag and drop is excluded from the current denominator because no accepted TF2 or shared economy consumer uses the generic panel drag/drop contract. Application file-drop behavior remains application-owned.
- The root Owner Registry does not currently register VGUI, and the KeyValues roadmap assigns VGUI semantics to web applications. That conflict is unresolved and blocks every row below.

## Behavior Families

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| One injected frame advances input rollover, focus, messages, ticks, layout, animation, and DOM publication in the declared order without reentrant partial state. | No VGUI runtime exists; owner assignment is unresolved. | **Virtual-frame trace comparison:** fixed clock schedules compare the complete ordered state, callback, queue, and DOM commit trace after every phase. | Blocked |
| Panel allocation, reparenting, child order, safe handles, auto-deletion, deferred deletion, and teardown maintain one acyclic live hierarchy. | No panel runtime exists; owner assignment is unresolved. | **Model-based hierarchy comparison:** generated create, reparent, delete, mark-delete, and stale-handle traces compare graph, handles, notifications, and live resource counts exactly. | Blocked |
| Signed z-order, stable equal-z order, front/back movement, popup order, and topmost-popup state determine paint and hit-test precedence. | No ordering runtime exists; owner assignment is unresolved. | **Order trace and DOM-stack comparison:** all z ties and movement operations compare ordered panel IDs, DOM order, paint callbacks, and hit targets exactly. | Blocked |
| Local and ancestor visibility plus enabled, mouse-input-enabled, and keyboard-input-enabled state independently govern layout, paint, focus, capture, input, and accessibility. | No state implementation exists; owner assignment is unresolved. | **State truth-table comparison:** cross-product vectors compare effective state, callback eligibility, computed CSS, focus/capture release, and accessibility properties. | Blocked |
| Integer local, absolute, minimum, inset, and clipped bounds plus 640×480 height-based proportional conversion produce deterministic CSS-pixel geometry. | No geometry implementation exists; owner assignment is unresolved. | **Geometry differential vectors:** negative, zero, boundary, nested-inset, popup, and viewport cases compare every rectangle and conversion exactly with an independent integer model. | Blocked |
| Parent anchors, fill and alignment forms, auto-resize, aspect-relative dimensions, title-safe edges, and sibling corner pins recompute in dependency order. | No anchor or resize implementation exists; owner assignment is unresolved. | **Resize schedule comparison:** fixed parent, viewport, sibling, and title-safe changes compare solved bounds and dependency order after each revision. | Blocked |
| Scheme application and layout invalidation propagate once through affected descendants, then control-specific layout completes before paint. | No invalidation implementation exists; owner assignment is unresolved. | **Invalidation trace comparison:** instrumented property, scheme, font, text, resource, and resize changes compare apply/layout/repaint call order and counts exactly. | Blocked |
| Solved clipping and front-to-back hit testing honor ancestor clips, insets, visibility, input interest, popups, and modal restrictions. | No clipping or hit-test implementation exists; owner assignment is unresolved. | **Analytic rectangle and pointer vectors:** compare clip rectangles, deepest target, popup precedence, and no-hit results for every edge-touch and fully clipped case. | Blocked |
| Paint traversal emits early border, background, foreground, children, late border, and post-child foreground under inherited alpha and nested paint contexts. | No paint traversal exists; owner assignment is unresolved. | **Golden paint-command trace:** synthetic panel trees compare command kind, panel, context origin, clip, alpha, and order exactly. | Blocked |
| Direct DOM and CSS present the inventoried filled, outlined, line, fade, polygon, textured, image, and text surface primitives without a framework reconciler. | No browser surface exists; owner assignment is unresolved. | **Primitive capture comparison:** retained browser screenshots and DOM/computed-style snapshots compare fixed primitive scenes at declared browser, font, viewport, and device-pixel ratio settings. | Blocked |
| Every accepted generic control identity has one factory, one state machine, one resource contract, one accessibility mapping, and deterministic cleanup. | No control implementation or generated control inventory exists; owner assignment is unresolved. | **Inventory-driven control conformance:** instantiate every accepted `CTRL-*` identity, apply boundary resources, replay keyboard and pointer schedules, and compare state, signals, DOM, accessibility, and cleanup. | Blocked |
| Parsed `.res` documents create, update, reuse, remove, and order controls atomically through exact factory and panel identities. | KeyValues has no implementation, the resource-property inventory has no generator, and owner assignment is unresolved. | **Parsed-resource tree comparison:** fixed generic and declared-build resources compare panel identities, types, settings, order, references, diagnostics, and repeated-load state exactly. | Blocked |
| Caller conditions, resolution suffixes, `_minmode`, and declared browser conditions apply deterministic overrides before control creation. | No condition registry or resource semantic implementation exists; owner assignment is unresolved. | **Condition power-set comparison:** fixed documents compare retained nodes and final property values for every declared condition selection and ordering collision. | Blocked |
| Localization tokens, literal text, dialog variables, formatted substitutions, and child notifications produce exact Unicode text without mutating source tables. | The localization format owner and implementation are absent; owner assignment is unresolved. | **Localized-string vector comparison:** fixed token tables and variable maps compare Unicode scalars, missing results, substitution order, child notifications, and measured text exactly. | Blocked |
| Scheme documents compose immutable named color, setting, font, border, bitmap-font, custom-font, and image registries selected by one scheme tag. | The scheme inventory has no generator and required format dependencies are absent; owner assignment is unresolved. | **Scheme registry snapshot comparison:** parsed scheme graphs compare names, values, aliases, handles, dependencies, selected candidates, and errors exactly. | Blocked |
| Literal and aliased scheme colors resolve ordered RGBA values and report missing or cyclic references without visual substitution. | No color registry exists; owner assignment is unresolved. | **Color graph comparison:** literal, alias-chain, override, repeated-name, missing, cycle, and channel-bound vectors compare RGBA and diagnostics exactly. | Blocked |
| Font candidates, custom and bitmap files, language ranges, flags, proportional selection, glyph metrics, wrapping, kerning, and text measurement agree for fixed browser and font bytes. | Font format ownership, browser font adapters, and target measurement captures are unavailable; owner assignment is unresolved. | **Font-metric and text-layout comparison:** fixed font bytes and multilingual strings compare selected candidate, ascent, height, widths, kerning, line breaks, and browser captures under declared tolerances. | Blocked |
| Line, image, and scalable-image borders plus image handles, tint, filtering, frame, rotation, tiling, nine-slice geometry, texture updates, and eviction are deterministic. | No border, image, or texture implementation exists; owner assignment is unresolved. | **Border/image command and capture comparison:** inventory vectors compare resolved identities, geometry, paint order, texture bytes, lifecycle, and fixed browser pixels. | Blocked |
| `Panel`, editable panels, labels, images, buttons, toggles, checks, radios, controller maps, URL labels, tooltips, frames, and HTML controls satisfy their inventoried state transitions. | No generic controls exist; owner assignment is unresolved. | **Control-state model comparison:** one state machine per accepted identity compares public values, signals, focus, DOM, accessibility, and typed owner requests after every input. | Blocked |
| `TextEntry` and `RichText` preserve bounded Unicode, selection, caret, wrapping, scrolling, clipboard, links, hidden text, numeric admission, and IME state. | No text controls exist; owner assignment is unresolved. | **Unicode editing trace comparison:** fixed grapheme, scalar, selection, clipboard, composition, wrap, scroll, and malformed-input schedules compare text, ranges, metrics, signals, and DOM exactly. | Blocked |
| Menus, combo boxes, list and tree controls, scrollbars, sliders, progress controls, and property sheets maintain deterministic composite state and navigation. | No composite controls exist; owner assignment is unresolved. | **Composite interaction traces:** fixed item graphs and keyboard/pointer schedules compare ordering, selection, expansion, values, scroll positions, pages, popup lifetime, and signals exactly. | Blocked |
| Registered animation variables inherit through control classes, initialize defaults, apply resources, expose typed values, and reject incompatible converters. | No animation-variable registry exists; owner assignment is unresolved. | **Animation-map reflection comparison:** generated class maps compare inherited names, types, defaults, resource values, get/set results, and exact type errors. | Blocked |
| Ordered HUD-animation manifests and scripts define named sequences, conditions, duplicate replacement, delays, durations, scoped start, cancellation, and viewport-size reload. | The UI-script format owner is absent and one manifest dependency is Missing; owner assignment is unresolved. | **Parsed-sequence snapshot and virtual-clock comparison:** compare sequence registries, command lists, durations, replacements, queues, cancellation, and resize results exactly. | Blocked |
| All 13 commands, 9 interpolators, 9 built-in variables, 15 converters, and 9 relative alignments in the candidate inventory execute with deterministic queue order and target resolution. | No animation controller or generated inventory exists; owner assignment is unresolved. | **Inventory-driven animation differential:** one vector per `ANIM-*` operation plus mixed sequences compares target lookup, sampled values, emitted requests, queues, and terminal state. | Blocked |
| Browser events enter one input context, edge states last one frame, and routing distinguishes mouse-over, mouse focus, capture, calculated key focus, committed focus, and modal state. | No browser event adapter or input runtime exists; owner assignment is unresolved. | **Browser-event normalization trace:** fixed pointer, keyboard, focus, visibility, cancellation, and context-switch schedules compare normalized events and complete input state exactly. | Blocked |
| Key press, key-code typed, Unicode typed, release, repeat, modifier, unhandled-key, and control parent-chain behavior preserve event type and order. | No keyboard routing exists; owner assignment is unresolved. | **Keyboard trace comparison:** physical and synthetic key schedules across focus changes compare edge queries, repeats, text input, handler order, unhandled notifications, and defaults exactly. | Blocked |
| Pointer enter, exit, move, press, double and triple press, release, mismatch, wheel, cursor, explicit position, and capture obey hit-test and initiating-button rules. | No pointer routing exists; owner assignment is unresolved. | **Pointer trace comparison:** coordinate and button schedules across clips, capture, deletion, popups, and modal changes compare targets, event order, state queries, cursor CSS, and capture exactly. | Blocked |
| Tab, hotkey, default-button, directional, delegated, and popup focus navigation skip ineligible panels, remain in scope, and terminate cycles. | No focus-navigation implementation exists; owner assignment is unresolved. | **Focus graph model comparison:** generated acyclic and cyclic navigation graphs compare requested and committed focus, messages, default activation, popup movement, and no-focus cases. | Blocked |
| Popups, topmost popups, application-modal surfaces, inclusive and excluded modal subtrees, outside-click notification, and dismissal preserve one deterministic input and paint scope. | No popup or modal implementation exists; owner assignment is unresolved. | **Modal/popup schedule comparison:** nested popup and modal operations compare popup order, clips, focus, capture, hit targets, outside notifications, dismissal, and DOM placement exactly. | Blocked |
| Message maps, queued and delayed messages, typed commands, action signals, info requests, and child messages preserve ownership, declared parameter types, target order, and dead-target safety. | No message or command runtime exists; owner assignment is unresolved. | **Message-log comparison:** generated message maps and nested posts compare dispatch map, parameter values, queue selection, timing, target order, ownership disposal, and exact rejection. | Blocked |
| Injected clock, clipboard, user-config, cursor-lock, external-open, HTML, and audio seams return typed browser results without granting routing, persistence, network, or audio authority to VGUI. | No system seam or application adapters exist; owner assignment is unresolved. | **In-memory adapter conformance:** deterministic success, denial, cancellation, unavailable, and failure schedules compare requests, results, state, and absence of direct global browser calls. | Blocked |
| Browser resize and device-pixel-ratio changes atomically update viewport, proportional values, font and image inputs, layout, scissor, and repaint without changing CSS geometry from ratio alone. | No viewport adapter or browser surface exists; owner assignment is unresolved. | **Resize/DPR capture schedule:** fixed viewport and ratio transitions compare geometry, selected resources, metrics, DOM, repaint count, and screenshots at every revision. | Blocked |
| Every accepted control exposes deterministic role, name, description, state, value, relationships, focus order, hidden state, and disabled state in the browser accessibility tree. | No accessibility mapping or accepted browser matrix exists; owner assignment is unresolved. | **Accessibility snapshot comparison:** Playwright accessibility snapshots and keyboard-only procedures compare every accepted control state and relationship against fixed expectations. | Blocked |
| Explicit reduced-motion mode publishes animation endpoints at start while preserving sequence command timing, event order, state changes, and completion time. | No animation or reduced-motion implementation exists; owner assignment is unresolved. | **Dual-mode virtual-clock comparison:** source and reduced schedules compare command logs and terminal state exactly while comparing visual samples to the declared mode contract. | Blocked |
| Malformed resources, missing logical inputs, unknown controls or properties, unsupported adapters, dead handles, invalid references, and invalid input transitions return exact classifications without partial publication. | No diagnostic model exists; owner assignment is unresolved. | **Mutation and fault-injection corpus:** corrupt every typed field and dependency edge; compare classification, code, logical identity, panel, span, offending value, and unchanged pre-state exactly. | Blocked |
| Every parser-facing, panel, queue, animation, text, font, image, texture, DOM, observer, timer, and retained-byte limit is enforced before allocation or mutation. | Exact browser and content execution limits have not been selected; owner assignment is unresolved. | **Boundary and allocation vectors:** run each operation at limit minus one, limit, and limit plus one; compare result, state, queue depth, DOM count, and measured retained bytes. | Blocked |
| Resource replacement, runtime reset, root replacement, panel deletion, application unmount, cancellation, and failure release every VGUI-owned DOM and browser resource exactly once. | No lifecycle implementation exists; owner assignment is unresolved. | **Cleanup instrumentation:** MutationObserver, listener/observer/timer registries, texture/font counters, weak references, and repeated mount/destroy schedules require zero owned survivors and no late callback. | Blocked |
| TF2 HUD bindings consume immutable game or replay presentation state, set generic panel values, start generic sequences, and emit typed commands without moving game behavior into VGUI. | No accepted TF2 roadmap or HUD inventory supplies the required bindings; owner assignment is unresolved. | **Immutable-state adapter integration:** fixed TF2 snapshots and events compare panel updates and command emissions while mutation traps prove zero writes to gameplay or replay state. | Blocked |
| Preact mounts one framework-independent VGUI root while direct Three.js world rendering remains outside Preact and VGUI; no React, Radix, Framer Motion, jQuery, Bootstrap, or Zustand implementation becomes VGUI authority. | No VGUI package implementation or Preact application integration exists; owner assignment is unresolved. | **Dependency and browser integration audit:** package import graph, DOM ownership instrumentation, fixed user flows, and repository search prove one VGUI DOM owner and one separate world renderer. | Blocked |
| All accepted inventories, format producers, Content, TF2, web applications, tools, and browser adapters use one current interface with no duplicate UI runtime, resource semantics, fallback, compatibility layer, or stale artifact. | Inventories are Candidate, required adjacent roadmaps and generators are absent, and owner assignment is unresolved. | **Clean-workspace integration audit:** regenerate all inventories, execute every declared producer and consumer, compare retained evidence, and search for duplicate factories, loaders, animation runtimes, input routers, and VGUI DOM owners. | Blocked |

## Generated Inventories

No generated inventory is accepted. Candidate item count: 300. Generated item count: 0. Accepted item count: 0.

| Output | Authority identity | Authority revision | Generator command | Candidate items | Generated items | Accepted items |
|---|---|---|---|---:|---:|---:|
| [`inventories/controls.md`](inventories/controls.md) | SDK generic build factories and public control contracts; declared-game `ControlName` occurrences | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003`; review Missing | Missing | 50 | 0 | 0 |
| [`inventories/resource-properties.md`](inventories/resource-properties.md) | SDK generic resource consumers; parsed declared-game VGUI resources | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003`; review Missing | Missing | 58 | 0 | 0 |
| [`inventories/scheme-properties.md`](inventories/scheme-properties.md) | SDK scheme, surface, border, image, and control lookups; parsed declared-game schemes | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003`; review Missing | Missing | 56 | 0 | 0 |
| [`inventories/animation-commands.md`](inventories/animation-commands.md) | SDK animation declarations; parsed declared-game animation manifests and scripts | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003`; review Missing | Missing | 66 | 0 | 0 |
| [`inventories/input-and-surface.md`](inventories/input-and-surface.md) | SDK panel, input, surface, system, focus, message, image, and border interfaces; browser DOM contract | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; review Missing | Missing | 70 | 0 | 0 |

The future generator is owned by `tools/playsrc`. It must consume retained authority snapshots, the accepted root Owner Registry, accepted format inventories, exact declared-content archive indexes, accepted game-owned control and HUD inventories, and a checked VGUI contract manifest. It emits all five files in stable-ID order, retains every coverage classification and owner conflict, and fails on an omitted identity, duplicate identity, missing owner, stale authority, malformed input, or item-count mismatch. No command name is declared before implementation exists.

## Exit Criteria

VGUI is Complete only when all of these predicates pass:

- All 40 behavior rows are Ready.
- All five inventories are generated, current, Accepted, contain exactly the reviewed 300-item set unless the same checkpoint changes the denominator, and record every metadata and review field required by the roadmap contract.
- The root Owner Registry, KeyValues roadmap, format-universe roadmap, TF2 roadmap, application roadmaps, and this roadmap assign every generic resource, control, HUD, and product behavior to one owner.
- Every accepted TF2 `.res`, scheme, localization, font, border, image, texture reference, HUD-animation manifest, script, sequence, command occurrence, and custom control identity has one handled result or exact required error under one owner.
- Synthetic vectors, fixed target captures, virtual-clock traces, DOM snapshots, accessibility snapshots, browser screenshots, malformed-input records, limit measurements, and cleanup instrumentation satisfy every declared comparison predicate.
- The current Preact applications mount the framework-independent package; every game HUD binding consumes immutable presentation state and emits typed commands; direct Three.js world rendering stays outside VGUI and Preact.
- Every required producer and consumer uses the current interface; no duplicate VGUI resource loader, control factory, animation runtime, input router, DOM owner, fallback, compatibility layer, legacy path, or stale generated artifact remains.
- No required item remains Blocked, Unsupported, Unknown, Missing, or Partial.

## Blockers

- **Owner Registry conflict:** the root Owner Registry declares three presentation leaves and does not register `packages/presentation/vgui`, while the KeyValues roadmap assigns VGUI control construction, layout, scheme, commands, and interaction to `apps/web/<product>`. The root registry, presentation package count and navigation, KeyValues exclusion, VGUI roadmap, and application exclusions must select one owner in the same checkpoint. Checked `ROADMAP.md`, `ARCHITECTURE.md`, `packages/presentation/README.md`, `packages/formats/keyvalues/ROADMAP.md`, and the assigned VGUI paths. This blocks every behavior row.
- **Format owners:** the format-universe candidate proposes localization, UI-script, and font owners, but those paths are absent from the root Owner Registry and expose no accepted interface. KeyValues is Not accepted and unimplemented. VGUI resource, localization, font, and animation integration cannot become Ready until those owners supply current typed outputs. Checked `packages/formats/{README.md,ROADMAP.md,inventories/formats.md}`, the KeyValues roadmap, and the root registry.
- **TF2 HUD owner inventory:** no accepted TF2 roadmap or HUD inventory supplies game-owned bindings. Parsed resources from the configured build contain 167 ASCII-insensitive non-factory `ControlName` identities without one accepted package, game, or application classification; the TF2 inventory must classify its subset's semantics, state inputs, commands, resource identities, and evidence. Checked the TF2 README, current SDK TF2 HUD and VGUI call sites, configured archive index, and root registry.
- **Configured content dependencies:** `scripts/hudanimations_manifest.txt` names both `scripts/hudanimations.txt` and `scripts/hudanimations_tf.txt`, but exact lookup found only the TF2-specific script. `resource/sourcescheme.res` composes `SourceSchemeBase.res`, but exact lookup found no `resource/sourceschemebase.res`. Checked the configured TF2 loose root and `tf2_misc_dir.vpk` index for build `10822003`. The content provider plan required to resolve those shared logical identities is Missing.
- **Inventory generators:** no checked-in command emits any assigned inventory or declared-content occurrence registry. Checked `tools/`, root package manifests, and the current VGUI path. All 300 items remain Candidate and contribute 0 denominator items.
- **Numeric execution contract:** no accepted values exist for the required resource, panel, hierarchy, registry, text, animation, queue, input, DOM, browser-resource, diagnostic, or retained-byte limits. Checked the public SDK constants, configured content maxima, current application READMEs, and existing limits contracts; none selects VGUI browser budgets.
- **Retained evidence:** no retained evidence set contains target panel, layout, clipping, paint, control, scheme, localization, text, animation, input, focus, modal, surface, resize, accessibility, reduced-motion, malformed-input, limit, cleanup, and TF2 integration observations for one declared content build and browser matrix. Checked current public fixture, capture, test, and evidence paths.
- **Application integration:** the TF2 and Tempus web roadmaps are absent and current application READMEs do not declare the required Preact mount, direct-DOM ownership, typed command sink, immutable HUD binding, accessibility configuration, reduced-motion selection, or VGUI lifecycle. This prevents the browser and final integration rows from becoming Ready.
