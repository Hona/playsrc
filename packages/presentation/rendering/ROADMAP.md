# Rendering Roadmap

[`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines rendering, presentation state, render frame, visual parity, delivery status, coverage classification, inventory, and Complete.

## Completion Denominator

This leaf roadmap contains exactly 54 behavior rows with stable identities `REN-001` through `REN-054`. [`inventories/presentation-families.md`](inventories/presentation-families.md) indexes the same 54 identities and contributes no additional denominator identity. It is Candidate because no checked-in generator or denominator review exists. The current completion denominator is therefore exactly 54 items.

The target is one WebGPU rendering module for canonical Source 1 world data and presentation inputs from Team Fortress 2, Counter-Strike: Source, and legacy Source 1 Counter-Strike: Global Offensive. It owns browser GPU lifetime, immutable scene residency, presentation-only interpolation and model posing, view construction, draw preparation, pass ordering, pixels, submission, and frame-pacing records. WebGL, native graphics backends, editable scene formats, product UI, and authoritative gameplay or replay advancement are excluded.

The denominator is Not accepted. The inventory generator, accepted producer interfaces, declared browser/GPU evidence matrix, retained aligned captures, reviewer identity, review date, reviewed commit, and passing review record are Missing.

## Inputs

- One `RendererCreateRequest` containing an `HTMLCanvasElement` or `OffscreenCanvas` with no conflicting context mode; required adapter power preference; required WebGPU features and limits; canvas format, `srgb` color space, opaque or premultiplied alpha mode, and tone-mapping mode; `ldr` or `hdr` lighting profile; depth format; multisample count; and one complete `RenderingLimits` value. No field is inferred from game content, another renderer, or a failed device request.
- `RenderingLimits` contains non-negative finite integers for `maxCpuBytes`, `maxGpuBytes`, `maxSceneObjects`, `maxBuffers`, `maxBufferBytes`, `maxTextures`, `maxTextureBytes`, `maxTextureDimension2D`, `maxSamplers`, `maxShaderModules`, `maxPipelines`, `maxBindGroups`, `maxUploadBytesPerFrame`, `maxUploadOperationsPerFrame`, `maxPendingUploads`, `maxScenes`, `maxViewsPerFrame`, `maxPassesPerView`, `maxDrawItemsPerView`, `maxDrawsPerPass`, `maxTransparentItemsPerView`, `maxLightsPerView`, `maxBonesPerPose`, `maxParticleVerticesPerFrame`, `maxDiagnostics`, and `maxFramesInFlight`. Every operation uses the lower of the caller limit and the acquired device limit; no unbounded mode exists.
- One immutable `CanonicalMap` and map-root closure from `packages/world/map`, including Source-space geometry, displacement, brush-model, prop, decal/overlay, lightmap, cubemap, sky, fog, water, rope, and visibility references. Rendering never opens BSP bytes or assembles a second map representation.
- Immutable `ResolvedMaterial` and evaluated material state from `packages/world/material`; decoded texture planes and metadata from `packages/formats/vtf`; and immutable content-addressed bytes from the configured asset consumer. Rendering receives exact object hashes, byte lengths, dimensions, formats, mip/frame/face/slice identities, color interpretation, and sampling roles. It never resolves logical paths or interprets VMT syntax.
- Immutable `StudioModelDocument` values from `packages/formats/studio-model`, plus game-, Simulation-, or Replay-owned model occurrence state containing model identity, transform, selected skin, selected bodygroup values, selected sequence and cycle, playback parameters, pose parameters, flex inputs, attachment state, and discontinuity markers. Rendering does not select gameplay activities, items, weapons, models, sequences, skins, or bodygroups.
- One immutable visibility result per view from `packages/world/visibility`, containing ordered leaves, world-surface and static-, dynamic-, and detail-prop candidates, area windows, sky classification, map-occlusion decisions, bounds, model origins, and radii. Rendering owns frustum tests, distance/fade policy, LOD choice, draw grouping, GPU occlusion, and final draw decisions.
- Runtime-neutral Entity, game, Simulation, or Replay presentation state for dynamic transforms, render modes, modulation, light styles, dynamic lights, fog/controllers, water state, decals, ropes, beams, sprites, glows, and viewmodels. The selected game owns game-specific presentation mappings. `packages/presentation/particle` supplies immutable particle render data after particle advancement.
- One `RenderFrameRequest` containing a strictly increasing frame ordinal; finite presentation timestamp and interval; immutable previous and current authoritative snapshots; exact interpolation fraction in `[0,1]`; discontinuity set; ordered presentation events; active scene identity; 1 through `maxViewsPerFrame` cameras; viewport size; visibility results at matching state revisions; particle render data; debug-view selection; and capture request. A request cannot advance gameplay or replay.
- One optional `FramePacingRequest` containing a caller-selected foreground/background policy, maximum queued frame opportunities, and a browser animation-frame callback. It schedules presentation only; elapsed wall time never advances gameplay or replay inside Rendering.
- W3C WebGPU Candidate Recommendation Draft `https://www.w3.org/TR/2026/CRD-webgpu-20260714/`: initialization, device loss, resources, limits, bindings, pipelines, copies, command encoding, queues, canvas configuration/sizing/color, errors, and debugging.
- Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`: `src/public/{ivrenderview.h,iclientrenderable.h,istudiorender.h,view_shared.h,cdll_int.h}`, `src/public/engine/{IClientLeafSystem.h,ivmodelrender.h}`, `src/public/materialsystem/{imaterialsystem.h,imaterial.h,imesh.h,itexture.h,materialsystem_config.h}`, and `src/game/client/{iviewrender.h,viewrender.h,viewrender.cpp,view.cpp,clientleafsystem.h,clientleafsystem.cpp,interpolatedvar.h,cdll_client_int.cpp,detailobjectsystem.h,detailobjectsystem.cpp,c_rope.h,c_rope.cpp,iviewrender_beams.h,view_beams.cpp,c_sprite.cpp,glow_overlay.h,glow_overlay.cpp,c_baseanimating.h,c_baseanimating.cpp}`.

## Outputs

- One `Renderer` interface with exactly `loadScene`, `render`, `resize`, `startFramePacing`, `stopFramePacing`, and `dispose`. Initialization is performed by `createRenderer`; device acquisition, caches, pass graph, encoders, queues, and recovery remain hidden implementation state.
- Lifecycle state exactly `Initializing`, `Ready`, `Recovering`, `Failed`, or `Disposed`; one monotonically increasing device generation; one optional active immutable scene generation; and a bounded queue of retiring generations. Only `Ready` accepts scene or frame work.
- A successful `loadScene` consumes one verified `MapRuntimeDescriptor` and returns an immutable scene identity, map-source identity, compiler/configuration identities, device generation, source/derived object closure, exact CPU/GPU resource ledger, upload ledger, handled or intentionally inert family classifications, and a frame-boundary activation token. Failure publishes no scene generation.
- A successful `render` writes the configured canvas image and returns a `FrameResult` containing frame and scene identities, interpolation inputs, ordered view/pass identities, candidate and draw counts, upload counts/bytes, submitted queue serial, resource totals, pacing record, capture identity when requested, and sorted diagnostics. Queue submission occurs exactly once after every encoder and pass for the frame has ended.
- `resize` returns the accepted physical width and height, viewport, device generation, and replacement attachment identities. Zero dimensions suspend submission without destroying immutable scene state. A dimension above the caller or device limit fails before canvas or attachment mutation.
- `dispose` stops pacing, rejects later work, unconfigures the canvas, destroys every explicitly destroyable owned GPU resource after submitted work is no longer using it, releases CPU scene data, and is idempotent. It never relies on garbage collection for predictable resource release.
- A structured error uses exactly `InvalidState`, `MalformedInput`, `MissingInput`, `IdentityMismatch`, `UnsupportedEnvironment`, `UnsupportedFeature`, `UnknownPresentation`, `BoundExceeded`, `GpuValidationFailed`, `OutOfMemory`, `DeviceLost`, or `CaptureFailed`. It records operation, renderer/device/scene/frame identity, family ID, offending value, required and available limit, GPU object label when available, and last successfully submitted queue serial.

## Invariants

- Rendering derives presentation state and GPU work only. It never mutates gameplay snapshots, Replay snapshots or cursors, Entity lifecycle, particle simulation state, canonical map/model/material values, or game-owned presentation mappings.
- Source positions remain Source XYZ and one-inch units in canonical inputs and immutable vertex buffers. View/projection matrices perform the Source-to-WebGPU clip transform exactly once. Instance, bone, attachment, light, fog, reflection, decal, and particle inputs use the same Source-space contract.
- A renderer owns exactly one canvas context and one live device generation. A GPU object belongs to exactly one device generation and is never reused after loss. Immutable CPU asset bytes survive recovery only while their scene lease remains live.
- GPU cache identity is `{deviceGeneration, objectSha256, semanticRole, descriptorSha256}`. Equal keys share one resource; unequal keys never alias. Scene activation and retirement change lease counts only at frame boundaries.
- Resource allocation, staging, upload, command encoding, draw-list insertion, diagnostics, and capture readback are charged before mutation. A bound failure emits no partial scene, pass, command buffer, or submitted frame.
- Scene loading resolves only the exact supplied object closure. Missing, hash-mismatched, malformed, unsupported, or unknown required input fails the scene. Rendering never substitutes placeholder geometry, texture, material, model, light, or effect.
- Device loss stops new encoding and submission, marks every GPU object unusable, drains no assumption about unfinished work, and enters `Recovering`. Recovery reacquires the same required profile and rebuilds leased scenes from immutable inputs. Capability mismatch or rebuild failure enters `Failed`; no reduced profile is selected.
- Every draw item has stable scene, view, family, occurrence, material, mesh, and source identities. Opaque batching may change command count but not depth, stencil, blend, modulation, material evaluation, or pixel results. Equal-key ordering uses stable identities, never host map order or asynchronous completion order.
- Visibility output is candidate input, not a draw command. Rendering may remove candidates only through the declared view frustum, area window, distance/fade, LOD, map-occlusion, GPU-occlusion, material, and debug policies. It cannot add a world candidate excluded by the matching visibility result.
- Opaque and alpha-tested draws establish depth before translucent interleaving. Translucent world surfaces, displacement surfaces, detail props, particles, ropes, beams, sprites, glows, and entities are interleaved back-to-front by visibility-leaf order and the family-specific stable tie rules. Ignore-depth draws execute only in their terminal declared phase.
- Each auxiliary view has its own camera, visibility result, render targets, fog, clip state, draw flags, and pass results. State pushed for shadow, reflection, refraction, 3D-sky, debug, or viewmodel work is restored before the next view.
- Presentation interpolation uses only the supplied snapshot pair, fraction, per-field policy, and discontinuities. It never extrapolates, smooths across create/delete/teleport/seek/reset, reads wall-clock state, or writes interpolated values back to authority state.
- One render frame performs immutable-input validation, interpolation and pose evaluation, view derivation, candidate pruning and sorting, bounded upload work, auxiliary passes, main opaque/depth work, main translucent work, viewmodels, color output, diagnostics/capture, command finalization, and one queue submission in that order.
- Color textures declare data or color interpretation. Lighting and blending execute in linear light; final output applies the selected LDR or HDR presentation and one declared transfer to the configured canvas color space. A texture is never decoded as sRGB merely because its bytes are RGBA8.
- Diagnostic and capture modes may replace presentation shaders or emit ID/depth/lightmap outputs, but cannot alter canonical state, resource identity, candidate enumeration, or subsequent ordinary frames.

## Ownership Exclusions

- BSP, VMT, VTF, StudioModel, particle-definition, and other Source-format byte parsing belongs to `packages/formats/*` and `packages/presentation/particle` where declared.
- Canonical geometry, occurrences, map sections, dependency closure, environment associations, and runtime descriptors belong to `packages/world/map`. Rendering owns only derived GPU buffers, atlases, draw items, and pixels. It accepts browser-WASM or native-precomputed data under the same derived identities and never requires GLB.
- Shader, parameter, proxy, texture-role, animation-frame, blend, depth, cull, lighting-model, water, sky, and decal material semantics belong to `packages/world/material`. Rendering owns GPU implementation of the supplied semantic state.
- PVS/PAS expansion, BSP and area traversal, portal windows, map occlusion, leaf membership, and ordered visibility candidates belong to `packages/world/visibility`.
- Entity lifecycle, hierarchy, I/O, controller transitions, rope state, and generic effect requests belong to `packages/world/entity`; gameplay and game-specific model/effect/sequence selection belong to `games/<game>`.
- Gameplay advancement and snapshots belong to Simulation and the selected game/ruleset. Recorded-state advancement, seeking, snapshot selection, and replay events belong to Replay. Rendering consumes immutable presentation inputs from either authority.
- Particle definitions, emitters, operators, state advancement, and runtime-neutral particle render data belong to `packages/presentation/particle`. Rendering owns only particle draw preparation, ordering, GPU resources, and pixels.
- Product routes, controls, focus, overlays, accessibility, browser support policy, and product-specific camera selection belong to applications. Rendering owns camera mathematics and submitted views after selection.
- Asset retrieval, object storage, publication, CDN behavior, and browser HTTP caching belong to Content, Asset Store, services, and applications. Rendering consumes supplied immutable bytes and owns their GPU residency only.
- Inventory and capture command orchestration belongs to `tools/playsrc` and `tools/inspector`.

## Behavior Families

The active migration checkpoints implement a browser-target Three.js/WebGPU owner for versioned direct `PSMP` payloads: bounded structural decoding, material-batched model-0 geometry, exact resolved world base-texture RGBA uploads, a deterministic 4,096×462 float LDR lightmap atlas, seven StudioModel geometry assets, 33 prop occurrences, exact rocket/sticky model requests, supplied translucency/alpha-test/cull features, Source-space Z-up perspective cameras, bounded resize, hash-checked atomic scene replacement, projectile/effect draw inputs, and deterministic disposal. A headed Chrome WebGPU workflow renders all 2,761 drawable model-0 surfaces in 10 batches with 12 exact base textures, first-style lightmaps, and prop/projectile geometry through cold/warm loads and replacement. Model textures/poses, viewmodels, water shaders, directional/style lightmaps, and target filtering remain explicit diagnostics rather than silent substitutes.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| `REN-001` — Renderer initialization validates the complete request, acquires one WebGPU interface, labels every GPU object, creates no scene resource, and publishes `Ready` or one atomic structured failure. | `createRenderer` requires `navigator.gpu` and one HTML canvas, initializes one Three.js `WebGPURenderer`, and exposes no device. Complete requested feature/limit profiles, labels, structured GPU failures, and recovery remain absent. | **Initialization state matrix:** compare lifecycle, requested/acquired features and limits, object labels, allocations, and exact errors for valid, missing-WebGPU, rejected-adapter, rejected-device, malformed-config, and limit-mismatch cases. | Partial |
| `REN-002` — Canvas and device lifecycle binds one `webgpu` context, configures the declared format/color/alpha/tone profile, owns one device generation, exposes no raw mutable device state, and unconfigures/destroys it exactly once. | No canvas or device owner exists. | **WebGPU lifecycle trace:** instrument context/configure/unconfigure, device creation/destruction, queue calls, and repeated disposal; compare call order and state with the W3C contract exactly. | Not started |
| `REN-003` — Device loss stops frame submission, invalidates every generation-owned object, reacquires the identical required profile, rebuilds leased scenes from immutable hashes, and resumes only after an explicit discontinuity; failed reacquisition is terminal. | No device-loss or recovery state machine exists. | **Forced-loss recovery test:** destroy or revoke the device before load, during upload, between passes, and after submit; compare generations, destroyed resources, zero stale-handle use, rebuilt hashes, discontinuity, and terminal mismatch behavior. | Not started |
| `REN-004` — Immutable scene loading validates one complete map-root closure and all material/model/texture dependencies before activation; cancellation or failure releases work and leaves the prior active scene unchanged. | Scene loading independently hashes and structurally validates the complete direct runtime payload, builds a staged Three.js group, and replaces prior world resources only after success. Map-root closure, cancellation, resolved material/model/texture inputs, activation tokens, and resource ledgers remain absent; diagnostic mode explicitly reports all missing materials. | **Atomic scene-load transcript:** fixed complete, missing, hash-mismatched, cancelled, reordered, and over-bound graphs compare exact requests, ledgers, activation token, prior-scene identity, and zero partial publication. | Partial |
| `REN-005` — CPU and GPU resources have one owner ledger, hash/role/descriptor deduplication, scene leases, frame-boundary activation/retirement, queue-completion-safe destruction, and idempotent disposal. | No resource ledger or scene-generation owner exists. | **Resource-lifetime ledger:** load shared/disjoint scenes, swap repeatedly, lose the device, and dispose; compare each create/share/lease/retire/destroy transition and require zero leak, double destroy, or cross-generation reuse. | Not started |
| `REN-006` — Every allocation and upload obeys caller and WebGPU limits, per-frame byte/operation budgets, pending-upload backpressure, alignment, row pitch, and texture-subresource bounds before GPU mutation. | No bounded uploader or backpressure mechanism exists. | **Upload boundary matrix:** exercise each limit at `limit-1`, `limit`, and `limit+1`, alignment edges, cancellation, and queue saturation; compare writes, retained bytes, exact errors, and unchanged GPU ledgers. | Not started |
| `REN-007` — World faces preserve source identity, topology, winding, normals, tangents, UVs, lightmap UVs, material occurrence, model ownership, and non-draw classification while producing bounded immutable draw buffers. | The runtime decoder validates every finite position/normal/UV/lightmap-UV scalar and triangle index, retains face IDs, excludes non-draw and non-world-model surfaces, and batches positions/normals/UVs/indexes by exact material index without GLB. Tangents, retained lightmap UV output, complete source occurrence metadata, and aligned pixel evidence remain absent. | **World-geometry differential:** compare indexed vertices, triangles, attributes, source IDs, bounds, draw disposition, and fixed-camera color/depth/ID captures for declared maps and malformed-reference vectors. | Partial |
| `REN-008` — Displacements use the canonical tessellation, allowed-vertex and triangle-tag result, preserve parent/material/lightmap identities, cull by supplied bounds, and participate in opaque, translucent, decal, overlay, shadow, and water phases as classified. | Map and BSP displacement outputs are Not accepted; no renderer exists. | **Displacement topology and capture comparison:** exact vertex/index/attribute comparison plus aligned wireframe, normal, lightmap, shadow, and color captures for each accepted power and tag pattern. | Blocked |
| `REN-009` — World model zero and every `*N` brush model render from one geometry authority with current entity transform, material/proxy binding, opaque/translucent split, lightmap updates, decals, shadows, and visibility policy. | Map and Entity expose no accepted brush-model runtime implementation. | **Brush-model state trace:** move, rotate, toggle, parent, fade, relight, decal, and cross-water fixed brush models; compare draw membership, matrices, phase, lightmap identity, and aligned captures. | Blocked |
| `REN-010` — Static props preserve occurrence transform, skin, fade, lighting origin or baked vertex-lighting input, leaf candidates, selected LOD, decals, shadows, and stable instancing without changing canonical prop identity. | Map and StudioModel prop contracts are Not accepted and expose no implementation. | **Static-prop matrix:** compare occurrence fields, culling/fade/LOD decisions, instance grouping, baked and sampled lighting, decal/shadow membership, and color/depth/ID captures for fixed prop sets. | Blocked |
| `REN-011` — Dynamic props consume current authoritative transforms and presentation selections, relink through Visibility revisions, render opaque/two-pass/translucent state, and never advance physics or gameplay. | Entity, Physics, game, Visibility, and StudioModel integrations are unavailable. | **Dynamic-prop revision trace:** fixed add/move/parent/skin/body/LOD/fade/delete snapshots compare visibility revision, interpolation, draw grouping, matrices, and aligned captures with zero authority mutation. | Blocked |
| `REN-012` — Studio models assemble accepted geometry, skeleton, materials, skin table, bodyparts, LODs, flexes, decals, attachments, and lighting into immutable GPU resources traceable to source identities. | StudioModel and Material denominators are Not accepted and no current outputs exist. | **Studio draw-input differential:** compare every selected mesh/primitive/material/vertex/source identity and fixed posed color/depth/normal/ID captures against declared model vectors. | Blocked |
| `REN-013` — Bone evaluation produces one bounded local and world matrix per required bone from supplied pose state, hierarchy, procedural policy, IK/flex inputs, and discontinuities, then skins vertices without changing model or authority state. | StudioModel has no implementation and game-owned pose inputs are not declared. | **Bone/skin matrix comparison:** exact or declared-ULP matrix and skinned-vertex vectors across bind pose, sequence pose, blends, attachments, procedural bones, limits, and discontinuities. | Blocked |
| `REN-014` — Animation samples caller-selected sequences at supplied presentation time, applies looping, blending, layers, pose parameters, flex rules, and transition state in declared order, and emits presentation-only pose and events. | StudioModel and game presentation-selection interfaces are unavailable. | **Animation timeline comparison:** fixed sequence/cycle/layer/pose/flex timelines compare every bone, flex weight, attachment, event crossing, loop boundary, and seek discontinuity at integer and fractional frames. | Blocked |
| `REN-015` — Skin selection maps the supplied skin family and material slot through StudioModel and Material outputs exactly; out-of-range or unresolved selection fails rather than choosing family zero. | StudioModel and Material current outputs do not exist. | **Skin-family matrix:** compare every accepted family/slot/material identity and fixed model capture; mutate family, slot, dependency hash, and material classification to require exact failure. | Blocked |
| `REN-016` — Bodygroup values select exactly one submodel per bodypart from supplied game/replay state; absent, empty, and out-of-range values retain their distinct declared dispositions. | StudioModel and game presentation mappings are unavailable. | **Bodygroup cross-product:** enumerate every accepted bodypart value and combination; compare selected geometry IDs, bounds, attachments, draw count, and color/depth/ID captures exactly. | Blocked |
| `REN-017` — LOD selection uses supplied model origin/radius, camera projection, accepted switch values, root restrictions, shadow-LOD rule, and stable hysteresis policy; it never rewrites model data or silently selects missing geometry. | StudioModel has no current LOD output or renderer integration. | **LOD threshold sweep:** camera paths sample each threshold below, at, and above the boundary; compare selected LOD, metric, triangle/material IDs, hysteresis, shadow choice, and captures. | Blocked |
| `REN-018` — Material binding consumes one evaluated Material state and creates the exact pipeline, bind groups, uniforms, blend/depth/cull/fog state, shader feature inputs, and proxy data required for the draw without reinterpreting VMT semantics. | Runtime materials consume Material-owned shader/feature outputs and apply supplied translucency, alpha-test, and no-cull state without parsing VMT. LightmappedGeneric currently uses Three.js unlit texture presentation; lighting, water, blend/additive, proxies, depth, fog, and shader-specific pipelines remain absent. | Exact `jump_beef` VMT composition resolves 14 material outputs; the browser consumes 10 drawable batches and reports unsupported water/lightmap semantics. Complete state and pixel probes remain absent. | Partial |
| `REN-019` — Texture binding uploads every declared mip/frame/face/slice with exact dimensions, format, color/normal/data interpretation, sampling and addressing state, then binds the selected evaluated frame without guessed or placeholder textures. | The WASM compiler decodes selected frame 0/face Right/slice 0/mip 0 base textures through VTF into exact RGBA dimensions/bytes. Rendering uploads 12 sRGB repeat-wrapped DataTextures and reports missing texture inputs. Remaining mips/frames/faces, normal/data roles, sampler policy, animation, and GPU byte-ledger evidence remain absent. | The source bundle retains all 34 selected raw VMT/VTF records; exact-map browser captures visibly sample the 12 decoded base textures. Complete subresource probes remain absent. | Partial |
| `REN-020` — Lightmaps bind each face to its profile-specific flat or directional atlas region, sample texel centers with declared filtering, combine normal or SSBump directions, and keep white/unlit dispositions distinct. | Rendering packs every drawable face's selected LDR first-style RGBExp32 samples into a bounded deterministic float atlas, maps Source texel coordinates to centers, binds `uv1`, and gives unlit faces a white sample. It uses nearest filtering and does not combine directional/SSBump sets or animated styles. | Exact `jump_beef` compilation emits a 4,096×462 atlas; structural range checks and the headed browser workflow pass. Probe/aligned captures, seams, directional arithmetic, style timelines, and accepted filtering remain absent. | Partial |
| `REN-021` — LDR presentation decodes selected compiled samples, applies styles and dynamic contribution in linear light, performs the accepted LDR reconstruction and output transfer once, and never consumes HDR members. | Selected-profile map outputs and retained LDR target captures are unavailable. | **LDR sample/capture suite:** exact CPU/WGSL sample vectors plus aligned lightmap-only and final-color captures with a retained per-channel difference histogram and accepted tolerance. | Blocked |
| `REN-022` — HDR presentation consumes only the complete selected HDR profile, retains radiance above one through lighting/blending, applies accepted exposure and tone mapping once, and never substitutes LDR members. | Selected-profile map outputs, an accepted color profile, and retained HDR target captures are unavailable. | **HDR radiance/tone suite:** compare CPU/WGSL radiance probes, exposure trajectory, saturation/overflow cases, and aligned pre-tone/final captures under one fixed HDR evidence profile. | Blocked |
| `REN-023` — Every active face/world/model light style samples its ordered pattern at supplied presentation time and scales all associated baked or dynamic contributions before draw; style zero and terminator handling remain explicit. | Entity/Map light-style state and accepted rendering inputs are unavailable. | **Light-style timeline differential:** fixed patterns, frame boundaries, toggles, custom styles, multiple face styles, seeks, and malformed IDs compare scalar, affected lightmap/model pixels, and ordered diagnostics. | Blocked |
| `REN-024` — Dynamic point and spot lights consume explicit state, style, color/exponent, radius, attenuation, cone, flags, lifetime, and visibility; bounded deterministic selection and contribution are shared across world, displacement, brush, and model draws. | Entity/game dynamic-light state and a current visibility integration are unavailable. | **Dynamic-light truth table:** exact attenuation/cone/style/selection vectors plus fixed camera captures for overlap, occlusion, expiry, moving lights, world/model masks, and budget boundaries. | Blocked |
| `REN-025` — Simple projected shadows, render-to-texture shadows, and shadow-depth projected lights use declared caster/receiver eligibility, bounds, clip planes, depth bias, opacity/falloff, model pose, and per-view lifetime without leaking pass state. | Shadow producer state, model/map implementations, and retained target captures are unavailable. | **Shadow pass and receiver matrix:** compare caster/receiver sets, matrices, depth textures, bias probes, model/world/displacement pixels, resource retirement, and state restoration for each accepted shadow type. | Blocked |
| `REN-026` — Cubemap binding selects the exact map sample and LDR/HDR six-face texture from supplied occurrence state, samples the declared reflection vector and mip, and reports Missing instead of using sky or scene environment. | Map/Material cubemap outputs and exact declared resources are unavailable. | **Cubemap selection/capture grid:** probe positions around sample boundaries, face axes, seams, mips, profiles, and reflective materials; compare selected identity and linear sample values plus aligned captures. | Blocked |
| `REN-027` — Planar reflection views reflect camera position and basis across the declared plane, use independent visibility/fog/clip/targets, include exactly eligible world/entities/sky, and restore main-view state before composition. | Reflection material state and current Map/Visibility integrations are unavailable. | **Reflection-view matrix and capture comparison:** exact reflected matrices, candidate sets, clip results, target identities, pass order, and aligned water/glass captures above, below, and crossing the plane. | Blocked |
| `REN-028` — The 2D sky draws the six selected face materials around the camera with translation removed, declared face orientation and seam handling, visibility gating, depth behavior, and no environment-light substitution. | Map and Material sky packages and retained captures are unavailable. | **Sky face/seam capture:** axis and corner cameras compare face identity, UV orientation, seam samples, depth, camera-translation invariance, profile selection, and Missing behavior. | Blocked |
| `REN-029` — The 3D sky renders as a separate scaled/transformed view with sky-camera area, origin, scale, visibility, fog, lights, entities, and depth clear/composition before the main world; 2D sky remains a separate pass. | Map, Entity, Visibility, and game sky-camera outputs are unavailable. | **3D-sky transform/pass differential:** exact camera/scale matrices, area candidates, fog and light state, pass order, depth reset, and aligned captures for 2D-only, 3D-only, both, and not-visible cases. | Blocked |
| `REN-030` — Fog consumes current main/sky/water presentation state, samples supplied controller transitions at presentation time, and computes linear or height mode, directional color blend, distance/density, and per-view restoration in linear light. | Entity fog-controller state and accepted Map water/sky associations are unavailable. | **Fog probe/capture matrix:** exact distance and directional color vectors at transition boundaries plus aligned main, 3D-sky, above-water, underwater, reflection, and refraction captures. | Blocked |
| `REN-031` — Water classifies the eye and visible surface, selects declared cheap, reflection, refraction, intersection, above/below, fog, normal/flow, Fresnel, blur, and overlay state, renders bounded auxiliary targets, and composes the surface once. | Map, Material, Entity, Visibility, and water resource outputs are unavailable. | **Water pass/camera matrix:** compare classification, pass graph, clip planes, target dimensions, shader probes, and aligned captures above, below, at the plane, opaque, translucent, cheap, reflective, and refractive. | Blocked |
| `REN-032` — World, displacement, brush-model, static-prop, and studio-model decals consume supplied receiver/projection geometry and material state, preserve stable identity and lifetime/fade, and draw in declared first/second and opaque/translucent receiver order. | Map/Entity decal geometry and Material/StudioModel integrations are unavailable. | **Decal projection/order differential:** exact clipped vertices/UV/lightmap IDs, receiver membership, lifetime/fade timeline, pass order, and aligned captures for every accepted receiver class. | Blocked |
| `REN-033` — Compiled and water overlays consume canonical fragments, basis, material, lightmap, render order, fade, activation, and receiver identity; fragments draw once in receiver order without runtime reprojection. | Map overlay fragments and Material integration are unavailable. | **Overlay fragment/order comparison:** exact vertices, UVs, alpha, material/lightmap keys, activation/fade, draw-once count, render order, and aligned world/displacement/water captures. | Blocked |
| `REN-034` — Detail models and sprites consume compiled occurrence, leaf candidate, orientation, animation, sway, lighting/style, distance/fade, and material/model state; translucent detail work interleaves with world and entities in the same leaf. | Map, StudioModel, Material, and Visibility detail-prop outputs are unavailable. | **Detail-prop occurrence and leaf sweep:** compare model/sprite IDs, orientation, fade/style/lighting values, interleaving order, and aligned captures at every distance and leaf boundary. | Blocked |
| `REN-035` — Rope draw preparation consumes Entity-owned segment state, endpoint transforms, subdivision, width, material/back material, scroll, color, and per-node light; it batches opaque/translucent work without advancing rope state. | No accepted rope-state producer or rendering input exists. | **Rope render-input differential:** fixed straight, sagged, subdivided, moving-endpoint, translucent-back, lit, and clipped ropes compare generated vertices/UV/color, phase, batch bounds, and captures. | Blocked |
| `REN-036` — Beam drawing consumes game/Entity-owned point/entity/attachment, ring, spline, follow, halo, frame, width, amplitude, color, brightness, scroll, segment, and lifetime state; it never creates or advances beam truth. | Game and Entity beam mappings are unavailable. | **Beam-family vector and capture matrix:** compare generated centerlines/rings/trails, billboard widths, UV/frame/halo/color, ordering, clipping, and expiration dispositions for every accepted beam family. | Blocked |
| `REN-037` — Sprites use the selected frame/material, declared orientation, world or screen scale, render mode/effect, color/alpha/HDR scale, visibility result, and camera basis to emit bounded quads with exact depth/blend policy. | Sprite state mappings, model resources, and Material outputs are unavailable. | **Sprite orientation/blend grid:** axis camera vectors, every accepted orientation/render mode, frame, scale, occlusion, depth, and color case compare quad vertices, bindings, and aligned captures. | Blocked |
| `REN-038` — Glows and glow overlays consume explicit source position/direction, layered sprite state, HDR scale, proxy radius, sky requirement, and visibility/occlusion samples; cached visibility is view- and frame-identified. | Glow mappings and accepted visibility-query inputs are unavailable. | **Glow visibility/layer comparison:** fixed obstruction, sky/non-sky, edge-of-screen, distance, multi-view, cache, HDR, and layer cases compare query identity, opacity/size/color, order, and captures. | Blocked |
| `REN-039` — Particle render data is accepted only after Particle advancement, retains system/particle/material/order identities, and produces bounded camera-facing, mesh, ribbon, trail, or beam draw items without selecting effects or mutating particles. | Particle has no roadmap, implementation, or accepted render-data interface. | **Particle-consumer contract test:** sentinel immutable packets for every accepted render primitive compare unchanged inputs, generated vertices/instances, material bindings, sort keys, limits, and fixed captures. | Blocked |
| `REN-040` — Viewmodels use a separate supplied camera FOV/near/far and depth range, accepted model/skin/bodygroup/pose/attachment state, handedness, and draw eligibility; opaque then translucent viewmodel work executes after world composition without changing authority state. | TF2 game and StudioModel viewmodel mappings and resources are unavailable. | **Viewmodel projection/pose timeline:** exact projection/depth vectors and aligned captures for each accepted class/item/team/skin/bodygroup/sequence, fire/reload/draw boundary, handedness, and occlusion case. | Blocked |
| `REN-041` — Transparent ordering interleaves world surfaces, displacement surfaces, detail props, entities, particles, ropes, beams, sprites, glows, and framebuffer-reading materials back-to-front by ordered visibility leaves and stable family tie keys; ignore-depth work is last. | No transparent draw-list builder exists. | **Transparent overlap permutation:** permute producer and completion order for fixed overlapping layers; compare draw-key transcript and color/depth/ID captures exactly for leaf ties, two-pass items, and framebuffer reads. | Not started |
| `REN-042` — Depth and stencil state covers clear/load/store, reversed or ordinary comparison as configured, opaque/alpha-test writes, translucent read-only behavior, viewmodel range, polygon/decal/shadow bias, clip planes, and state restoration. | No pass, depth, or stencil implementation exists. | **Depth/stencil truth table:** synthetic overlapping primitives compare pass descriptors, depth/stencil values, draw outcomes, bias boundaries, clears, nested views, and final pixel/depth captures. | Not started |
| `REN-043` — Draw culling applies declared front/back/two-sided policy, view-frustum and area windows, distance/fade, LOD, map-occlusion, optional GPU-occlusion, and zero-alpha rules in fixed order while preserving stable diagnostics. | No draw-culling implementation exists. | **Culling decision trace:** one candidate per decision boundary and all pairwise conflicts compare ordered tests, decision/reason, untouched inputs, candidate counts, and captures; cached and uncached results must agree. | Not started |
| `REN-044` — Visibility input identity, camera, state revision, ordered candidates, area windows, sky state, and occlusion decisions must match the frame/view; Rendering never decodes PVS or traverses BSP to replace a missing result. | Visibility has no current implementation and its integration row is Blocked. | **Visibility seam contract:** sentinel results and mismatched map/view/revision mutations compare accepted candidate order and exact rejection; repository audit requires zero PVS decode or BSP visibility traversal in Rendering. | Blocked |
| `REN-045` — Perspective, orthographic, off-center, and explicitly supplied projection cameras validate finite viewport/FOV/planes/origin/angles, derive Source-consistent view/projection/frustum/pixel matrices, and keep world and viewmodel cameras distinct. | The current perspective camera validates finite Source position/yaw/pitch/vertical-FOV/near/far values, retains Source XYZ with Z-up, and derives one Three.js look direction. Orthographic, off-center, viewmodel, matrix-vector, frustum, and pixel evidence remain absent. | **Camera matrix vectors:** compare all matrix components and projected points for axis, aspect, FOV, near/far, off-center, orthographic, resize, and invalid-boundary cases with declared ULP tolerance. | Partial |
| `REN-046` — Per-view state has one stable identity and isolated camera, viewport, targets, clear flags, visibility, fog, clip, draw families, framebuffer-copy bindings, and debug selection; nested push/pop semantics restore the prior state exactly. | No view-state stack or immutable view plan exists. | **Nested-view state trace:** main, 3D sky, reflection, refraction, shadow, capture, and viewmodel permutations compare every entered/restored state field, target identity, clear, and leaked-state sentinel. | Not started |
| `REN-047` — Presentation interpolation samples supplied scalar/vector/angular/transform/pose fields by declared policy and fraction, snaps discrete fields, rejects extrapolation, and resets on every supplied discontinuity without wall-clock smoothing. | No presentation-state interpolator exists. | **Interpolation policy vectors:** exact endpoints, fractions, angle wraps, quaternion paths, teleports, create/delete, pause, seek, reset, missing neighbor, and out-of-range fractions compare values and unchanged authority bytes. | Not started |
| `REN-048` — Frame phases execute the single declared order from input validation through one queue submission, and each phase consumes only frozen prior-phase output; exceptions abort before submission and release transient encoders/resources. | No frame graph or submission implementation exists. | **Phase-order/fault injection:** record every phase for ordinary and auxiliary-view frames, fail each boundary, permute async preparation, and compare transcript, submitted command count, transient ledger, and last good frame. | Not started |
| `REN-049` — Frame pacing schedules at most the configured queued opportunities, coalesces browser callbacks while a frame is in flight, records requested/start/submit/present times, suspends when hidden or zero-sized as configured, and never advances authority state. | No frame scheduler, backpressure, or pacing record exists. | **Synthetic-clock pacing trace:** fixed callback timelines, long frames, visibility/size changes, start/stop, and queue saturation compare accepted/suppressed opportunities, intervals, submissions, and zero authority calls. | Not started |
| `REN-050` — Resize converts declared CSS size and device-pixel ratio to one bounded physical size, replaces canvas/depth/color/auxiliary attachments atomically before the next frame, updates all camera aspects, and preserves immutable scenes. | Resize validates finite non-negative CSS dimensions and positive DPR against an 8,192-pixel physical-axis bound, updates Three.js size/pixel ratio and perspective aspect, and preserves the world group. Attachment-generation, rapid-race, zero-submit, and device-limit evidence remain absent. | **Resize sequence comparison:** zero, ordinary, fractional-DPR, rapid, aspect-changing, maximum, and over-limit sizes compare canvas/attachment generations, viewport/camera matrices, clears, submissions, and resource retirement. | Partial |
| `REN-051` — Color output distinguishes data, linear, and sRGB textures; performs lighting/blending in linear light; applies exactly one selected LDR/HDR tone and transfer path; and emits the configured canvas color/alpha mode without implicit exposure or framework tone mapping. | No accepted Rendering color profile or aligned LDR/HDR target captures exist. | **Color-pipeline probe and capture suite:** compare shader-stage probe values, blend arithmetic, transfer samples, alpha composition, canvas configuration, and aligned color charts under an accepted numeric tolerance. | Blocked |
| `REN-052` — Debug views expose stable wireframe, normals, tangents, lightmap page, lightmap-only, albedo-only, material ID, model/face/leaf/cluster ID, overdraw, depth, shadow target, reflection/refraction target, culling reason, and resource-ledger outputs without changing later ordinary frames. | No debug-view registry or capture output exists. | **Debug isolation matrix:** render every mode before/between/after ordinary frames; compare mode-specific IDs/probes and require byte-identical subsequent ordinary frame inputs, draw transcript, and capture. | Not started |
| `REN-053` — Every encountered presentation family, record variant, material/model requirement, WebGPU feature, and runtime state is classified as Handled, Intentionally inert, Unsupported, Malformed, Unknown, or Missing; required non-handled state prevents scene activation or frame submission. | Ordinary scene loading fails when resolved materials are unavailable. Caller-selected diagnostic mode emits one `MissingMaterial` record for every exact runtime material and uses deterministic debug colors; it is never classified as ordinary material parity. Other family classifications and the generated coverage gate remain absent. | **Coverage mutation audit:** inject every classification at every family seam, compare sorted diagnostics and scene/frame publication, regenerate the inventory, and require no placeholder, silent omission, or alternate renderer. | Partial |
| `REN-054` — Visual evidence aligns target and playsrc by content build, map state, authority snapshot, presentation timestamp, camera transform, viewport, profile, asset hashes, and capture environment, then retains color plus applicable depth/normal/object-ID probes and one declared comparison/tolerance per claim. | No checked capture command, immutable declared-content index, browser/GPU matrix, target capture set, or accepted tolerance manifest exists. | **Aligned-capture gate:** regenerate the fixed capture manifest twice, validate every alignment field and hash, reject each single-field mismatch, run each named pixel/probe comparison, and retain pass/fail artifacts. | Blocked |

## Generated Inventories

No generated Rendering inventory is accepted. Accepted item count: 0. Candidate item count: 54.

| Output | Authority identity | Authority revision | Generator command | Candidate items | Accepted items |
|---|---|---|---|---:|---:|
| [`inventories/presentation-families.md`](inventories/presentation-families.md) | Rendering behavior rows; W3C WebGPU initialization/resource/canvas/error contracts; official SDK render, material, model, client-leaf, view, lighting, decal, beam, sprite, interpolation, and client-entity contracts; accepted adjacent producer registries; exact declared-content indexes and visual-evidence manifest | playsrc `1c2184c11353a618d8ef941c7f161e488346203e`; WebGPU CRD `2026-07-14`; SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; producer registries, content indexes, and evidence manifest Missing | Missing | 54 | 0 |

The future generator is owned by `tools/playsrc`. It must consume a checked rendering-contract manifest, the accepted Owner Registry, exact accepted producer registries, the immutable WebGPU and SDK authority identities above, exact archive indexes for one declared content build of each target game, and the accepted visual-evidence manifest. It must emit stable `REN-###` order, retain Unsupported, Malformed, Unknown, and Missing discoveries, and fail on an omitted family, duplicate identity, unowned seam, changed authority, unclassified input, missing evidence method, or item-count mismatch. No command name is declared before that implementation exists.

## Exit Criteria

Rendering is Complete only when all of these predicates pass:

- All 54 behavior rows are Ready.
- The 54-identity presentation-family inventory is generated, current, and Accepted with authority revisions, generator command, output path, item count, reviewer identity, review date, reviewed commit, and a passing denominator review.
- Map, Material, StudioModel, Entity, Visibility, Particle, Simulation, Replay, TF2, Content, and Asset Store expose accepted current interfaces consumed by Rendering; CS:S and legacy Source 1 CS:GO inputs enter when their Active targets declare accepted game mappings.
- Every declared WebGPU browser/device profile passes initialization, resource, context-loss, resize, color, capture, and frame-pacing evidence at its required limits.
- Every accepted world, displacement, brush, prop, studio, lighting, environment, decal/overlay, detail, effect, particle, viewmodel, view, interpolation, ordering, depth, and culling item has one handled or intentionally inert result and aligned evidence.
- Every fixed scene and camera path retains exact map/content/object hashes, authority snapshot identities, presentation timestamp, viewport, render profile, browser, GPU, driver, and comparison tolerance.
- Repeated ordinary frames from identical immutable inputs produce the same draw transcript, classifications, resource ledger, and comparison result; debug and capture work cannot change the next ordinary result.
- Device loss, scene replacement, cancellation, resize, failure, and disposal leave zero stale handles, leaks, duplicate owners, partial scenes, partial submissions, or unbounded work.
- No duplicate renderer, direct Source parser, material semantic resolver, visibility traversal, gameplay/replay mutation, placeholder, fallback, compatibility layer, legacy path, stale inventory, or required Unsupported, Unknown, Missing, Malformed, Partial, or Blocked item remains.

## Blockers

- **Current producer interfaces:** Map, Material, StudioModel, Entity, Visibility, Simulation, and Replay roadmaps are Not accepted and expose no current implementations; Particle and TF2 roadmaps are absent. Exact current geometry, model, material, entity-presentation, visibility, particle-render, and game-mapping values required by `REN-007` through `REN-040` and `REN-044` are unavailable. Checked every named owner README, roadmap, inventory, package tree, and the root Owner Registry.
- **Rendering package implementation:** the assigned package contains only `README.md`; no manifest, source, tests, fixtures, resource contract, WebGPU adapter, or capture integration exists. Checked the complete assigned package tree and root manifests.
- **Inventory generation and review:** no checked-in command or rendering-contract manifest emits [`inventories/presentation-families.md`](inventories/presentation-families.md), and no review record exists. Checked `tools/playsrc`, `tools/inspector`, package manifests, and the assigned package tree.
- **Declared-content and visual inputs:** local configuration supplies a TF2 root, but no checked-in immutable TF2 archive index, rendering capture set, aligned target/playsrc capture manifest, or CS:S/legacy-CS:GO content-build input exists. `REN-054` cannot execute. Checked local-configuration shape, current asset/fixture trees, adjacent roadmap blockers, and declared tool trees without filesystem discovery.
- **Browser/GPU evidence matrix:** applications define no accepted browser/version, operating-system, GPU/driver, canvas color, device-feature, or device-limit matrix. Rendering can define a device profile but cannot make cross-product readiness claims until exactly one application owner declares the Active browser targets. Checked the root Owner Registry and current web-application README and roadmap paths.
- **Color comparison decision:** no accepted evidence manifest fixes LDR and HDR target capture configuration, canvas format/color/alpha/tone mode, color-space normalization, image metric, per-channel or perceptual tolerances, and depth/normal/ID auxiliary predicates. `REN-021`, `REN-022`, `REN-051`, and `REN-054` remain Blocked until that manifest and retained captures exist.
