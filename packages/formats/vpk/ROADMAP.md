# VPK Roadmap

[`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines delivery statuses, coverage classifications, logical paths, and Complete. [`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines denominator and evidence requirements.

## Completion Denominator

The current completion denominator contains exactly the 33 Behavior Families rows below. [`inventories/versions.md`](inventories/versions.md) contains 7 candidate version and target-layout decisions and 0 accepted items. Candidate inventory items do not enter this denominator until a checked-in generator emits the inventory and its denominator review is Accepted.

The VPK denominator is not accepted because the format-universe prerequisite is not accepted, the exact TF2, CS:S, and legacy Source 1 CS:GO content-build archive indexes are Missing, entry data-descriptor multiplicity has conflicting public evidence, and numeric execution bounds have not been selected.

## Inputs

- One immutable directory-file source with a stable provider identity, revision, exact byte length, positioned-read operation, and cancellation signal. The source identity is either split `<base>_dir.vpk` or standalone `<name>.vpk`.
- For a split archive, one adapter that resolves only the exact segment identity `<base>_<index>.vpk` requested by the parsed index and exposes its revision, exact byte length, and positioned-read operation.
- One canonical slash-separated logical path for lookup, an optional entry-relative byte range, and operation-wide read, memory, concurrency, and queue bounds.
- An optional expected public key when the caller requires a signed archive to match a selected game/content-build trust policy.
- Valve Source SDK 2013 `src/public/vpklib/packedstore.h` at commit `88fa198fba3fb85d46d4c95018254693fdc3af0a` and Valve Developer Community `VPK (file format)` revision `496391` as the public VPK contracts.
- Exact archive indexes and directory-file bytes for one accepted content build each of TF2, CS:S, and legacy Source 1 CS:GO. These inputs are Missing.

## Outputs

- An immutable archive index containing the directory-file identity and revision; signature and version; exact section ranges; archive layout; every entry's stored extension, directory, basename, canonical logical path, CRC-32, preload range, ordered data descriptors, and total length; every archive-MD5 record; all three optional self-MD5 values; and parsed signature material.
- Exact entry lookup returning one entry, `Missing`, or a deterministic malformed-index error. Lookup never searches another archive family or provider.
- Full-entry or entry-relative range bytes assembled from preload bytes followed by the indexed data descriptors in their accepted order, with an explicit integrity result for CRC-32, archive MD5, self MD5, and signature checks that were applicable to that operation.
- A complete verification report for directory structure, directory-file self-integrity, every declared archive-MD5 range, every entry CRC-32, missing segments, and signature state.
- Deterministic `Malformed`, `Unsupported`, `Missing`, cancellation, bound, short-read, changed-source, and I/O errors containing the archive identity, operation, logical path or segment index when applicable, and no substitute bytes.

## Invariants

- All integers are unsigned little-endian values. Every addition, multiplication, conversion, section boundary, entry range, and read range is checked before allocation or I/O.
- Accepted Source 1 directory files start with `0x55AA1234` and declare version 1 or 2. Headerless archives and Source 2 VPK v3 never enter the accepted denominator.
- A version 1 directory file is `12-byte header | tree | directory-contained data`. A version 2 directory file is `28-byte header | tree | file-data section | archive-MD5 section | other-MD5 section | signature section`.
- The tree is consumed within exactly `TreeSize` bytes. Its extension, directory, and basename lists each end with an empty C string; the final empty extension ends the tree; no unconsumed tree byte is permitted.
- A single-space component means an absent extension, root directory, or absent basename. Every other component is non-empty ASCII. Reconstructed paths contain `/`, no absolute prefix, no empty, `.` or `..` component, no backslash, no NUL, and no ASCII control character.
- The index preserves stored component spelling and exposes one ASCII-lowercased canonical logical identity. Two entries with the same canonical identity make the complete index `Malformed`; no first-wins or last-wins result exists.
- Preload bytes immediately follow their entry metadata inside the tree. Entry data is uncompressed and equals preload bytes followed by its accepted ordered data ranges. A zero-length range is valid; an empty entry has zero preload bytes and zero data bytes.
- Archive index `0x7fff` addresses the directory-file data section. Every other accepted archive index addresses only the exact segment derived from the archive family identity.
- Split segments contain raw addressable bytes and have no VPK header. Segment indexes need not be contiguous, and unreferenced bytes or gaps are not entries and do not make an archive malformed.
- CRC-32 uses CRC-32/ISO-HDLC over the complete assembled entry: reflected polynomial `0xEDB88320`, initial value `0xffffffff`, and final XOR `0xffffffff`.
- A range read never reports entry CRC-32 as verified unless the complete entry has been examined. It reports every integrity mechanism as `Not present`, `Not evaluated`, `Verified`, or `Invalid` without promoting one mechanism to another.
- Archive-MD5 records are 28 bytes each and cover the exact declared segment index, offset, and length. The other-MD5 section is absent at length 0 or contains exactly the 16-byte tree MD5, 16-byte archive-MD5-section MD5, and 16-byte whole-file MD5 at length 48.
- The whole-file MD5 covers directory-file bytes from offset 0 through the byte immediately before the stored whole-file MD5. It excludes that digest and the signature section.
- A non-empty signature section contains exactly a little-endian public-key length, public-key bytes, little-endian signature length, and signature bytes. RSA PKCS#1 v1.5 with SHA-256 verifies all directory-file bytes preceding the signature section.
- Parsing, lookup, and reads never extract an archive, recursively scan a directory, infer an installation, inspect an unreferenced segment, or continue to another provider after an indexed failure.
- The index is immutable. Concurrent reads use positioned I/O and produce the same bytes, integrity states, and errors as the same reads in serial order; no shared file cursor affects a result.
- Cancellation returns no partial success, publishes no verified state from unfinished work, and releases operation-owned buffers and source handles.

## Ownership Exclusions

- `packages/content` owns provider registration, path-ID eligibility, provider precedence, cache selection, first-provider choice, and archive-family selection. VPK owns the selected family's structure and exact indexed reads.
- `games/tf2`, `games/css`, and `games/csgo` own content-build-specific archive-family membership, expected signing keys, and policies that require or permit unsigned archives. VPK parses and verifies supplied archive and key bytes without choosing game policy.
- Concrete filesystem, browser, HTTP, and object-store adapters own positioned byte I/O. VPK owns the requested offsets, lengths, ordering, cancellation propagation, integrity checks, and errors.
- `packages/formats/bsp` owns BSP embedded PAK ZIP data. A BSP PAK is not a VPK segment or fallback source.
- The leaf owner assigned by `packages/formats/inventories/formats.md` parses each resource returned from an entry read; VPK never interprets entry payload bytes.
- `tools/playsrc` owns command orchestration, local configuration loading, listing, verification, and extraction workflows. Filesystem extraction-path creation is not part of the VPK package interface.
- `packages/asset-store` owns playsrc-compiled objects, manifests, catalogs, channels, and publication. VPK returns Source bytes and never stores compiled output.
- VPK creation, mutation, incremental packing, and signing are not declared operations in the current project denominator. Activating one requires adding its complete encoding behavior family to this roadmap before implementation.

## Behavior Families

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Parse the four-byte `0x55AA1234` signature and unsigned little-endian common header fields without alignment assumptions. | No implementation exists. | Synthetic byte vectors at every header byte offset, swapped-endian values, wrong signatures, and 0- through 11-byte truncations compared with exact typed fields and error codes. | Not started |
| Accept exactly the Source 1 version set selected by the accepted format inventory and reject every other declared version before tree parsing. | The candidate set is versions 1 and 2, but the format-universe denominator is not accepted. | Inventory-generation result plus fixed version 0, 1, 2, 3, and maximum-`u32` headers proving accepted, excluded, and unsupported outcomes. | Blocked |
| Parse the 12-byte version 1 header fields `Signature`, `Version`, and `TreeSize`, and derive the remaining directory-file bytes as directory-contained data. | No implementation exists. | Byte fixtures compared field-for-field and range-for-range with Valve `vpk` listing results, including empty tree, empty embedded data, and maximum accepted tree boundary. | Not started |
| Parse the 28-byte version 2 header fields `Signature`, `Version`, `TreeSize`, `FileDataSectionSize`, `ArchiveMD5SectionSize`, `OtherMD5SectionSize`, and `SignatureSectionSize` in order. | No implementation exists. | Byte fixtures independently encoding each field and comparing all section starts and lengths against the public format contract. | Not started |
| Derive every version 2 section with checked arithmetic and require the declared final section end to equal the directory-file length. | No implementation exists. | Table vectors covering exact fit, each truncated section, each overflowing sum, overlapping interpretations, and one trailing byte; compare deterministic section diagnostics. | Not started |
| Parse the extension → directory → basename tree, its three nested empty-string list terminators, and the final tree terminator within exactly `TreeSize` bytes. | No implementation exists. | Synthetic trees covering zero and multiple nodes at each level, missing terminators, premature terminators, and trailing tree bytes; compare entry sequence and failure offset. | Not started |
| Interpret a single-space extension, directory, or basename as the format's absent-component marker without treating the list terminator as a component. | No implementation exists. | Root-file, extensionless, basename-less, and invalid all-absent component vectors compared with reconstructed logical identities. | Not started |
| Validate ASCII component syntax and reconstruct one stored logical path plus one canonical ASCII-lowercased slash identity. | No implementation exists. | Component matrix covering nested paths, uppercase ASCII, dot characters inside names, separators, absolute prefixes, empty components, `.`/`..`, controls, NUL, backslash, and non-ASCII bytes. | Not started |
| Parse CRC-32, preload length, ordered data descriptor fields, and the `0xffff` descriptor terminator using the accepted descriptor multiplicity. | The public format page specifies one descriptor, its talk page reports repeated descriptors for fragmented files, the official public SDK interface exposes one chunk, and no target archive index is available to resolve the conflict. | Fixed one- and multi-descriptor files compared with the selected target-game archives and Valve `vpk` detailed listings; require one accepted grammar and deterministic rejection of the other. | Blocked |
| Consume exactly `PreloadBytes` immediately after entry metadata and keep those bytes in tree order. | No implementation exists. | Vectors with zero, one, boundary, truncated, and terminator-looking preload bytes; compare exact preload slices and next-entry alignment. | Not started |
| Resolve archive index `0x7fff` relative to the end of the header and tree into the directory-file data section. | No implementation exists. | Version 1 and 2 embedded-entry vectors at section start/end, zero length, overflow, and one-byte overrun; compare positioned reads and exact errors. | Not started |
| Derive a split segment as `<base>_<index>.vpk` with decimal index width of at least three digits and read offsets relative to byte zero of that segment. | No implementation exists. | Segment-name vectors for indexes 0, 1, 999, 1000, `0x7ffe`, `0x8000`, and `0xfffe`; sentinel `0x7fff`; terminator `0xffff`; mixed-case directory filenames; and wrong family names; compare requested adapter identities and ranges. | Not started |
| Open standalone `<name>.vpk` archives only when every data descriptor addresses directory-contained data; split archives use `<base>_dir.vpk`. | No implementation exists. | Standalone and split fixtures compared with Valve `vpk` listings, including a standalone file that illegally references an external segment. | Not started |
| Reject duplicate exact or ASCII-case-folded logical identities while preserving non-duplicate tree order. | No implementation exists. | Duplicate matrix at extension, directory, basename, and case-only spelling levels; require one archive-level malformed result and zero readable entries. | Not started |
| Enumerate every entry once and perform lookup only by its canonical logical identity, with deterministic tree-order listing. | No implementation exists. | Mixed extension/path/name fixture compared entry-for-entry with Valve `vpk l` and repeated exact lookups, including `Missing` and invalid request identities. | Not started |
| Return `Missing` for a referenced segment adapter identity that is absent and never substitute directory data, a neighboring segment, or another archive family. | No implementation exists. | Fault-injected segment sets with missing referenced, missing unreferenced, and non-contiguous indexes; compare requested identities and zero fallback calls. | Not started |
| Validate every entry and archive-MD5 offset/length against its selected source length before allocation or read, and distinguish malformed range, source truncation, short read, and changed source. | No implementation exists. | Boundary vectors at source end and end-plus-one, integer overflow vectors, source-length changes, and short positioned reads; compare exact operation and error classification. | Not started |
| Return a full entry as exact preload bytes followed by all accepted indexed data bytes, including empty and preload-only entries. | No implementation exists. | Full-entry fixtures crossing tree, directory-data, and segment sources; compare byte-for-byte output, length, source ranges, and zero unrelated reads. | Not started |
| Return an entry-relative byte range that may lie in preload, data, or cross their seam without reading unrelated segment bytes. | No implementation exists. | Range matrix for zero length, first/last byte, seam crossing, full entry, end position, and end-plus-one; instrument every positioned source read and compare returned bytes. | Not started |
| Verify the stored entry CRC-32 against the complete preload-plus-data byte sequence before a full-entry read or full-entry verification succeeds. | No implementation exists. | Fixed CRC-32/ISO-HDLC vectors, preload/data splits, empty entry, and one-bit corruption in each source compared with Valve `vpk checkhash`. | Not started |
| Parse a version 2 archive-MD5 section only when its size is a multiple of 28 and retain each `ArchiveIndex`, `StartingOffset`, `Count`, and 16-byte MD5 in source order. | No implementation exists. | Zero-, one-, and multiple-record fixtures plus every 1-through-27-byte remainder; compare typed records and malformed section-size errors. | Not started |
| Verify each archive-MD5 record over exactly its declared segment range without requiring contiguous coverage or hashing unreferenced bytes. | No implementation exists. | Multi-segment range vectors with gaps, final partial range, missing segment, short read, overlap, duplicate record identity, and one-bit corruption compared with Valve `vpk checkhash`. | Not started |
| Parse an absent other-MD5 section at length 0 or exactly three 16-byte MD5 values at length 48; reject every other declared length. | No implementation exists. | Section-length vectors 0, 1, 47, 48, and 49 plus truncation at each digest boundary; compare states and failure offsets. | Not started |
| Verify tree MD5 over the tree, archive-MD5-section MD5 over that complete section, and whole-file MD5 over bytes preceding the stored whole-file digest. | No implementation exists. | Independent digest generation plus one-bit corruption in header, tree, file data, archive-MD5 section, each self digest, and signature-only bytes; compare which check fails. | Not started |
| Parse a signature section as two exact length-prefixed byte strings and reject internal lengths that do not consume the declared section exactly. | No implementation exists. | Unsigned, target-sized, zero-key, zero-signature, truncated-length, oversized-length, and trailing-byte vectors; compare parsed material and deterministic errors. | Not started |
| Verify RSA PKCS#1 v1.5 SHA-256 over every byte preceding the signature section and distinguish unsigned, wrong expected key, read failure, invalid signature, and valid signature. | No implementation exists. | Fixed generated key/signature vectors, target signed archives, altered signed bytes, altered signature, and mismatched expected key compared with Valve `vpk checksig`. | Not started |
| Report each integrity mechanism independently as `Not present`, `Not evaluated`, `Verified`, or `Invalid`; no successful read or report claims a check that did not execute. | No implementation exists. | Operation matrix across structural open, range read, full read, self verification, archive verification, and full verification; compare complete integrity-state snapshots. | Not started |
| Parse and read through positioned sources without archive extraction, segment-wide buffering, filesystem discovery, or a shared mutable cursor. | No implementation exists. | Instrumented multi-gigabyte sparse sources proving bounded requested ranges, zero discovery calls, and stable results after arbitrary prior reads. | Not started |
| Execute concurrent reads against one immutable index with serial-equivalent bytes, integrity states, source ranges, and errors under bounded backpressure. | No implementation exists. | Deterministic schedules for same entry, distinct segments, preload-only data, one failing reader, queue saturation, and repeated serial/concurrent result equality. | Not started |
| Cancel queued, parsing, hashing, and active positioned-read work without returning partial bytes or retaining successful verification from incomplete work. | No implementation exists. | Controlled cancellation before dispatch, during tree parsing, between preload and data, during CRC/MD5 hashing, during adapter read, and after completion; assert resources and integrity state. | Not started |
| Emit stable errors for invalid file signature, unsupported version, truncated header, section overflow, tree syntax, component syntax, descriptor terminator, duplicate identity, invalid range, missing segment, short read, CRC/MD5 mismatch, wrong key, invalid cryptographic signature, cancellation, exhausted bound, changed source, and adapter I/O. | No implementation exists. | Error golden table generated from one isolated vector per error kind; compare code, archive identity, operation, path/index context, and absence of fallback bytes across runtimes. | Not started |
| Enforce accepted maxima for directory-file bytes, tree bytes, entries, component bytes, logical-path bytes, cumulative preload bytes, descriptors per entry, segments, integrity records, public-key bytes, signature bytes, one read, queued reads, concurrent reads, and in-flight memory. | Exact browser and native maxima have not been selected; public field widths and SDK implementation constants do not establish the playsrc execution contract. | Boundary vectors at every selected maximum and maximum-plus-one, queue saturation schedules, and peak-memory measurements with fixed sparse sources. | Blocked |
| Generate the exact version, section-profile, archive-family, and segment-index layout for one accepted TF2, CS:S, and legacy Source 1 CS:GO content build. | The three selected content-build archive indexes are Missing and the 7-item inventory has no checked-in generator. | Regenerate from exact configured archive indexes and directory-file headers; compare every family, filename, index, size, hash, version, and section profile, with no filesystem scan. | Blocked |

## Generated Inventories

[`inventories/versions.md`](inventories/versions.md) contains 7 candidate decisions: 2 required Source 1 versions, 2 excluded version families, and 3 blocked target-game archive layouts. Accepted item count: 0.

The future generator must consume the accepted format-universe identity, the fixed public VPK contracts, and exact configured archive indexes plus directory-file bytes for one content build of each declared game. It must emit every archive family, directory-file identity and hash, version, section profile, referenced segment index and hash, standalone/split classification, and unresolved classification. Generation must fail on an unclassified version, family, segment, section profile, duplicate identity, malformed directory file, or missing referenced segment.

## Exit Criteria

The VPK package is Complete only when all of these predicates pass:

- All 33 Behavior Families rows are Ready.
- The format-universe prerequisite and this leaf denominator have Accepted review records for the same VPK identity.
- The version/layout inventory is emitted by one checked-in command, contains exactly the accepted item set, and includes exact TF2, CS:S, and legacy Source 1 CS:GO content-build archive indexes.
- Fixed vectors cover versions 1 and 2; standalone and split archives; every section-presence profile in the accepted inventory; root, extensionless, preload-only, directory-contained, segmented, empty, duplicate, missing, truncated, corrupt, unsigned, signed, and wrong-key cases.
- Full-entry bytes and listings match Valve `vpk` output, CRC/MD5 verification matches `vpk checkhash`, and signature results match `vpk checksig` for the same fixed archives.
- Content provider integration resolves a VPK winner through the immutable index and exact reader without duplicate parsing, archive extraction, provider-order logic, fallback, or an alternate integrity path.
- Concurrent and cancelled reads satisfy the accepted queue, concurrency, byte, and memory bounds on browser and native adapters.
- No required entry, version, section profile, archive family, producer, consumer, or behavior remains Unsupported, Unknown, Missing, Partial, or Blocked.

## Blockers

- **Format-universe prerequisite:** `packages/formats/ROADMAP.md` records its 62-item candidate as Not accepted and the VPK inventory cannot select its final version set. Checked that roadmap, `packages/formats/inventories/formats.md`, and the root Owner Registry.
- **Target content builds:** `playsrc.local.json` is Missing and the local configuration contract names no CS:S or legacy CS:GO root. Exact archive indexes, directory-file hashes, section profiles, and segment sets are unavailable. Checked `playsrc.local.example.json`, Source SDK `game/mod_tf/gameinfo.txt`, public TF2 depot 441 manifest `1804278129270892792`, public CS:S depot 241 manifest `6941588918651947824`, and public legacy CS:GO depot 731 records; none selects all three playsrc target builds.
- **Entry descriptor multiplicity:** Valve Developer Community `VPK (file format)` revision `496391` specifies one archive descriptor followed by `0xffff`; its public talk page reports repeated descriptor triples for fragmented entries; Source SDK `VPKContentFileInfo_t` exposes one chunk; no selected target archive index resolves whether multiple descriptors enter the denominator.
- **Execution bounds:** exact browser and native maxima for directory bytes, tree bytes, entries, path bytes, cumulative preload, descriptors, segments, integrity records, key/signature bytes, queued reads, concurrent reads, one read, and in-flight memory are undecided. Source SDK `packedstore.h` establishes a 200 MiB default writer chunk and 512 cached archive handles, but those implementation constants do not establish playsrc limits.
- **Inventory generator:** no checked-in command generates `packages/formats/vpk/inventories/versions.md`; its 7 candidate items therefore contribute 0 accepted denominator items.
