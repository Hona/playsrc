[package]
name = "playsrc-bsp"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
crc32fast = "1.5.0"
lzma-rs = "0.3.0"
zip = { version = "8.6.0", default-features = false, features = ["lzma"] }

[lib]
path = "src/lib.rs"
