# StudioModel Roadmap

[`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines delivery status, coverage classification, inventory, and completion terms.

## Completion Denominator

This leaf roadmap contains exactly 50 behavior rows. The candidate inventories contain 90 decisions: 8 file/version profiles and 82 serialized record families. They contribute 0 denominator items until checked-in generators emit them and a denominator review accepts them. The current completion denominator therefore contains 50 items and is Not accepted. Acceptance of all 90 current candidate inventory items would make the denominator 140 items.

The target is the little-endian PC StudioModel family assigned by the parent format inventory for Team Fortress 2, Counter-Strike: Source, and legacy Source 1 Counter-Strike: Global Offensive: `IDST` MDL, `IDAG` ANI, `IDSV` VVD version 4, VTX version 7, and VHV version 2. The MDL candidate set is versions 44, 45, 46, 47, and 48. Exact declared-game occurrence remains Missing. Console byte order, console VTX profiles, Source 2 models, PHY payload decoding, model-source formats, GPU resources, and animation-state decisions are excluded.

## Inputs

- A `StudioModelLoadRequest` containing one exact `.mdl` Source logical path, one explicit profile from [`inventories/versions.md`](inventories/versions.md), one explicit VTX identity from `.vtx`, `.dx80.vtx`, `.dx90.vtx`, or `.sw.vtx`, and the root bytes with provenance and SHA-256 hash.
- A dependency callback that receives the requesting logical identity, role, and exact requested logical path, then returns immutable bytes with provenance and content hash or `Missing`. The package never selects a provider, searches a directory, probes another extension after failure, or discovers content.
- Optional VHV bytes supplied through a separate request containing the exact map-owned logical identity and the expected MDL checksum. VHV identity is not derived from the MDL basename.
- A required limits record containing `maxFileBytes`, `maxAggregateInputBytes`, `maxDependencyCount`, `maxIncludeDepth`, `maxIncludedModels`, `maxRecords`, `maxStrings`, `maxStringBytes`, `maxVertices`, `maxIndices`, `maxAnimationFrames`, `maxDecodedAnimationSamples`, `maxOpaqueBytes`, `maxOwnedBytes`, and `maxDiagnostics`. The package defines no unbounded mode.
- A `PresentationRequest` containing one completed document, an explicit `World` or `ViewModel` profile, exact caller-selected material-resolution manifests produced by the VMT/material owners, immutable dependency responses with SHA-256 hashes, presentation limits, and a cancellation token. Material manifests enumerate their complete ordered VMT include closure and typed VTF references; StudioModel does not parse VMT syntax or infer texture roles.
- Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`: `src/public/studio.h`, `optimize.h`, `studio.cpp`, `studio_generic_io.cpp`, `studio_virtualmodel.cpp`, `bone_setup.{h,cpp}`, `src/public/materialsystem/hardwareverts.h`, `src/common/studiobyteswap.{h,cpp}`, `src/utils/motionmapper/motionmapper.h`, and StudioModel consumers under `src/utils/vbsp` and `src/utils/vrad`.
- Public Valve Developer Community MDL, ANI revision `428841`, VVD, VTX, VHV, and Studio model contracts. Revisions other than the ANI revision are `Unknown` until retained immutable snapshots are recorded.
- Exact archive indexes for one declared TF2, CS:S, and legacy Source 1 CS:GO content build. These inputs are Missing.

## Outputs

- An immutable `StudioModelDocument` containing the selected profile; root identity; ordered source-file identities, provenance, hashes, versions, checksums, and byte lengths; exact dependency trace; model header metadata; raw unknown fields; and every typed item in [`inventories/record-families.md`](inventories/record-families.md).
- A runtime-neutral skeleton containing ordered bones, parent indexes, base transforms, pose-to-bone transforms, flags, procedural-bone parameters, linear-bone arrays, source-bone transforms, controllers, hitbox records, and bone-driven flex metadata.
- Ordered animation and sequence data containing descriptors, block and section references, exact encoded track records, decoded integer-frame local transforms, movements, IK metadata, local hierarchies, events, autolayers, pose keys, sequence weights, transition records, and embedded KeyValues byte ranges. It contains no selected sequence, current cycle, blended pose, entity state, or event effect.
- Ordered material, skin, bodypart, model, mesh, LOD, vertex, tangent, topology, attachment, eyeball, mouth, and flex metadata. Geometry identities are `(bodypart, model, lod, mesh, stripGroup, primitive)` and vertices retain both VVD source identity and VTX mesh-local identity.
- A composed include-model view containing dependency order and local-to-root remap tables for bones, sequences, animations, attachments, pose parameters, nodes, and autoplay IK locks, while retaining each source document independently.
- A VHV document containing its exact source identity, version, checksum, `VERTEX_COLOR` format, four-byte stride, declared vertex count, ordered mesh ranges, exact BGRA8 records, and alignment ranges.
- A `StudioModelDependencyTrace` recording every exact companion and include request, result, provenance, checksum decision, cycle decision, and limit decision in request order.
- One structured diagnostic containing coverage classification, stable error code, source identity, byte range, record-family identity, index stack, dependency chain, offending value, and required bound.
- A deterministic `PSMP` presentation artifact containing one world/viewmodel descriptor, Source model basis, dependency identities/hashes/roles, material slots, geometry/LOD identities, skeleton and bind transforms, composed animations and sequence/activity maps, pose parameters, attachments, skins/bodygroups, and unsupported-feature dispositions. The encoded artifact is independently cacheable and is always smaller than 64 MiB.
- A sampled pose containing one model-space 3×4 skinning matrix per root bone and one model-space 3×4 transform per attachment. Sampling accepts normalized cycle, exact pose-parameter values, and ordered weighted layers; it performs no entity-state selection, camera transform, or GPU work.

## Invariants

- Parsing is pure over supplied bytes and dependency responses. It performs no filesystem, archive, cache, network, rendering, game, or platform discovery operation.
- The selected version profile is mandatory. Signature, version, extension role, and little-endian field contract must match it; no profile is inferred from record size, path, checksum, another companion, or a failed parser.
- MDL, ANI, VVD, VTX, and VHV declared file lengths, table ranges, relative offsets, strings, and child records are validated with widened checked arithmetic before access or allocation.
- MDL is the checksum authority. VVD, VTX, and supplied VHV checksums equal the MDL checksum. A PHY dependency retains the expected MDL checksum for validation by `packages/formats/phy`.
- An MDL with one or more bodyparts requires the selected VVD and VTX companions. An MDL with no bodyparts produces a valid metadata/animation document without invented geometry companions.
- ANI is requested exactly when an animation descriptor or section references a nonzero external block. Block 0 remains MDL-local; external block indexes begin at 1. A required missing ANI produces `Missing`, never an inline-data fallback.
- Every include model is resolved by its exact stored logical identity. Include cycles, duplicate dependencies, missing includes, and depth/count exhaustion are reported deterministically; none are replaced by root-local records.
- Source order is retained for every table. Duplicate names and records remain distinct unless the declared include-composition contract produces a separate remap view; source documents are never mutated by composition.
- Raw string bytes, original separators, termination, and source range are retained. Derived Source logical paths are slash-separated and reject empty, `.` and `..` components without rewriting the stored bytes.
- The hard format ceilings are 128 bones, 4 bone controllers, 8 LODs, 3 bone influences per vertex, 32 texture/material references, 1,024 flex descriptors, 96 flex controllers, 24 pose parameters, and 256 animation blocks. A lower caller limit takes precedence.
- VVD fixups produce exactly the declared vertex count for each retained root LOD. VTX bodypart, model, LOD, and mesh cardinalities match the corresponding MDL hierarchy before geometry assembly.
- VTX indices are strip-group-local, VTX vertices identify MDL mesh-local vertices, MDL mesh/model offsets identify VVD vertices, and VVD fixups select the source vertex for one LOD. These identities remain distinct in output.
- Triangle-list and triangle-strip topology is represented exactly as encoded. A derived triangle list is additional output and never replaces source strips, degenerate indexes, strip flags, or bone-state changes.
- Flex deltas retain fixed-point or binary16 source encoding, scale, target thresholds, pair, vertex-animation type, and affected mesh-local vertex. Wrinkle data is never invented for a normal flex.
- Accepted VHV v2 uses `VERTEX_COLOR`, a four-byte vertex stride, BGRA byte order, and 512-byte-aligned vertex-data start and file extent. Mesh vertex counts sum to the header total and every mesh range lies inside the aligned payload.
- Unknown flags, opcodes, procedural-bone types, record variants, reserved words, padding, and unconsumed tails are preserved with exact bytes and a coverage classification. An unknown value never selects another layout.
- Every count, product, sum, recursion step, dependency byte, decoded sample, opaque record, retained source byte, and diagnostic is charged to the required limits before allocation.
- A failed load emits diagnostics and dependency trace but no partially authoritative `StudioModelDocument`.

## Ownership Exclusions

- Exact logical-path canonicalization, provider ordering, VPK/directory lookup, byte retrieval, caching, and provenance acquisition belong to `packages/content`.
- QC/QCI, SMD/VTA, model-schema DMX, model compilation, and command orchestration belong to the proposed `packages/formats/model-source` owner and `tools/*`.
- PHY header and payload parsing, collision shapes, constraints, mass interpretation, and physics simulation belong to `packages/formats/phy`, `packages/world/collision`, and `packages/runtime/physics`. StudioModel retains only MDL collision references and expected checksum.
- VMT parsing belongs to `packages/formats/vmt`; material candidate resolution and surface-property meaning belong to `packages/world/material`; texture decoding belongs to `packages/formats/vtf`.
- Embedded MDL and sequence KeyValues syntax parsing belongs to `packages/formats/keyvalues`. StudioModel owns the containing byte range and record association.
- Static-prop instance identity and association of a map VHV logical path with a model instance belong to `packages/formats/bsp` and `packages/world/map`.
- Hitbox selection, collision queries, contents meaning, and gameplay damage rules belong to `packages/world/collision` and `games/<game>`.
- Activity choice, weighted sequence choice, bodygroup/skin choice, animation cycle, playback rate, event effects, entity pose state, prediction, and TF2-, CS:S-, or legacy CS:GO-specific model behavior belong to `games/<game>`, `packages/runtime/replay`, and the gameplay authority. StudioModel retains sequence/activity mappings and evaluates only the exact sequence indexes, cycles, pose values, and layer weights supplied by those owners.
- Procedural-bone evaluation, IK solving, flex-controller values, and flex-rule application are explicit unsupported presentation dispositions. StudioModel owns integer-frame decoding, sequence-grid and frame interpolation, ordered caller-supplied layer composition, model-space bone matrices, and attachment transforms.
- GPU buffers, materials, shaders, VTX hardware-profile selection for a device, VHV lighting use, LOD selection by camera, skinning, culling, scene nodes, and pixels belong to `packages/presentation/rendering`.
- Audio or particle effects referenced by animation events belong to `packages/presentation/audio` and `packages/presentation/particle`; game-owned event meaning remains in `games/<game>`.
- Content-addressed compiled artifacts, manifests, and publication state belong to `packages/asset-store`.

## Behavior Families

The current implementation covers explicit MDL 44/45/46/47/48, VVD 4, and VTX 7 profiles; exact hashed model/companion/include/ANI/PHY traces; recursive include animation/sequence/pose/attachment composition; MDL skeletons, sequence grids/weights/pose keys, pose parameters, texture/search paths, skins/bodygroups, attachments, bodypart/model/mesh hierarchy; VVD/VTX geometry/LOD/topology plus VTX material replacements; integer animation frames and codec metadata; deterministic bounded `PSMP` artifacts; and caller-selected pose sampling. The fixed declared-game presentation matrix remains blocked by missing configured content inputs.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Every accepted StudioModel input has one explicit little-endian PC profile covering its MDL/ANI version and accepted VVD, VTX, and VHV companion versions. | Native profiles explicitly select MDL/ANI 44–48, VVD 4, and VTX 7. VHV and the legacy CS:GO companion set remain absent. | Synthetic profile mismatch vectors and exact TF2 MDL 44, 45, and 48 inputs pass. | Partial |
| The root MDL deterministically requests exactly the required VVD, selected VTX, ANI, include-model, and externally supplied VHV inputs, while distinguishing geometry-free MDLs from missing companions. | `load` emits one exact batch for bodypart companions, externally referenced ANI blocks, direct includes, and optional same-stem PHY. The selected VTX suffix is explicit; VHV remains caller-supplied and unimplemented. | Geometry-free, bodypart, optional PHY, direct include, and five exact-map model sets pass. | Partial |
| MDL, VVD, VTX, VHV, and PHY-reference checksums and all cross-file cardinalities agree before a validated view is emitted. | MDL checksum gates VVD, VTX, and ANI; VTX bodypart count matches MDL. PHY checksum validation belongs to PHY and VHV is absent. | VVD/VTX checksum mutations fail; five exact model sets validate. | Partial |
| MDL headers preserve `IDST`, version, checksum, internal name bytes, declared length, eye/illumination positions, hull/view bounds, raw flags, root-LOD fields, mass, contents, and secondary-header presence. | Signature, profile, checksum, raw name, length, six vector bit arrays, and flags are retained. Root-LOD, mass, contents, and secondary headers are not yet represented. | Synthetic truncation/profile/length vectors and exact MDL 44/45/48 headers pass. | Partial |
| Bones preserve names, parent graph, controller references, position, quaternion, Euler rotation, compression scales, pose-to-bone transform, alignment, flags, physics-bone reference, surface property, and contents. | Every listed field is retained with raw float bits; controller ranges and parent order are validated; composed animation weights remap by bone name. | Exact models retain skeletons from one through 86 bones; synthetic controller, hierarchy, sampling, and artifact vectors pass. Complete field boundaries remain absent. | Partial |
| Axis-interpolation, quaternion-interpolation and triggers, jiggle, aim-at-bone, and aim-at-attachment records are selected only by their declared procedural type. | Bone type/offset are retained; known payload extents, control references, axes, and trigger tables are validated; `PSMP` classifies every known and unknown type without evaluation. Typed payload fields are not retained. | Missing known payload and all present/unknown classification vectors pass; complete typed-field vectors remain absent. | Partial |
| Secondary-header linear-bone arrays and source-bone pre/post transforms preserve every bone-indexed value and match the root bone count. | No secondary skeleton parser exists. | Present/absent secondary-header vectors compare flags, parents, transforms, scales, alignments, source names, and exact count/range failures. | Not started |
| Bone controllers and bone-flex drivers preserve target bone/component, input field, ranges, controller references, and driver-control order. | No controller representation exists. | Boundary vectors cover 4/5 bone controllers, every motion component, invalid bone/flex references, empty drivers, and non-finite ranges; compare values and classifications. | Not started |
| Hitbox sets and hitboxes preserve names, set order, bone, group, bounds, and empty-name state without applying collision or damage semantics. | No hitbox parser exists. | Fixed multi-set models with empty/repeated names, empty sets, invalid bones, reversed/non-finite bounds, truncation, and count limits; compare exact records and errors. | Not started |
| Animation descriptors resolve inline data, external block directories, and section tables, including the separately stored final frame, without changing source indexes. | Descriptor, section, model block, inline/ANI range, and final-frame section selection are retained and resolved by source index after ANI validation. | Exact root animations for Soldier and props decode; historical sectioned-v45 comparison remains blocked. | Partial |
| Bone animation tracks decode raw Vector48, Quaternion48, Quaternion64, RLE position/rotation values, delta/default transforms, fixed alignment, terminators, and zero-frame spans. | Vector48, Quaternion48/64, RLE position/rotation, delta/default transforms, ordered bone tracks, terminators, and integer frames decode to raw float-bit outputs. Zero-frame auxiliary spans remain metadata-only. | Exact Soldier and frog root animations decode four total integer frames; exhaustive codec and numeric comparison remains absent. | Partial |
| Piecewise movement records preserve end frame, motion flags, endpoint velocities, yaw, direction, and accumulated position without becoming entity movement. | No movement representation exists. | Fixed movement blocks compare parsed bits and SDK movement extraction at every block boundary within absolute `1e-6`; invalid ordering and non-finite values are deterministic errors. | Not started |
| Animation IK rules, compressed/uncompressed errors, local hierarchies, and their animation-value streams retain exact influence windows, targets, and frame coverage. | No IK or hierarchy parser exists. | Fixed old/new IK records and local hierarchy streams compare every decoded key with SDK accessors; cover invalid chains/bones, windows, offsets, RLE coverage, and external-block placement. | Not started |
| Sequence descriptors preserve label/activity-name bytes, raw activity fields, flags, bounds, blend grid, parameter axes/ranges, timing, nodes, phases, next sequence, cycle pose, and local references. | Every listed scalar is retained; event/autolayer/IK/activity-modifier child payloads remain count-only unsupported metadata. | Synthetic full-descriptor vectors and exact inputs retaining 1–361 ordered sequences pass; complete child-payload evidence remains absent. | Partial |
| Sequence animation indexes, pose keys, per-bone weights, and blend dimensions retain source order and validate every animation, pose, and bone reference. | Animation grids, axis pose keys, per-bone weights, dimensions, and animation/active-pose references are retained and validated. | Synthetic 1D/2D grids, include remaps, pose sampling, malformed references, and artifact round trips pass; exhaustive dimension boundaries remain absent. | Partial |
| Sequence events, autolayers, IK locks, activity modifiers, and sequence KeyValues ranges remain ordered metadata with no game, audio, particle, or playback effect. | No sequence metadata parser exists. | One vector per child family plus repeated names/IDs, empty bytes, invalid references, unknown event IDs, and truncation; compare exact bytes, values, and classifications. | Not started |
| Pose parameters and the named transition matrix preserve ranges, loop values, node names, and every directed transition byte. | Pose names, flags, ranges, loops, include remaps, and sampled values are retained; transition nodes/matrix remain absent. | Synthetic looping/non-looping pose artifacts and sequence-grid sampling pass; transition evidence remains absent. | Partial |
| Attachments preserve name, flags, local bone, and 3×4 local transform without producing scene nodes. | Ordered attachment records retain every listed field and validate bone references. | Soldier retains 24 exact attachments; synthetic malformed-bone evidence remains absent. | Partial |
| Texture records and material-directory strings produce ordered raw names plus derived `.vmt` logical-path candidates without resolving a material. | Raw names/search paths and ordered lowercase candidate identities are emitted without Content access. | Exact models retain 1–20 material records. Path-shape boundary evidence remains absent. | Partial |
| The rectangular skin table preserves every family and slot and validates each texture reference without selecting a skin. | Every family/slot is retained and bounded against the texture table. | Synthetic zero table and exact model skin tables pass; full boundary matrix remains absent. | Partial |
| Bodyparts, models, and meshes preserve hierarchy, base values, names, material slots, vertex counts/offsets, mesh IDs, centers, per-LOD counts, and flex ranges. | Structural hierarchy and every listed scalar are retained; flex child records are deferred. | Empty-model synthetic bodypart and exact bodypart/model/mesh hierarchies pass. | Partial |
| Eyeballs and mouths preserve skeletal, texture, iris, lid-flex, target, forward-vector, and non-FACS metadata without evaluating gaze or speech. | No eyeball or mouth representation exists. | Fixed ordinary/non-FACS eyes and mouths compare all fields; invalid bones/textures/flexes and non-finite values fail with exact record identity. | Not started |
| VVD version 4 validates `IDSV`, checksum, LOD count/vertex counts, and fixup ranges and produces the exact selected vertex order for every root LOD. | VVD retains every fixup and resolves destination vertices through the profile's LOD filter while validating each produced count. | Synthetic zero-vertex and exact multi-LOD companion sets pass; full malformed-fixup matrix remains absent. | Partial |
| VVD vertices preserve three stored weights and bone indexes, influence count, position, normal, UV, and separate tangent, with exact source indexes. | Every listed field is retained as encoded bits/bytes with one stable source index. | Exact TF2 geometry sets decode all referenced vertices; invalid influence count is checked, while weight/bone semantic boundaries remain incomplete. | Partial |
| VTX version 7 validates packed header limits, checksum, LOD count, material-list offset, and bodypart hierarchy against MDL and VVD. | The complete bodypart/model/LOD/mesh hierarchy and its cardinality against MDL are validated after header checks. | Synthetic checksum/cardinality and exact `.dx90.vtx` sets pass. | Partial |
| VTX strip groups preserve ordered packed vertices, original mesh-vertex IDs, bone mappings, raw unsigned-16 indices, flags, and source ranges. | Original mesh vertex IDs and encoded index arrays remain ordered; source vertices are joined through MDL mesh/model offsets and VVD fixups. Packed VTX bone mappings and raw ranges are not yet exposed. | Exact map models produce 72 geometry primitives with bounded vertex/index references. | Partial |
| VTX strips preserve triangle-list/triangle-strip identity, index and vertex subranges, bone count, flags, degenerates, and ordered bone-state changes. | Each strip retains list/strip flags and index/vertex ranges; parity and degenerates produce an additional triangle list without replacing encoded indices. Bone-state changes remain absent. | Exact TF2 list/strip inputs produce bounded triangles; unsupported flags fail. | Partial |
| Per-LOD VTX material replacement lists preserve material IDs, raw names, and order and validate the referenced MDL texture slot. | Ordered LOD/material/name records are retained, validated, converted through authored MDL search directories, included in `PSMP`, and selected by LOD. | Fixed empty/populated list, exact candidate, artifact closure, LOD selection, invalid slot, offset, string, and truncation vectors pass; duplicate/full-LOD boundary evidence remains absent. | Partial |
| MDL, VVD, and VTX assemble one runtime-neutral geometry view whose bodypart/model/mesh/LOD/vertex/index identities remain traceable to every source record. | Geometry primitives carry the full hierarchy identity, switch point, material slot, VTX mesh-local IDs/indices/strips, VVD source vertices, and derived triangles. | Exact Soldier, frog, cow, and resupply models yield 72 primitives, 32,972 referenced vertices, and 35,154 triangles without GLB. Independent byte comparison remains incomplete. | Partial |
| LOD count, root-LOD restrictions, per-mesh/per-file vertex counts, switch points, shadow-LOD marker, and material replacements agree without selecting an LOD for a camera. | Header/model LOD counts, VVD per-LOD vertices, mesh cardinality, switch points, fixup selection, and replacement materials agree. Root restrictions and the shadow marker remain absent; caller-supplied LOD selects an artifact primitive/material view without camera policy. | Exact multi-LOD models plus synthetic replacement parse/artifact/selection vectors pass. | Partial |
| Flex descriptors, controllers, controller UI remaps, and bone-flex drivers preserve names, ranges, remap type, stereo state, and references. | No flex metadata parser exists. | Fixed pass-through, two-way, N-way, eyelid, stereo, and bone-driven records compare fields; unknown remaps and invalid references retain exact classification. | Not started |
| Flex rules preserve ordered opcodes and operand bits, validate stack shape for every known opcode, and retain unknown opcodes without evaluating presentation values. | No flex-rule representation exists. | One expression vector per SDK opcode plus underflow, overflow above the 32-entry SDK stack, invalid indexes, non-finite constants, and unknown opcodes; compare typed operations and diagnostics. | Not started |
| Mesh flexes preserve descriptor, four targets, pair, vertex-animation type, source scale, vertex indexes, position/normal deltas, side/speed, and optional wrinkle delta. | No mesh-flex parser exists. | Fixed normal/wrinkle and fixed-point/binary16 records compare source bits and decoded deltas; duplicate/out-of-range vertices, unknown types, bad scales, and truncation fail exactly. | Not started |
| Include-model composition recursively produces deterministic local-to-root remaps and deduplicated sequence, animation, attachment, pose, node, IK-lock, and autoplay views while preserving every source document. | Recursive authored-order loading composes name-deduplicated/override sequences, animations, pose parameters, attachments, root-bone maps, weights, and dependency/source traces. Nodes, IK locks, autoplay views, complete source documents, and independent SDK-output comparison remain absent. | Synthetic nested closure, animation/sequence composition, unmatched bones, optional dependencies, cycles, and configured depth/direct-count bounds pass. | Partial |
| MDL-level and sequence-level KeyValues ranges retain exact bytes, source range, containing record, empty state, and parse request boundary. | No embedded-data representation exists. | Fixed empty, NUL-containing, nested KV1, truncated, and over-limit ranges compare exact bytes; integration invokes the current KeyValues parser without embedding another tokenizer. | Not started |
| Surface properties, mass, contents, physics-bone indexes, hull/view bounds, and the derived PHY logical identity form collision references without parsing PHY or deciding collision behavior. | Bone surface/physics fields and hull/view bounds are retained; exact same-stem PHY request carries the MDL checksum and returns Present or Missing. Header mass/contents remain absent. | Synthetic missing PHY and exact present/missing model sets pass. | Partial |
| VHV version 2 validates the expected MDL checksum, `VERTEX_COLOR`, four-byte BGRA stride, total vertex count, 512-byte payload alignment, mesh LOD/count/offset ranges, and exact color records. | No VHV parser exists. | Fixed `sp_<index>.vhv` and `sp_hdr_<index>.vhv` inputs compare headers, alignment ranges, mesh ranges, and every BGRA byte; mutate version, checksum, flags, stride, totals, alignment, overlap, overflow, and truncation. | Not started |
| Reserved words, padding, unconsumed tails, unknown flags/types/opcodes, and unknown child records remain byte-exact and receive one coverage classification. | No unknown-record contract exists. | Mutation vectors set each reserved/unknown field and append bounded tails; compare retained ranges/bytes/classification and require no effect on adjacent known records. | Not started |
| Negative counts, malformed relative/absolute offsets, unterminated strings, count×stride overflow, reversed ranges, cross-file mismatches, and truncation fail before out-of-range access. | Checked table/range/string arithmetic gates parsed MDL/VVD/VTX/ANI records, sequence children, procedural extents, replacements, companions, and `PSMP`; failures produce no document/artifact. | Synthetic profile, count, range, reference, checksum, include-cycle, missing, hash, canonical-artifact, and limit failures pass. Exhaustive mutation and instrumented-read evidence remains absent. | Partial |
| Parsing, dependency composition, geometry assembly, animation decoding, and VHV retention obey every caller limit before allocation on browser, Node, and native targets. | Parsing, recursive dependency closure, decoded samples, and `PSMP` dependency/material/texture/bone/animation/geometry/string/owned/encoded bytes are bounded; VHV and retained allocation counters remain absent. | Synthetic decoded-sample equal/below and artifact equal/below/64-MiB boundaries plus cancellation pass; exhaustive per-limit and browser/Node evidence remains absent. | Partial |
| Every failure is exactly `Malformed`, `Unsupported`, `Unknown`, or `Missing`, reports stable location and dependency context, and never invokes a fallback parser. | Model load errors retain classification, code, identity, range, and dependency chain; presentation errors retain stable code/identity and never fall back. Record/index stacks, offending values/bounds, `Unknown`, and one unified diagnostic remain absent. | Current malformed/unsupported/missing/cancellation/hash/artifact vectors compare stable codes and no fallback; complete error-contract evidence remains absent. | Partial |
| Every accepted inventory item has one current parser path, and content, map, collision, game, replay, rendering, and tools consume the sole runtime-neutral representation. | The parent inventory is Not accepted, both leaf inventories lack generators, and no implementation or consumer integration exists. | Regenerate inventories from accepted authorities; audit each item and every named consumer for one owner, one interface, and no embedded parser, fallback, compatibility layer, legacy reader, or stale artifact. | Blocked |
| Material search emits every exact `materials/<cdtexture>/<texture>.vmt` candidate in authored directory order, selects the first caller-confirmed present candidate, and never probes or searches a filesystem. | Base and per-LOD replacement candidates are emitted in authored order and first-present selection requires explicit `Present`/`Missing` responses. | Fixed base/replacement order, first-present, all-missing, path validation, and artifact-selected LOD vectors pass; the exact TF2 matrix is Missing. | Partial |
| A selected VMT consumes one caller-supplied resolved material manifest and emits its complete ordered include closure plus every typed source VTF request without parsing VMT syntax or inventing texture roles. | `MaterialResolutionManifest` supplies the VMT/material-owner boundary; StudioModel validates ordered include ancestry, built-ins, typed source VTFs, hashes, and missing responses. | Fixed patch/base VMT, base VTF, built-in environment, LOD VTF, hash mismatch, missing closure, and no-parser vectors pass; the complete role matrix remains absent. | Partial |
| Include-model closure composes animations, sequences, pose parameters, and attachments in deterministic depth-first authored order with case-insensitive name deduplication and root-bone remaps. | Recursive loading and composition implement those records, dependency chains, override semantics, weights, and unmatched-bone state. | Fixed nested dependency, animation/sequence composition, unmatched root bone, missing optional PHY, and cycle vectors pass; full duplicate/override/pose/attachment matrix and exact target comparison remain absent. | Partial |
| One `World` or `ViewModel` `PSMP` artifact preserves geometry/LOD/material slots, skins/bodygroups, root skeleton/bind transforms, animation sections/codecs/frames, sequences/activities, poses, attachments, Source model basis, and dependency hashes. | Canonical `PSMP` v1 contains every listed family plus root/companion/include/material/texture dependency roles, lengths, and SHA-256 values; decode requires canonical byte-for-byte re-encoding. | Two complete generations and decode/re-encode compare byte-identically; world/viewmodel profiles differ; SHA-256 uses the standard `abc` vector. Exact TF2 artifacts are Missing. | Partial |
| Every presentation artifact is smaller than 64 MiB and obeys dependency, material, texture, bone, animation-sample, geometry, string, owned-byte, and encoded-byte limits before allocation or append. | Required finite limits gate source counts, dependency phases, model cloning, each binary append, decode allocations, total bytes, and cancellation; the hard maximum is 64 MiB exclusive. | Animation samples and encoded bytes pass at equality and fail one below; a 64 MiB configured maximum fails; cancellation and malformed/trailing bytes fail. Exhaustive per-limit evidence remains absent. | Partial |
| A normalized base sequence plus exact pose values and ordered layers samples deterministic local transforms, sequence-grid blends, integer-frame interpolation, model-space skinning matrices, and attachment transforms. | Base sequences, 1D/2D pose grids, looping/clamped cycles, frame nlerp/lerp, per-bone weights, ordered ordinary/delta layers, hierarchy matrices, skin matrices, and attachments are implemented. | Fixed half-frame, pose-grid, parent, attachment, activity, skin/bodygroup/LOD, and delta-layer vectors compare exact outputs. Exact TF2 timelines are Missing. | Partial |
| World descriptors cover Soldier/Demoman idle, run, jump, crouch, and fire activity mappings plus rocket/sticky world models; viewmodel descriptors cover draw, idle, primary-fire recoil, return-to-idle, and reload mappings. | Separate profile descriptors and the complete generic activity/state seam exist; synthetic world/viewmodel activity vectors pass. | [`evidence/presentation.md`](evidence/presentation.md) defines the exact model/hash/timeline matrix. `playsrc.local.json` and retained target objects are Missing, so target evidence is Blocked. | Blocked |
| Axis/quaternion interpolation, jiggle, aim-at-bone, aim-at-attachment, IK, flex, and sequence autolayer behavior has one exact `RetainedNotEvaluated`, `NotPresent`, or `Malformed` classification and is never silently applied or discarded. | `PSMP` records a count and disposition for all named families plus unknown procedural types; known procedural payload ranges are validated and no unsupported evaluator runs. | Fixed present/absent family counts, malformed known procedure, unknown procedure, round trip, and sampling vectors pass; exhaustive malformed flex/IK payload evidence remains absent. | Partial |

## Generated Inventories

No generated StudioModel inventory is accepted. Accepted inventory item count: 0.

| Output | Authority identity | Authority revision | Generator command | Candidate items | Accepted items |
|---|---|---|---|---:|---:|
| [`inventories/versions.md`](inventories/versions.md) | Official SDK StudioModel IDs, version constants, historical conversion contract, VTX/VVD/VHV headers, public format contracts, and three declared-game archive indexes | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; ANI public revision `428841`; other public revisions `Unknown`; content indexes Missing | Missing | 8 | 0 |
| [`inventories/record-families.md`](inventories/record-families.md) | Official SDK serialized-record declarations and byte-swap traversals plus exact declared-game StudioModel indexes | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; content indexes Missing | Missing | 82 | 0 |

Current manually derived candidate count: 90. Current generated count: 0.

The future generator is owned by `tools/playsrc`. It must consume the accepted parent format-inventory identity, retained official/public contracts, and exact archive indexes under the configured `sourceCacheDir`; emit both assigned inventories in stable-identity order; retain blocked, empty, unknown, malformed, and unsupported discoveries; and fail on an unclassified signature, version, extension role, record family, companion relation, checksum relation, flag/type/opcode, or declared-content identity. No command name is declared before implementation exists.

## Exit Criteria

StudioModel is Complete only when all of these predicates pass:

- All 50 behavior rows are Ready.
- The parent Source format-universe denominator and both StudioModel inventories have Accepted denominator reviews with current authority revisions, generator command, output paths, item counts, reviewer identity, review date, and reviewed commit.
- Exact TF2, CS:S, and legacy Source 1 CS:GO content-build archive indexes are generator inputs; every indexed StudioModel file has one profile, role, dependency relation, and record-family classification.
- Historical MDL/ANI behavior, including version-45 sectioned animation data, is resolved for every encountered declared-content input.
- The legacy CS:GO companion decision is resolved in the parent inventory and every accepted companion is assigned to exactly one package.
- Every handled animation track, VVD fixup, VTX topology record, flex delta, include remap, and VHV range satisfies its exact comparison predicate.
- Every malformed offset, overflow, truncation, missing companion, unknown record, unsupported value, and allocation boundary satisfies its declared diagnostic and no-fallback predicate.
- `packages/content`, `packages/formats/phy`, `packages/formats/keyvalues`, `packages/world/map`, `packages/world/material`, `packages/world/collision`, `packages/runtime/replay`, `packages/presentation/rendering`, declared games, and declared tools consume the current interface without a duplicate parser, compatibility layer, fallback, or legacy reader.
- No required declared-content item remains `Unsupported`, `Unknown`, `Missing`, `Malformed`, `Partial`, or `Blocked`.

## Blockers

- **Parent inventory gate:** [`../ROADMAP.md`](../ROADMAP.md) and [`../inventories/formats.md`](../inventories/formats.md) remain Candidate and Not accepted. The parent has no generator, five unresolved decisions, and 0 accepted inventory items.
- **Declared content indexes:** `playsrc.local.json` is Missing and no exact TF2, CS:S, or legacy Source 1 CS:GO archive index is available. Checked `playsrc.local.example.json`, the parent format inventory, and the assigned StudioModel package tree. Filesystem discovery was not attempted.
- **Inventory generators:** no checked-in command emits either assigned StudioModel inventory. Checked `tools/`, root manifests, and the assigned package tree.
- **Historical MDL/ANI occurrence:** the official SDK current contract is MDL/ANI version 48 and converts older headers, but exact occurrence of versions 44, 45, 46, and 47 in declared content is Missing. The SDK conversion contract identifies sectioned version-45 ANI data as incompatible and clears some pre-48 zero-frame metadata; exact required output for encountered old files needs declared-content vectors and retained target observations.
- **Legacy CS:GO vertex companion:** the parent format inventory leaves candidate VVC ownership, identifier, version, checksum relation, and declared-build occurrence unresolved. Checked official SDK `studio.h` and `optimize.h`, the public MDL/VVD/VTX contracts, and the absent legacy CS:GO archive index.
- **Public documentation revisions:** the checked MDL, VVD, VTX, VHV, and Studio model pages expose no retained immutable revision in this repository. Their revisions remain `Unknown` until a snapshot records an immutable revision or SHA-256 content hash.
