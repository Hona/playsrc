# Source 1 Format Inventory

Owner: [`../ROADMAP.md`](../ROADMAP.md)

State: Candidate; Not accepted.

Authority identity: Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`, the public contracts named per row, and exact TF2, CS:S, and legacy Source 1 CS:GO content-build archive indexes. The archive indexes are Missing.

Generator command: Missing.

Candidate item count: 62. Accepted item count: 0.

Outcome counts: 13 existing-leaf assignments, 9 internal-format assignments to existing packages, 28 proposed-leaf assignments to 18 proposed packages, 7 exclusions, and 5 blocked decisions.

An outcome assigns parser ownership only. Game, world, runtime, presentation, application, service, and tool behavior remains with the semantic owner named by [`../ROADMAP.md`](../ROADMAP.md).

| Format | Extensions or identities | Declared consumers | Authority | Owner | Outcome |
|---|---|---|---|---|---|
| KeyValues 1 text | Caller-declared KV1 documents; commonly `.txt`, `.res`, `.vdf`, and `.pop`; quoted or bare tokens, braces, comments, conditionals, `#base`, and `#include` | VMT and typed script parsers, games, VGUI consumers, content tools | Source SDK `src/public/tier1/KeyValues.h` and `src/tier1/KeyValues.cpp`; Valve Developer Community KeyValues contract | `packages/formats/keyvalues` | Existing leaf package owner |
| Binary KeyValues 1 | Type-tagged KV tree emitted and consumed by `WriteAsBinary`/`ReadAsBinary`; extension is caller-defined | Games, tools, caches that declare binary KV input | Source SDK `src/public/tier1/KeyValues.h` and `src/public/tier1/kvpacker.h` | `packages/formats/keyvalues` | Existing leaf package owner |
| VGUI resource and scheme documents | `.res`, scheme `.res`, and panel resource identities using KV1 syntax plus resolution/condition keys | TF2 HUD and menus, future CS:S UI, web-application migration tools | Source SDK `src/public/vgui_controls/EditablePanel.h`, `Panel.h`, and TF2 `LoadControlSettings` call sites | `packages/formats/keyvalues` | Internal format owned by existing package |
| VICE/ICE encrypted script envelope | `.ctx` and caller-selected encrypted extensions; 8-byte ICE blocks around exact script bytes | Legacy game script loading and content tools | Source SDK `src/utils/vice/vice.cpp` and public VICE documentation | `packages/formats/encrypted-script` | Proposed new leaf package owner |
| Valve Map Format | `.vmf`; Hammer chunk document with world, entity, solid, side, displacement, connection, camera, cordon, and instance records | Map compiler, map tools, world compiler | Source SDK `src/public/chunkfile.h` and `src/utils/vbsp/map.cpp`; Valve Developer Community VMF contract | `packages/formats/vmf` | Proposed new leaf package owner |
| Valve BSP | `.bsp`; `VBSP`, versions 19/20 for TF2 and CS:S and version 21 for legacy CS:GO; 64 indexed lump slots | World map, collision, visibility, rendering, gameplay, tools | Source SDK `src/public/bspfile.h`; Valve Developer Community BSP contract and CS:GO static-prop supplement | `packages/formats/bsp` | Existing leaf package owner |
| BSP game-lump directory and payloads | BSP lump 35; four-character identities including `sprp`, `dprp`, `dplt`, and `dplh`, each with its own version | BSP parser, world map, rendering, collision | Source SDK `src/public/gamebspfile.h`; Valve Developer Community BSP game-lump contract | `packages/formats/bsp` | Internal format owned by existing package |
| BSP embedded PAK | BSP lump 40; ZIP central directory and entries, stored method 0 and Source-used LZMA method 14 | BSP parser, content resolver, map compiler and packer | Source SDK `src/public/zip_uncompressed.h`, `zip_utils.h`, and `src/utils/vbsp`; BSP PAK documentation | `packages/formats/bsp` | Internal format owned by existing package |
| BSP lump compression envelope | `LZMA` lump identity, declared compressed and uncompressed sizes, exact lump bytes | BSP parser and map compiler | Source SDK BSP lump helpers and `src/common/lzma`; Valve Developer Community compressed-lump documentation | `packages/formats/bsp` | Internal format owned by existing package |
| External BSP lump patch | `<map>_l_<index>.lmp`, indices 0 through 127; lump-file header plus one replacement lump payload | BSP parser, map patching tools, map compiler | Source SDK `src/public/bspfile.h`, `src/public/lumpfiles.*`, and `src/utils/common/bsplib.cpp`; public lump-patching documentation | `packages/formats/bsp` | Internal format owned by existing package |
| Portal interchange | `.prt`; portal-file header, leaf/cluster count, portal polygons, and endpoints | VVIS-compatible visibility compiler and map diagnostics | Source SDK `src/utils/vbsp/prtfile.cpp` and `src/utils/vvis/vvis.cpp` | `packages/formats/map-compile` | Proposed new leaf package owner |
| Leak line | `.lin`; ordered Source-coordinate points identifying a map leak path | VBSP-compatible compiler, Hammer-compatible diagnostics | Source SDK `src/utils/vbsp/leakfile.cpp` and `src/utils/vvis/flow.cpp` | `packages/formats/map-compile` | Proposed new leaf package owner |
| RAD lighting definitions | `.rad`; material emission values, include/global file ordering, and map-local overrides | VRAD-compatible lighting compiler | Source SDK `src/utils/vrad/vrad.cpp` and `src/utils/vbsp/detailobjects.cpp`; public RAD documentation | `packages/formats/map-compile` | Proposed new leaf package owner |
| Valve Pak | `_dir.vpk`, standalone `.vpk`, and numbered `_*NNN.vpk` segments; versions 1 and 2 | Content providers, tools, all asset consumers | Source SDK `src/public/vpklib/packedstore.h`; Valve Developer Community VPK file-format contract | `packages/formats/vpk` | Existing leaf package owner |
| Valve Texture Format | `.vtf`; `VTF\0`, versions 7.0 through 7.5, resource dictionaries, image formats, mip levels, frames, faces, and slices | Material, rendering, particle, UI, texture tools | Source SDK `src/public/vtf/vtf.h`; Valve Developer Community VTF contract | `packages/formats/vtf` | Existing leaf package owner |
| Valve Material Type | `.vmt`; shader root, parameters, proxies, conditionals, patch include/insert/replace composition | World material, rendering, particles, map compiler | Source SDK material interfaces and `src/utils/vbsp/materialpatch.cpp`; Valve Developer Community VMT contract | `packages/formats/vmt` | Existing leaf package owner |
| StudioModel MDL | `.mdl`; `IDST`/`IDAG` identities, checksums, skeleton, hitboxes, sequences, animations, materials, skins, bodyparts, attachments, flexes | Rendering, animation, collision, gameplay, model tools | Source SDK `src/public/studio.h`; Valve Developer Community MDL contract | `packages/formats/studio-model` | Existing leaf package owner |
| StudioModel vertex data | `.vvd`; `IDSV`, checksum-linked fixups, vertices, bone weights, normals, texture coordinates, tangents, and LOD counts | StudioModel assembly and rendering | Source SDK `src/public/studio.h`; Valve Developer Community VVD contract | `packages/formats/studio-model` | Existing leaf package owner |
| StudioModel optimized mesh | `.vtx`, `.dx80.vtx`, `.dx90.vtx`, and `.sw.vtx`; checksum-linked bodyparts, LODs, meshes, strip groups, strips, vertices, and indices | StudioModel assembly and rendering | Source SDK `src/public/optimize.h`; Valve Developer Community VTX contract | `packages/formats/studio-model` | Existing leaf package owner |
| StudioModel animation blocks | `.ani`; MDL-referenced external animation sections | StudioModel animation and replay/game presentation | Source SDK `src/public/studio.h`; Valve Developer Community MDL and ANI documentation | `packages/formats/studio-model` | Existing leaf package owner |
| StudioModel hardware vertex lighting | `.vhv`; checksum-linked per-mesh/per-vertex static-prop lighting records | StudioModel assembly, static-prop rendering, map lighting tools | Source SDK `src/public/materialsystem/hardwareverts.h` and `src/utils/vrad/vradstaticprops.cpp` | `packages/formats/vhv` | Existing leaf package owner |
| Legacy CS:GO vertex-color companion | Candidate `.vvc` identity and relationship to MDL/VVD/VTX | Legacy CS:GO model rendering and tools if present in the declared build | Checked Source SDK `studio.h` and `optimize.h`, public MDL/VVD/VTX pages, and no configured legacy CS:GO archive index | Unassigned | Blocked: authoritative declared-build companion registry is missing |
| VPhysics model asset | `.phy`; header/checksum, solid encodings, collision geometry, constraints, and textual key data | Collision, physics, StudioModel assembly, map compiler | Source SDK `src/public/phyfile.h` and VPhysics public interfaces; public PHY documentation | `packages/formats/phy` | Existing leaf package owner |
| Source demo container | `.dem`; `HL2DEMO`, demo/network protocol identities, fixed header, ticked command stream, and length-delimited command payloads | Demo parser, networking-state decoder, replay | Source SDK `src/public/demofile/demoformat.h`; Valve Developer Community DEM contract | `packages/formats/demo` | Existing leaf package owner |
| Source 2007/2013 demo protocol records | Bit-packed signon/packet, data-table, string-table, user-command, console-command, and network-message records used by TF2 and CS:S | Demo parser, networking, replay, game modules | Source SDK demo, data-table, network-message, and string-table public contracts | `packages/formats/demo` | Internal format owned by existing package |
| Legacy CS:GO demo protobuf mapping | DEM commands and network-message identifiers mapped to exact protobuf payload schemas for one declared content build | Demo parser, networking, replay, legacy CS:GO game | Checked Source SDK demo header, official protobuf declarations, public DEM documentation, and no configured legacy CS:GO archive index | Unassigned | Blocked: complete command-to-payload authority is missing |
| Demo action list | `.vdm`; ordered demo-edit commands, parameters, timing, and selection state | Replay editing and demo tools | Valve Developer Community VDM contract and Source demo-action interfaces | `packages/formats/demo` | Internal format owned by existing package |
| Navigation mesh | `.nav`; `0xFEEDFACE`, version/subversion, BSP size, places, areas, connections, hiding spots, encounters, ladders, and game custom data | TF2 bots, CS:S bots, legacy CS:GO bots, gameplay tools | Source SDK `src/game/server/nav.h`, `nav_file.cpp`, and game NAV subclasses; public NAV documentation | `packages/formats/nav` | Proposed new leaf package owner |
| AI node graph | `.ain`; graph version, BSP map version, nodes, links, hull connectivity, lookup data | NPC navigation, map tools, future declared-game behavior requiring node graphs | Source SDK `src/game/server/ai_networkmanager.cpp`; public AIN documentation | `packages/formats/ain` | Proposed new leaf package owner |
| Data Model Exchange container | `.dmx` and DMX-header-bearing bytes; `binary`, `keyvalues`, `keyvalues2`, and `keyvalues2_flat` encodings with named format/version | PCF, model-source tools, replay metadata tools | Source SDK `src/public/datamodel/idatamodel.h`, `dmxloader`, and DMX public documentation | `packages/formats/dmx` | Proposed new leaf package owner |
| Particle definition file | `.pcf`; DMX particle format containing named systems, operators, renderers, initializers, emitters, forces, constraints, and children | Particle presentation, rendering, particle tools | Source SDK `src/public/movieobjects/dmeparticlesystemdefinition.h`, particle loader contracts, and public PCF documentation | `packages/formats/pcf` | Proposed new leaf package owner |
| Choreography scene | `.vcd`; actors, channels, events, timing, flex tracks, gestures, speech, loops, sections, and references | Gameplay scene entities, animation, audio, replay presentation, Faceposer-compatible tools | Source SDK `src/game/shared/choreoscene.*`; public VCD documentation | `packages/formats/vcd` | Proposed new leaf package owner |
| Compiled scene image | `scenes.image`; `VSIF` version 2, scene entries, CRC identities, string table, compiled VCD payloads, and summaries | Choreography scene cache and tools | Source SDK `src/public/scenefilecache/SceneImageFile.h` and `src/game/shared/sceneimage.cpp` | `packages/formats/vcd` | Proposed new leaf package owner |
| Sound-event script | `scripts/game_sounds*.txt` plus `game_sounds_manifest.txt`; event names, channels, levels, volume/pitch ranges, waves, random-wave sets, and operator stacks where declared | Audio presentation, game emitters, choreography, content tools | Source SDK SoundEmitterSystem public interfaces and TF2 sound-script call sites; public sound-script documentation | `packages/formats/audio-script` | Proposed new leaf package owner |
| Soundscape definition | `scripts/soundscapes*.txt` plus `soundscapes_manifest.txt`; named soundscapes, looping/random sounds, positions, attenuation, volume, pitch, and nested soundscapes | Audio presentation and map entities | Source SDK `src/game/client/c_soundscape.cpp` and `src/game/server/soundscape_system.cpp`; public soundscape documentation | `packages/formats/audio-script` | Proposed new leaf package owner |
| Sentence definition | `scripts/sentences.txt`; sentence names, words, timing, pitch, volume, emphasis, and caption metadata | Audio presentation, choreography, speech tools | Source SDK `src/public/sentence.h` and `sentence.cpp`; public sentence documentation | `packages/formats/audio-script` | Proposed new leaf package owner |
| Forge Game Data | `.fgd`; class declarations, inheritance, properties, choices, flags, inputs, outputs, helpers, material exclusions, visgroups, and map bounds | Hammer-compatible editor, VMF tools, game entity inventories | Source SDK `src/public/fgdlib`; Valve Developer Community FGD contract | `packages/formats/fgd` | Proposed new leaf package owner |
| Console command script | `.cfg` and `.rc`; ordered command text, separators, quoting, comments, and nested `exec` identities | Client/server startup, game/ruleset configuration, tools | Source SDK command-buffer interfaces and TF2/CS:S CFG execution call sites; public CFG documentation | `packages/formats/console-script` | Proposed new leaf package owner |
| Source VScript Squirrel source | `.nut` under `scripts/vscripts`; Squirrel source syntax and includes | TF2 and legacy CS:GO gameplay scripts, map scripts, tools | Source SDK `src/game/shared/vscript_shared.cpp` and Squirrel integration headers; public VScript documentation | `packages/formats/squirrel` | Proposed new leaf package owner |
| Response-rule script | Commonly `scripts/talker/*.txt`; criteria, rules, response groups, alternatives, weights, odds, delays, and includes | TF2 voice response behavior and tools | Source SDK `src/game/server/AI_ResponseSystem.*` and TF2 response-rule loading; public response-system documentation | `packages/formats/response-rules` | Proposed new leaf package owner |
| Valve localization resource | `resource/*_<language>.txt`; encoding marker, language name, token map, and exact localized strings | Games, HUD, web applications, captions | Source SDK `src/public/vgui/ILocalize.h` and localization call sites; public localization-file documentation | `packages/formats/localization` | Proposed new leaf package owner |
| Compiled caption dictionary | `resource/*.dat`; `VCCD` version 1, hash-sorted lookup directory, blocks, offsets, lengths, and UTF-16 caption bytes | Audio captions, HUD, localization tools | Source SDK `src/public/captioncompiler.h` and `src/utils/captioncompiler/captioncompiler.cpp` | `packages/formats/localization` | Proposed new leaf package owner |
| Source WAV sound asset | `.wav`; RIFF/WAVE PCM or ADPCM plus cue, sampler, and Valve sentence-data chunks | Audio presentation, choreography, phoneme tools | Source SDK `src/public/tier2/riff.h`, `soundutils.h`, `sentence.*`; official TF2 game-file documentation | `packages/formats/sound` | Proposed new leaf package owner |
| Source MP3 sound asset | `.mp3`; MPEG audio frames and metadata accepted as Source game sound | Audio presentation and game emitters | Official TF2 game-file documentation and Source SDK sound interfaces | `packages/formats/sound` | Proposed new leaf package owner |
| Source bitmap font | `VFNT` identity, version 3; atlas dimensions, style, ascent, translation table, and glyph metrics | HUD and rendering | Source SDK `src/public/BitmapFontFile.h` and VGUI bitmap-font interfaces | `packages/formats/font` | Proposed new leaf package owner |
| TrueType/OpenType font input | `.ttf` and `.otf`; standard font bytes named by Source schemes | HUD, rendering, web applications | Source SDK VGUI scheme and surface font interfaces | `packages/formats/font` | Proposed new leaf package owner |
| Legacy CS:GO VFONT envelope | `.vfont`; exact encryption/container identity and payload are Unknown | Legacy CS:GO HUD and Panorama if present in the declared build | Checked Source SDK bitmap-font and VGUI font interfaces, public VFONT tool references, and no configured legacy CS:GO archive index | Unassigned | Blocked: exact public format authority and declared-build use are missing |
| StudioModel QC build document | `.qc` and `.qci`; directives, includes, model inputs, sequences, LODs, materials, collision, flexes, and output identity | StudioModel compiler and model tools | Source SDK utility manifests and model-source headers; Valve Developer Community QC command contracts | `packages/formats/model-source` | Proposed new leaf package owner |
| StudioModel SMD/VTA source records | `.smd` and `.vta`; nodes, skeleton samples, triangles, vertex weights, and flex vertex animation | StudioModel compiler, motion mapper, model tools | Source SDK `src/public/movieobjects/dmsmdserializer.h`, `src/utils/motionmapper`, and public SMD/VTA documentation | `packages/formats/model-source` | Proposed new leaf package owner |
| StudioModel DMX source schema | `.dmx` with model, mesh, skeleton, animation, combination-operator, and makefile format identities | StudioModel compiler and model tools | Source SDK `src/public/movieobjects`, data-model format updaters, and public DMX model documentation | `packages/formats/model-source` | Proposed new leaf package owner |
| Texture-source raster interchange | `.tga`, `.pfm`, `.psd`, `.bmp`, `.jpg`, `.jpeg`, and `.png` when imported to or exported from VTF tooling | VTF compiler/converter, texture inspection, map-lighting diagnostics | Source SDK bitmap/image utilities, `vtf2tga`, `height2normal`, and public VTEX documentation | `packages/formats/vtf` | Internal format owned by existing package |
| VGUI HUD animation script | `scripts/HudAnimations*.txt` and animation-script identities; named sequences, commands, properties, interpolation, delay, duration, and conditions | TF2 HUD, future game HUDs, UI tools | Source SDK `src/public/vgui_controls/AnimationController.h` and game HUD animation loaders | `packages/formats/ui-script` | Proposed new leaf package owner |
| Protocol Buffer schema and wire encoding outside DEM framing | `.proto` schemas and protobuf wire records used by declared game/network interfaces | Runtime networking and game modules | Source SDK official TF protobuf declarations and bundled protobuf wire contract | `packages/runtime/networking` | Internal format owned by existing package |
| Precompiled platform shader binaries | `.vcs` and hardware/API-specific shader records | Original native renderer only | Declared browser renderer uses playsrc shader implementations; native gameplay servers do not render | None | Excluded: no declared target executes Source platform shader binaries |
| Console-specific Source asset variants | `.xwv`, `.xtf`, XZIP data, `.360.bsp`, `.360.nav`, `.360.ain`, and `.360.wav` | Xbox 360 engine and tools only | Source SDK platform branches; declared targets are browsers and future native gameplay servers | None | Excluded: Xbox 360 is not a declared platform |
| Source download and cache wrappers | `.bz2` HTTP download payloads, `.ztmp` temporary downloads, and generated sound/cache files | Original Source client distribution/cache paths | Declared content resolution consumes exact configured source bytes and archive indexes; asset delivery uses playsrc services | None | Excluded: no declared workflow consumes original Source download/cache artifacts |
| Source save game | `.sav` and autosave metadata | Single-player save/restore | Declared games target multiplayer gameplay, replay, tools, and future gameplay servers; no save-game experience is declared | None | Excluded: no declared target consumes Source save games |
| Legacy Steam depot container | `.gcf` and `.ncf` | Historical Steam content distribution | Declared content resolution uses exact configured Source roots, cache entries, and archive indexes | None | Excluded: Steam depot transport is not a Source content-provider contract |
| GoldSrc format families | GoldSrc WAD, SPR, BSP30, DEM, and MDL identities | GoldSrc games and tools | `TERMINOLOGY.md` declares only TF2, CS:S, and legacy Source 1 CS:GO | None | Excluded: GoldSrc games are outside the declared targets |
| Source 2 format families | KV3, VPK v3, compiled `_c` resources, Source 2 maps, models, textures, materials, demos, and particles | Source 2 games and tools | `TERMINOLOGY.md` categorical Source 2 exclusion | None | Excluded: Source 2 is outside playsrc |
| Legacy CS:GO Panorama resource set | Exact source and compiled layout, style, script, vector-image, and package identities are Unknown | Legacy CS:GO browser product if its accepted experience inventory requires original Panorama resources | Checked Source SDK Panorama client code, public Source SDK directory documentation, and no configured legacy CS:GO archive index | Unassigned | Blocked: exact declared-build identity registry is missing |
| Declared-game video asset input | Candidate `.bik` and `.webm` game-content identities; required set is Unknown | Browser products and presentation only if accepted experiences consume shipped videos | Checked Source SDK `src/public/video/ivideoservices.h`, public game-file documentation, and no configured declared-game archive indexes | Unassigned | Blocked: no authority establishes a required video set for the declared products |
