# TF2 Browser HUD Bindings Roadmap

## Contract

This module consumes one immutable TF2 gameplay or replay HUD snapshot and one ordered bounded HUD event stream. It produces generic VGUI panel values, named HUD-animation requests, presentation notifications, and typed game-command requests. It never reads DOM state, wall time or mutable gameplay objects and never invokes a gameplay transition.

Authority is Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a` plus TF2 build 24245096, patch 10828683, exact configured `scripts/HudLayout.res`, `scripts/HudAnimations_tf.txt`, `resource/ClientScheme.res`, `resource/UI/HudPlayerHealth.res`, `HudPlayerClass.res`, `HudAmmoWeapons.res`, `HudWeaponSelection.res`, `Scoreboard.res`, and `FreezePanel_Basic.res`.

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Player health, maximum health, maximum buffed health and death-warning/overheal modes bind exact dialog variables, visibility, fill and pulse sequences. | One immutable health binding emits `%Health%`, `%MaxHealth%`, bonus/death-warning mode and exact named animation edges. | Fixed threshold, overheal, death and recovery timelines compare panel and animation transcripts. | Ready |
| Active-weapon clip/no-clip/reserve state and strict low-ammo threshold bind `HudAmmoWeapons.res`. | One ammo binding preserves unavailable state, reload phase, label mode, float32/ties-to-even adjustment and exact low-ammo pulse edges. | Clip/reserve/threshold/reload/regenerate timelines compare every boundary. | Ready |
| The Demoman pipe panel reads the current launcher's authoritative count on visible 100 ms ticks, updates both authored count branches only when the count changes, and remains independent of the active weapon and rendered projectile roster. `HudDemomanCharge` separately shows the active chargeable launcher's float-time progress. | Session count/charge publication, WASM/codec transport and the two authored HUD roots are being connected; shield-charge behavior is not substituted for launcher charge. | Current configured resource, launcher lifecycle/rollback, wire and HUD-clock regressions; visible end-to-end proof remains required. | In progress |
| Weapon selection retains ordered slot/position/item facts and never performs selection inside HUD presentation. | Immutable selection facts bind six resource slots; UI actions return typed selection requests only. | Selection transcript and transition-spy tests. | Ready |
| TF2 default icon `crosshair_default` resolves to the fully transparent authored `sprites/crosshairs` atlas rectangle `(32,0,32,32)`; decrypted rocket-launcher and sticky-launcher scripts replace it with their visible weapon icon rectangle `(32,32,32,32)`; nonempty `cl_crosshair_file` selects exactly `materials/vgui/crosshairs/<file>.vmt` and its authored VTF. | One game-owned crosshair presentation consumes only verified authored icon/weapon scripts, atlas/style materials, texture dimensions, crop rectangles, and current active-weapon facts; missing assets remain classified failures. | Exact build-24245096 encrypted-script and VPK-index identities, transparent fallback versus visible weapon source-crop vectors, every declared paired custom style, and visibly headed authored-pixel/style captures. | Ready |
| Stock crosshair dimensions use authored icon width/height times `cl_crosshair_scale / 32` times weapon scale; custom crosshairs use a `32×32` nominal size and a final rectangle spanning twice the rounded nominal dimensions; both are centered at the rounded current viewport aim point and use opaque archived RGB tint. | The authored-pixel owner applies exact stock/custom geometry, source alpha multiplied by opaque convar RGB, weapon scale, viewport replacement, and no background/CSS substitute. | Fixed atlas-alpha, opaque tint, stock/custom dimensions, half-pixel rounding, weapon-scale, and viewport-transition vectors plus visibly headed center-pixel comparisons. | Ready |
| Desktop crosshair visibility requires the enabled toggle, local player/view entity, allowed alive/in-eye/roaming state, no loading/pause/VGUI input/frozen state, client-mode permission, no TF2 ghost/taunt/minigame/match-summary/countdown suppression, and either an allowed stock weapon or a selected custom style. | Pure HUD bindings retain explicit observer, lifecycle, loading, pause, console/menu, client-mode, weapon, custom-style override, and TF2 suppression predicates; the DOM owner shows authored pixels only when that one binding permits drawing. | Exhaustive player/observer/weapon/custom-style/suppression decision vectors, visibly headed authored-pixel suppression/restoration captures, and complete visibly headed application acceptance. | Ready |
| Archived `cl_crosshair_red`, `cl_crosshair_green`, `cl_crosshair_blue`, `cl_crosshair_scale`, and `cl_crosshair_file` default to `200`, `200`, `200`, `32.0`, and empty; Multiplayer editing clamps RGB to `0..255` and scale to `16..48`, while unbounded manual ConVar writes preserve the declarations' lack of numeric clamps. | The existing settings authority owns one crosshair state for Multiplayer draft/preview, Apply/Cancel/defaults, manual developer-console writes, persistent archived values, class/weapon replacement, and map replacement. | Fixed default/range/manual-write/transaction/persistence vectors, owner-batched Multiplayer Apply/Cancel vectors, map-replacement integration snapshots, and complete visibly headed application acceptance. | Ready |
| The TF2 Multiplayer style selector starts with an empty default selection and then exposes only exact indexed crosshair VTF entries with matching VMTs; the `64×64` configured preview immediately reflects draft tint, `scale / 48`, style, and authored frame progression without committing gameplay before Apply. | The game-owned Options presentation derives admitted styles from verified exact content, enables the configured preview, keeps draft pixels separate from the live crosshair, restores committed values on Cancel, and atomically commits on Apply. | Fixed eight-style inventory, missing-pair rejection, slider/style preview, Apply/Cancel/default reset, authored frame-sequence vectors, and visibly headed authored-style pixels. | Ready |
| Damage messages append scale, lifetime and canonical direction in message order; missing direction appends nothing. | Ordered damage events emit typed indicator commands only when a supplied direction is available. | Direction-present/absent, clamp and coalesced-event vectors. | Ready |
| Nine class identities and RED/BLU team identities select exact class-image resources; five condition words select exact status images with buff-class coalescing. | Class/team and reviewed condition-panel inventories produce immutable image/visibility values. | All class/team images and every reviewed condition collision/ordering vector. | Ready |
| Death hides live status, publishes local respawn, and supplies freeze-panel killer facts independently. | Lifecycle values retain active/dying/observer state, explicit respawn eligibility and optional freeze facts; typed respawn action cannot mutate gameplay. | Death/respawn timeline and transition-spy tests. | Ready |
| Successful regeneration and pickup facts publish after authoritative state changes; TF2 does not draw generic pickup history. | Ordered typed notifications accompany resulting immutable health/ammo/condition facts; no pickup-history panel is invented. | Regenerate and health/ammo/weapon pickup transcripts. | Ready |
| Death notices retain ordered participant/team/icon/crit/assist/custom/local facts with a four-entry configured presentation bound. | Killfeed facts are copied into typed append commands; lifetime remains a generic virtual-clock concern. | Ordered typed notice and unavailable-producer vectors. | Ready |
| Scoreboard team, row and selected-player detail facts remain gameplay/replay facts rather than visual inference. | A bounded immutable scoreboard-ready snapshot retains exact team variables, rows and detail counters without sorting or scoring in VGUI. | 64-player bound, identity/order, unavailable-class and immutable-input tests. | Ready |
| Current canonical nine-class publications retain every selected simulation tick and ordered gameplay/lifecycle event; regeneration or pickup atomically supersedes every earlier ammo value for the same weapon before final snapshot consistency checks. | An adapter preserves canonical class/team identities and all per-tick health/ammo/reload/weapon/lifecycle/regenerate edges; one chronological weapon tracker compares the final authoritative clip/reserve/reload after fire→regenerate, regenerate→fire, pickup replacement, and weapon replacement. | Soldier fire/reload/death coalescing, fire→regenerate and regenerate→fire event-order vectors, pickup-after-fire replacement, malformed-final-state rejection, and Demoman class/team/overheal vectors. | Ready |
| HUD input produces typed selection, respawn and scoreboard requests only. | Pure action binding validates current immutable facts and returns frozen requests or explicit unavailability. | No gameplay transition call and input non-mutation tests. | Ready |
| `cl_hud_playerclass_use_playermodel` selects exactly one configured 3D class model/background or 2D class image/background; class/team changes replace model, skin, held-item and image facts together. | The canonical HUD snapshot retains the immutable setting and exact class/team/model/skin/weapon facts; one binding controls `classmodelpanel`, `classmodelpanelBG`, `PlayerStatusClassImage`, and `PlayerStatusClassImageBG` without a fallback identity. | Soldier/Demoman x RED/BLU x 2D/3D integration snapshots assert one visible presentation and exact image/model/skin/item values; initialization rejects incomplete dynamic image closures. | Game-owned ready; shared image closure and setting input pending |
| Code creates one player-status, class, health, ammo and weapon-selection root, then applies each configured resource once; RED/BLU custom image panels resolve the current team. | HUD integration owns one named root instance and explicitly binds every team-qualified class, health and ammo background from the same snapshot. | Panel ancestry/count/resource-owner and RED-to-BLU replacement snapshots pass. | Ready |
| Every health condition panel starts hidden, then exact condition words activate only the matching independent panels and the first active member of each SDK buff class; inactive Halloween status remains hidden. | One complete condition inventory publishes a false baseline before exact grouped/independent visibility and image values. | All-zero, one-at-a-time, overlapping grouped and complete removal/reset vectors pass. | Ready |
| Active-weapon identity, display name, optional item definition, slot/position, clip/reserve/maxima, reload and display mode remain distinct facts on one ammo/weapon presentation. | Known identity/name never become `unknown` when item definition is unavailable; one active ammo root and selection model bind all supplied facts. | Three compact weapons, reload phases, switch/regenerate and unavailable-item integration snapshots pass. | Ready |
| The configured `CarryingWeapon` panel is visible only when the active item has an established different owner, a valid carrying label, and every Source player/disguise/mode eligibility predicate; ordinary locally owned stock weapons never expose its unresolved `%carrying%` dialog variable. | The stock compact session has no distinct carried-item owner, so the game-owned HUD binding explicitly hides the complete carrying panel and its label/background descendants. | Exact `tf_hud_playerstatus.cpp` ownership predicates plus Soldier/Demoman stock, class/team switch, reset, and configured panel visibility snapshots reject every visible `[unknown]` carrying label. | Ready |
| Class/team/weapon/lifecycle resets and ordered HUD animations update the sole active panel instances once. | Map/disconnect reset clears stale presentation caches; each ordered event starts each named sequence once. | Switch, death/respawn, regenerate and map/disconnect reset transcripts pass; shared app reset routing is pending. | Game-owned ready; shared lifecycle routing pending |
| The proportional HUD viewport and configured bottom/right-relative player health and weapon-ammo roots resolve from every current physical viewport size; a duplicate viewport notification performs no reload, layout or publication. | HUD integration applies one atomic VGUI viewport commit, refreshes dynamic bounds from the new configured geometry, and suppresses an identical width/height/device-pixel-ratio notification. | Fixed configured-resource transitions cover 1,280×720, 1,024×768, 1,600×900, 2,560×1,080, 390×844, 844×390, DPR 2, restoration and duplicate notification. | Ready |

## Exclusions

- Generic VGUI resource parsing, layout, focus, animation execution, window ownership, and reusable DOM/CSS primitives; game-owned authored crosshair DOM and Multiplayer preview remain included.
- Gameplay authority, replay decoding, concrete browser storage, generic app input routing, and WASM/transport changes; crosshair console/catalog and archived settings integration remain included.
- Main-menu, loading-dialog, pause-menu, GameUI, and unrelated Options-page behavior; their exact crosshair visibility transitions remain included.
