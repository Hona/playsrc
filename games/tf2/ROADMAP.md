# TF2 Game Roadmap

[`../../docs/roadmap-contract.md`](../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../TERMINOLOGY.md`](../../TERMINOLOGY.md) defines delivery status, coverage classification, inventory, and completion terms.

## Completion Denominator

This leaf roadmap contains exactly 65 behavior rows. The nine broader candidate inventories contain 21,206 class, item, attribute, weapon/presentation, entity, objective, condition, cvar, and network/prediction/event/message items. The bounded core-state inventory contains 322 generated items. All inventories contribute 0 denominator items until a denominator review accepts them. The current completion denominator is therefore 65 behavior rows. Acceptance of all 21,206 broader candidate items without a roadmap change would make the denominator 21,271 items; accepting the bounded inventory first requires a denominator arithmetic update that removes its overlap with the broader candidates.

The denominator covers TF2 behavior over generic Source packages: nine playable classes; player, team, class, item, loadout, attribute, condition, health, damage, ammo, weapon, projectile, building, movement-specialization, respawn, entity, objective, game-rule, prediction, replicated-state, replay-interpretation, event, user-message, and game-specific presentation-mapping behavior. Mode lifecycle, scoring, restrictions, and completion belong to [`rulesets/ROADMAP.md`](rulesets/ROADMAP.md).

The denominator is Not accepted. The bounded 322-item core-state inventory has a generator but no denominator review; the nine broader inventories have no generator or review record; the TF2 ruleset universe is absent; required generic package interfaces are not accepted; and no arithmetic profile or field-specific comparison tolerance has been accepted.

## Inputs

- One immutable prior TF2 state with stable simulation tick and entity identities: player lifecycle, team, class, class data, health/overheal, ammo/metal, loadout, item instances, attribute providers/cache state, conditions and sources, active/last weapons, clips/reload/attack timers, projectiles, buildings, pickups, TF2 entity extensions, objective primitives, common game-rule state, prediction state, and replicated-field source values.
- One ordered admitted-command set supplied by Simulation for the tick. Each player command contains the canonical movement and weapon-selection/attack fields accepted by Simulation and Networking. TF2 validates game eligibility and emits state/events; it does not decode transport bytes or schedule ticks.
- One positive finite fixed tick interval, simulation time, separate deterministic authority and predicted-presentation random stream identities/restorable states, and one immutable validated snapshot of every consumed item in [`inventories/cvars.md`](inventories/cvars.md).
- One selected TF2 ruleset policy containing only mode-owned decisions: lifecycle phase, spawn permission/timing, class/loadout restrictions, objective eligibility/scoring/completion, overtime, and win conditions. TF2-wide mechanics cannot be overridden through this policy.
- One exact content-build input: configured class records, `scripts/items/items_game.txt`, its signature, `resource/modevents.res`, models, weapon scripts, sounds, particles, and animation metadata resolved through Content with logical path, provider identity, source location, and SHA-256 hash.
- Generic package interfaces: Movement state/results; Collision queries; Physics bodies/contacts; Entity state, I/O, triggers, parenting, and mover requests; Simulation ordering/publication; Networking commands/snapshots; Replay decoded records; Material surface properties; and typed Rendering, Particle, and Audio requests. TF2 never substitutes a second implementation for these packages.
- Valve Source SDK 2013 commit `88fa198fba3fb85d46d4c95018254693fdc3af0a`: TF2 shared, server, and client game code under `src/game/{shared,server,client}/tf`; generic `src/game/shared/econ`; generic teamplay/player/weapon/entity code inherited by TF2; and the exact symbols and paths named by the nine inventories.
- TF2 content build `10822003`: `steam.inf` SHA-256 `b8d7c1eb4517a806d514087facf42e3d8f407bf14393ac5fdc5d4c69e40adc7f`; `gameinfo.txt` SHA-256 `a85196fdeebeb4e2bae9d412862794d18a4970d118ea0a0d84817c44b8c982da`; `scripts/items/items_game.txt` SHA-256 `47900e0d174971625a76625fe311a012910031171d0b121ff5f628078c83214d`; `tf2_misc_dir.vpk` SHA-256 `63f7db0d1c509e303ca9002fee9e3d805e9220ea5afdd639d8a6b68b8a3710b9`; and `tf.fgd` SHA-256 `e8985f85da985c11e22e4a68b8ea5031bf775f3f471a8d5d4db622e1e7204a49`.

## Outputs

- One immutable next TF2 state containing every advanced game-owned field and stable references to unchanged generic state. It is suitable for authoritative publication, prediction storage, deterministic evidence, and replay comparison.
- One ordered TF2 gameplay-event journal: class/team/lifecycle changes; item/loadout/attribute/condition changes; damage/healing/death; ammo and weapon actions; projectile/explosion/impulse facts; building changes; pickup/entity/objective/common-rule facts; and typed ruleset requests. Random outputs separately retain stream context, decision identity, raw draw, accepted result, and post-tick restorable state.
- One canonical TF2 replication source and prediction result containing every accepted logical item in [`inventories/networked-state.md`](inventories/networked-state.md), without network encoding or transport state.
- One ordered game-event and user-message journal with exact typed payloads. Presentation-only messages cannot mutate gameplay state.
- Typed TF2 presentation requests selecting model/skin/bodygroup, animation/viewmodel activity, particle effect, and script sound from authoritative or replay facts. Rendering, Particle, and Audio execute those requests without feeding results back into gameplay.
- A structured failure containing operation, simulation tick, entity/player identity, inventory stable identity, input field or package-query identity, violated invariant, offending value, configured limit, and exactly one `Malformed`, `Unsupported`, `Unknown`, or `Missing` classification. Failure publishes no partial authoritative next state.

## Invariants

- Simulation is the sole gameplay authority. TF2 advances only game-owned state inside an admitted fixed tick; rendering, replay, networking transport, applications, and presentation systems cannot independently advance or repair gameplay.
- Nine and only nine playable class identities are accepted. Team gameplay identities are RED and BLU; unassigned, spectator, auto-assign, Halloween, PvE aliases, Civilian, random, and enum sentinels retain explicit non-playable dispositions.
- Every content-derived lookup binds the configured content build and exact source hash. A changed class record, schema, event file, model, sound, particle, or animation input creates a different evidence target and cannot reuse stale output.
- Item-schema processing preserves source order and duplicate occurrence, resolves prefabs before dependent item definitions, validates cross-references, and never falls back to hardcoded item behavior when a required definition is missing or malformed.
- Attribute application uses one provider graph and one accepted order for local attributes, providers, owner propagation, type conversion, caching, and invalidation. A provider is applied at most once per hook query and cycles fail without partial output.
- Conditions retain numeric identity, source/provider identity, duration or permanent state, add/remove ordering, expiry behavior, prediction state, and presentation facts. Mode-specific grant policy cannot redefine a condition's TF2-wide effect.
- Health, overheal, healing, damage, crit/minicrit, resistance/immunity, death, impulse, and post-damage effects advance in one declared order. Damage randomness consumes only the supplied deterministic random stream and records every draw identity.
- Weapon switching, deployment, holstering, attacks, clips, reserve ammo, reload states, charge states, projectile creation, impact, explosion, deflection, and item meters use simulation time. Render-frame time and animation sampling cannot unlock or delay gameplay actions.
- Collision, movement, rigid-body, mover, trace, and line-of-sight truth come only from the owning generic package snapshot. TF2 selects masks, groups, eligibility, and response policy but cannot perform an alternate mesh ray, hull sweep, or physics solve.
- Building placement, construction, upgrade, repair, damage, sap, carry, redeploy, detonation, and destruction preserve one owner/builder, object identity, object type/mode, upgrade level, health, ammo, and state machine. Sentry, dispenser, and teleporter specializations cannot bypass shared building state.
- TF2 objective entities produce typed primitive state and requests. A mode-specific ruleset exclusively owns lifecycle, objective eligibility beyond the primitive's TF2 contract, scoring, restrictions, overtime, completion, and win decisions.
- Authoritative and predicted execution call the same TF2 transition implementation. Identical prior state, commands, ruleset policy, content identity, package results, cvar snapshot, tick context, and random state produce identical serialized next state and journals under the accepted arithmetic profile.
- Replicated fields are derived from canonical TF2 state once per publication boundary. Demo/replay interpretation maps decoded recorded fields to TF2 meaning and never resimulates missing gameplay.
- Presentation mappings are pure functions of canonical TF2, replay, and immutable content state. Particle lifetime, audio playback, animation interpolation, viewmodel sampling, visibility, and frame rate cannot affect gameplay or replay truth.
- Every inventory identity receives exactly one coverage classification. Unknown, malformed, missing, unsupported, duplicate, owner-conflicting, and excluded discoveries remain visible and cannot be silently dropped.
- Fixed gameplay counts and sentinels come from the accepted inventories and cvar snapshot. Simulation and generic packages own execution-time, memory, queue, entity-count, contact-count, and network backpressure bounds.
- No fallback implementation, compatibility layer, legacy schema branch, duplicate game authority, stale inventory, or ambient installation discovery is permitted.

## Ownership Exclusions

- KeyValues, VPK, StudioModel, BSP, VTF, VMT, PHY, and Demo own format parsing. Content owns logical-path/provider resolution. TF2 owns interpretation of successfully parsed TF2 game content.
- Movement owns generic command movement, hull motion, ground/air/water/ladder/noclip/observer/toss behavior, stepping, and clipping. TF2 owns class/condition/item/weapon movement policy, air dashes, bunnyhop cap, backward clamp, TF2 duck/water differences, charges, grappling hooks, parachutes, rocketpack, karts, ghosts, lost footing, collision groups, and movement events.
- Collision owns geometric queries and trace truth. Physics owns rigid bodies, persistent contacts, constraints, and solver response. Entity owns generic state, I/O, parenting, trigger/touch lifecycle, mover advancement, and rollback publication. TF2 owns game-specific filters, masks, eligibility, damage, and typed responses.
- Simulation owns command admission, fixed-tick scheduling, random-stream provisioning, transition ordering boundary, gameplay publication, and session lifecycle. Networking owns command/snapshot encoding, transport, tables, baselines, deltas, acknowledgement, recipients, and reconciliation mechanics.
- Replay owns recorded-byte decoding and timeline advancement. TF2 owns semantic interpretation of decoded TF2 classes, fields, events, user messages, entities, items, and conditions.
- Rendering, Particle, and Audio own presentation execution. TF2 owns the mapping from TF2 facts to model/skin/bodygroup, animation/viewmodel, particle, and sound requests.
- [`rulesets/ROADMAP.md`](rulesets/ROADMAP.md) owns every mode-only lifecycle, objective, scoring, spawn, restriction, overtime, completion, win, and mode-state transition. TF2 owns reusable mechanics and objective primitives consumed by those rulesets.
- Applications own input bindings, browser UI, HUD layout, menus, settings storage, catalog experiences, and product assembly. Services own hosted processes and transport endpoints. Tempus owns records, courses, rankings, APIs, and product data.
- Store, market, trading, crafting, loot selection, account inventory persistence, matchmaking, tours, contracts/quests, and social progression are excluded from TF2 gameplay advancement. Their schema records remain classified inputs and cannot acquire gameplay meaning implicitly.
- Achievement award logic and bots/AI are excluded because no Active product target declares them as required behavior. Map/entity/item/event fields that mention either remain classified and inert or delegated; adding either target requires a denominator change before implementation.
- Source 2, undeclared games, vehicle driving, NPC locomotion, product-specific camera/UI behavior, legal/publication classification, and content acquisition are excluded.

## Behavior Families

The selected jump-movement checkpoint projects Soldier/Demoman class speed, ordinary 30-unit air cap, 1.2-times bunnyhop cap, 0.9 backward clamp above 100 units/s, standing/crouched hulls and views, 289-unit jump impulse, condition/item scalar inputs, crouch/jump eligibility, and ruleset-supplied noclip permission through one TF2 policy interface consumed by generic Movement. Its fixed `jump_beef` schedule starts at `[5328,3376,-3120]`, yaw 180, uses the browser's 450-unit command magnitude, and retains exact per-tick Movement state for grounded acceleration, jump, release, and repeated air ticks. Browser bindings, console commands, water/ladder behavior, movement abilities, and ruleset validity decisions remain outside this checkpoint.

The SDK-established jump-gameplay checkpoint replaces local combat and mover approximations with content-bound stock weapon state, exact TF2 self-blast damage/force, Entity-owned button damage and game-bound inputs, and typed Collision/Physics/Movement result seams. Custom Tempus run semantics and IVP sticky integration remain separate blocked owners; TF2 emits their complete required inputs without inventing either transition.

The tracked playable-runtime checkpoint replaces that compact local path with one atomic map-session transition over the accepted Entity and Movement interfaces. Its declared output is a bounded current binary contract containing the complete Movement snapshot; Soldier/Demoman team, health, stock clip/reserve, cooldown/reload and accepted loadout state; all 22 `jump_beef` `func_regenerate` zones and three-second touch cooldown; Entity mover transforms and ordered I/O/contact facts for five buttons, four doors, one movelinear, 22 multiple triggers, two hurt triggers and 56 teleports; Jump inputs/outputs; and physical projectile identity, owner, launcher, team, state, quaternion orientation, angular velocity, contact normal and ordered `fire`/`impact`/`stick`/`arm`/`fizzle`/`explode` events. Presentation resource selection and browser application bindings remain consumers and cannot mutate these facts.

The tracked random/audio checkpoint replaces caller-invented sticky values and absent explosion samples with two restorable TF2 stream contexts. Stock launch processing consumes the non-emitting and emitting sound-parameter queries before server sticky spread; client explosion selection consumes volume, pitch, current no-repeat wave rank, and level in Source order. TF2 publishes selected original wave ordinals and normalized parameter samples with exact weapon-single or explosion-special event identities; Audio resolves only the supplied definition/ordinal/parameters and cannot resample from entity identity, hashes, clocks, or process randomness.

The active combat/entity-automation checkpoint corrects the Source `AngleVectors` right basis; preserves stock, crouched, player-flipped, item-flipped and center-fire muzzle policy plus distinct aim and brush-muzzle traces; executes one held-primary decision per admitted tick; separates the honorbound first-primary lock from the next-primary firing deadline; and preserves that deadline through regeneration. Solid rocket results route their direct target through TF2 damage admission into Entity `Damage`, where button output/context/state drives linked doors. Regenerate volumes publish enter/stay/leave facts and exact associated-model animation/body inputs. Entity mover requests and Movement results retain cyclic platform requests, blocked reversals, carry/support facts and endpoint outputs without TF2-owned trajectory math.

The active core-state checkpoint owns one atomic TF2 transition and immutable snapshot for the nine playable classes; RED, BLU, unassigned and spectator player state; the 19 class and three account loadout positions; selected stock item definitions and their complete prefab closure; item instances; the selected core attribute-definition/hook set and provider graph; all 131 condition identities and generic duration/provider lifecycle; ordinary health, overheal, healing, damage, crit, resistance, death and respawn facts; and ordinary map health/ammo, dropped-ammo and dropped-weapon pickup state. Specialized weapon/class/building effects, mode permission and scoring, transport, replay and presentation execution remain downstream owners. [`inventories/core-state.md`](inventories/core-state.md) is the generated bounded inventory; the broader nine candidate inventories remain visible and unaccepted.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Every playable class and exact class-data record has one stable item in [`inventories/classes.md`](inventories/classes.md). | The bounded core-state generator decrypts and validates all nine configured class records, emits their complete parsed class fields and hashes, and joins stock item definitions. The broader inventory is Not accepted. | **Class-inventory regeneration:** regenerate enum/content joins at the pinned revisions; compare identity, order, hashes, fields, exclusions, and exact count. | In progress |
| Every item-schema record and loadout position has one stable item and explicit gameplay disposition in [`inventories/items.md`](inventories/items.md). | The bounded core-state generator selects the 19 class and three account positions plus stock item definitions and their complete prefab closure. The broader 13,396-item inventory remains visible and Not accepted. | **Item-inventory regeneration:** parse the pinned schema and loadout enums; compare all selected records and retained broader counts, then complete every broader section/key/occurrence disposition. | In progress |
| Every attribute definition and TF2 hook identity has one stable item in [`inventories/attributes.md`](inventories/attributes.md). | The bounded core-state generator selects definitions and exact hook identities consumed by class health, healing, damage, crit, resistance and pickups. The broader 1,346-item inventory remains visible and Not accepted. | **Attribute-inventory regeneration:** compare selected IDs/classes/formats and SDK hook sites, then complete all broader definitions and hooks. | In progress |
| Every weapon ID, projectile ID, and gameplay-to-presentation mapping site has one stable item in [`inventories/weapons.md`](inventories/weapons.md). | The 2,344-item inventory is manually derived, has no generator, and is Not accepted. | **Weapon-inventory regeneration:** preprocess ID declarations and request sites; compare numeric order, item/entity joins, mapping identities, fingerprints, and count. | Blocked |
| Every TF2 FGD or runtime entity classname has one stable identity and one TF2/generic/ruleset disposition in [`inventories/entities.md`](inventories/entities.md). | The 352-item lexical/FGD union is manually derived, contains unresolved macro candidates, has no generator, and is Not accepted. | **Entity-inventory regeneration:** preprocess client/server bindings, parse the configured FGD, join inherited owners, compare aliases/duplicates/dispositions, and require no macro artifact. | Blocked |
| TF2 MapRuntime reads each selected integer, float, and boolean entity field through the Source typed accessor selected by its SDK data-map consumer; numeric prefixes are admitted while raw entity bytes and canonical Map identity remain unchanged. | Filter negation/team/class/condition, teleport flags, bodygroup, disabled state, push speed, and catapult speed use the sole KeyValues numeric projection with caller-owned defaults and semantic range checks. Vector fields retain their separate exact structural parser. | Fixed conversion vectors plus exact `filter_activator_tfteam` descriptive `Negated` text produce false, preserve `TeamNum=3`, and compile without a key/entity/map exception; existing strict semantic bounds remain tested. | Ready |
| Every reusable objective primitive, flag type, and game-type routing identity has one stable item in [`inventories/objectives.md`](inventories/objectives.md). | The 32-item inventory is manually derived, has no generator, and is Not accepted. | **Objective-inventory regeneration:** join enums, FGD declarations, runtime bindings, fields, and exact TF2/ruleset ownership; compare all records and count. | Blocked |
| Every runtime TF2 condition value has one stable item in [`inventories/conditions.md`](inventories/conditions.md). | The bounded core-state generator tracks all 131 numeric identities and generic lifecycle, plus the core health/damage semantic set. Specialized effect producers/consumers remain broader candidates, so the inventory is Not accepted. | **Condition-inventory regeneration:** compare numeric order and core lifecycle/effect ownership, then complete every specialized producer/consumer before denominator acceptance. | In progress |
| Every TF2-declared cvar has one stable identity, complete value contract, consumer set, snapshot rule, and sole owner in [`inventories/cvars.md`](inventories/cvars.md). | The 1,278-name inventory is a lexical candidate with unresolved owner/consumer classification, has no generator, and is Not accepted. | **Cvar-inventory regeneration:** preprocess all TF2 targets, merge proven duplicate declarations, compare names/defaults/flags/bounds/callbacks/reads/owners, fingerprint, and count. | Blocked |
| Every TF2 network/prediction declaration, modevent record, and user-message registration has one stable item in [`inventories/networked-state.md`](inventories/networked-state.md). | The 2,318-item inventory is a declaration-level candidate, has no generator, and is Not accepted. | **State-inventory regeneration:** pair preprocessed send/receive fields, expand arrays/tables, join prediction, parse events/messages, and compare types/quantization/owners/fingerprints/count. | Blocked |
| Authority gameplay and predicted presentation use separate explicitly seeded, restorable TF2 random streams; stock launch and explosion sound queries consume volume, pitch, wave and level slots in exact order; no-repeat state selects original configured wave ordinals; and every consumed raw draw has one stable decision identity. | The local producer selects invariant seed 0 by policy or accepts explicit seeds/restored 34-word states; journals context, decision, raw value and typed result; performs both single-wave launch queries before sticky spread; retains independent rocket/sticky explosion availability; and publishes exact weapon-single/explosion-special definition, source, owner and normalized samples. The current WASM/browser snapshot does not publish these new events/states. | Fixed uniform/integer vectors, state restore/rejection, launch-plus-spread transcript, independent six-event no-repeat cycles, atomic mismatch rollback, and selected-ordinal Audio tests pass. | Partial |
| One TF2 transition advances game-owned state from immutable prior state, admitted commands, ruleset policy, package results, cvar snapshot, content identity, tick context, and deterministic random state. | `CoreState::transition` atomically advances the bounded class/loadout/item/attribute/condition/health/damage/death/pickup family and emits one immutable `CoreSnapshot`. Existing Soldier/Demoman weapon/projectile/map execution remains unchanged pending later whole-session composition. | **Whole-tick differential trace:** compare immutable prior/input/next core state, ordered journals, random-crit draws and atomic failures; later compose package transcripts. | In progress |
| Player lifecycle accepts only legal welcome, active, observer, dying, death, and respawn transitions and atomically resets or preserves every declared field. | The bounded transition implements welcome, active, observer and dying state plus admitted team/class selection, stock spawn, lethal damage, ordered cleanup and respawn resets. Disconnect and ruleset timing remain downstream. | **Lifecycle timeline comparison:** replay selection, spawn, death, observer, respawn and invalid-transition sequences; compare every bounded field and event. | In progress |
| RED, BLU, spectator, unassigned, auto-assign, and declared aliases produce exact hostility, collision, visibility, recipient, disguise, objective, and spawn classifications. | Bounded player state retains RED, BLU, spectator and unassigned identities; active spawn requires RED/BLU and damage admission distinguishes self, teammate and enemy. Auto-assign, aliases, recipients, disguises and objectives remain absent. | **Team decision table:** cross every team identity and context; compare enemy/friendly/neutral decisions, masks, recipients, presentation team, and classified rejection exactly. | In progress |
| Class selection validates the nine playable identities and applies class data, model/hands, hull/view policy, speed, health, ammo, stock equipment, buildables, and class-change resets in target order. | The bounded core state implements all nine configured class identities, complete generated class records, stock item instances/loadouts, RED/BLU spawn state and class-change resets; generic Movement still owns hull advancement. | **Class spawn matrix:** spawn/change every class on RED and BLU with fixed stock loadouts; compare bounded state/resources/events field-by-field. | In progress |
| The 19 class and 3 account loadout positions select one valid definition or explicit empty/default state per class, preset, restriction, and content build. | The bounded core state implements immutable selected definitions, four presets, explicit empty slots, class/slot eligibility, atomic equip replacement, stock spawn composition and account-slot state. | **Loadout selection matrix:** cross class, slot, preset, base/custom item, restriction, missing definition and class change; compare selected identities and failures. | In progress |
| Item-schema initialization preserves ordered records, resolves prefab overlays and aliases, validates cross-references/signature/content identity, and atomically publishes one immutable schema. | `ItemSchema::compose` preserves selected source order, applies recursive prefab lists right-to-left, validates duplicate/cycle/reference/content failures, and publishes immutable default/stock item and attribute state atomically; broader schema sections remain candidates. | **Schema semantic comparison:** compare every selected normalized record, inherited field, reference, error and atomic publication decision. | In progress |
| Item instances combine definition, quality, level, style, paint, team/class model, static/runtime attributes, equipped slot, wearable/econ entity state, and gameplay eligibility without account-service authority. | Bounded item instances retain selected definition, quality, level, style, paint, team/class, runtime-over-static attributes and equipped slot; class/slot/level/team failures are atomic and wearable presentation remains downstream. | **Item-instance vectors:** instantiate every selected definition at boundaries; compare canonical state, equip result, providers and classified failures. | In progress |
| Attribute queries apply local values, providers, owner propagation, combination rules, type conversion, item-list output, cache lookup, invalidation, and provision parity in one exact order. | `AttributeGraph` implements selected numeric/string combination rules, local/provider/owner order, active-weapon queries, weapon loop suppression, item-list bypass, cache replacement, recursive invalidation, six-bit parity, graph bounds and provider-cycle failure. | **Attribute-order transcript:** compare fixed selected-hook provider graphs step-by-step; broader 503-hook coverage remains required. | In progress |
| Conditions add, refresh, stack, source-track, expire, remove, trigger callbacks, modify state, and emit presentation facts according to all 131 inventory items. | `ConditionState` implements every numeric bit/provider/duration lifecycle, crit-boost object minimum duration and reported-duration behavior, healing-accelerated debuffs, ordered callbacks and core health/damage effects. Specialized callbacks remain downstream. | **Condition timeline matrix:** replay generic lifecycle for all 131 identities and every core semantic condition; compare words, provider, duration and events. | In progress |
| Base/max health, class/item modifiers, overheal cap, fractional healing, multi-healer state, overheal decay, health-pack scaling, and health-condition transitions produce exact integer health. | `HealthState` implements every class, selected player/active-item modifiers, five-point-rounded overheal, bounded fractional multi-healer accumulation, combat ramp, healing debuff, decay and overheal condition edges. Disguise health remains specialized. | **Health timeline comparison:** cross classes, modifier boundaries, healer counts, pack sizes, damage interruption and decay ticks; compare integer/fraction/condition/events. | In progress |
| Direct, radius, passive, on-hit, dispenser, payload, medigun, health-pack, and regeneration healing validate targets and apply rate, cap, debuff, received-healing, and attribution rules once. | The bounded transition implements direct, sustained player/dispenser and health-pack healing with target/team/lifecycle admission, rate/cap/debuff/active-weapon received-healing and attribution. Specialized medigun charge, payload and weapon effects remain downstream. | **Healing-source matrix:** compare every bounded source/target/team/condition/attribute branch and rejection. | In progress |
| Damage admission resolves attacker, inflictor, weapon, victim, team/friendly-fire, self-damage, world, trigger, crush, fall, drowning, building, condition, and invulnerability eligibility before mutation. | The bounded transition implements player/world/trigger source identity, weapon-position identity, self/friendly/enemy admission, force-friendly-fire, active/alive state, phase, invulnerability and typed immunity. Building policy remains downstream. | **Damage-admission decision table:** exhaust bounded source/victim relationships and blockers; compare reason, normalized identity and atomic failure. | In progress |
| Accepted damage applies range/direct-hit, hitgroup/headshot/backstab, weapon/item/condition modifiers, bonus/base split, resistance, crit flags, integer/fraction handling, force, health mutation, post-hit effects, and events in target order. | `apply_damage` implements caller-established base/range facts, core active-weapon/player attributes and conditions, pre-resistance base/bonus split, resistance/immunity, nearest-integer health subtraction, force facts, prevent-death and ordered stages. Specialized hit tests remain weapon-family owners. | **Damage-stage transcript:** compare every bounded intermediate, order, force fact, condition, health, death and event. | In progress |
| Random crit eligibility, rapid-fire buckets, observed-damage multiplier, melee chance, deterministic rolls, guaranteed crit/minicrit sources, conversions, and no-crit states produce exact critical classification and damage. | Per-weapon `CritState` implements supplied 0–9999 draws, ranged/rapid/melee chances, two-second rapid periods, quantized damage-history multiplier, observed-ratio gate, token bucket, guaranteed conditions and active-weapon crit/minicrit conversion. | **Crit sequence comparison:** compare fixed histories/draws across bounded weapon facts, conditions, attributes and cvars. | In progress |
| Bullet, blast, fire, melee, sentry, crit, active-weapon, condition, medigun, rune, and attribute resistances/immunities modify base/bonus damage, force, sounds, and conditions in exact precedence. | The bounded damage stages implement bullet/blast/fire/melee/crit/general player and active-weapon attributes, defense conditions, bullet/blast/fire immunity and invulnerability while retaining admitted force. Sentry, medigun and rune specializations remain downstream. | **Resistance precedence matrix:** cross every bounded source and overlapping defense; compare base/bonus, blocked flags, damage/force and events. | In progress |
| Lethal damage produces one death state, scorer/assister and custom-kill facts, weapon/item drops, ammo pack, ragdoll/gib or special death mapping, condition/building cleanup, and respawn eligibility. | The bounded transition emits one dying edge, attacker/condition-assister/custom facts, ordinary dropped weapon/ammo state, blast/crit/overkill corpse facts, death-before-drop-before-condition-cleanup order and admitted respawn. Building and special-death systems remain downstream. | **Death-result comparison:** compare bounded lethal attribution, cleanup, drops, corpse request, events and lifecycle. | In progress |
| Class reserves, clips, metal, grenade slots, pickup fractions, dropped packs, dispenser/resupply grants, denied types, caps, and empty/full behavior update exact ammo state once. | The bounded core state supplies all nine class primary/secondary/metal/grenade maxima plus small/medium/full and death-dropped pickup grants/caps. The existing direct-map producer retains Rocket Launcher/Original 4/20 and Stickybomb Launcher 8/24 clip/reserve, fire, reload, respawn and resupply. Specialized dispenser grants and remaining weapon clip families remain absent. | Class/ammo/pickup matrices plus fixed profile, fire, reload interruption, respawn and resupply matrices pass; exact `jump_beef` compilation registers all 22 regenerate volumes. | Partial |
| Weapon selection, last-weapon state, switch eligibility, holster, deploy, visibility, attack lockout, item-provider changes, and switch timing advance atomically. | The local accepted loadout retains per-weapon Rocket Launcher, Original, or Stickybomb Launcher resource/timer state; class and accepted selection switch active state atomically. Last weapon, holster/deploy timing, item providers and restrictions remain absent. | Command/snapshot contract and class/weapon selection tests pass. | Partial |
| Primary/secondary attack admission consumes exact buttons, edge/hold state, fire mode, clip/reserve cost, cooldown, burst/charge/spin state, recoil/spread state, and first-execution policy. | Stock rockets test held primary once per admitted tick and set the next-primary deadline from the accepted shot time plus the configured 0.8 seconds. Regeneration refills stock resources without delaying that deadline; the one-second shared first-primary field remains an honorbound switch lock, not a stock firing gate. Deploy after class/weapon/spawn transitions applies the stock 0.5-second attack lock. Stock stickies begin charge on press, release at button-up or four seconds, remap 900–2400 launch speed, consume two `[-10,10)` velocity draws then one inclusive `[-1200,1200]` angular draw after launch sound queries, and enforce configured 0.6-second cadence. Primary fire aborts single reload; secondary fire tests each owned sticky against its SDK live time. Other weapons remain absent. | Held-command batches cross exact due ticks, resupply deadlines, auto/manual reload, class/weapon deployment, death and respawn; existing sticky stream/charge/detonation matrices remain byte-stable. | Partial |
| Magazine, single-round, animation-event, overload, auto-fire, abort, and regenerate reload paths preserve exact phase, ammo transfer, timers, interruption, and activity output. | Stock launchers retain ready, start, insert and finish phases; emit draw/fire/reload-start/reload-loop/reload-finish activities; use exact configured model durations for stock viewmodels and script fallback for Original replacement activities; transfer one round after each loop; auto-reload when eligible; and abort on fire, holster, respawn or resupply. Overload and other reload families remain absent. | Four-phase timelines cross `due-1`/`due`, full/empty reserve, auto/manual start, fire interruption, charge holster and regeneration. | Partial |
| Hitscan weapons produce exact shot count, deterministic spread pattern/random draws, range, masks, lag-compensation request, penetration, hitgroups, tracer, impact, and damage requests. | No TF2 hitscan behavior exists. | **Hitscan transcript comparison:** fixed shooter/target scenes across all hitscan families compare rays, draws, hit order, damage identities, penetration, and presentation requests. | Not started |
| Melee weapons produce exact swing/smack timing, hull/line query sequence, range/bounds, target selection, cleave, back/side tests, custom effects, damage, and miss behavior. | No TF2 melee behavior exists. | **Melee trace timeline:** controlled range/angle/entity scenes for every melee family compare ordered queries, selected targets, timing, damage/custom identity, conditions, and events. | Not started |
| Common projectile creation owns shooter/launcher/team/weapon/item identity, spawn transform, velocity, gravity/drag, collision group/mask, lifetime, deflection count/owner, water/surface response, and removal facts. | The TF2 producer retains stable owner/launcher/team identity and emits typed point-rocket trace requests plus `[-2,2]`, gravity 0.4, friction 0.2, elasticity 0.45 sticky-body create/step/disable/destroy requests. Each flying sticky requires one same-order accepted result for the preceding create/step request before its transform, age, arm state, or game decision advances. Missing, stale, duplicate, reordered, unsolicited, non-finite, non-unit-orientation, and motion-disabled flying results fail atomically. Launch random state is internal and restorable. The result cannot yet represent multiple contacts, sleep/wake, enable motion, or force-at-point. | Atomic request/result tests cover missing/stale/duplicate/reordered/unsolicited/malformed results, rollback, exact request order, flying and stuck transitions, and ordered fire/impact/stick/arm/fizzle/explode facts. | Partial |
| Rocket and sentry-rocket families implement muzzle correction, aim, speed, direct impact, radius damage request, deflection, lifetime, and item/attribute variants. | Stock Rocket Launcher uses the Source forward/right/up basis and `(23.5,12,-3)` or crouched Z 8 muzzle offset; item/player viewmodel flip negates lateral offset and Original's configured center-fire attribute sets it to zero. Both use the 2,000-unit `MASK_SOLID` aim trace, `MASK_SOLID_BRUSHONLY` eye-to-muzzle trace, 1,100 speed, velocity-derived source orientation and typed sky/solid/direct-target result. The browser Collision adapter preserves a hit Entity object's source identity as the direct target. Accepted impact pulls one unit from the plane, emits impact then explosion, publishes radius damage, and routes an admitted direct non-player target into Entity damage. Existing authored model orientation remains a presentation input and is never rewritten by a model-name branch. Deflection, water, sentry and non-target variants remain absent. | Fixed signed-right vectors reject the mirrored origin; standing/crouched/flipped/center-fire and both trace masks, near-wall event order, sky removal, Collision object identity, direct Entity target and source quaternion vectors pass. | Partial |
| Grenade, pipebomb, sticky, cannonball, practice, ornament, and sticky-ball families implement charge/spread, VPhysics launch, contact/bounce/stick, arming, limit, remote detonation, fizzle, and variant damage. | Stock sticky gameplay owns charge, exact muzzle hull rejection, deterministic launch-stream ordering/values, creation/think/live timing, eight-bomb oldest detonation scheduling, remote detonation, owner cleanup and world/dynamic-prop stick policy. Arm and team-pulse eligibility occur at stock live time while flying or stuck; an airborne arm has no contact normal. Rejected-surface normals remain impact facts and do not become retained sticky contact. IVP alone owns solved bounce, sleep and body transforms through the typed Physics seam; TF2 contains no substitute integration. Shot displacement/re-stick and pipes/variants remain blocked on expanded Physics inputs/results. | Charge, stream restore/order, muzzle-start-solid, strict body request/result, flying-arm, rejected-surface bounce, world/dynamic contact, arm, limit, detonate and cleanup matrices pass; exact IVP and displacement traces remain required. | Partial |
| Explosions select origin, radius/falloff, direct target, occlusion traces, target order, self-damage, damage type/custom identity, force, and effect requests exactly once. | Rocket 90/146/121 and sticky 120/146/146 profiles emit one typed radius request, use direct-hit distance zero, player min(origin, center) distance, half falloff, sticky self 0.75, airborne Soldier self 0.60, integer health rounding and occlusion admission. External candidate enumeration remains Collision/Simulation work. | Direct/splash/self matrices cross center/origin distance, radius edge/outside, visibility, class and ground state. | Partial |
| Damage, airblast, weapon abilities, movement, buildings, triggers, and movers produce exact impulse/knockback, ground detachment, support-velocity inheritance, blast-jump state, and landing/fall consequences. | Self blast applies health before impulse; computes `damageForForce × standingVolume/currentVolume × scale`, substitutes crouched Z 55, caps at 1000, uses Soldier ground/air scales 5/10 or Demo 9, derives direction from lowered inflictor center, sets blast-jump state, and adds velocity without prematurely clearing ground. Other sources remain absent. | Standing/crouched × grounded/airborne × Soldier/Demo fixed-bit and integration matrices pass. | Partial |
| Flamethrower, flame entity/manager, afterburn, Dragon's Fury, flare ignition, water extinguish, compression blast, extinguish, projectile deflection, and airblast movement effects preserve exact timing and ownership. | No flame, afterburn, extinguish, or airblast behavior exists. | **Fire-family timeline:** replay fixed flame samples, targets, water, airblast cones, projectiles, conditions, and attributes; compare state, damage, deflection, impulses, ammo, and effects. | Not started |
| Jarate, milk, gas, cleaver, lunchbox, drink, throwable, brick, bread monster, buff item, and powerup-bottle families implement throw/use, recharge, impact, condition, heal/ammo, team, and pickup effects. | No throwable or consumable family exists. | **Consumable-family matrix:** execute every accepted item family at charge/team/target/condition boundaries; compare projectile/use state, resource cost, conditions, effects, and cooldown. | Not started |
| Bows, bolts, syringes, flares, energy balls/rings, mechanical-arm orbs, charged SMG, sniper charge, minigun spin, particle cannon, rescue bolts, grappling hook, and rocketpack preserve their distinct charge, flight/hitscan, penetration, meter, and effect contracts. | No precision, energy, charge, grappling, or rocketpack weapon behavior exists. | **Special-weapon family vectors:** one fixed vector set per accepted family compares all family-specific state, queries, damage/healing, resources, timers, conditions, and events. | Not started |
| Rage, hype, heads, shield charge, focus, cloak, charge meters, item meters, revenge crits, frontier-justice state, and class/item ability resources gain, decay, spend, reset, and replicate exactly. | No TF2 meter or ability-resource state exists. | **Meter ledger comparison:** cross gain sources, caps, decay, spend, holster/death/resupply/class change, and prediction; compare values, timers, conditions, and effects per tick. | Not started |
| Spy cloak watches consume/regenerate meter, activate/fade/blink, apply speed/damage/pickup/attack restrictions, feign death, decloak, reveal, and presentation mappings exactly. | No cloak, watch, stealth, or feign-death behavior exists. | **Cloak timeline comparison:** replay each watch/item/condition with movement, damage, pickups, attacks, water, and death; compare meter, conditions, visibility facts, restrictions, and sounds. | Not started |
| Spy disguise selection, timing, target/team/class, model/skin/weapon, fake health/overheal, movement cap, action breaks, reveal, and removal preserve exact real and apparent state. | No disguise or apparent-state behavior exists. | **Disguise state timeline:** cross target team/class, disguise target availability, heal/damage, weapon switch, attacks, cloak, resupply, death, and prediction; compare real/apparent fields and events. | Not started |
| Backstab eligibility/damage, knife variants, sapper placement/attachment, building disable/damage, removal, paired-teleporter behavior, and Spy attribution execute in target order. | No backstab, knife-specialization, or sapper behavior exists. | **Spy attack matrix:** controlled angle/range/building/teleporter/item scenes compare trace/eligibility, custom damage, sapper state, building response, attribution, and effects. | Not started |
| Mediguns acquire/retain targets, validate line/range/team, heal/overheal, build and preserve charge, deploy each charge/resist type, share conditions, mirror eligible movement, detach, and replicate exactly. | No medigun, charge, resist-cycle, or heal-target state exists. | **Medigun timeline comparison:** replay all medigun definitions across targets, obstructions, rates, charge boundaries, deploy/holster/death, resist cycling, and prediction; compare all state/events. | Not started |
| Shared buildings validate type/mode/placement, spend/refund metal, construct, upgrade, repair, take damage, sap, disable, carry, redeploy, detonate, destroy, drop gibs, and notify builder/team. | No TF2 building state machine exists. | **Building lifecycle trace:** for every object type/mode, replay placement through destruction with fixed collisions, metal, damage, sap, carry, and owner changes; compare complete state and events. | Not started |
| Sentries acquire valid targets, rotate, track, lose targets, consume shells/rockets, fire levels/mini/controlled variants, apply attributes, and preserve upgrade/ammo/shield state. | No sentry state or targeting/firing behavior exists. | **Sentry controlled-scene comparison:** fixed target sets, visibility, disguises/cloak, ranges, levels, ammo, wrangler/shield, and sappers compare target decisions, angles, shots, damage, and events. | Not started |
| Dispensers construct a touch volume, select eligible recipients, heal, refill ammo/metal/cloak, generate/store metal, apply radius/rate/denial attributes, and stop effects on separation/disable/destruction. | No dispenser state, touch volume, or grant behavior exists. | **Dispenser recipient timeline:** cross teams, disguise/cloak, player/building state, range, level, stored metal, attributes, sap, and separation; compare grants, timers, recipients, and effects. | Not started |
| Teleporter entrance/exit pairing, direction/mode, placement, matching upgrades/sappers, recharge, queue/reservation, eligibility, teleport transform, velocity, condition, and destruction remain synchronized under one pair identity. | No teleporter pair, queue, recharge, or teleport behavior exists. | **Teleporter pair timeline:** replay build order, pairing, upgrade, sap, queue, blocked exit, eligible/ineligible users, teleport, carry/redeploy, and destruction; compare both objects and player output. | Not started |
| TF2 movement specializes generic Movement with class/item/condition speed, air dashes, bunnyhop cap, backward clamp, duck/water differences, disabled ladders, charges, grappling, parachutes, rocketpack, karts, ghosts, lost footing, and collision policy. | One TF2 policy projects Soldier 240/Demoman 280 speed, separate condition/item speed and jump factors, 30-unit air control, 1.2-times bunnyhop cap, 0.9 backward clamp above 100, 82/62 hulls, 68/45 views, 0.2-second duck/unduck, 289 jump impulse, surface inputs, eligibility, high-first steps, and ruleset-approved noclip into one Movement transition. The current command carries up/speed/mode request, and every snapshot carries the complete 96-byte Movement state. Air dashes, water differences, abilities, special conditions, and complete collision policy remain absent. | Policy-boundary and atomic mode tests pass; browser codec validates every Movement field. Exact accepted-spawn schedules retain target grounded/jump/air values. | Partial |
| Spawn selection, class/team eligibility, hull clearance, round/ruleset permission, respawn waves/override inputs, observer/death timing, and spawn-state reset produce one exact respawn result. | The bounded core transition requires RED/BLU plus one of nine desired classes and atomically restores class health/ammo/stock loadout/attributes/conditions on admitted spawn. The direct-map session retains its selected descriptor, explicit Dying/projectile cleanup and Movement reset. Hull clearance, ruleset waves and alternate selection remain absent. | All class/team stock spawn vectors plus death/duplicate-death/explicit-respawn/class-change cleanup timelines and exact selected `jump_beef` spawn evidence pass. | Partial |
| Spawn rooms, visualizers, forcefields, change-class volumes, regeneration/resupply, no-build/no-grenade volumes, and forced-respawn entities apply exact team/class/filter and touch behavior. | The registry owns `func_respawnroom` edge-counted contacts, Source team 2/3 identity and the exact visualizer team-contents collision predicate. All 22 `func_regenerate` volumes accept game-bound Enable/Disable/Toggle, publish geometric enter/stay/leave, apply TF-player/taunt/team-win/stalemate policy and one shared three-second player cooldown, restore exact accepted health/ammo/clip/reload/condition state, preserve active next-primary cadence, update the honorbound first-primary lock, and publish the first matching associated dynamic prop's initial packed body plus `open`/two-second `close` animation facts. Other spawn-area classes remain absent. | Team/class/filter, enter/stay/leave, cooldown, denied-state, high condition-word, held-fire cadence, strict dynamic-prop association, activity/body timing and exact 22-volume matrices pass. | Partial |
| Dropped weapons, ammo packs, health kits, currency/powerup/bonus pickups, pickup eligibility, owner/team locks, lifetime, collision, and respawn update state and presentation exactly once. | `Pickup` implements small/medium/full map health/ammo, ten-second materialization, 30-second death-dropped ammo/weapon state, owner delay, three-pack cleanup, dropped-weapon duplicate/limit behavior, class/team/alive eligibility and exact grants. Currency/powerup/bonus behavior remains downstream. | **Pickup lifecycle comparison:** compare every bounded size/kind/state/team/owner/full-empty branch, grants, leftovers and events. | In progress |
| Every TF2 entity/filter/trigger inventory item consumes generic Entity state/I/O/touch facts and applies only its classified TF2 key, input, output, filter, damage, collision, and presentation behavior. | Entity accepts a selected game binding registry and emits typed external inputs. TF2 routes admitted rocket direct targets into Entity `Damage`; buttons fire `OnDamaged` before health/flag/state/attacker/lock admission, then `OnPressed` and their mover request. Linked door `Open` actions execute through the same ordered queue. Doors/buttons/movelinear consume external Movement progress/completion/block results; replacement cyclic/reversal requests survive until consumed; endpoint outputs retain class-specific activator/caller context. Hurt owns pulse/end-touch state; TF team/class/condition filters fail closed; teleport supports target order, landmarks, complete angles and preserved velocity; no game adapter advances a mover trajectory. The complete inventory remains absent. | Rocket-to-button-to-door ordering, legacy-health/missing-attacker admission, touch/use/lock/state, cyclic platform, blocked reversal, carry/support, endpoint context, external input, filter, hurt, landmark/preserve-angle and exact configured count matrices pass. | Partial |
| Every current non-world BSP brush model crosses the selected-game publication boundary as the immutable, source-ordered Entity record for one expected Entity revision and the exact Collision snapshot revision used by that gameplay tick. Source identity, selected-game registry identity, Entity tick/revision, handle, source ordinal, model, local/world transform, parent, render mode/color/FX/effects, draw admission, and complete mover state remain Entity-owned. | `Session::entity_presentation` reads the current Collision snapshot revision before and after delegating the expected Entity revision directly to `EntityWorld::brush_model_presentation`. It returns the unchanged generic batch plus the joined Collision revision; unavailable or mismatched revisions return no batch and mutate no game state. TF2's selected-game registry marks regenerate and respawn-room brush volumes hidden inside Entity authority. | Synthetic visible/hidden/render/hierarchy/model/mover/limit/revision/restore/rollback vectors pass. Exact configured `jump_beef` evidence publishes 122 source-ordered records, 17 drawn brushes, all four doors/five buttons/one linear mover, and 105 hidden trigger/regenerate/respawn-room volumes. | Ready |
| Flag, control-point, capture-area, train-watcher, cart-dispenser, timer, objective-resource, round-win, and intermission primitives produce exact reusable TF2 state/facts without embedding one mode's score or completion policy. | No TF2 objective primitive or objective-resource state exists. | **Objective primitive traces:** controlled carrier/capper/train/timer/team sequences compare primitive fields, replication, events, entity I/O, ruleset requests, and absence of embedded mode decisions. | Not started |
| TF2-wide game rules own common team/class/item/damage/death/respawn/entity policy and legal round-state inputs/outputs while consuming, not replacing, generic teamplay and selected ruleset decisions. | No TF2 common game-rule state or transition exists. | **Common-rule state trace:** run fixed pre-round/active/team-win/stalemate/intermission inputs across selected rulesets; compare only TF2-wide state, decisions, events, and delegated requests. | Not started |
| Every mode-only game type, logic entity, objective transition, scoring rule, spawn rule, restriction, overtime rule, completion, and win decision resolves to one item in [`rulesets/ROADMAP.md`](rulesets/ROADMAP.md). | The TF2 ruleset-universe roadmap and accepted ruleset inventory do not exist, so mode-only ownership and integration cannot be closed. | **Ruleset ownership audit:** compare all mode-routed entities, cvars, events, objectives, and game-rule branches against an accepted ruleset inventory; require one owner and no TF2-game implementation. | Blocked |
| Prediction replays TF2 class/item/attribute/condition/health/ammo/weapon/projectile/building/movement state through the same transition and restores/reconciles every accepted prediction field. | No TF2 prediction state, adapter, history, or reconciliation integration exists. | **Authority/prediction byte comparison:** execute every gameplay vector authoritatively and predicted, then replay under correction; compare serialized state, journals, random state, package transcripts, and restored fields. | Not started |
| TF2 canonical state produces every accepted replicated logical field, and decoded demo state maps every recorded TF2 field/entity/event to canonical replay meaning without gameplay resimulation. | No TF2 replication source or replay interpretation exists; Networking and Replay interfaces and inventories are Not accepted. | **Replication/replay field audit:** compare target snapshots and fixed demos with playsrc logical fields and replay state at each tick; require exact field/proxy/event meaning and one generic transport/decoder. | Blocked |
| Every accepted TF2 modevent and user message has exact typed payload construction, producer order, recipient/locality, gameplay or presentation disposition, and consumer contract. | No TF2 event or user-message schema, producer, or consumer integration exists. | **Event/message transcript comparison:** run fixed compound ticks and demos; compare ordered names, duplicate occurrence handling, payload fields/bytes, recipients, consumer effects, and classified unsupported messages. | Not started |
| TF2 player/item/weapon/projectile/building/entity/objective facts map to exact model/skin/bodygroup, animation/viewmodel, particle, and script-sound requests while presentation execution remains read-only. | The local stock rocket/Original/sticky producer publishes typed weapon-single and explosion-special sound events with exact configured definitions, source/owner, original wave ordinal and normalized parameter samples. The complete mapping inventory, binary publication and non-projectile families remain absent. | Fixed local launch/explosion stream transcripts and Audio selected-ordinal/channel/volume/pitch/level/resource tests pass; complete presentation differential remains required. | Partial |
| Every numeric input, random draw, timer, count, enum, bitset, content reference, package result, and intermediate has an accepted arithmetic/tolerance contract and exact malformed/unsupported/unknown/missing outcome. | No accepted TF2 arithmetic profile, platform set, correction policy, per-field tolerance table, or structured failure contract exists. | **Boundary/fault matrix:** test adjacent values at every inventory bound, non-finite/overflow inputs, unknown IDs/bits, missing content/entities, package failures, random-stream exhaustion, and no-partial-output behavior. | Blocked |
| Simulation, Movement, Physics, Entity, Collision, Networking, Replay, Content, presentation packages, selected TF2 rulesets, applications, tools, and inspectors consume one current TF2 interface with no duplicate game logic. | Required generic package denominators and the ruleset universe are not accepted, and no TF2 interface or producer/consumer integration exists. | **Current-interface integration audit:** exercise every named producer/consumer through one interface and repository-audit for duplicate TF2 state transitions, fallbacks, compatibility layers, legacy paths, or stale inventories. | Blocked |

## Generated Inventories

No generated TF2 inventory is accepted. Accepted inventory item count: 0.

| Output | Authority identity | Authority revision | Generator command | Candidate items | Accepted items |
|---|---|---|---|---:|---:|
| [`inventories/classes.md`](inventories/classes.md) | SDK playable-class enum/parser plus configured class records | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003`; per-record hashes in output | Missing | 9 | 0 |
| [`inventories/items.md`](inventories/items.md) | SDK TF2/economy schema contracts plus configured `items_game.txt` and loadout enums | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003`; schema hash in output | Missing | 13,396 | 0 |
| [`inventories/attributes.md`](inventories/attributes.md) | Configured attribute definitions plus SDK TF2 hook/provider consumers | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003` | Missing | 1,346 | 0 |
| [`inventories/weapons.md`](inventories/weapons.md) | SDK weapon/projectile IDs, implementations, item joins, and presentation mapping sites | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003` | Missing | 2,344 | 0 |
| [`inventories/entities.md`](inventories/entities.md) | Configured `tf.fgd` plus SDK TF2 runtime bindings and generic inheritance seams | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003`; FGD hash in output | Missing | 352 | 0 |
| [`inventories/objectives.md`](inventories/objectives.md) | SDK objective/flag/game-type declarations plus configured objective FGD classes | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003` | Missing | 32 | 0 |
| [`inventories/conditions.md`](inventories/conditions.md) | SDK condition enum and all TF2 producers/consumers | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a` | Missing | 131 | 0 |
| [`inventories/cvars.md`](inventories/cvars.md) | Preprocessed SDK TF2 shared/server/client cvar declarations and consumers | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a` | Missing | 1,278 | 0 |
| [`inventories/networked-state.md`](inventories/networked-state.md) | SDK TF2 network/prediction declarations and user messages plus configured modevents | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003`; modevents hash in output | Missing | 2,318 | 0 |
| [`inventories/core-state.md`](inventories/core-state.md) | Bounded SDK class/loadout/schema/attribute/condition/health/damage/death/pickup contracts plus configured class records and `items_game.txt` | SDK `88fa198fba3fb85d46d4c95018254693fdc3af0a`; TF2 build `10822003`; exact hashes in output | `cargo run --locked --manifest-path games/tf2/rust/inventory-generator/Cargo.toml` | 322 | 0 |

Current manually derived broader candidate count: 21,206. Current bounded generated count: 322. Current accepted generated count: 0.

The bounded core-state generator consumes the pinned SDK checkout at the declared source-cache identity plus configured TF2 content and emits [`inventories/core-state.md`](inventories/core-state.md) atomically. The future broader generator is owned by `tools/playsrc`; it must consume a retained manifest pinning the SDK revision, configured TF2 content build, exact logical inputs/hashes, VPK index, FGD, and current Owner Registry; emit all nine broader outputs atomically in stable-identity order; retain every non-Handled classification and exclusion; and fail on a changed authority, duplicate identity, unresolved owner/reference, malformed record, unclassified discovery, stale output, or count mismatch.

## Exit Criteria

The TF2 game module is Complete only when all of these predicates pass:

- All 64 behavior rows are Ready.
- All nine inventories have an Accepted denominator review with current authority revisions, generator command, output path, exact item count, reviewer identity, review date, and reviewed commit.
- Every candidate and subsequently discovered class, schema record, item, loadout slot, attribute, hook, weapon, projectile, presentation mapping, entity, objective, condition, cvar, network/prediction field, event, and user message has one accepted stable identity, sole owner, and coverage classification.
- Every fixed authoritative gameplay vector matches target next state, package transcript, random draws, events, failures, and presentation requests under its declared comparison predicate.
- Authoritative and predicted execution use the same TF2 transition and produce accepted identical output for identical inputs; correction/replay count cannot change the result.
- Fixed TF2 demos map every accepted recorded field, entity, event, and user message to canonical replay meaning without gameplay resimulation.
- The accepted TF2 ruleset universe owns every mode-only branch, and TF2 objective/game-rule code contains no embedded one-mode lifecycle, scoring, restriction, overtime, completion, or win decision.
- All generic package producers and consumers use their current interfaces; all rulesets, applications, tools, and inspectors consume one current TF2 interface with no duplicate authority.
- Presentation packages consume typed TF2 requests and cannot mutate gameplay or replay state.
- The accepted arithmetic profile, platform set, random contract, correction policy, field-specific tolerances, and structured failure contract are retained with evidence.
- No required item remains Blocked, Unsupported, Unknown, Missing, Malformed, Partial, stale, duplicated, owner-conflicting, or dependent on a fallback, compatibility layer, or legacy path.

## Blockers

- **Broader inventory generator:** no checked-in command emits any of the nine broader assigned inventories. The bounded core-state generator does not claim broader coverage. The broader candidate outputs are Not accepted and contribute zero inventory items to the denominator.
- **Denominator review:** no review record supplies `Accepted`, reviewer identity, review date, reviewed commit, or passing results for the eight review predicates. The candidate cvar owner/consumer set, network logical-field merge, entity macro preprocessing, item-schema dispositions, and presentation mapping expansion cannot pass finite-authority review before generation.
- **Ruleset universe:** [`rulesets/ROADMAP.md`](rulesets/ROADMAP.md) and its accepted mode inventory do not exist. Mode logic, objective transitions, scoring, spawning, restrictions, overtime, completion, and win ownership cannot be closed. Checked the root roadmap, TF2/ruleset READMEs, SDK game-type/logic-entity branches, and the assigned objective/entity/cvar/event candidates.
- **Generic interfaces:** Movement, Physics, Simulation, Networking, Replay, Entity, Collision, Content, Material, and format-package denominators are Not accepted; Rendering, Particle, and Audio leaf roadmaps are absent. Exact integrated package inputs/outputs, replication/replay seams, and presentation consumers cannot be completed. Checked the root roadmap and every named package roadmap/README.
- **Arithmetic and comparison policy:** no accepted decision names binary arithmetic boundaries, fused-operation policy, deterministic random algorithm/state serialization, supported platform set, target-reachable correction behavior, or per-field exact/ULP/absolute tolerances for TF2 state. Checked the roadmap contract, Terminology, SDK declarations named in Inputs, and current package roadmaps.
