# playsrc Tool Roadmap

[`../../docs/roadmap-contract.md`](../../docs/roadmap-contract.md) defines evidence and denominator-review requirements. [`../../TERMINOLOGY.md`](../../TERMINOLOGY.md) defines Tool, logical path, stale artifact, work directory, channel, bound, backpressure, current contract, and Complete.

## Completion Denominator

The completion denominator contains exactly the 22 Behavior Families rows below. The supported command surface contains exactly these 10 commands:

```text
bun run setup
bun run inventory <owner>
bun run compile <map>
bun run dev <map>
bun run export-glb <map>
bun run verify <evidence-target>
bun run publish <root>
bun run deploy <application-build>
bun run release <release>
bun run rollback <release>
```

Each parameter is one stable logical identity from an accepted owner registry. No command accepts a routine machine path, executable path, output root, dependency pin, content-build pin, texture limit, execution limit, retry policy, or concurrency policy. The denominator is Not accepted because required owner interfaces and operation manifests are unavailable and no review record exists.

## Inputs

- Repository-root `playsrc.local.json`, encoded as UTF-8 JSON and containing exactly three non-empty absolute native paths: `tf2Dir`, `sourceCacheDir`, and `assetDir`. Environment variables and command arguments cannot replace or override a field.
- One checked `ToolExecutionPolicy` containing positive integer limits for queued operations, concurrent compilation jobs, concurrent verification jobs, concurrent immutable uploads, owned processes, per-process captured bytes, diagnostics, report steps, graceful-shutdown duration, forced-shutdown duration, retries, and total operation duration. No command supplies or overrides these values.
- One checked dependency and toolchain manifest with immutable runtime, package-lock, compiler, and external-tool identities plus their supported host triples. Setup consumes the manifest and does not discover tools on `PATH` as a substitute for a missing declared identity.
- The current root Owner Registry plus accepted machine-readable operation, target, inventory, evidence, application-build, environment-binding, and release manifests from their sole owners.
- Exact owner interfaces: Content for provider plans and logical-path resolution; format parsers; Map preparation and finalization; Material, StudioModel, Entity, Collision, and Visibility semantic producers; game and ruleset compilers; Asset Store object, root, validation, synchronization, channel, and rollback operations; application build/deployment operations; infrastructure-provided adapters; and owner-defined evidence operations.
- Accepted application and environment lifecycle seams: web product state from `Booting` through `Ready` or a terminal state; service process state from `Configuring` through `Ready`, `Draining`, and a terminal state plus `GET /readyz`; and Infrastructure validation, read-only plan, approved-plan apply, binding, verification, drift, and teardown results. The tool observes and sequences these operations but never recreates their state machines.
- One cancellation signal propagated to every invoked owner operation and every owned child process.

## Outputs

- One final `ToolOperationReport` written as a single JSON object to standard output. Progress and child logs use standard error. The report contains command, logical target, operation identity, status (`succeeded`, `rejected`, `failed`, or `cancelled`), started and finished timestamps, duration, ordered steps, owner/interface identities, input and output hashes, artifact or deployment identities, process outcomes, channel precondition/result when applicable, bounded diagnostics, cancellation boundary, and cleanup result.
- Every step contains exactly owner, operation, sequence, status, input identities, output identities, duration, and one nested owner result or error. Step order is execution order, never asynchronous completion order.
- Success exit code `0`; malformed invocation, configuration, or target exit code `2`; unavailable required owner or dependency exit code `3`; operation, verification, publication, deployment, channel, or cleanup failure exit code `4`; and cancellation exit code `5`.
- Failures use exactly `MalformedInvocation`, `ConfigurationMissing`, `ConfigurationMalformed`, `ConfiguredRootUnavailable`, `TargetMalformed`, `TargetMissing`, `OwnerUnavailable`, `OwnerRejected`, `ProcessFailure`, `PreconditionFailed`, `BoundExceeded`, `Cancelled`, `CleanupFailure`, or `IoFailure`. A nested owner error retains its owner-defined kind and fields unchanged.
- Reports identify configured roots by `tf2Dir`, `sourceCacheDir`, or `assetDir` plus a contained relative path. They contain no credential, raw Source byte, absolute machine path, or command line carrying a secret.

## Invariants

- Every command resolves the repository root from the checked executable location, reads the one repository-root local configuration, validates invocation and configuration before other work, and produces the same invariant outputs from every caller working directory.
- Configuration schema validation precedes filesystem mutation. `tf2Dir` must already be an exact readable directory. Setup may create the exact configured `sourceCacheDir` and `assetDir`; every other command requires all three roots to exist as directories. The three resolved roots must be pairwise distinct and non-nested.
- A map target resolves through its accepted game map registry to exactly `maps/<map>.bsp`, then through Content's configured provider plan. The tool never scans a directory, expands a wildcard, queries an installation registry, searches a library, downloads a substitute map, or probes another extension.
- Compilation invokes owner interfaces in declared dependency order: resolve exact source inputs, parse formats, prepare the map assembly plan, invoke semantic producers from that frozen plan, finalize the canonical map/runtime descriptor, validate every derived object, and optionally submit exact source/derived bytes and descriptors to Asset Store. The tool contains no parser, compiler, semantic resolver, game behavior, or serializer owned by another module.
- An artifact is current only when its recorded exact input hashes, dependency hashes, build configuration, compiler-behavior identity, tool commit, and complete Asset Store closure equal the required values. A mismatch is `build-required`; missing output is `build-required`; a corrupt immutable object is an integrity failure and is never repaired or overwritten. There is no timestamp, schema-version, newer-version, or partial-output freshness path.
- Inventory generation consumes one accepted owner declaration, invokes only that owner's public readers and classifiers, writes only the owner-declared generated path, retains every coverage classification, and installs output atomically only after exact count and metadata validation. The tool does not define inventory items or hand-edit generated output.
- Development performs configuration validation, exact target resolution, stale evaluation, required compilation, owner verification, local immutable publication, then starts the declared local asset service and selected application in dependency order. It prints the exact application URL only after every readiness contract passes.
- The tool owns each spawned operating-system process tree, signal forwarding, startup ordering, readiness timeout, output bound, shutdown ordering, and final reap. Applications and services own their internal lifecycle and readiness semantics. Shutdown requests graceful termination in reverse dependency order, waits the checked grace duration, then terminates only surviving owned trees within the checked force duration.
- `verify` executes the exact checked command or fixed procedure declared by the selected owner evidence target. It cannot invent, weaken, update, or reinterpret the target comparison predicate. An evidence failure is an operation failure and leaves retained expected evidence unchanged.
- `publish` starts from one locally validated retained root, freezes an Asset Store synchronization plan, uploads and verifies missing immutable closure members, and returns without changing a channel. `deploy` starts from one prepared immutable application build and one application-owned deployment plan, invokes the infrastructure-bound adapter, proves readiness, and never compiles or publishes assets.
- `release` starts only from a prepared local root, application build, expected channel revision, and immutable release manifest. It validates and publishes assets, deploys and verifies the application, rechecks the expected channel revision, then requests one conditional Asset Store channel change. Failure before that final change leaves the prior channel unchanged; a stale precondition is reported and never retried against a newly observed revision.
- `rollback` consumes one exact prior release identity naming a retained and remotely verified root plus the expected current channel revision. It changes only the channel through Asset Store. It performs no compilation, immutable upload, application build, object mutation, deletion, or fallback selection.
- Compilation and deployment are separate operations. `publish`, `deploy`, `release`, and `rollback` never compile. `compile` and `export-glb` never deploy or change a channel. Browser cache-miss compilation does not invoke a deployment operation.
- Every long operation propagates cancellation, stops scheduling new work, drains or terminates only owned work, preserves every already committed immutable object, and never changes a channel after cancellation is observed before the channel commit. A cancellation observed after a successful atomic channel commit is reported after that committed result and is never mislabeled as a rollback.
- Each invocation owns one work directory created through the host temporary-directory adapter; no command supplies its path and no artifact or report retains that machine path. Success, rejection after creation, failure, cancellation, and forced child termination all close handles and remove that directory. Cleanup never removes a cache entry, immutable object, retained root, channel, source file, repository file, foreign temporary path, or another invocation's work.
- Supported hosts are exactly the triples in the checked dependency/toolchain manifest. Host adapters normalize logical output, process-tree control, atomic installation, path containment, and signals across accepted Windows, macOS, and Linux triples. Shell parsing and platform-specific command recipes never form the current interface.
- Queues reject new work at their declared capacity. In-flight jobs never exceed `ToolExecutionPolicy`; cancellation and one failed prerequisite remove unscheduled dependents. Parallel completion cannot change target order, artifact bytes, report step order, or failure selection.

## Ownership Exclusions

- Content owns logical-path normalization, provider eligibility and order, archive lookup, raw-source cache validity, provenance, and exact checked-location reports. The tool loads configuration and invokes Content.
- Format, world, runtime, presentation, game, and ruleset modules own parsing, compilation, semantics, state transitions, artifact bytes, media types, and evidence predicates. The tool owns only their executable ordering and reports.
- Map owns map identity, assembly plans, canonical map output, and complete dependency descriptions. Asset Store owns object identity, roots, reachability, retention, synchronization, channel mutation, and rollback semantics.
- Applications own build contents, deployment configuration, internal process lifecycle, readiness semantics, routes, and product behavior. Infrastructure owns provisioned environments, credentials bindings, storage, CDN, compute, networking, and deployment adapters.
- Inspector owns interactive diagnostics. The playsrc tool may start Inspector as an owned process through its declared application contract but cannot implement or alter a diagnostic view.
- Source content acquisition, account authentication, production authorization, infrastructure provisioning, and secret creation are outside setup. A command fails when its exact configured input or authorized adapter is unavailable.

## Behavior Families

The active migration checkpoint implements `bun run dev jump_beef` as one in-process listener owner: exact target acquisition, checked release WASM build, immutable BSP/WASM/PSDB/ledger installation, fixed loopback Asset Service and Vite application bindings, generated browser descriptors without machine paths, dual readiness checks, exact URL output, and reverse shutdown on one `SIGINT` or `SIGTERM`. The browser evidence owner starts the same checked CLI `dev` operation directly as a bounded child, waits for its exact URL, drives one fresh headed browser through cold/warm acceptance, sends one `SIGINT`, requires a successful child exit, and proves both listeners unavailable. General stale/root/channel planning, startup cancellation during Cargo, descendant process trees, structured reports, and cross-platform evidence remain absent.

The active TF2 visual-gameplay evidence integration extends the existing `verify tf2-wasm` and `verify browser` targets rather than adding a command. One checked browser workflow must cover StudioModel facing and reload planes, viewmodel overlap, alpha discard, receiver-qualified moving marks, rocket transform/cadence/transient lifecycle, projectile and Particle timelines, Entity automation, configured brush collision, complete Water views, first-gesture audio, and fixed throughput records. Exact VPhysics and aligned target captures retain explicit blocked results; browser regression hashes cannot replace them.

The resource-driven TF2 UI integration extends the same source-bundle, `dev jump_beef`, and headed `verify browser` operations. The deterministic bundle must include every found UI source, font, VMT, and VTF admitted by the immutable build-24207079 descriptor, retain every configured absence in the ledger, and publish browser-presentable derived UI image records without replacing or omitting the authoritative Source objects. Headed evidence must begin at Main Menu before gameplay acquisition, load `jump_beef` only through Console, cover HUD and persistent Options, repeat cold and warm runs at 1,280×720, add one 390×844 run, preserve all existing gameplay assertions, and shut down through the existing single-interrupt owner.

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Validate the exact three-field local configuration before work, reject overrides and overlapping roots, and apply setup-specific versus ordinary root-existence rules. | The Bun tool resolves the repository root from its module location, accepts exactly the three absolute fields, canonicalizes and rejects equal or nested roots, requires all ordinary-command roots, and permits setup to create only missing cache/store roots after schema validation. Windows/Linux execution evidence remains absent. | **Configuration rejection matrix:** `bun test tools/playsrc/tests/config.test.ts` covers missing files, malformed UTF-8/JSON, missing/extra/wrong-type/empty/relative fields, unavailable roots, equal/nested roots, exact setup creation/idempotence, the checked macOS configuration, and a caller working outside the repository. Windows/Linux vectors remain required. | Partial |
| `setup` creates only the configured cache/store roots, installs the checked dependency/toolchain identities, prepares exact archive indexes through owner interfaces, and verifies every resulting version and hash. | `bun run setup` verifies the checked Bun revision, selects one of six pinned rustup host installers, verifies its SHA-256 and byte length, and installs/verifies Rust 1.97.1 with Cargo, rustfmt, Clippy, and `wasm32-unknown-unknown` under `sourceCacheDir` without modifying global paths. Archive-index preparation and Windows/Linux runs remain absent. | **Clean-host setup replay:** macOS arm64 installed the exact native toolchain and WASM target; a warm replay performs local identity checks. Bun configuration tests pass. Other hosts, fault injection, and archive-index evidence remain required. | Partial |
| Resolve each command's one logical target through the accepted owner registry and Content provider plan without path arguments, wildcard expansion, recursive search, or alternate-extension probing. | The checked TF2 registry currently accepts only exact `jump_beef`, binds it to `maps/jump_beef.bsp` and one immutable download declaration, and rejects malformed, aliased, or undeclared names before Content access. Full accepted owner/game registries and configured provider plans remain absent. | **Exact-resolution trace:** target unit vectors cover exact, missing, traversal, case alias, punctuation alias, and undeclared names; live cold/warm `bun run dev jump_beef` runs resolved only the declared source-cache objects before reaching the next unavailable owner. Other games/providers remain required. | Partial |
| Stock-map visual inspection resolves `pl_upward` and `pl_badwater` from their exact `maps/<name>.bsp` entries in the configured TF2 GAME provider plan, emits one verified source object plus target-specific dependency ledger/bundle, and starts the same browser application without claiming payload rules. | Only the downloaded `jump_beef` source can enter `dev`. A bounded installed-provider experiment resolved and parsed both exact stock BSP entries, then `playsrc_map::compile` rejected each with `UnsupportedDisplacement` before BSP, PSDB, ledger, or browser publication. No incomplete target remains declared. | **Installed-map development trace:** require complete Map displacement output first; then run both targets and require selected VPK provenance, exact BSP descriptor, target-qualified PSDB/ledger, Ready state, noclip command admission, nonempty world draw, and clean one-interrupt shutdown. | Blocked on Map displacement representation |
| `inventory <owner>` regenerates exactly one owner inventory from its accepted authority declaration, classifications, and current interfaces and replaces output only after exact metadata/count validation. | Owner inventory declarations are Candidate, generators are absent, and package denominators are Not accepted. | **Inventory regeneration comparison:** run each accepted owner target twice in clean work directories; require byte identity, exact authority/revision/command/path/count metadata, retained non-Handled items, unchanged output on failure, and no write outside the selected owner path. | Blocked |
| `compile <map>` drives one complete canonical-map build through Content, format, Map, and semantic producer interfaces, emits the same derived-cache identities as browser WASM, and publishes no authoritative partial output. Asset Store population is optional and does not change compiler output. | Required package interfaces are Not accepted or unimplemented; Map's staged producer seam is not adopted by Collision and Visibility. | **Map-build orchestration transcript:** fixed empty/warm cache and one-fault-per-stage native/WASM builds compare owner call order, frozen plan identity, exact hashes, derived objects, runtime descriptor, failure boundary, and zero tool-owned domain interpretation. | Blocked |
| Decide `current` or `build-required` from exact input, dependency, configuration, behavior, tool, and closure identities; treat immutable corruption as failure rather than stale output. | No stale evaluator or current compiled artifact exists. | **Stale-decision table:** mutate each identity/hash independently, remove each output, alter timestamps only, inject corrupt objects, and compare decision, invoked work, and unchanged immutable bytes exactly. | Not started |
| Publish compiled artifacts, roots, retention state, and the declared local development channel exclusively through Asset Store after complete local validation. | Local development verifies and atomically installs the exact raw BSP and release WASM objects through Asset Store before either listener starts. Root envelopes, retention, channel publication, complete closure validation, and stale evaluation remain absent. | **Local publication trace:** fixed shared-object and multi-root builds compare put/validate/retain/channel order, deduplication, conditional revision, poisoned excluded stores, interruption, and exact local root selected by the asset service. | Partial |
| `dev <map>` resolves the exact map source, starts declared processes, and permits the browser worker to compile missing derived objects or reuse verified cache objects before Ready; native cache prepopulation is optional. | `bun run dev jump_beef` resolves the declared cache source, 867-entry Source closure, and separate 240-entry browser UI presentation bundle; publishes exact BSP/WASM/PSDB/PUIB/ledger objects; then starts the checked listeners. | Cold/warm headed Main Menu, Console load, gameplay, HUD, Options, world-capture, and shutdown evidence. | Partial |
| Generate, verify, cache, and locally publish one immutable dependency ledger beside each declared source bundle, then reconcile browser diagnostics against that ledger without changing parser, gameplay, or presentation ownership. | The report and ledger bind app `440`, build `24207079`, patch `10822003`, 15 providers, 906 requests, 867 Source entries, 39 authoritative absences, 240 derived UI entries, and exact object descriptors. Development independently recomputes all descriptors before Asset Store publication. | Repeated generation fixes Source PSDB `38f967ad…a983`, UI PUIB `5a31b00c…811d`, and ledger `cce86978…1b1d`; source-bundle report vectors reject descriptor disagreement. | Ready |
| `export-glb <map>` compiles the current canonical map and emits one bounded GLB export without changing canonical identities, map availability, gameplay inputs, or derived-cache requirements. | No Map compiler or GLB export adapter exists in playsrc; the independent `bsp-to-glb` tool remains available. | **Export isolation comparison:** compile one fixed map with and without GLB export; require byte-identical canonical/runtime outputs and compare the GLB against its declared geometry/material export contract. | Not started |
| `verify <evidence-target>` invokes the selected owner's declared evidence procedure with fixed inputs and preserves that owner's comparison predicate and retained outputs. | The current registry dispatches exact `tf2-wasm` and `browser` targets. WASM verifies map bytes/hash, triangle orientation, spawn descriptor, and gameplay framing; browser starts the checked CLI development operation and one fresh headed WebGPU session, then verifies configured Main Menu/Console/GameUI, HUD operations and animation, Options transactions/bindings/persistence, 390×844 VGUI, cold/warm cache and canvas identity, exact blocker partition, fixed interior pixels, right/down pointer direction, raw-or-adjusted pointer mode, continuous display revisions over repeated Simulation-prepared state, movement/jump/combat, map replacement, one-interrupt process exit, and listener release. The complete accepted cross-owner registry remains absent. | **Evidence-dispatch audit:** `bun run verify:tf2-wasm jump_beef` and `bun run verify:browser jump_beef` pass their checked fixed procedures and emit one JSON result each. The Playwright profile independently verifies continuous-look display cadence. Fault injection and all other roadmap evidence targets remain absent. | Partial |
| `publish <root>` validates one local retained closure, executes the exact Asset Store synchronization plan, verifies remote bytes at origin, and never mutates a channel. | No selected remote adapter, publication environment, or Asset Store synchronization implementation exists. | **Remote publication schedule:** controlled origin cases cover present, missing, corrupt, concurrent, interrupted, cancelled, and capacity states; compare sorted plan, immutable writes, readback hashes, report, and zero channel calls. | Blocked |
| `deploy <application-build>` invokes one application-owned deployment plan against one infrastructure-bound environment, proves readiness, and performs no compilation, asset publication, or channel mutation. | Application, service, and infrastructure roadmaps are Draft; no accepted deployment plan, hosted environment binding, adapter, or implementation exists. | **Deployment adapter conformance:** fixed application builds and isolated environments compare plan identity, artifact selection, configuration binding, deployment calls, readiness, cancellation, failure cleanup, and zero compiler/Asset Store mutation calls. | Blocked |
| `release <release>` validates prepared identities, publishes assets, deploys and verifies the application, then conditionally changes one channel against the manifest's expected prior revision. | Release manifests, deployment adapters, remote Asset Store operations, and channel implementation are unavailable. | **Release fault schedule:** fail before and after every publication/deployment/readiness/precondition boundary; compare immutable residue, deployment identity, exact channel bytes, stale-race result, and one final channel call only after all prerequisites pass. | Blocked |
| `rollback <release>` changes one channel to the exact prior retained verified release root without compiling, uploading, redeploying, mutating, deleting, or selecting an inferred fallback. | No release history manifest, retained remote release set, or channel implementation exists. | **Pointer-only rollback audit:** three retained releases cover ordinary, repeated, stale-precondition, missing-closure, and concurrent rollback; compare channel history, zero immutable/deployment calls, final target, and unchanged object bytes. | Blocked |
| Cancellation stops admission, propagates once to every owner and child, preserves committed atomic results, and distinguishes pre-commit cancellation from cancellation observed after commit. | No command runtime or cancellation propagation exists. | **Cancellation boundary matrix:** interrupt before dispatch, in each owner step, during process startup/readiness, upload, deployment, channel commit, and cleanup; compare calls, committed identities, report status, exit code, and leaked-resource count. | Not started |
| Work-directory and process cleanup runs after success, rejection after allocation, failure, cancellation, timeout, and forced shutdown without touching foreign or durable state. | Development startup failure and terminal signal close Vite then Asset Service, restore generated environment values, and leave verified immutable objects intact. No operation work directory, startup-Cargo cancellation, forced child termination, fault injection, or cross-platform cleanup evidence exists. | **Cleanup fault injection:** hold files, fail close/remove, terminate children, preseed foreign names, and restart after interruption; compare owned survivors, durable bytes, exact cleanup error, and idempotent retry. | Partial |
| Every command is independent of caller working directory and resolves no routine path from process working directory. | Configuration, targets, Cargo, application config, and Vite paths resolve from the tool module's repository root. Setup/config tests and the development implementation consume no caller-relative path. Complete command working-directory permutations remain absent. | **Working-directory permutation:** invoke every command from repository root, each tool directory, a sibling directory, and a temporary directory; compare invariant report fields, owner inputs, artifact hashes, and exact config identity. | Partial |
| The checked host triples produce equal logical identities, invariant report fields, artifact bytes, ordering, and owner results through native path/process/signal adapters. | No host adapter or cross-platform command evidence exists. | **Cross-platform conformance:** run fixed setup, stale, compile-stub, process, cancellation, report, and cleanup vectors on every declared Windows, macOS, and Linux triple; compare canonical outputs and declared environment-only fields. | Not started |
| Every queue, process tree, compiler set, verifier set, upload set, retry loop, output stream, and operation duration obeys the checked policy with deterministic backpressure and serial-equivalent results. | No execution policy or bounded scheduler implementation exists. | **Bound/backpressure schedule:** exercise zero work, each maximum, maximum-plus-one, saturation, one failed prerequisite, retry exhaustion, and cancellation; compare admissions, peak counts/bytes/time, stable order, and serial-equivalent outputs. | Not started |
| Every command emits one complete bounded machine-readable report with deterministic invariant fields and ordered owner steps; progress never corrupts standard output. | No report contract implementation exists. | **Structured-output golden comparison:** permute asynchronous completion and log chunking across success, rejection, failure, and cancellation; parse one stdout object, compare invariant fields byte-for-byte, and validate bounded stderr separately. | Not started |
| Every failure identifies the command, logical target, operation step, owner, exact configured-root-relative location or Content candidate set, stable tool error, nested owner error, bound/precondition, and cleanup result without fallback or partial-success wording. | No exact error mapper or failure report exists. | **Exhaustive failure matrix:** inject every declared tool error and representative owner error at each command boundary; compare exit code, selected primary error, nested cause, checked locations, committed results, cleanup, and absence of substitute work. | Not started |

## Generated Inventories

This roadmap owns no generated inventory. Its finite supported-operation set is the 22 rows above. `inventory <owner>` generates outputs owned by the selected package, game, ruleset, application, tool, or infrastructure roadmap; those inventory items remain in that owner's denominator.

The delegated target universe is not yet executable because accepted machine-readable inventory, evidence, application-build, environment-binding, and release manifests do not exist. The command fails with `OwnerUnavailable` for an unregistered target rather than enumerating repository files or inferring a command.

## Exit Criteria

The playsrc tool is Complete only when all of these predicates pass:

- All 22 Behavior Families rows are Ready and the denominator has an Accepted review record.
- The ten-command surface is the sole repeated workflow for its declared operations; no checked document requires a manual command recipe or routine path argument.
- Every delegated target comes from an accepted owner manifest and every generated inventory remains byte-identical under repeated clean regeneration.
- Fixed clean, warm, stale, corrupt, interrupted, cancelled, concurrent, rollback, and cross-platform runs satisfy their declared comparisons.
- Every producer, consumer, application, service, Asset Store adapter, deployment adapter, and infrastructure binding uses one current interface and appears in integration evidence.
- Compilation, local publication, remote publication, deployment, channel mutation, and rollback retain their declared separation and ordering.
- Every owned process, handle, work directory, queue entry, and temporary write is released after terminal settlement.
- No required operation, target, interface, inventory, environment, evidence method, host triple, or bound remains Unsupported, Unknown, Missing, Partial, or Blocked.

## Blockers

- **Target and Content interfaces:** Content, current game provider plans, and game map target registries are Not accepted or unimplemented. Checked `packages/content/ROADMAP.md`, current game roadmaps and inventories, the root Owner Registry, and the local-configuration contract.
- **Inventory generation interfaces:** package and game inventories are Candidate and name Missing generator commands; no accepted machine-readable owner declaration can drive `inventory <owner>`. Checked every current roadmap Generated Inventories section and `tools/`.
- **Map compilation interfaces:** BSP, Map, Material, StudioModel, Entity, Collision, Visibility, game, and Asset Store denominators are Not accepted or unimplemented. Map's preparation/finalization seam is not yet adopted by Collision and Visibility. Checked every named roadmap and inventory.
- **Development process acceptance:** the fixed local TF2/Asset Service graph, bindings, readiness checks, and URL are implemented, but its process manifest and denominators are not accepted; startup cancellation, forced termination, cross-platform adapters, and retained empty/warm browser evidence remain absent.
- **Evidence targets:** owner roadmaps name comparison methods but no accepted complete machine-readable evidence-target registry or checked command set exists. Checked every current roadmap Evidence cell, Generated Inventories section, and `tools/`.
- **Remote publication and release:** Asset Store remote operations are unimplemented; Infrastructure is Draft, declares only `local`, and selects no remote adapter, capability revision, credentials binding, or isolated hosted environment. Accepted application deployment and release manifests are absent. Checked `packages/asset-store/ROADMAP.md`, `apps/**/ROADMAP.md`, `infra/ROADMAP.md`, and the root Owner Registry.
- **Rollback identity:** no accepted release manifest records the exact prior retained root and expected current channel revision consumed by `rollback <release>`. Checked Asset Store channel outputs, application READMEs, `infra/README.md`, and the current tool tree.
