# TF2 Web Application Roadmap

[`../../../docs/roadmap-contract.md`](../../../docs/roadmap-contract.md) defines the normative roadmap schema and denominator review gate. [`../../../TERMINOLOGY.md`](../../../TERMINOLOGY.md) defines gameplay authority, replay authority, Application, Catalog, delivery status, evidence, and Complete. [`../ROADMAP.md`](../ROADMAP.md) defines the shared browser-application contract.

## Completion Denominator

This leaf roadmap contains exactly 15 behavior rows. The application exposes exactly two Active experiences:

| Experience identity | Canonical route | Composition |
|---|---|---|
| `tf2.jump-practice` | `/jump/<map>` or transient `/jump/external` | One local Simulation session containing TF2 and `ruleset.jump` for a catalog-selected immutable map/course root or one explicitly submitted ephemeral HTTPS BSP source. |
| `tf2.recorded-replay` | `/replay/upload` | One user-selected local DEM handed to Demo, Networking, the TF2 recorded-state decoder, and Replay without Simulation. |

`<map>` is the exact lower-ASCII catalog map identity and cannot contain `/`, `\`, `.`, `..`, percent-decoded separators, controls, query, or fragment bytes. `/jump/external` never serializes a source URL, hash, provider plan, or reusable authority; direct navigation and reload return to explicit console submission. The application root contains one ordered catalog whose entries select either `tf2.jump-practice` with a map source/course identity or `tf2.recorded-replay` without a map source.

Online multiplayer, hosted sessions, matchmaking, store/account inventory, and additional TF2 rulesets are outside the Active denominator. Adding one changes this experience set and the root target dependencies before implementation.

The denominator is Not accepted. `ruleset.jump`, Replay, the TF2 recorded-state decoder, presentation interfaces, service endpoints, and the shared web denominator are not accepted, and no review record exists.

## Inputs

- The complete shared configuration, limits, browser profile, channel, application root, catalog, lifecycle, input, settings, persistence, accessibility, observability, and deployment inputs from [`../ROADMAP.md`](../ROADMAP.md).
- One application root whose identity is `application=tf2`, one catalog identity, and only the two experience identities above.
- For `tf2.jump-practice`: one immutable catalog entry naming TF2 content build, exact `MapSource`, optional derived map root, `ruleset.jump`, exact `JumpCourseDefinition` object identity, player-input profile, Rendering profile, and VGUI profile.
- The current TF2 game, TF2 Jump, Simulation, Movement, Collision, Physics, Entity, Networking where required by package composition, Rendering, Particle, Audio, VGUI, Content-consumer, and Asset Store/Service interfaces.
- For `tf2.recorded-replay`: one user-selected file supplied through the File API, declared maximum 2,147,483,648 bytes, exact byte length, lowercase SHA-256 computed while streaming, file-selection generation, and explicit `tf2-demo3-net24` profile selection. File name, MIME type, modification time, and path never select profile or game.
- One successful immutable Demo value, current Networking codec, TF2 recorded-state decoder, and Replay interface. The application never reads DEM records or network messages directly.
- One application-owned Preact shell root, one Rendering canvas, and one separate VGUI mount root. Preact cannot reconcile the latter two owners' subtrees.
- One VGUI developer-console adapter receiving exact opaque submissions and completion requests, supplying immutable command/convar catalog snapshots and bounded output, and owning command tokenization and effects. Its map operation implements the shared catalog and explicit HTTPS acquisition contract; VGUI never loads, validates, routes, or compiles a map.

## Outputs

- One TF2 product state extending the shared state with selected experience, catalog entry, map identity where applicable, Simulation or Replay session identity, presentation readiness, VGUI readiness, input mode, and upload identity where applicable.
- One ordered physical-binding stream for Jump practice, and typed Simulation commands only after the TF2 game adapter maps those bindings through its accepted command schema.
- One local replay-source result containing file byte length/hash, selected profile, Demo identity, Replay identity, progress, and classified failure; the application retains no file bytes after handoff and shutdown.
- One product UI containing catalog selection, upload selection, settings, progress, browser permission/recovery state, user-visible failures, and route navigation around the package-owned canvas and VGUI subtree.
- One complete experience-shutdown result releasing the active Simulation or Replay session and all product-owned browser adapters before another experience becomes Ready.
- One bounded console operation stream with generation, progress, cancellation, classified output, and terminal result. Observations identify the operation and source hash after validation and never contain the unrestricted submitted URL.

## Invariants

- Exactly one of `tf2.jump-practice` and `tf2.recorded-replay` is active. One cannot remain loaded behind the other, share mutable workers, or retain authority state after replacement.
- Jump practice advances only through Simulation with TF2 and the selected Jump ruleset. The application never advances movement, weapons, projectiles, damage, entities, course timers, checkpoints, or results.
- Recorded replay advances only through Replay operations over decoded recorded state. The application never feeds recorded commands into Simulation or fills missing values from TF2 gameplay.
- Physical bindings, pointer lock, touch controls, camera selection, and route state are application-owned. TF2 owns command meaning and game-specific presentation mappings; Jump owns course transitions and result validity.
- The upload route is intentionally not a shareable replay identity. Reload and copied URLs return to file selection. A user must reselect bytes whose hash matches before an in-memory replay cursor can be restored.
- A rejected, cancelled, changed, over-bound, or mismatched file publishes no Demo or Replay session. File processing is streaming and never requires a second full-file copy in application state.
- The product shell never reads canonical game/replay state to derive missing HUD truth. TF2 bindings provide immutable VGUI values and typed commands; product UI displays only app-owned selection, lifecycle, progress, settings, and failure state.
- Experience suspension, GPU recovery, audio interruption, route change, and close obey the shared lifecycle and cannot advance gameplay or replay from elapsed wall time.
- `map <catalog-map-name>` selects exactly one declared catalog map/course identity, starts one new application operation generation, preserves the prior Ready experience until atomic replacement, and uses the direct BSP/VPK/WASM path without search or extension probing.
- `map https://<allowed-origin>/<path>/<map-name>.bsp` is a playsrc application/content-acquisition extension, not an authentic TF2 command capability. Only an explicit submission can start it; the shared HTTPS, origin, redirect, CORS, length, time, integrity, BSP-validation, provider, cache, progress, cancellation, privacy, atomic-replacement, and reload-resubmission rules apply.

## Ownership Exclusions

- [`../../../games/tf2/ROADMAP.md`](../../../games/tf2/ROADMAP.md) owns TF2 players, teams, classes, items, weapons, entities, game rules, prediction, recorded-state meaning, events, and presentation mappings.
- [`../../../games/tf2/rulesets/jump/ROADMAP.md`](../../../games/tf2/rulesets/jump/ROADMAP.md) owns Jump course definitions, eligibility, timers, checkpoints, validity, reset/restart effects, completion, and run results. This application selects and displays them.
- Simulation owns gameplay scheduling and authority; Networking owns protocol codecs and reconciliation; Demo owns DEM parsing; Replay owns recorded-state progression, seeking, snapshots, and event ranges.
- Rendering, Particle, Audio, and VGUI own presentation execution and resources behind their current interfaces. This application owns mount points, permission gestures, product layout, settings policy, and user-visible lifecycle.
- Asset Store owns application/catalog/root envelopes and immutable objects; Asset Service owns HTTP delivery. This application owns the two catalog experience entries, their labels/order, and application-root contents.
- Online multiplayer belongs to the Future TF2 online-multiplayer target and its service applications. Tempus records, rankings, course discovery, external APIs, and product data belong to [`../tempus/ROADMAP.md`](../tempus/ROADMAP.md).

## Behavior Families

The hard-foundations integration consumes reviewed TF2 random/audio, Collision snapshot, linear-pusher, per-face visibility, model-shader/authored-mip/eye, stock hand-plus-item viewmodel, configured HDR sky encoding, and platform-font capability producers through one replacement WASM/browser contract. It supplies empty dynamic-light/shadow families for the configured target and retains Missing or Unsupported for absent lightcache/eye, Water tangent/projected-depth/underwater-overlay, explicit-command-encoder backend, shadow, and aligned-capture inputs. The package-owned all-class TF2 core and Demo/Networking/Replay foundations remain outside the current product authority because their complete game/application adapters do not exist. Headed browser hashes remain regression evidence, not aligned TF2 target captures.

The active migration checkpoint implements one local `/` jump-practice assembly for declared `jump_beef`: exact browser configuration and immutable BSP/WASM/raw-dependency fetches, IndexedDB-derived payload reuse, one game-owned worker, bounded fixed-tick pacing, selected-teamspawn view initialization, Source-signed desktop movement/combat bindings with physical left/right Shift crouch and no Control crouch binding, exact translated-brush map teleports, Three.js/WebGPU world presentation with normalized front faces, 12 resolved base textures, UV1-bound first-style LDR lightmaps, 33 dynamic-prop occurrences, rocket/sticky and class-selected viewmodel geometry, exact stock projectile Audio buffers, and explicit water/directional-style-lightmap/model-material/pose/particle diagnostics, an application blocker ledger, one Preact-owned shell/canvas mount, one separate VGUI mount, exact configured-build composed SourceScheme console values, movable/sizeable frame interaction, Backquote/pointer-lock neutralization, typed console completion/submission, bounded `cl_showfps`/`cl_showpos` convars and one VGUI diagnostic panel, generation-safe `map jump_beef`, bounded allowed-origin HTTPS BSP acquisition, and complete application-owned shutdown. The checked headed-browser workflows pass byte-identical cold/warm fixed-camera canvas captures, floor/ceiling/wall pixel regions, right/down pointer vectors, audio resume, pointer lock/release, movement, jump, Soldier/Demoman projectile controls, VGUI history/completion/focus/repeated visibility, 480p/720p/1080p/200%-zoom console captures, center/edge/corner drag and eight-direction resize, diagnostic/convar schedules, left/right Shift and Control exclusion, blur neutralization, generation-2 replacement, one `SIGINT`, child exit code zero, and listener release. Jump course state/timers, remaining Entity classes, water/directional-style-lightmaps, model materials/poses/animation and viewmodel FOV, particles, replay, canonical route/catalog roots, and the eight-profile matrix remain absent.

### Resource-driven GameUI, HUD, and Options integration

This integration refines `TF2-WEB-004` through `TF2-WEB-008` and `TF2-WEB-012` through `TF2-WEB-015` and adds no denominator identity.

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| TF2 GameUI starts with the configured ordinary BasePanel chapter background below the full-viewport proportional override and independently weighted character, retains dashboard/playlist/account/settings controls in source order, and hides/restores the base layer across loading/gameplay/pause/disconnect. | The build-`24245096` PUIB supplies one typed chapter/background descriptor plus exact standard/widescreen browser textures. The game integration owns VGUI layers and aspect/lifecycle; the app tags immutable identities for evidence only. No CSS background or online image exists. | Fixed source/material/texture hashes, isolated nonblack base pixels, character independence, lifecycle, accessibility, and headed 1,192×1,339/desktop/mobile/landscape/DPR/reload/disconnect captures pass. | Ready |
| The Valve startup movie, developer console, local-map loading dialog, in-game pause menu, resume, disconnect, failure, and quit request follow startup/GameUI lifecycle and owner confirmations. | The exact configured WebM owns visible startup while hidden inert Main Menu VGUI initializes concurrently; completion or Escape reveals it atomically. `jump_beef` uses the exact generic stamp and 380×112 no-banner dialog driven by typed owner milestones; no BSP/WASM gameplay initializes before the map request. | Cold/warm 1,280×720 and 390×844 captures cover first/middle/final movie frames, gesture/audio admission, skip, hidden menu exposure, loading layers/progress, gameplay, disconnect, no replay, and failure vectors. | Ready |
| TF2 HUD resources consume immutable player, class, team, health, ammo, reload, weapon-selection, crosshair, condition, lifecycle, scoreboard, freeze, pickup, killfeed, animation, and archived 2D/3D presentation-setting facts without gameplay mutation. The screen-sized client viewport is proportional before HUD children are parented and reapplies layout after every integer screen-size change. | Every bounded canonical nine-class publication flows through `adaptSessionHud` and `bindTf2Hud`; one VGUI adapter consumes the complete generated dynamic image closure, current `cl_hud_playerclass_use_playermodel`, and producer-authorized commands. | Producer binding vectors plus headed Soldier/Demoman RED/BLU 2D/3D, zero-condition, ammo/reload, exact panel-count and lifecycle-reset captures pass. | Ready |
| Video, Audio, Mouse, Keyboard, Multiplayer, and Advanced Options stage one current settings profile, apply or cancel atomically, reset defaults, capture/unbind conflicting bindings, and report restart requirements. | Configured Options resources present one `TF2_SELECTED_OPTIONS` state. VGUI applies exact alignment, scheme state colors/borders, SectionedList flags, slider geometry and ComboBox popup state; untouched clamped controls cannot become pending changes. | Deterministic codec/state vectors plus resource-derived control inventory and headed 1,280×720/390×844 default/armed/selected/disabled/dropdown/edit/cancel/apply/default/binding/conflict/unbind/reload schedules pass. | Ready |
| Startup media, GameUI, loading VGUI, HUD, Options, Console, Rendering, Audio, input, Simulation, immutable objects, and derived caching retain sole ownership and release every browser resource in reverse lifecycle order. | Preact owns only application mounts and orchestration; startup owns its media URL/listeners and hidden-menu handle; VGUI owns Main Menu/loading/HUD descendants, modal state, input, animation clock, and cleanup. Map/disconnect transitions reset one mounted HUD runtime; application release destroys it once. | DOM-owner snapshots, exact one-HUD/one-GameUI panel counts, hidden/inert accessibility checks, listener/font/image/object-URL/worker/audio/cache cleanup, strict builds, privacy scans, and repeated destroy evidence pass. | Ready |
| Application frame admission advances visible Options and active GameUI/HUD owners; hidden Options retain immutable state without layout, animation, DOM, raster, input, or request work until shown. Standard and Advanced Options initialize independently from complete shared settings state. | The browser lazily constructs the selected Options family, advances only that visible runtime, rebases its post-construction frame clock, and tracks deferred GameUI owner requests for cleanup. MainMenu no longer creates or frames either Options runtime; standard Options does not create Advanced's 88 rows. | **Owner suspension trace:** checked MainMenu/standard-Options profiles plus GameUI/HUD/Options unit and browser schedules compare owner frame work, resumed clocks, snapshots, requests, DOM mutations, pixels, and teardown. | Ready |
| The client surface and proportional HUD share one current physical presentation viewport through size, visual-viewport, orientation, fullscreen, pointer-lock, and device-pixel-ratio transitions. | One application owner measures the shared mount content box, admits only positive integer CSS dimensions plus DPR, coalesces notifications into one animation-frame commit, and publishes one immutable record to Rendering and every mounted VGUI owner. Zero size suspends publication; an identical record is inert. | **Composed viewport schedule:** headed fixed-size, height-only, width-only, portrait, landscape, ultrawide, DPR, restore, duplicate and teardown vectors compare owner revisions, complete DOM rectangles, configured bottom-panel offsets, layer alpha/color occupancy, and final composed rows. | Ready |

### Startup, dual-map replacement, sky, interpolation, and Escape lifecycle

This integration refines `TF2-WEB-004` through `TF2-WEB-008`, `TF2-WEB-012`, `TF2-WEB-013`, and `TF2-WEB-015` and adds no denominator identity.

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Startup, Main Menu, map loading, active-map replacement, failure recovery, disconnect, and close admit one current lifecycle generation; stale fetch, Worker, Simulation, presentation, Particle, and GameUI operations never overwrite its authority. Every visible VGUI owner receives one finite monotonically nondecreasing frame timestamp. | Both exact catalog maps load from fresh, ready, failed, and repeated generations; an initial nondefault target selects its complete catalog graph; failed preactivation replacement preserves prior authority, and postactivation failure remains classified instead of publishing false Ready. | Deterministic operation-generation, cancellation, timestamp, mapper/encoder-identity, and reset vectors pass; headed transitions retain generations `1,2,3,4`, one prior-map rollback, one initial failure, and successful recovery. | Ready |
| BSP sky visibility and an authored `sky_camera` are separate facts: an authored controller admits its area-restricted transformed 3D-sky pass, while a sky-classified leaf without a controller renders the unchanged main view without inventing sky geometry. | Exact `jump_beef` contains zero `sky_camera` entities; exact `pl_upward` contains one controller and retains its separate authored sky pass, area, origin, scale, fog, visibility, depth reset, and lifecycle. | Official SDK `src/game/server/playerlocaldata.cpp` and `src/game/client/viewrender.cpp`; deterministic controller vectors, a headed 548-surface authored pass, exact controller-free leaf `129`, and 4,229 controller-free whole-map views pass. | Ready |
| The local predicted player uses the Simulation-owned interpolation fraction between consecutive authoritative fixed-tick positions; current mouse angles are applied immediately, discontinuities reset history, and no independent interpolation RPC, guessed easing, delayed input, or reduced tick cadence exists. | One bounded JavaScript presentation adapter consumes only ordered immutable tick snapshots and the producer-owned interpolation fraction while keeping source Collision, authority, commands, visibility-call count, and projectile time unchanged. | Official SDK `src/game/client/c_baseentity.cpp`, `src/game/client/c_baseplayer.cpp`, `src/game/client/view.cpp`, and `src/game/client/prediction.cpp`; repeated headed comparisons reduce the maximum/median movement-step ratio from `2.00` to `1.02–1.07` with zero movement-only visibility RPCs, sampled allocation, nonregressed frame percentiles, and unchanged `66.4–66.8 Hz` Simulation. | Ready |
| Default TF2 has `cl_wpn_sway_interp=0.0`, so the first-person hand and item retain authored activity, bob, and offsets but do not lag the current world-view yaw/pitch; enabled authored lag remains game-owned rather than being fabricated by the application. | Every independent display frame carries immutable eye-local hand/item poses through the latest displayed camera without world-offset duplication, additional model-pose WASM work, simulation replay, VR offsets, or guessed smoothing. | Official SDK `src/game/shared/tf/tf_viewmodel.cpp`, `src/game/shared/baseviewmodel_shared.cpp`, `src/game/client/view.cpp`, and `src/mathlib/mathlib_base.cpp`; deterministic Source-basis vectors plus headed rapid-yaw/pitch captures retain zero angular error, an unchanged crosshair pixel, `98.0%` changed world pixels, and `0.18%` changed weapon pixels. | Ready |
| One accepted Escape key-down opens in-game GameUI and releases pointer lock; the next accepted Escape closes GameUI, resumes gameplay input, and restores browser pointer-lock admission without duplicate activation from the same event. | The application routes one physical nonrepeat Escape edge through the game-owned `{ kind: "escape" }` transition and waits for owner acknowledgement before resuming gameplay and restoring pointer-lock admission. | Deterministic edge, repeat, propagation, pending-request, and acknowledgement vectors plus three headed pointer-lock release/resume cycles pass. | Ready |
| Profiles select map objects from the one current ordered dual-map catalog and preserve exact classified external failures during authoritative map traversal. | The cold-map profile resolves `defaultTarget` through `targets[].objects.bsp`; the headed coverage profile selects and traverses both declared maps without obsolete configuration aliases, fabricated sky controllers, omitted rocket transitions, or swallowed game-owned errors. | Headed current-schema cold-map loading and dual-map coverage pass; `pl_upward` traverses 328.3 units with 367 authored sky surfaces, and `jump_beef` retains 278 coverage samples, 16,864 frames, and 4,229 controller-free sky views. | Ready |

### Browser frame pacing, input, and publication ownership

This integration refines `TF2-WEB-005` through `TF2-WEB-008`, `TF2-WEB-012`, `TF2-WEB-014`, and `TF2-WEB-015` and adds no denominator identity.

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Render-frame mouse samples and fixed user commands observe one current view independently; held and impulse input, ordered tick batches, teleport view snaps, 15 ms Simulation cadence, and newly visible projectile effects remain exact under delayed presentation. Every admitted pointer-lock mouse delta reaches the same current view exactly once even while Rendering, visibility, browser frame scheduling, or publication are delayed. | Completed authoritative preparations offer display immediately; each display captures camera angles, complete view revision, mouse revision, and authoritative snap revision together before asynchronous visibility/rendering. Indexed physical bindings retain exact chords, modifier fallback, held identities, and one-shot command admission. Semantic headed test automation owns console commands, declared map selection, signed mouse-look, bounded player movement, and one explicitly reported native-or-emulated Pointer Lock adapter; production input always uses the real browser API. | Official SDK `src/game/client/in_main.cpp` `CInput::ExtraMouseSample`/`CreateMove`, `src/game/client/in_mouse.cpp` `MouseMove`/`ApplyMouse`, and `src/game/server/triggers.cpp` `CTriggerTeleport::Touch`; headed baseline exposes 35–36 inconsistent frames per 600 ms sample, while atomic publication retains zero, 12 focused accelerated-rAF rounds admit all 1,818 dispatched mouse events, the complete headed verifier preserves every input/pixel/depth/lifecycle predicate, ignores external user gameplay input, records actual native-versus-emulated Pointer Lock ownership, and retains comparable frame percentiles, allocation, seven long tasks, and 66.5–66.9 Hz Simulation. | Ready |
| Visible GameUI, loading, HUD, and Options owners advance their own animation clocks; hidden owners perform no frame, layout, raster, or DOM work, and resumption uses the current absolute browser frame timestamp. | One allocation-free owner mask advances GameUI only while revealed and visible, loading only for loading/failure, HUD only for gameplay/pause, and Options only while open. All owners receive the same admitted browser-frame timestamp. | Fixed hidden/revealed/loading/failure/gameplay/pause/options/closed masks; headed startup, menu, loading, HUD, gameplay, and owner-owned pause/Options integration suites pass. | Ready |
| Application lifecycle, HUD, authority, camera, projectile, visibility, and diagnostic observations publish the same complete values without deriving gameplay truth or reconciling presentation-owned DOM. | One Preact shell mounts 16 exact initial attributes once; an application publisher incrementally maintains the 102 original attributes plus the current authority generation, preserves canvas accessibility and startup text, retains stable blocker/model/history identities, and uses one bounded HUD probe plus lifecycle-cached root counts. | All 432 package/application tests, fixed attribute order/identity audit, duplicate-write rejection, absent/false/zero/empty distinctions, stable JSON, owner masks, and exact canvas main/sky/runtime-prop observations pass; the headed dual-map coverage traverses the formerly blocked fire/regeneration path across 16,864 frames. | Ready |
| Main and authored 3D-sky views retain exact camera position, angles, viewport, tick, Water plan, frustum/PVS membership, area ownership, draw order, and generation. | Main visibility remains independently recomputed for changed view angles. One bounded 3D-sky result is reused only for identical generation, viewport revision, authoritative tick, position, origin, area, angles, FOV, and clip planes; independent known main/sky requests overlap, and replacement/teardown clear retained results. | Exact-key, signed-zero, controller origin, area, viewport, tick, generation, canvas-observation, and reset vectors pass; headed dual-map profiles retain 548 authored `pl_upward` sky surfaces, zero `jump_beef` 3D-sky surfaces, and four replacement generations. | Ready |

### Active visual-output audit

This audit refines `TF2-WEB-007`, `TF2-WEB-014`, and `TF2-WEB-015` and adds no denominator identity.

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| The default TF2 world camera uses 75-degree horizontal-4:3 FOV, near Z 7, and map-extent far Z; first-person rendering uses a separate 54-degree horizontal-4:3 projection, near Z 1, world far Z, and viewport depth `[0,0.1]`. | The application supplies SDK-derived world camera values and verifies Rendering's separate viewmodel viewport enter/restore contract. Stock first-person composition and aligned target captures remain blocked. | Fixed camera metadata, projection vectors, and headed depth/visibility probes pass. | Partial |
| Stock items 18 and 20 attach their configured `c_model` weapons to Soldier/Demoman hand viewmodels and copy the selected team skin. | PTF2 v6 and PMRQ/PMPO v2 use class hands, animation libraries, item `c_model` attachments, bone merge, translated activity, shared team skin, explicit bodygroups/LOD, and separate hand/item draw parts. No legacy `v_model` path remains. | Official SDK item/viewmodel call paths, fixed producer composition hashes, native/WASM two-part vectors, and headed browser timeline/depth probes. | Ready |
| Every map prop consumes one exact producer matrix and matching material/skin/bodygroup/pose input without name-specific correction. | Rendering admits all 33 occurrence matrices atomically, removes the angle fallback, and verifies representative cow/frog/locker/Soldier matrices. Complete materials/lighting remain blocked. | Exact occurrence-set validation and fixed matrix observations pass; aligned material captures remain required. | Partial |
| Decals preserve transparent character pixels, receiver orientation, Material state, and PVS membership. | The application supplies 13 authored texture chains and 73 collision-selected world/brush fragments; Rendering applies exact state, current receiver transforms, and receiver-qualified visibility. | All 13 alpha/state probes and configured 39/38/73 stream pass; fixed spawn admits 13 fragments. Aligned target captures remain required. | Partial |
| Source texture sampling and model drawing consume complete authored mip chains plus typed model shader, eye, lighting, proxy, material, and viewmodel composition state; sky, cubemaps, water, and fog consume separate complete environment inputs. | PTF2 v6 serializes 55 typed model materials and all 71 authored texture chains; Rendering validates and uploads exact selected mip/sampler/color records. Current model lightcache selections, game eye targets, environment resources/state/views/candidates, and aligned captures remain categorical blockers. | Native model closure, PMDL/PMIP parser checks, headed `55:71:<planes>` probe, and exact blocker ledger. | Partial |
| A parity capture aligns target and browser map/build, camera, projection, viewport, profile, exposure, tick, item/team/skin/bodygroups, and capture planes. | The checked workflow retains deterministic browser metadata and hashes but has no controlled target capture. | Target/browser color/depth/ID manifest with explicit tolerances. | Blocked on target capture |

### Authority-parity consumer integration

This integration refines `TF2-WEB-004`, `TF2-WEB-006`, `TF2-WEB-007`, `TF2-WEB-014`, and `TF2-WEB-015` and adds no denominator identity.

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| TF2 publishes complete current weapon, lifecycle, condition, random draw/state, selected audio, projectile, damage, Entity, mover, resupply, Collision, and Physics facts at one tick boundary. | PCMD v5 retains only game input and sticky-Physics results. PSSN v6 carries restorable streams/draws, selected audio, Collision snapshots/rocket results, and ordered configured-target mover results. Browser random, wave, rocket, and mover selectors do not exist. | Cross-language binary vectors plus native/WASM/browser random/audio, standing/crouched blast, resupply, configured mover completion/carry, rocket, teleport, and blocker traces. | Ready |
| Sticky motion changes only from accepted IVP results, and custom Tempus runs change only from an authoritative core plus exact zone contacts. | The application must retain both unavailable transitions as exact blockers, expose their typed seams, and remove the inferred BSP-trigger course configuration and every solver substitute. | Rejected missing/stale/malformed external-result vectors, zero sticky integration without a result, no configured local course, and blocker-ledger inspection. | Partial |
| Exact content closure distinguishes unavailable bytes from unavailable behavior or platform input. | The configured 899-object Source ledger plus separate 258-object browser UI presentation bundle include every static resource occurrence and 17 code-selected HUD images and classify missing content independently from unsupported VGUI/material/font behavior. | Cold/warm/corrupt/cancel content and Asset Store vectors plus headed blocker-ledger partitioning. | Partial |
| Current visual consumers require exact occurrence matrices, team skins, decal receiver/PVS state, world/viewmodel projections and depth, and authored mip diagnostics. | Rendering and the application consume the reviewed contracts without angle reconstruction, projection-matrix depth emulation, generated mip substitution, or diagnostic-resource promotion. | Fixed decal/PVS, prop-matrix, viewmodel projection/depth/skin, and missing-mip browser traces. | Partial |

### Source-parity repair consumer integration

This integration refines `TF2-WEB-004` through `TF2-WEB-008`, `TF2-WEB-012`, `TF2-WEB-014`, and `TF2-WEB-015` and adds no denominator identity.

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| Source host frames admit wall-clock samples independently of presentation, group every selected fixed tick, publish only final-tick state, retain every ordered event batch, and retain held plus impulse-down input until command sampling; suspension rebases time without becoming game pause. | The worker injects `performance.now()` into `MetricsClock` and owns one `FixedStepHost`. Browser input remains event-driven; the application admits one latest wall-clock sample at each 15 ms deadline instead of converting every display callback into an RPC, keeps at most one ordinary sample behind the in-flight observation, preserves ordered suspension transitions separately, and leaves one-shot controls latched until command admission. PSIM publishes final state plus every tick batch independently of presentation throughput. | Official SDK `src/game/client/in_main.cpp` `KeyDown`, `KeyUp`, `CInput::CreateMove`, and `CInput::GetButtonBits`; deterministic 400-sample coalescing and suspension-transition vectors; producer fast/delayed byte comparison; native PSIM probe; headed native/165 Hz input, observation-rate, queue-depth, fixed-tick, and presentation-rate profiles. | In progress |
| Source raw mouse deltas update one engine view-angle state on render-frame samples; fixed commands independently capture that current state; a map teleport angle snap occurs at its authoritative transition without deleting later mouse movement. | Desktop pointer lock requests unadjusted movement and retries adjusted movement only when raw input is unsupported. Every locked mouse event advances one cumulative view state. Simulation preparation requests position-qualified PVIS in parallel with model posing. Bounded display work independently samples the current view, reuses only a no-Water plan's immutable PVS with its sole main-pass camera replaced, requests a fresh angle-qualified plan for visible Water, and coalesces while one frame is in flight. A returned teleport angle rebases movement accumulated after its command sample instead of overwriting newer deltas. | Official SDK `src/game/client/in_mouse.cpp` `ActivateMouse`, `GetAccumulatedMouseDeltasAndResetAccumulators`, `MouseMove`, and raw-input default; `src/game/client/in_main.cpp` `CreateMove`/`ExtraMouseSample`; `src/game/server/triggers.cpp` `CTriggerTeleport::Touch`; deterministic sensitivity, raw-retry, malformed-input, and teleport-rebase vectors. A passing headed `jump_beef` profile selected raw input, displayed 154 frames across one 800 ms continuous-look window with 98 frames independent of new Simulation preparation and zero angle snaps, and retained 159.4 Hz browser callbacks beside 66.5 Hz Simulation. | Ready |
| Every BSP inline model uses its model-local face range while current Entity state supplies one revision-matched world transform, draw admission, render state, parent, and mover state; solidity never substitutes for visibility. | PTF2 v10 carries all 123 model descriptors, PSSN v11 joins Entity to Collision v3, and Rendering instantiates current drawn records plus model-local marks. | Native 122-record/17-drawn join and headed probes pass. Fixed aligned door/button planes remain required. | Partial |
| Stock remote stickies require ordered Physics create/step/result/disable/destroy transitions; airborne arm/pulse is valid without contact; exact IVP body motion remains unavailable. | Codecs and mapper accept airborne arm without a normal. The application atomically rejects Demoman fire as `StickyPhysicsSolverUnavailable` before command publication or projectile creation. | Producer lifecycle vectors, browser airborne-arm test, and headed zero-projectile unsupported-state assertion pass. | Ready |
| Projectile PCFs execute complete material, shader, blend, color-space, sheet, orientation, quantized-alpha, child, trail, sort, bounds, and lifecycle contracts. | WASM emits version 3 and Rendering consumes producer trail geometry, blend, color, opacity, stable order, and sheet samples. | Exact-content hash, native rockettrail records, and headed rocket/explosion sprite/trail capture pass; complete named visual-region timelines remain required. | Partial |
| Source draws the hand and attached item in one post-world viewmodel view, orders item/hand by item translucency, partitions each part by primitive opacity, uses depth `[0,0.1]`, prevents world depth from occluding the viewmodel, applies handed culling, and restores projection/view/depth/cull/material state. | PMRQ v5/PMPO v4 move complete phases, bob, bodygroups/events, item/hand partitions, effective facing, transform, and projection into Rust; Rendering performs a depth-only clear before the separate post-world pass and records restoration. | Native four-primitive item-before-hand output plus headed level/downward/wall visibility, depth isolation and restoration pass. Fixed aligned ID/world-overlap planes remain required. | Partial |
| SourceScheme composes its exact base chain, filters desktop conditions, selects the current platform candidates, and keeps controls functional when exact native raster evidence is absent. | Source bundle retains exact scheme/font bytes; the application resolves through VGUI for macOS and mounts selected sources while retaining native-raster requirements. | Headed macOS focus/submission/completion/close/reopen/replacement/accessibility and Pointer Lock lifecycle pass; native pixels remain blocked. | Ready |

### TF2 visual, gameplay, and Physics consumer integration

This integration refines `TF2-WEB-004` through `TF2-WEB-008`, `TF2-WEB-012`, `TF2-WEB-014`, and `TF2-WEB-015` and adds no denominator identity.

| Source/TF2 behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| StudioModel encoded-clockwise exterior facing, transform parity, reflected viewmodel culling, opaque/translucent part order, and nearest-surface depth apply to world models, players, projectiles, hands, and items without per-model repair. | PTF2 v10 carries PSMP v3 facing; PMPO v4 carries effective reflection parity and complete Soldier/Demoman frame plans; Rendering uses one-sided Three/WebGPU admission and restores the separate viewmodel pass. | Exact producer facing/frame hashes, native/browser part probes, cold/warm world captures, and reload-phase geometry pass. Aligned target color/depth/normal/material/ID planes remain absent. | Partial |
| Alpha-tested texels discard fragments; translucent glass, marks, model masks, and SpriteCard output preserve their supplied alpha owner, blend, depth, cull, and ordering state. | PMST v2 carries typed ownership/discard, and Rendering applies exact greater versus greater-or-equal discard to authored alpha/mip chains without black-alpha quads. | Exact configured alpha counts, browser decal/Particle probes, and cold/warm captures pass; aligned target overlap planes remain absent. | Partial |
| Static marks remain bound to the collision-selected receiver, model-local geometry, placement/snapshot revisions, current mover transform, receiver visibility, and normal/decal-bias categories. | PENV v3 carries every receiver/revision/visibility field; Rendering joins current brush transforms and uses the categorical WebGPU decal-bias adapter. | Configured 39/38/73 receiver stream, fixed 13 visible spawn fragments, revision vectors, and door receiver identities pass; aligned moving-door planes remain absent. | Partial |
| Water consumes selected surface/bottom materials, authored normal frames/mips/sampling, cubemap, WaterLOD, fog, above/below classification, and reflection/refraction/main/intersection view plans with complete state restoration. | PENV v3/PVIS v3 transport configured HDR sky plus exact PVS/frustum/Water inputs; Rendering owns animated normal textures, auxiliary targets, clips, fog/material composition, per-pass surfaces, and restoration. Surface target sampling and underwater overlay remain blocked. | Native above/below/intersection plans fix frame 30 and pass order; configured sky admission and headed spawn main-pass restoration pass. Aligned visible-Water target/browser captures remain absent. | Partial |
| Stock and Original rockets use the supplied Source basis/flip/center-fire/aim/muzzle/crouch transforms; held attack, deploy, reload, death, respawn, and regeneration advance only at Simulation ticks. | PSSN v11 preserves source requests/events; the application consumes tick state and never schedules gameplay from presentation time. | Native stock `+12` right offset, centered Original, 54-tick held cadence with regeneration, deploy/reload/death/respawn tests, and browser near-wall fire pass. | Ready |
| Rocket damage reaches Entity damage and ordered button/door I/O; linear movers preserve replacement requests, collision, carry, completion, wait, and automatic return; regeneration emits enter/stay/leave and locker animation state. | PSSN v11, Collision v3, PMRQ v5 packed-body animation, Rendering transforms, and application state consume the reviewed game/Entity output without configured-identity branches. | Configured five button links/four doors, cyclic platform/carry, 22 regenerate zones, locker `open/close`, and model-109/fence collision evidence pass. | Ready |
| Projectile presentation preserves every per-tick active fact and event, per-fire attachment pose, graceful stop source tick, complete rocket/sticky effects, stable draw order, at least one displayed output for each newly visible effect, and natural cleanup even when no final projectile survives a host publication. | The application retains ordered event batches independently from coalesced authority state; PPTX v2 and Particle v3 execute delayed source events and graceful stops transactionally. PTF2 supplies each configured Particle material state exactly once. A bounded required-display queue retains only the first nonempty prepared frame for each new effect identity while ordinary trail-follow frames continue to coalesce. | Near-wall transient lifecycle, fixed 12-state hash, exact-content wall/mid-air/sticky timelines, headed far-flight trail and controlled wall-impact explosion child pixels across repeated runs, blend/sheet/order, queue bounds/reset, repeated-shot cleanup, replacement, and disconnect pass. | Ready |
| Sticky launch is atomic only when an accepted exact VPhysics result source exists. | The application rejects launch before command publication while retaining supplied-Physics presentation evidence; no stationary projectile or substitute solver exists. | Headed unsupported-state schedule and fixed supplied-Physics sticky trail/pulse timelines pass. | Ready |
| Source volume is unmuted by default and the first accepted gameplay or pointer gesture attempts browser activation; Simulation cadence remains independent of presentation throughput. | Audio control remains a retry/status surface. The application records phase timings, allocations, domain WASM calls, queue maxima, output bytes, and delayed-publication batches. | First-pointer running-audio evidence, Simulation fast/delayed differential, benchmark phases/allocations/queues, and headed performance records pass. | Ready |
| Stock payload maps can be loaded as exact world/presentation inputs for local visual inspection while payload objectives, carts, checkpoints, timers, teams, rounds, HUD, and win conditions remain categorically unavailable. Noclip changes only the existing local Movement mode. | Exact configured `pl_upward` reaches Ready with displacement terrain, all 1,244 static props, the four runtime-lit boulders and authored 3D sky under the existing Jump/noclip session. `pl_badwater` remains undeclared. | Checked headed cold/replacement/noclip/shutdown evidence retains two terrain captures, exact 1,184-main/60-sky prop ownership, four source-qualified runtime-boulder captures, a 548-surface sky pass and zero Payload dataset claims. | Partial |

| Target behavior | playsrc behavior | Evidence | Status |
|---|---|---|---|
| `TF2-WEB-001` — The application satisfies all 27 shared cross-child requirements in [`../ROADMAP.md`](../ROADMAP.md) for every TF2 route and both experiences. | No TF2 web implementation exists, and shared asset, presentation, service, and deployment requirements contain blockers. | **Shared-contract application audit:** execute every shared vector through `/`, `/jump/<map>`, and `/replay/upload` on all eight browser profiles and compare child-specific identities and cleanup. | Blocked |
| `TF2-WEB-002` — The immutable TF2 catalog contains exactly `tf2.jump-practice` and `tf2.recorded-replay`, preserves declared entry order and labels, and rejects every undeclared experience or ruleset selection. | No TF2 application root, catalog, route registry, or product shell exists. | **Two-experience catalog vectors:** load exact, reordered, duplicate, missing, extra, wrong-game, wrong-ruleset, and dangling-root catalogs; compare visible choices, route eligibility, and exact rejection. | Not started |
| `TF2-WEB-003` — `/` presents the two catalog entries; `/jump/<map>` selects one exact catalog map source/course identity; transient `/jump/external` requires a new explicit console URL submission after direct navigation or reload; `/replay/upload` selects no map source; navigation never infers an experience from content, URL, or file names. | No TF2 router or route-state model exists. | **Route/selection decision table:** direct-load, link, reload, history, malformed map, absent entry, external submission/success/reload, and uploaded filename cases compare canonical URL, selected map source/course, ephemeral state, and failure exactly. | Not started |
| `TF2-WEB-004` — Jump practice creates one composition containing TF2 and `ruleset.jump` against the exact catalog map/course identities and publishes Ready only after tick-zero Simulation, Rendering scene, and VGUI bindings agree. | TF2, Jump, Simulation, map/package producers, and their interfaces are not accepted or implemented. | **Composition startup trace:** fixed valid and identity-mismatched compositions compare participant manifest, map/course hashes, initial snapshot, scene/VGUI readiness, product state, and reverse unwind. | Blocked |
| `TF2-WEB-005` — Jump practice wall-clock pacing requests bounded Simulation advancement, presents immutable snapshots/events, pauses on shared lifecycle suspension, and shuts down without an application tick loop or catch-up mutation. | The application converts animation opportunities into at most four fixed 15 ms worker ticks, never advances while hidden or while a phase is in flight, drops catch-up beyond 100 ms, consumes immutable snapshots, and terminates the worker on close. Generic Simulation and Jump participants remain absent. | **Gameplay authority call trace:** fixed frame/focus/visibility schedules compare coarse Simulation calls, completed ticks, presentation snapshots, pause state, worker messages, and zero domain advancement outside Simulation. | Partial |
| `TF2-WEB-006` — Desktop and mobile physical bindings map through one current binding profile to typed TF2 command inputs; focus loss, pointer-lock loss, touch cancellation, route replacement, and suspension emit one neutral state before the next admitted tick. | Desktop W/A/S/D, Space, left/right Shift crouch, Source-signed mouse-look, primary fire, secondary detonation, all nine canonical class selections, and Original selection map through PCMD v5; Control has no crouch binding. Each physical button resolves once on its accepted down edge, retains that action through modifier changes, ignores duplicate downs, releases only its retained action, and keeps an action held until every physical source releases. Exact configured chords take precedence and an unmodified physical binding remains active while unrelated modifiers are held. The command atomically includes bounded external gameplay results when supplied. Positive horizontal pointer motion subtracts yaw, positive vertical motion adds bounded pitch, raw pointer movement is requested when supported, and each staged map supplies its selected teamspawn angles before the first command. Display frames and fixed commands independently sample one current view state; delayed teleport results preserve post-command mouse deltas. Blur, visibility change, pointer-lock loss, console activation, replacement, and close clear held state including crouch. Mobile/touch, remapping, trust/timestamp revisions, and retained input transcripts remain absent. | **Binding-to-command seam trace:** fixed command/result, every W/A/Shift press and release ordering, exact-chord precedence, duplicate downs, two-source holds, mouse release after modifier change, ShiftLeft/ShiftRight/Control, and blur/pointer/visibility/console/replacement schedules compare bytes and neutralization; deterministic sensitivity/raw-retry/teleport-rebase vectors and headed-browser raw-mode, continuous display-cadence, and positive right/down pointer traces pass. | Partial |
| `TF2-WEB-007` — Jump practice supplies canonical Simulation snapshots/events and TF2 presentation requests to Rendering, Particle, Audio, and VGUI, selects one app-owned camera/input mode, and never derives game or Jump truth in product UI. | Game-owned mappings produce HUD/camera/projectile presentation from each snapshot. Rendering receives only mapped camera/effects. The application supplies immutable view/player/map/frame inputs to one bounded VGUI `cl_showfps`/`cl_showpos` panel; mode 2 reports unavailable player absolute angles rather than deriving them. Unresolved model, material, particle, and audio inputs enter the visible blocker ledger and create no silent substitute except caller-selected diagnostic geometry/model views. Ordinary presentation resources and Jump/VGUI HUD state remain absent. | **Gameplay presentation integration:** run fixed Jump ticks and virtual frame schedules; compare owner call transcript, immutable input hashes, camera selection, FPS/position VGUI lines, unsupported distinctions, canvas/VGUI outputs, audio permission state, and zero authority writes. | Partial |
| `TF2-WEB-008` — Preact mounts one product shell and one framework-independent VGUI root; TF2-owned immutable HUD bindings update VGUI and typed VGUI commands return through the application without either owner adopting the other's DOM. | Preact creates the product shell and canvas plus one otherwise-empty VGUI mount. The integrated VGUI package exclusively creates/reconciles/destroys its console and client-diagnostic hosts; the application consumes typed requests, supplies immutable diagnostic inputs, and consumes game-owned HUD values. Retained headed evidence records one Preact mount, two VGUI service hosts, accessibility, and deterministic cleanup. Complete TF2 HUD VGUI bindings remain absent. | **DOM ownership and HUD seam audit:** instrument shell, VGUI, and canvas subtrees under fixed gameplay/replay states; compare node owners, bindings, typed commands, focus/accessibility state, and deterministic destruction. | Partial |
| `TF2-WEB-009` — Replay upload streams one selected file through size/hash progress and cancellation, selects `tf2-demo3-net24` explicitly, hands bytes to Demo once, and publishes either one immutable source identity or one exact failure. | Demo implements bounded protocol-3 framing and fixed-capture verification. No upload adapter, browser streaming hash path, cancellation state, or profile handoff exists. | **Upload boundary matrix:** fixed valid, empty, maximum, maximum-plus-one, changed, cancelled, wrong-profile, truncated, and malformed files compare stream reads, digest, Demo calls, progress, retained bytes, and zero partial session. | Blocked |
| `TF2-WEB-010` — The replay route exposes open, play, pause, positive rational rate, indexed step, seek, snapshot status, event range, end, and close only through one Replay session; reload requires the same file hash before restoration. | Demo, Networking, and Replay implement transport-neutral recorded-state parsing, protocol-24 state, deterministic controls, seek/restore, and zero-Simulation audits. The TF2 recorded-state decoder and every application route/control adapter remain absent. | **Replay control integration:** fixed DEM and control schedules compare application calls, Replay cursor/snapshot/event outputs, URL state, reselect/hash behavior, user-visible controls, and zero Simulation calls. | Blocked |
| `TF2-WEB-011` — Replay presentation supplies Replay snapshots/events and TF2 recorded-state mappings to Rendering, Particle, Audio, and VGUI with discontinuity-safe camera selection and no product-side state reconstruction. | Replay exposes immutable presentation tuples, but no TF2 recorded-state decoder/mapping or browser presentation consumer exists. | **Recorded-presentation owner trace:** fixed play/pause/seek/step/end schedules compare snapshots, discontinuities, event ranges, camera/HUD selections, package calls, pixels/accessibility captures, and zero inferred events. | Blocked |
| `TF2-WEB-012` — Replacing either experience cancels its complete load tree, closes its sole authority, releases all workers/browser resources, resets product-only state, and then starts the next generation with no shared mutable state. | Catalog and external map loads stage a generation, preserve the active worker and renderer on fetch/compile/cache/scene failure, activate after scene validation, and reset pacing at the replacement boundary. Version-2 map and presentation cache operations use exact Blob records and 30-second terminal bounds; cached model headers consume complete sequence records and rebuild omitted model artifacts from the supplied exact dependency bundle. Close cancels pacing, removes listeners, releases pointer lock, destroys VGUI/Rendering/Particle/Audio, closes IndexedDB, and disposes/terminates the worker. Replay/cross-experience replacement, cancellation during synchronous WASM compilation, and real resource-ledger evidence remain absent. | **Cross-experience replacement ledger:** alternate both experiences through loading, Ready, suspended, cache timeout/failure, recovery, failure, and close; compare generation IDs, authority shutdown, worker/resource counts, settings retention, and no late publication. | Partial |
| `TF2-WEB-013` — The application console adapter accepts exact VGUI submissions, completes from owner snapshots, and implements typed convars, `map <catalog-map-name>`, plus the playsrc-only `map https://<allowed-origin>/<path>/<map-name>.bsp` operation without moving command, convar, Content, map, or Simulation authority into VGUI. | The adapter owns Backquote, completion for exact map/class/convar catalog values, bounded tokenization/output, status/class/clear execution, typed `developer 0|1`, typed `cl_showfps 0|1|2`, typed `cl_showpos 0|1|2`, current/default/min/max display, and `map jump_beef`. Catalog revisions and displayed values change atomically with accepted convar writes. The HTTPS form rejects credentials/query/fragment/unlisted origins/redirects/unknown or over-limit lengths, uses omitted credentials plus 30-second cancellation, hashes while streaming, validates through staged BSP/WASM compilation, retains no URL, and activates atomically. CSP evidence, controlled external origin, progress records, and complete mutation vectors remain absent. | **Console adapter transcript:** fixed convar default/query/set/invalid/extra-argument/catalog schedules plus empty, catalog boundary, unknown, exact URL, credentials, fragment, port/origin, redirect, CORS, absent/incorrect/limit-plus-one length, timeout, cancellation, malformed/corrupt BSP, missing dependency, provider failure, stale generation, success, reload, and publication schedules compare VGUI requests/output, owner calls, values, progress, hashes, route, prior-state preservation, observations, and zero VGUI execution. | Partial |
| `TF2-WEB-014` — `jump_beef` composes profile-selected map, current TF2 producer state, StudioModel artifacts, projectile/PCF presentation, and world-environment/visibility outputs without duplicate authority. | The application uses profile-distinct generations, caches exact models, executes mapper-produced PCF phases, consumes PENV v3/PVIS v3 sky/marks/Water views plus separate PVS and frustum surface sets, and requests one Rust view phase per camera frame. New generic Entity/contact/angular/path-mover APIs remain package-owned; the current compatible linear subset is unchanged. No inferred BSP-trigger course exists. | Native/WASM and cold/warm headed evidence cover current schemas, 899 Source dependencies, LDR/HDR identities, PCF output, PVS/frustum/Water plans, replacement, and cleanup. | Partial |
| `TF2-WEB-015` — `jump_beef` consumes the reviewed world-material/decal, StudioModel v3, model shader/mip/eye/lighting, stock viewmodel, Movement, Collision/pusher, TF2 gameplay/Entity/random/audio, projectile-particle, Source-audio, platform-font, and content-closure producers through exact source-root roles without placeholder behavior or resources from an unselected map. Blocked IVP, Tempus, current model lightcache/eye state, remaining opaque world mips, native font-raster capture, and aligned-capture facts remain explicit. | Startup selects 2 entries and 1,323,980 encoded bytes; Main Menu selects 860 entries and 62,171,063 encoded bytes, including the exact chapter-1 ordinary GameUI background closure; gameplay selects 309 entries including the exact empty v6 static-prop section inputs without GameUI-only objects. Native/WASM compilation retains the exact LDR payload and matches the gameplay-set HDR payload. | Native/WASM and headed evidence retain existing model/frame, sky, alpha/marks/Water, random/audio, Collision/movers, Particle, font, blocker, performance, capture, replacement, and shutdown records while proving startup-video-only, Main Menu, selected-map gameplay, warm-cache, and generation-cancellation request sets. | In progress |
| `TF2-WEB-016` — One immutable TF2 browser configuration declares exactly `jump_beef` and `pl_upward`, one default target, one shared WASM/catalog pair, and target-qualified BSP, resource-root, dependency-ledger, content-build, and loading identities. Console `map` accepts only exact declared target names, stages the selected closure without mutating the active generation, atomically activates it through the existing Worker/WASM authority, and releases replaced presentation, resources, input, and listeners. | The current configuration contains the sorted two-target table and defaults to `jump_beef`. Replacement pauses input, drains the active Simulation and complete model/Particle/visibility presentation tasks, clears queued publications, stages graph/BSP/resources, replaces renderer state, activates the new generation, and only then publishes it. Fixed-tick continuation retains button/movement levels while consuming physics results, selectors, mode requests, and activation targets once. Unknown names fail before fetch. | Parser mutation vectors pass. Headed `jump_beef → pl_upward → jump_beef` generations 1/2/3 each verify exact BSP/root/build/WASM/tick identity, final-Ready leaf/area/PVS, 25 bounded color/depth/face/batch/material samples, noclip movement over at least three fixed ticks, replacement cleanup, and shutdown. Standalone map, displacement, static-prop lighting, 3D-sky, and area-connectivity evidence remains unchanged and passes. | Ready |

## Generated Inventories

No generated TF2 web-application inventory is accepted or required by the current denominator. Generated item count: 0. Accepted item count: 0. The two experiences, four routes, one selected ruleset, and two input-profile classes are enumerated directly above.

## Exit Criteria

The TF2 web application is Complete only when all of these predicates pass:

- All 15 behavior rows are Ready and the denominator review is Accepted.
- The catalog contains exactly the two declared experiences and every canonical route/history/reload case selects the same identity or exact failure.
- Jump practice composes one current TF2/Jump/Simulation authority and consumes current map, presentation, content, asset, and browser interfaces without product-side game or ruleset behavior.
- Recorded replay composes one current Demo/Networking/TF2-decoder/Replay authority and makes zero Simulation advancement calls.
- Both experiences pass all eight shared browser profiles, including desktop pointer lock, mobile touch, audio permission, GPU loss, accessibility, cancellation, and shutdown evidence.
- Every application root, catalog entry, map/course root, user file, Demo, Simulation/Replay session, and presentation lease retains exact identity through its full lifecycle.
- Both console map forms preserve one operation generation and direct-source authority; URL acquisition passes every shared security, integrity, provider, privacy, cancellation, atomicity, and reload-resubmission predicate.
- No required item remains Blocked, Unsupported, Unknown, Missing, Malformed, Partial, stale, duplicated, owner-conflicting, or dependent on a fallback, compatibility layer, or legacy path.

## Blockers

- **Whole-map HUD event ordering:** `games/tf2/browser/src/hud/bindings.ts` retains an earlier explicit ammo event in its final-state consistency ledger after a later regeneration replaces that weapon's clip/reserve. The exact coalesced sequence `tick 2 fire clip 3 → tick 3 regenerate clip 4 → final clip 4` throws `InconsistentPublication: final HUD ammo differs from the event stream`; headed `jump_beef` noclip coverage reproduces the failure at goal 265, tick 3,147. The game-owned HUD binder must update its regeneration ledger; application-side event suppression, fabricated ammo events, publication splitting, and ignored failures are prohibited.
- **Headed dual-map performance evidence:** `tools/playsrc/profile/input-latency.profile.ts`, `cold-map.profile.ts`, and `map-coverage.profile.ts` each select only `jump_beef`; none exposes a checked `pl_upward` or cross-generation target schedule. `cold-map.profile.ts` additionally requires the removed single-target `configuration.bsp` instead of the current `targets[].objects.bsp` contract. These shared profile owners must supply exact dual-map evidence; the application cannot add a compatibility alias or fabricate sky captures.
- **Jump composition:** the TF2 game, TF2 ruleset universe, and Jump roadmaps are Not accepted; Jump's course-behavior inventory has 0 accepted items and records unresolved timer, checkpoint, reset/restart, death/respawn, save/restore, and multiplayer contracts. A current game/ruleset composition cannot be integrated. Checked all three roadmaps and inventories.
- **Recorded-state composition:** Demo, Networking, and Replay implement bounded transport-neutral recorded-state foundations, but their denominators are Not accepted. TF2 recorded-state decoding/presentation, browser upload/control adapters, and exact originating build identity for the retained provider DEM remain absent. This roadmap accepts the consumer role without claiming product integration.
- **VGUI, HUD, and Options presentation:** the application now consumes the reviewed descriptor, GameUI model, HUD binding, and settings state. Configured scalable/image borders, distance-alpha two-texture HUD materials, seven generic control identities, exact native font metrics/raster, a configured Main Menu Console control, TF2 Advanced page control generation, and complete HUD scalar/color application remain explicit blockers. `TF2-WEB-008` cannot pass full integration review.
- **Console map adapter:** catalog and bounded HTTPS forms are implemented, but no controlled allowed-origin server/CSP evidence, `/jump/external` route lifecycle, complete command/convar registry, or accepted external-origin policy review exists.
- **Asset/service delivery:** local immutable objects, whole/range service delivery, browser HTTP verification, and IndexedDB derived caching are implemented. Immutable TF2 application/catalog/map roots, dependency closure, VPK browser ranges, deployed origin, and accepted service/store denominators remain absent.
- **Presentation interfaces:** one `jump_beef` world/sky/decal/projectile/Particle/launch-Audio slice passes headed Chrome/WebGPU regression checks. Current model lightcache/eye records, Water tangent/projected-depth/underwater-overlay resources, a one-command-encoder backend, shadow producer records, aligned target captures, remaining opaque world mips, post-IK/flex player state, WebGPU recovery, remaining browser profiles, non-local weapon attachments, and game-supplied explosion random samples remain absent.
